# Frontend design capability + Liquid Glass completion

**Date:** 2026-07-14

## What was done

Three threads this session. Git note: the main `Fireplace` worktree was on `autoresearch/session-20260713` (== `origin/master`); work was moved onto two proper feature branches off master (the other agent's branch untouched). Local `master` in the ping-deploy worktree is stale (behind origin/master).

### A. Flutter design skill (machine-level, not in repo)
- Created `~/.claude/skills/flutter-frontend-design/SKILL.md` — a Flutter-native anti-slop design skill: render→screenshot→critique loop, token discipline, capped/reduce-motion motion spec, banned motion zones, built-ins-first with deps only when the project already uses them. Leaves the shared web `frontend-design` plugin intact; overrides it for Flutter. Appears in the skills roster on next session start.

### B. Branch `feat/frontend-design-tooling` (pushed, PR-ready, NOT merged)
- Added `flutter_animate`, `animations`, `skeletonizer` to `frontend/pubspec.yaml`.
- Conversation-list showcase (`conversations_screen.dart` + new `conversation_list_skeleton.dart`): one-shot staggered entrance (viewport-capped, reduce-motion aware, never replays on provider rebuilds) + shimmer skeleton gated on `hasLoadedConversationsOnce` + `ConnectionProvider.errorMessage` (static effect under reduce-motion).
- `docs/design/flutter-ui-playbook.md` (new) + `frontend/CLAUDE.md` §9 (anti-slop doctrine).
- Commit `a5bffe0`. Widget test: `conversation_list_skeleton_test.dart` (4 tests).

### C. Branch `feat/glass-theme-migration` (pushed, PR-ready, NOT merged)
Finished the Liquid Glass look the previous agent left partial. Architecture clarified: `RpgTheme` = colors/fonts; `GlassTheme` = glass paint only — so this is applying glass to still-solid surfaces, not replacing RpgTheme. Owner ratified Tier A+B+C via an in-session `ask`.
- **Tier A** (commit `c217007`): blocked-users, privacy-safety → `AppBar`→`GlassTopBar` + scroll-behind clearance; add/invitations → bounded floating **tabbed-glass** header (back circle + title pill + inset tab capsule, never a band); user-card hero back button → `GlassCircle`.
- **Tier B** (commit `ac6a0a1`): auth screen → login/register form wrapped in a floating `GlassSurface` card over the hieroglyph wallpaper (blur needs texture to read).
- **Tier C** (commit `08496ce`): new `GlassDialog` (drop-in for `AlertDialog`, Flexible+scrollable content) + `showGlassMenu` (real `PopupRoute`: system back + barrier-tap dismiss, edge-collision via layout delegate, transparent barrier, `GlassSurface` body + transparent `Material` for ink). Migrated all `AlertDialog` callsites and the block/mute popup menus; context-menu reaction bar + action panel → `GlassSurface` (each with a transparent `Material` so `InkWell`/`IconButton` rows keep ink; inner Material clips to rounded shape for the opaque fallback path). Reaction-picker emoji grid stays opaque by design.
- `SPEC.md` §9/§11 updated to record the ratified completion.
- Session summary commit `9e37e47`.

## Key files
- New: `frontend/lib/widgets/glass/glass_dialog.dart`, `glass_menu.dart`, `frontend/lib/widgets/conversation_list_skeleton.dart`, `frontend/test/widgets/glass_components_test.dart`, `docs/design/flutter-ui-playbook.md`
- Changed: `frontend/lib/screens/{blocked_users,privacy_safety,add_or_invitations,auth,user_card,conversations}_screen.dart`, `frontend/lib/widgets/message/{message_context_menu_overlay,message_action_panel}.dart`, `frontend/lib/widgets/conversation_tile.dart`, `frontend/lib/widgets/dialogs/message_delete_dialog.dart`, `frontend/lib/widgets/message/file_message_content.dart`, `frontend/lib/screens/chat_detail_screen.dart`, `frontend/test/preview/glass_preview.dart`, `docs/design/liquid-glass/SPEC.md`, `frontend/CLAUDE.md`

## Verification
- `flutter analyze` clean on all touched files (only pre-existing lint at `privacy_safety_screen.dart:429`, unrelated).
- Full `flutter test`: **684 passed, exit 0**. The glass change first broke 8 theme-dependent tests (`GlassSurface` needs the `GlassTheme` extension; `BackdropFilter`/gap assertions in the context-menu test) — fixed by giving those harnesses `RpgTheme.themeDataDarkGray`, keying the scrim (`context-menu-scrim`) and asserting it (the 2 glass filters are implementation detail), and setting the reaction-bar `GlassSurface` `height` so the border inset keeps the equal-gap contract. Added `glass_components_test.dart` (5 tests incl. edge-anchor clamp + keyboard-inset overflow).
- Rendered via the `test/preview/glass_preview.dart` harness (browser screenshots): conversation-list skeleton (all 4 themes + reduce-motion static); Tier A blocked/privacy/add (dark+light); auth (dark+light); live glass block-menu + context-menu (dark).
- Design-review checkpoint (designer subagent) on the tooling showcase: SHIP-WITH-NITS; 3 nits fixed.
- `graphify update .` run; both branches pushed and even with origin.
- Production deploy (frontend-only, unmerged branch): first 0.0.112→0.0.113 (`2278fe2`), then after the review fixes 0.0.113→0.0.114 (`baf7aed`) via `deploy-web.ps1` (flutter clean + release build + atomic swap). Live-verified each: `/health` ok, `/version.json` matches, served `main.dart.js` contains the commit, fresh Chromium boots the glass auth. Backend never redeployed (0.0.112 / a10ae1c).
- **Post-deploy review:** code review (`reviewer`) = SHIP-WITH-NITS, no blockers. Design review (`designer`) hung ~40 min on its own `flutter run` render server and was aborted (no verdict); design pass done inline. Fixed 5 items: GlassDialog AlertDialog route semantics (P2 a11y regression — base Dialog dropped scopesRoute/namesRoute/role), GlassMenu 48px min tap target, add/invitations `SafeArea(top:false)` (doubled-gap), user_card mute `mounted` guard, user_card back arrow `onSurface` color. Added a GlassDialog route-semantics regression test. Full `flutter test` **685 passed**, exit 0. Reviewer also noted pre-existing (master, out-of-scope) issues left untouched.

## Notes for next session

- **PR #67** (`feat/frontend-design-doctrine`, off master, commit `8d9616a`): the trimmed design-tooling — playbook + `CLAUDE.md` §9 + `skeletonizer` loading skeleton ONLY. Dropped `flutter_animate` + `animations` per owner decision. `flutter analyze` clean, `flutter test` **683 passed**, skeleton dogfood-rendered (dark shimmer + reduce-motion static). Version NOT bumped (master 0.0.112); overlaps the glass branch on 2 files (`pubspec.yaml`, `glass_preview.dart`). Supersedes `feat/frontend-design-tooling` (close/delete that branch).
- **Glass branch deployed to production frontend as `0.0.114` (commit `baf7aed`) while UNMERGED** (first 0.0.113/`2278fe2`, then 0.0.114 after review fixes) (owner requested a branch deploy via `deploy-web.ps1`). Verified live: `/version.json`=0.0.113, `/health`={status:ok,db:ok}, served `main.dart.js` contains `2278fe2`. Backend untouched (0.0.112 / a10ae1c). NOT permanent — the next `master` frontend deploy overwrites it; merge to master to make it stick. Design-tooling branch NOT deployed.
- Tooling quirk: the `edit` tool can't disambiguate two files named `CLAUDE.md` (root vs tier) — a tag-anchored edit resolved to root once. Edit `frontend/CLAUDE.md` via filesystem (eval) if it misfires.
- Tier C (glass dialogs/popup menus) was SPEC-deferred ("owner to ratify at PR review"); ratification came from the in-session Tier C selection — call it out at PR review as the most opinionated change.
- user-card was NOT live-rendered (heavy `UserCardVisualData` fixture); its change is a trivial `GlassCircle` wrap + dialog/menu swaps, analyze-clean and covered by component tests.
