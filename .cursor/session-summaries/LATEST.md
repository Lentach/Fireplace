# Latest session summary

**Date:** 2026-06-10

**Topic:** **E2E decrypt regression: Bad-MAC → session-reset cascade root-caused and fixed** — the frequent `[Decryption failed]` reports had two classes. **Class B (mid-conversation, the 7657/7658 log):** `_retryDecryptForPeers` *deleted* the local Signal session (wiping current+archived ratchet states) as "recovery" for inbound failures — every message the peer had already encrypted became a permanent Bad-MAC loss; the sticky `_liveDecryptFailedPeers` set (only cleared on decrypt success, which terminal rows can never deliver) re-fired the reset on every reconnect/history pass; and `_mergeHistorySnapshot`'s `!serverIds.contains` exclusion (since 05-17 `d6a851c`) made every server snapshot resurrect terminal rows as `[encrypted]`, re-arming the machinery (the 20:14:54–:57 repeats). Verified the 06-06 split and 06-09 policy extraction were behavior-faithful — defects predate; push deep-linking raised pass frequency. **Fix:** inbound path never deletes sessions (rebuild *request* only — lossless), terminal-row skip in retry, `_pruneLiveDecryptFailedPeers()`, deduped recover-unresolved emits, merge pre-seeds all local rows. **Class A (first 1–3 msgs after cache clear):** confirmed from source the in-app clear touches only `e2e_*_decrypted_*` plaintext — the user's wipe was browser site-data/reinstall (web stores identity+sessions in localStorage only) → cryptographically unrecoverable; new `notifyPeerRebuild` on identityReset emits one `requestSessionRebuild` so the peer re-keys on their next send (loss shrinks to ~1 msg). **Instrumentation (privacy-safe):** `DECRYPT_START` +`ctype`/`hasSession`, new `DECRYPT_DECISION`, `SESSION_RESET` +`trigger`, new `SESSION_DELETE`/`LIVE_RETRY_PRUNED`/`IDENTITY_RESET_REBUILD_REQUESTED`/`CACHE_CLEAR`. New regression tests fail pre-fix (4 failures via stash) and pass post-fix; full suite **325 green**; analyze clean (1 pre-existing info). **Next: deploy + capture E2eDiagLog from an affected device.** → [2026-06-10-session-decrypt-cascade-fix.md](./2026-06-10-session-decrypt-cascade-fix.md)

**Previous:** 2026-06-10 — **Task 6 — NotificationCleaner conditional-import triple** — stub/io/web `NotificationCleaner` for the push-notification sweep (Android `flutter_local_notifications` v21 named-param `cancel`; web `package:web` SW `getNotifications` + badge postMessage). Commit `4cf8943`; 4/4 unit tests; analyze clean. Not yet wired (entrypoint + callsites in later tasks). → earlier entry preserved in that day's file.

**Earlier:** 2026-06-09 — **Live-receive drop diagnostic (iOS PWA) + v0.0.46** — added 3 observability-only `E2eDiagLog` entries (`RECV_MSG` enriched, `RECV_QUEUED`, `ADD_TO_STATE`) to catch the active-conversation desync; capture via Privacy & Safety long-press shield when it recurs. Commit `8203dff`. → [2026-06-09-session-live-receive-diagnostic.md](./2026-06-09-session-live-receive-diagnostic.md)
