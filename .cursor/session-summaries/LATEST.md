# Latest session summary

**➡ FRESH AGENT: read `2026-08-03-HANDOFF-post-b2b-state.md` FIRST** — it carries the ⛔ B2b deploy gate (merged-but-undeployed sealing rides the NEXT frontend deploy), watch items, and the fresh traps. Its work queue is now DONE (§2 + §3 closed this session, entry below); what remains is owner blockers + the Android track. The older `2026-08-03-HANDOFF-signal-grade-queue.md` remains the long-form queue with dated addenda.

**✅ P0 CLOSED AND CONFIRMED: `[Decryption failed]` fixed in 0.0.139, several days with zero new reports.** The four hypotheses in `2026-07-29-HANDOFF-decryption-failed-incident.md` are all DEAD (killed by measurement) — that file is historical, do not work from it. Root cause: `2026-07-29-session-decryption-failed-root-cause.md`. **Never uninstall / clear site data.**

**🔴 FCM IS DISABLED IN PRODUCTION.** `FIREBASE_SERVICE_ACCOUNT` is absent from `~/fireplace/.env`, so every backend boot logs "FCM disabled". `google-services.json` IS tracked, so an APK would register tokens the server can never push to — **Android notifications are dead until that var is set.** Web Push VAPID is present, so the PWA is unaffected, which is why this went unnoticed. Fix it before any APK work.

- **Still binding (applies to every entry below):** never run `dart format lib/` (format only what you edited); **ask before opening the browser tool**; `node scripts/lint-ratchet.mjs` runs ONLY in CI — run it before pushing backend changes; `cmd | tail` hides exit codes; never re-derive test counts by arithmetic across a merge; `flutter run -d web-server` serves a blank scaffold once its hot-restart client is lost — restart the process, do not reuse the tab; the E2E register throttle is 10/hr/IP in memory, so `docker compose restart backend` between full `test_e2e` runs and allow 2–5 min to boot.

**Date:** 2026-08-04 (latest) — **FRONTEND AUDIT REMEDIATION: 19 findings closed on branch `fix/frontend-audit-findings`. NOTHING MERGED, NOTHING DEPLOYED — the ⛔ B2b gate is untouched.** Flutter **1236→1247 + 10 skipped**, verifier synced, analyze clean, real `flutter build web --release` done as a gate (below). Detail: **`2026-08-04-session-frontend-audit-fixes.md`**; findings (uncommitted): `.planning/full-audit/frontend/FINDINGS.md`.
- **Every HTTP call was unbounded.** `api_service.dart` 21/21 now `.timeout()` by class (auth 15 s / upload 60 s / rest 20 s; multipart under ONE deadline). **`refreshSession` timeout maps to `SessionRefreshTransientException`, NEVER `Invalid`** — transient holds the session, invalid clears local auth, so the wrong mapping manufactures the very logout this repo chased. Closed the dead-app case: a stalled refresh left `_isRestoringSession` true forever and AuthGate spinning until force-quit. Same class fixed in `sendGif`, `gif_service`, both `download_utils_*`.
- **A voice note could be destroyed silently.** Teardown during `recorder.stop()` ran `dispose()` → file deleted → unguarded `setState` threw BEFORE the `try` calling `onVoiceSent`. Now `dispose()` defers while a stop is in flight, setStates degrade to field writes once unmounted, and the conversation id is captured at send time (`ChatDetailScreen.dispose` clears `activeConversationId`).
- **⚠️ wasm would have deleted the multi-tab Signal lock.** `session_cross_context_lock.dart` + `storage_persist.dart` keyed on `dart.library.html`, FALSE under `--wasm` ⇒ stub, no lock, no persist request, zero diagnostics — and `verify-session-lock-probe.mjs` stays GREEN because it tests the browser API, not which Dart file compiled. Both now key `dart.library.js_interop`; verified in the built bundle.
- Also: `socketReady` watchdog (a connect that never reaches ready scheduled no reconnect and showed an authenticated empty shell forever); 20 s ack bound on accept/decline/send; mute no longer dropped by the pin handlers (`copyWith` + new clear flags); 1 Hz metadata-row rebuild gated on ephemeral; FCM `onTokenRefresh` no longer holds the boot JWT; denied `storage.persist()` now durable in `E2ePersistentDiag`; timer/blob-URL leaks; `DateTime.tryParse` on socket timestamps.
- **HELD for separate owner-approved PRs (decrypt path, per the audit's rule):** FE-013, FE-020 (also blocked on a backend answer), FE-006, FE-001/2/3/5. **FE-019 was already fixed** by `991a6b2` — the audit read a stale shape. **FE-024 skipped deliberately**: failing a send the server received releases the exactly-once latch and invites a duplicate.
- Traps paid: Codex-backed `task` subagents still die `usage_limit_reached` instantly (9/9) — Anthropic-only. Two tests failed *because* the fixes bounded previously-unbounded work (a held refresh / a held accept now park a real timer past teardown); both drain the in-flight action after their assertions, neither assertion weakened.

**Date:** 2026-08-04 — **BACKEND SECURITY/RELIABILITY AUDIT, READ-ONLY, NO CODE CHANGED. 52 findings: 0 CRITICAL, 4 HIGH, 20 MEDIUM, 27 LOW. Coverage 109/109 non-spec files (100%).** Findings are **LOCAL-ONLY in `.planning/full-audit/backend/` (gitignored) and deliberately never committed** — a concentrated inventory of unfixed vulns in a live E2E messenger. `2026-08-04-session.md` is the committed trace; read `FINDINGS.md` for anything beyond it. Baseline was green first: 589/589 / 47 suites, ratchet 816, tree clean at close.
- **Verdict: the tier is well built.** The unrecoverable class is **confirmed absent** — nothing advances ratchet/session/prekey state on a failed op. OTP claim atomically single-serve; the `[rows,rowCount]` trap correct everywhere; secret-note reveal quotes `"expiresAt"` and only `POST /reveal` burns (no bot/GET path); SSRF holds both layers; advisory lock releases on every throw; all 13 entities match migrations; the 08-03 hard-delete guard fails closed with exact `ANY` matching.
- **HIGH-1 `BE-300/301` — the ONLY permanent data-loss path.** 03:00 orphan cron snapshots disk+DB then unlinks; a row committed in the gap dies anyway, and grace is measured from **file mtime, not row-insert**. `resolveGraceMs` does `Number('')`→`0`, which passes `isFinite && >=0`, so a **present-but-empty `MEDIA_CLEANUP_GRACE_MS` silently means ZERO grace** (prod checked below: absent, so not exposed). Fix BE-300 first (~10 lines), it de-risks BE-301.
- **HIGH-2 `BE-101` — block is incomplete on BOTH tiers (confirmed).** Block doesn't delete pending requests, `getPendingRequests`/`acceptRequest` have no block check, and the client filters nothing (`friends_provider.dart:111-118` → `invitations_screen.dart:413-420`, live Accept for every request; `blockUser()` prunes `_friends` only). Request → block → **Accept still works** → friends with a blocked user; unblock skips consent entirely. Block also leaves the conversation alive, so reactions/typing bypass it (`BE-202`).
- **HIGH-3 `BE-250`** duplicate conversation rows — `findOrCreate` is check-then-insert with **no unique index**, so its catch-based race recovery is dead code. ⚠️ **Dedupe BEFORE adding the index or `CREATE UNIQUE INDEX` aborts boot**; no `CONCURRENTLY` inside a migration. **HIGH-4 `BE-004`** 8 WS events have **no** rate limit: `HttpThrottlerGuard` is `if (getType() !== 'http') return true`, so the global limiter is a no-op for sockets and 100/15min is only a default *value* for `WsThrottlerGuard`. Worst: `markConversationRead` (unbounded writes + re-stamps expiry) and `uploadKeyBundle` (unbounded identity overwrite, while its neighbour is the tightest-limited event). **Measure real rates before throttling `uploadKeyBundle`** — blocking key republication is worse than the abuse.
- **Cross-tier answers.** `BE-402`/`BE-002` **DROPPED — non-issues**: `onSessionRebuildNeeded` only adds an id to a Set, consumed solely by the next outbound send to that id (inert for strangers), and rebuild **archives, never deletes** — the old `deleteSession` that "wiped current + all 40 archived states" was already removed. Listeners attach before connect. **Push suppression HOLDS** (4 native states + `visibilitychange` + reconnect re-assert); one unverifiable desktop-web alt-tab edge. `BE-001` **stays live but is NOT message loss** — fetches and `sendMessage` wait for `socketReady`, but `initializeE2E` fires `uploadKeyBundle`/`uploadOneTimePreKeys` from the raw `connect` handler ungated, so a dropped upload leaves the server without a key bundle and **new peers can't open a session** until the next reconnect (self-healing).
- **✅ PROD VERIFIED (owner-authorised read-only SSH + aggregate SQL, no PII).** `MEDIA_CLEANUP_GRACE_MS` **absent** → 15-min default live → **BE-300 not exposed**. **The `/messages/` link-preview suspicion was WRONG — withdrawn**: host edge `/etc/nginx/sites-enabled/fireplace:46` DOES proxy it. **Trap: `frontend/nginx.conf` is the CONTAINER nginx, NOT the edge** — different route sets; I concluded from the wrong file. X-Real-IP set on all 11 proxied edge locations → throttles non-spoofable. **DB evidence:** 0 duplicate conversation pairs (BE-250 unfired — **so add the unique index NOW, the "dedupe first" prerequisite is free today**); conversations FK = **NO ACTION** → **BE-352 CONFIRMED, and it aborts the delete txn → BE-351 zombie account is the real branch**; statuses `accepted` only; **1 backend replica**; **1 of 1 blocked pairs also has an `accepted` friendship** — invariant violated in live data, but n=1 and `blocked_users` has no `createdAt`, so cause is **undetermined** among BE-006/101/103/data-age. **NEW `BE-005`** (LOW) XFF fallback reads first hop (spoofable, dead code today). **NEW `BE-006`** (MEDIUM, `blocked.service.ts:38-39`) block commits BEFORE `unfriend` with no txn and **no try/catch** (unlike the cleanup below it) — and the `:31-33` early-return means a re-block **never retries**, making the split state permanent.
- ➡ Detail: **`2026-08-04-session.md`**; full findings (uncommitted): `.planning/full-audit/backend/FINDINGS.md`.

**Date:** 2026-08-03 (latest) — **QUEUE ITEMS §2 + §3 CLOSED, ON MASTER, NOTHING DEPLOYED — the ⛔ B2b deploy gate stands** (ANY frontend deploy from master ships B2b; next BACKEND deploy ships the §2 fix; each needs a fresh owner OK). Backend **578→589**, Flutter **1224→1229 + 10 skipped**, both count verifiers synced, lint-ratchet held at 816, CI green on `2d3fb81` (backend) — verify `b1893c6` (frontend) before any deploy.
- **§2 (`2d3fb81`)**: `hideMessageForUser` hard-deletes once EVERY participant hid the row (set from the conversation relation, never hardcoded 2; empty/missing set = never delete; media before row). Append is ONE atomic `UPDATE…RETURNING`, killing the read-modify-save clobber race. Three guard falsifications run RED first (every→some; empty-set guard; detach removed). **Discovery: `messages.reply_to_message_id` self-FK has NO ON DELETE (`0001_baseline.sql:757`)** — any hard delete of a replied-to row threw 23503. `detachReplies()` now runs in the hide path, `deleteById` (delete-for-everyone was LIVE-BROKEN in prod for replied-to messages), and the per-minute expiry cron (which would have wedged, media already unlinked).
- **§3 (`b1893c6`)**: `sweepDestroyablePlaintext` logs `PLAINTEXT_SWEEP` to the RING on every acting pass ({expired, retired, removed, ids capped 30}); zero passes log once per transition into nothing-due (per-tick zeros would churn the 200-ring — the 0.1.6 noise class). Unconfirmable-clock silence is deliberate (root §7) and now test-pinned. One zero entry per idle stretch in a dump is CORRECT, not a stuck sweep.
- ➡ Detail: **`2026-08-03-session-queue-items-2-3.md`**. Remaining: owner blockers (keystore backup, `FIREBASE_SERVICE_ACCOUNT` + real push, fingerprint-verify 54 + 90), then Android track.

**Date:** 2026-08-03 — **B2b `sig_*` AT-REST SEALING IMPLEMENTED + DUAL-REVIEWED, ON MASTER (`13a9fd1`), CI 4/4 — ⛔ DEPLOY HELD: needs a dump proving `CANARY_OK {ageDays > 7}` AND a fresh owner OK. Any `CONTENT_KEY_LOST`/canary-lost before then = STOP signal.** All web Signal key material (identity, sessions, prekeys, trusted pins) seals as `fpsig1:<kid>:<b64(IV||GCM)>`; keys `fp_sig_key_<kid>` in the canary-measured IndexedDB store, a SEPARATE namespace from content keys (cross-family test). Async seam → no RAM view (`SealedWebSignalKv` behind new `SigWebKv`, DualStorage memoized opener). Control records cleartext. Analyze clean, verifier OK.
- **Load-bearing rules (each red-proven, 15 mutations total):** `read()` on an unsealable value THROWS, never null (absence = identity regen / ratchet reset / prekey reuse); `readAll()` presence-preserving, never value-throwing — two real callers (`_hasPriorInstallResidue`, `_highestStoredPreKeyId`) swallow enumeration throws into absent-equivalents, both hardened fail-closed (residue inconclusive→present; prekey mint aborted, `PREKEY_MINT_SKIPPED`); fallback legal ONLY on a successful zero-envelope probe, and a fallback session re-probes before EVERY write; drain = ONE lock per row NEVER nested — the reviewed design's batch→per-peer nesting was an ABBA deadlock against the ratchet path's per-peer→lazy-open→sig-keys order (advisory catch); session + trusted rows drain under the per-peer lock; nothing destroyed at open, no retirement for key material, rollback = E2E down but intact.
- **Gauntlet:** design review REVISE (2 CRITICAL: swallowed-throw regeneration, drain-lock ratchet rollback — plus 6 more, all folded); data-loss review **SHIP** (no destruction/deadlock path; 2 LOW self-healing residuals, one fixed at source); spec review **SHIP-WITH-FIXES** (fixed same session, incl. dedicated §5.2/§5.3/§5.7 tests run red).
- Post-deploy expectations (when it ships): `SIG_SEAL_OPEN {sealed, legacy, ms}` ring per boot, `legacy` ~126→0, one `SIG_SEAL_DRAIN_DONE` durable; escalate on `SIG_KEY_UNAVAILABLE` / `SIG_ROWS_UNREADABLE` / post-first-boot `SIG_STORE_FALLBACK`.
- ➡ Detail: **`2026-08-03-session-b2b-sig-sealing.md`**; spec: **`docs/design/web-sig-sealing.md`**.

**Date:** 2026-08-04 — **RELEASED `0.1.8 / c01317c` (0.1.7/faa9dcd superseded same night: add socket now LEADS the comb — trailing socket vanished below the fold with many friends, position test-pinned), FRONTEND ONLY, BRANCH BUILD — smoke 5/5, merge CI 4/4 green. INVITATION UI HEX PASS (two rounds), `feat/invitations-hex-ui` merged to master (`84737bc`) but prod serves the BRANCH commit: owner chose branch-only deploy so the ⛔ B2b gate stays intact — prod frontend (`c01317c`) deliberately ≠ master; the NEXT master frontend deploy ships B2b + §3 and still needs the canary dump + fresh OK (will be 0.1.9).** Round 1: invite-a-friend door in the Chats `+` picker, hex count badges, de-genericized Invitations tab, hex chat-header avatars. Round 2 (owner picked ideas 2/3/4/5): forge animation on accept, hex `+` badge, honeycomb waiting-inbox, hex ping lattice. Branch Flutter 1203 + 10 green; MERGED tree **1236 + 10 skipped** green, analyze clean, count verifier synced (CLAUDE.md §3 1229→1236). Owner approved in the preview harness (`test/preview/invitations_preview.dart`, `?view=inbox|picker|picker-empty|header|ping`). Reviews: designer SHIP-WITH-FIXES (blue-ring HIGH fixed), spec 0 findings, standards 3 P3 left as-is.
- **Shapes:** `HexCountBadge` (`hex_avatar.dart`) = the EXACT pointy-top `hexPath` hexagon for numeric badges (owner rejected a flat-top elongated first cut mid-session: "must be exact hex as rest of them"); `HexPill` (`widgets/hex_pill.dart`) = elongated 120°-cap hexagon, ONLY for word pills (`InvitationStatusPill`). `DashedHexPainter` (public, `hex_avatar.dart`) replaced both private dashed painters in `contact_network_view.dart`, visuals identical.
- **Picker:** comb ends in a dashed "+" socket → new `ChatPickerInviteNew` → `InvitationsScreen` (same door as the Contacts add hex; accept still NEVER inline per §6). Empty picker gets an `Invite someone` button instead of a lone socket (new ARB `chatPickerInviteButton` en+pl). +2 picker tests.
- **Chat header:** both `AvatarCircle`s in `chat_detail_screen.dart` → `HexAvatar` (36 embedded / 52 top-bar) — supersedes the round-4 bare-circle ruling by owner ask.
- **Design review SHIP-WITH-FIXES; HIGH fixed same session:** `HexAvatar` ring = `convItemBorder` at the painter's 0.6 alpha ≈ 1.3:1 on blue — invisible. Touched callsites now ring with `mutedText`. **`conversation_tile` still passes `convItemBorder` — check it on blue.** LOWs left for owner: accent-on-glass in picker hints, "Pending" used both directions, triple-accent accepted card, SPEC §10 title-pill doc drift.
- **Round 2 specifics:** `_ForgeHexAvatar` (invitation_row.dart) plays a 280 ms dash-solder overlay on pending→accepted — depends on the expanded card being a DIRECT sibling of outcome rows (shared peer-id key ⇒ element reuse); `_WaitingComb` (invitations_screen.dart) renders inbound requests as a lattice, tap = expand card, **a lone request auto-expands** (that rule is what kept every single-request test green; `-1` sentinel = user-collapsed); ping rings are const `_PingHexPainter` hexes preserving the file's build-once contract. **Codex-backed `task` subagents died `usage_limit_reached` instantly — Anthropic-only rule reconfirmed; specialist agents (reviewer/designer) were fine.**
- ➡ Detail: **`2026-08-03-session-invitations-hex-ui.md`**.

