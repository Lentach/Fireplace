# Session 2026-05-25

## Accomplished

- **Release 0.0.14** (earlier): trailing send + voice lock-up on `master`.
- **iOS PWA composer keyboard — Phase 1** (plan `docs/superpowers/specs/2026-05-25-ios-pwa-composer-keyboard-send-plan.md`):
  - **P1.1:** `_ComposerTapSendOverlay` — `onPointerDownRetainFocus` + sync `requestFocus()` on trailing send `pointerDown`.
  - **P1.2:** `_send()` — `hadFocusBeforeSend` gates post-frame `showSoftKeyboardIfHidden`.
  - **P1.3–P1.4:** `force` param on `showSoftKeyboardIfHidden`; reply / action-panel / `_requestComposerFocus` use `force: true`.
  - **P1.5:** `TapRegion` with stable `_composerTapRegionGroup` on `ChatInputBarState` (composer row only).
  - **P1.6:** Extended `chat_input_bar_trailing_send_test.dart` (TapRegion, pointerDown focus, `TextInput.show` mock).
  - **P1.8:** `CLAUDE.md` updated (§1, §7, §9).
- **Version:** still **0.0.14** (no bump until manual M1–M3 + M9 pass).
- **graphify** updated after lib changes.

## Key files

| File | Change |
|------|--------|
| `frontend/lib/widgets/input/chat_input_bar.dart` | pointerDown focus, `_send` gating, TapRegion, `sendMessageForTest` |
| `frontend/lib/utils/soft_keyboard*.dart` | `force` param |
| `frontend/test/widgets/input/chat_input_bar_trailing_send_test.dart` | Phase 1 regression tests |
| `CLAUDE.md` | iOS PWA Phase 1 notes + manual QA gate |

## Tests (CI-local)

```text
flutter test test/widgets/input/chat_input_bar_trailing_send_test.dart  → 22 passed
flutter test test/providers/messaging_provider_composer_focus_test.dart → 1 passed
flutter test test/utils/web_viewport_scroll_test.dart → 2 passed
```

## Rollback

If iPhone PWA QA fails: revert to **`087fdb5f664ae85c2760e428c52af380936f44f7`**.

## Manual QA gate (user — iPhone)

**Merge/deploy blocker:** M1–M3 on **Home Screen PWA**; **M9** on Safari tab. Full matrix: plan §6.2.

| # | Check |
|---|--------|
| **M1** | Trailing Send ×5 — keyboard stays, no jump |
| **M2** | IME Send ×5 — same |
| **M3** | After trailing send, type next message without re-tapping field |
| M4 | Tap message list — keyboard dismisses |
| M5–M8 | Reply, action panel, voice lock, rotation |
| **M9** | Safari tab trailing Send ×3 |

On **M1–M3 pass:** bump `pubspec.yaml` to **0.0.15** and deploy. If M1–M3 still fail → Phase 2 (do not bump).

## Notes

- Phase 2 (`visualViewport` inset) **not** started.
- Widget tests do not run WebKit; manual iPhone validation required.
