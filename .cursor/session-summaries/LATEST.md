# Latest session summary

Entries are newest-first, capped at 5 by `.githooks/pre-commit`. Each one links its dated file,
which holds the full account — **rotating an entry out of this file loses nothing.**

> **The "this file is two tickets behind" banner is gone on purpose.** It was `master`'s copy warning
> readers that the multi-device programme lived on a branch. That branch is now merged into this
> file's history, so the banner had become self-referential and its figures were stale ("(a)–(xxxiv)",
> "T1–T8"). **Standing warnings and history from entries this cap has rotated out** — read the dated
> files before touching those areas: the composer/attachment picker
> (`2026-08-19-session-composer-regression.md` — owner: *"we made huge regress on composer and now
> all old bugs are back"*, ship nothing there without a green repro AND his explicit OK, and
> **never `git revert 0cbf17b`**), PWA notifications (`2026-08-20-session-notif-regression.md`),
> the Actions billing/CI economics history (`2026-08-18-session-actions-billing-and-0.1.16.md`),
> and this programme's own T7/T8 tickets (`2026-08-22-session-t7-edit-refan.md`,
> `2026-08-22-session-t8-harness-sweep.md`), the T10 reset-addressability entry rotated out on
> 2026-09-02 (`2026-08-26-session-t10-reset-addressability.md`, spec (xlv): a completed §6.2 reset
> left the account unreachable for the whole programme; **a harness can only find bugs in the shapes
> it builds** — the never-enrolled reset had a live population of exactly 0; hostile
> `nextListVersion` inflation is bounded by a plausibility ceiling), and the Umbra rename/app-icon
> entry rotated out the same day (`2026-08-26-session.md`: brand sweeps MUST eyeball the RENDERED
> page — split-span wordmarks defeat grep; re-verify subagent compliance claims, one fabricated its
> version bump; owner still owes README screenshot recapture, GitHub repo renames, the domain
> decision), and the T11 entry rotated out on 2026-09-02 (spec (xlvi) is recorded in full at
> `docs/design/multi-device.md` §12, amendment 2026-08-26), and the D1/D2 recovery entry rotated out
> on 2026-09-03 (`2026-08-29-session-d1-d2-recovery.md`, spec (xlvii)–(xlix): read its THREE
> addenda — a warning the user cannot act on is a dead end; recovery state must survive a restart), and the
> 08-30b bounded-gate entry rotated out on 2026-09-03 (`2026-08-30-session-bounded-gate-lxiv.md`, spec (lxiv):
> the revoked-device relogin CLOBBER — a `registrationId` may not change under an unchanged identity, the OTP
> replenishment path bypasses `upsertKeyBundle` so BOTH uploads carry the guard; the own-device confirmation is
> per-socket; residual 8 `_reenrollAfterReset` has no in-flight latch, recorded not fixed), and the 09-02a
> FCM/APK entry rotated out on 2026-09-03 (`2026-09-02-session-fcm-e2e.md`: prod FCM proven on the Pixel_7
> emulator; **versionCode floor 10024**; `am force-stop` silently drops FCM; `FLAG_SECURE` blocks screencap;
> verify an installed APK's dart-defines by byte-searching `kernel_blob.bin`; the signed 0.1.24 APK release
> waits on the owner's passcode feature), and the 09-03 evening (lxxii) entry rotated out on 2026-09-06
> (`2026-09-03-session-lxxii-reset-door.md`: **`deploy-web.ps1` overwrites `frontend/build/web` with the PROD bundle — rebuild
> locally before any 8093 check; a mutation runner once left the mutant ON DISK — `git diff` after every mutation script**;
> the owner still owes the thief-with-password matrix answers), and the 09-04 `feat/video-messages` merge entry
> rotated out on 2026-09-05 (`2026-09-01-session-video-messages.md`: **merging an older release branch must never
> roll the pubspec back**; prod served that branch as a test until the 09-02 `0.2.0` deploy, then master's 0.2.x). For
> multi-device specifically the permanent record is `.planning/multi-device/` (`FINISH-HERE.md`, `progress.md`,
> `task_plan.md`), not this file. The 09-05 night (lxxiii)/(lxxiv) entry rotated out on 2026-09-06
> (`2026-09-05-session-optin-lock-gate.md`: the registration lock is OPT-IN — it arms only once
> `account_authorizations` has a row; `DeviceLinkGateScreen` replaced three banners; a client that sees no
> `linkingEnabled` field must fail CLOSED to the old gate; deploy order backend FIRST — done 2026-09-05).
> The 09-05 (late) video-nits entry rotated out on 2026-09-08 (`2026-09-05-session-video-nits.md`: the
> RETRY branch rebuilt a video payload from url/key/iv/duration only, so a once-failed clip reached the
> peer as a 220 px square with a blank poster — the image branch never had that hole; the WebKit
> `videoWidth == 0` probe fix is still **NOT iOS-verified**, the owner owes a recording from the iPhone
> PWA camera; a route pushed over the chat must RELEASE the inline video slot, and CI never runs on a
> plain branch push — only `master` pushes and pull requests).
> The 09-05 (evening→late) video-merge/e2e entry rotated out on 2026-09-08
> (`2026-09-05-session-video-merge-e2e-fix.md`: "merge it and deploy, I've tested it" meant the
> BRANCH, not master — confirm WHICH build was tested; a red `e2e-wire` was three STALE tests while
> a red `e2e-isolated-probes` was a real gap ((lxxv)) — never assume a red harness is the harness's
> fault; **never-enrolled is the DEFAULT population, so a probe can die on its own premise with its
> real assertions never running**; and the attach picker must stay exempt from the passcode lock or
> a 0 s auto-lock loses the pick).
> The 09-06 auth-clarity entry rotated out on 2026-09-08 (`2026-09-06-session-register-409-diagnosis.md` +
> `2026-09-06-session-auth-clarity.md`: a 409 the client could not SPEAK created a duplicate account; `ApiException`
> carries the STATUS and `classifyAuthFailure` maps per door; registration signs the user in and a 409 tries the typed
> credentials BEFORE refusing; **`Desktop/Fireplace` is on `feat/passcode-lock`, not master**, and `deploy-web.ps1`
> exits 1 in a worktree after `PUBLISHED_OK` — run the gate from the main checkout, never `-SkipVerify`).

**✅ DEPLOY STATE — MASTER IS LIVE, BOTH TIERS (2026-09-09): FRONTEND `0.2.34 / c912ae4` (web-only, CI 34311433153 green 5/5, smoke PASSED), BACKEND `0.2.33 / 469f0500` (0.2.34 touched no backend file; backend unchanged since 0.2.24 — `git diff --name-only 8908814..HEAD -- backend/` is EMPTY, so 0.2.25–0.2.30 were web-only deploys and **0.2.31 deployed BOTH tiers (backend `1e73567c`, `/auth/recover` answering 401 on prod for an unknown name)**; 0.2.30 = CI 34288713539 green 5/5; 0.2.29 = CI 34278230233 green 5/5; 0.2.28 = CI 34273754093 green 5/5; 0.2.27 = CI 34265297599 green 5/5; CI run 34252083326 green on all 5 jobs BEFORE the 0.2.26 deploy, 34245056739 before 0.2.25, 34240058258 before 0.2.24). Multi-device v2 (D26, 0.2.23), D27 (0.2.24) and the two post-review clause-6/clause-7 fixes (0.2.25, 0.2.26) are ALL RELEASED. Deploy order was BACKEND FIRST for D26 (migration `0017_identity_backup.sql`) and D27 (`platform`/`identityReplacedTo` wire additions). **ROLLBACK HAZARDS:** never serve a web build below `d446a9d` to a browser with the passcode ON; never below 0.2.23 to an account with a phrase backup (data-loss door); never below 0.2.26 — 0.2.25 gates an install for the life of the process after ONE mistyped phrase ((lxxx) clause 7). Rollback tag `pre-multidevice-master` = `9b6ea1a`; migration `0015` is not code-reversible, `0017` is additive.**

**Date:** 2026-09-09 — **THE MULTI-DEVICE PROGRAMME'S LAST OPEN ASK IS SHIPPED: THE 12 WORDS ARE OFFERED RIGHT AFTER "UTWÓRZ KONTO" (WITH "PÓŹNIEJ") AND EVERY ACCOUNT WITHOUT A BACKUP GETS ONE MUTED LINE AT THE TOP OF CZATY. Spec (lxxxiii), 0.2.34, web-only; the 0.2.33 review's three judgement calls folded; the ~1 ms counter residual recorded as accepted.** ➡ **`2026-09-09-session-c9-phrase-at-the-door.md`**.
- **Owner's framing, which the spec now carries:** PWA users never need to link; the phrase does NOT lock the account (only "Włącz łączenie" enrols — `setRecoveryKey` has no enrolment precondition); for a plain browser user the 12 words mean exactly one thing: forgot password → still get in ((lxxxii) clause 2). Losing the browser on an un-enrolled account still means new keys; contacts still see the muted note. Password recovery, not history.
- **Clause 1:** `AuthProvider.consumeFreshRegistration()` — stamped ONLY on the two `register` paths that end signed in with an account these credentials created (clean 201; lost answer → probe). The taken-but-mine path is an existing account that may already hold a phrase → not stamped (F42). `ConversationsScreen` consumes it once and pushes `RecoveryKeyScreen(deferrable: true)` when `ownKeyBundleStatus` reports `hasIdentityBackup: false` — never on unknown, never on true (F39). **Clause 2:** the screen's generate button is disabled until `isE2EReady` (F41) — `exportIdentityForBackup` throws before `initialize()`; the failure snackbar is context-neutral now (`recoveryKeyBackupFailed` deleted — it claimed "linking was not enabled" on doors that have no linking). **Clause 3:** `BackupNudgeLine` above the list in both layouts, `shouldShowBackupNudge` (`utils/backup_nudge.dart`, F40), snooze `SettingsProvider.snoozeBackupNudge` → prefs `backup_nudge_dismissed_at_<uid>` 7 days; Później / back / X all snooze; a saved backup removes it.
- **Proof:** F39–F43 each 1 substitution, printed, killed, restored (F43 = the review's cache-invalidation-by-convention concern: an in-place append while a filtered view is cached). Ratchet 898 PASS, backend diff empty. **Live on a rebuilt bundle, three fresh accounts:** `c9nudge3` (id 209) register → offer ≤1 s after the status (durable probes, removed) → phrase → Czaty clean → Postgres `recovery_keys` 1 / `account_authorizations` 0 → **`POST /auth/recover` 201 with the words, new password logs in, old 401**. `c9nudge2`: Później → line gone; un-snooze + reload → line back on Grafit, no re-offer; tap → door without Później; X → gone. **Not iOS-verified.**
- **Traps:** a `str.count`/Grep matcher must be CRLF-aware (F42 found 0 occurrences until it was); a cold Chrome profile needs ~15 s before the first form paints; `find.textContaining('dev')` in the footer test also matched "device" in the new subtitle — pinned to `'· dev'`.
- **0.2.35 (BOTH tiers, hours later) — (lxxxiii) clause 4: THE LINE ASKED FOR A BLOB, NOT A PHRASE.** `goonboy` (id 48, phrase from 2026-09-02) got the line and twelve NEW words with no hint they would replace his paper; prod had 2 verifier-only rows and 0 blobs, so 0.2.34 nagged exactly the two people who had already done it. He had not confirmed (row `createdAt` unchanged) — old paper still valid. Fix: `ownKeyBundleStatus.hasRecoveryPhrase` (any `recovery_keys` row; additive; absent = UNKNOWN); the Czaty line and the door offer key off THAT, `hasIdentityBackup` stays for the devices-screen nudge only; both flags now reset in `clearAll()`; the screen shows "Masz już frazę. Nowe słowa ją zastąpią — stare przestaną działać." above the generate button whenever a phrase exists. F44/F44b/F45/F45b killed. Backend 1119/62, Flutter 2094/14. Live: a verifier-only account gets NO line and the red replace warning; a no-row account still gets the line. **Backend deployed FIRST** (a 0.2.35 client on a 0.2.33 server reads the missing field as UNKNOWN → nothing shown).

**Date:** 2026-09-08 (part 5) — **THE FRIEND'S "SERVER ERROR" WAS AN iOS SAFARI SOCKET STALL, PROVEN FROM NGINX + AUDIT LOGS; REGISTRATION NOW SETTLES A LOST ANSWER ITSELF INSTEAD OF DESCRIBING IT (0.2.28, web-only). TEXT-PASS PROPOSAL DRAFTED, NOT CODED.** ➡ **`2026-09-08-session-c8-register-ceremony.md`**.
- **Evidence:** IP `31.175.148.17`, iPhone, UA carries `Safari/604.1` = a browser TAB, never the PWA. 19:03 UTC app load; nothing reaches nginx for 4 minutes; 19:08:23 `login ___` ×3 (401) + `register ___` (201) in the SAME second; 19:08:25 login 201 → shell, 15 min of use as `___#7868` (id 117). iOS froze the tab's socket while he was in Telegram; the first POST after resume hung; our 15 s timeout showed `authStatusRegisterOutcomeUnknown`, which he read as an outage; everything he tapped flushed in one burst. No backend bug; `___` is a valid name (exact match, no LIKE).
- **0.2.28 (`00ee34e`):** `AuthProvider.register` — 201/409 → sign in with the same credentials (unchanged); **lost → sign-in probe; a refusal proves the server is back and the account is not ours, so the register is retried ONCE (its 409 lands in the taken branch); only a second loss is reported.** `login` retries a lost answer once. `AuthStatusCode.registerOutcomeUnknown` + its two paragraphs DELETED; `authStatusServerUnreachable` = `Brak połączenia. Spróbuj ponownie.` / `No connection. Try again.` Worst case 45 s of spinner before the one-liner — accepted.
- **Proof:** mutants F25 (no settle) / F26 (no retry) / F27 (login no retry), 1 substitution each, killed, restored. Suite **2064/14**, CI 34273754093 5/5. Live on a rebuilt bundle with the backend `docker pause`d mid-request: stall → unpause → shell in 6 s (server saw probe 401, flushed register 201, retry 409, sign-in 201 — the race path); paused throughout → the one-liner, ONE register sent; someone else's name → taken + sign-in door.
- **Text pass SHIPPED as 0.2.29 (`eb54641`, web-only)** — owner said "go" on all 68: every non-plural string ≥90 chars is now one line ≤~70 chars PL that says what to do, mechanism removed (full old→new table in the session file). Two shipping adjustments: `passcodeNote` EN keeps "erasing this app's data" (substance test), and the enable-linking dialog's two strings were de-duplicated (they rendered the same sentence twice). Screenshot pass on gate/Settings/Privacy/delete dialog/Devices/enable-linking/passcode; NOT screenshotted: reset-ceremony statuses, revoke/mismatch notices, fingerprint sheet, phrase reveal, note reveal — **owner, look at those on the phone.** Suite 2064/14. The friend stays in a Safari tab (no push; installing the PWA later = new device → gate) — owner: fine for now, native app coming.
- **0.2.30 (`2b66d56`, web-only) — release 1 of the "is it finished" list.** (lxxxi): pre-link `none_for_device` rows are hidden at `MessagingProvider.messages` and the thread shows ONE pill "Historia sprzed połączenia tego urządzenia" (also in the all-hidden empty state). **(lxxxi) clause 2: (lxxx) clause 5 FAILED its first live proof** — wipe → restore still raised the own-account banner, because the FIRST hydration reaches the empty install before any identity exists and reports the row; the own-key branch now RETRACTS the alarm for that same instant. Re-driven silent; the foreign-key arm alarmed live on the old browser. Video self-sync RECEIVE proven by test. Mutants F28–F31 killed. Suite **2071/14**. **Owner decisions recorded in spec, NOT built: 6 h reset + phrase-alone password reset (release 2, waits for his "confirmed" after the combined-effect warning).**
- **0.2.31 (`4ba17ab`, BOTH tiers) — (lxxxii) "THE SEED IS THE ACCOUNT", owner "confirm all" after the combined-effect warning.** §6.2 reset delay **72 h → 6 h** (age floor follows); **`POST /auth/recover`: the 12 words alone set a new password** (same Argon2 verifier + counter + 1 h lockout as the shortcut, never spent; unknown name refused before any verify; 5 / 15 min per IP) and answer with login tokens after waiting out the JWT second (`iat <= passwordChangedAt` would have killed a same-second token — found by test). Client: "Nie pamiętam hasła" → recover form → session → the gate. Mutants F32–F36 killed; live on the local stack via curl and UI. Backend 1116/62 (ratchet floor 898), frontend 2078/14. CI 34294263704 green 5/5 (first run 34293535781 red: e2e asserted the 72 h delay, backend count 1116 vs 1117 — fixed in `1e73567`).
- **0.2.32 (`110c200`, BOTH tiers) — the recover door was a timing oracle:** an unknown identifier answered instantly while a known one paid the 19 MiB Argon2id verify. Now the unknown/ambiguous path verifies against `TIMING_SAFE_DUMMY_ARGON2` (login's bcrypt twin), no counter touched; F37 killed; spec (lxxxii) + root §7 corrected. CI 34295938649 green; prod `/auth/recover` on an unknown name now ~120 ms. Backend 1118/62.
- **0.2.33 (`469f050`, BOTH tiers) — the oracle's second half:** a KNOWN account with no `recovery_keys` row answered instantly too. `verifyRecoveryPhrase` now pays the shared `TIMING_SAFE_DUMMY_VERIFIER` on the no-row branch; the native argon2 module cannot be spied on, so the test asserts the cost (>15 ms); F38 killed. Prod single samples after deploy: unknown 103 ms, known-unenrolled 109 / 122 ms. (A CRLF-blind `sed` missed the pubspec bump — caught before deploy, fixed in `469f050`.) CI 34297265419 green.

**Date:** 2026-09-08 (part 4) — **THE FINAL PRE-SHIP REVIEW OF 0.2.25 FOUND THAT CLAUSE 6's THIRD CHANGE OVER-HELD THE GATE: ONE MISTYPED PHRASE GATED THE INSTALL FOR THE LIFE OF THE PROCESS — THEN LINKING BY QR COULD NOT GET OUT, AND THE NEXT ACCOUNT ON THE SAME INSTALL INHERITED IT. Spec (lxxx) clause 7, 0.2.26, web-only, smoke 5/5.** ➡ **`2026-09-08-session-c7-final-review.md`**.
- **The defect:** `restoreUnfinished` was `_restoreStage` neither `idle` nor `done`, and NOTHING returned the stage to `idle` — not `clearAll()` (which resets every OTHER per-account flag on the provider), not `onConnect(false)`, not another door succeeding. So a restore that failed BEFORE adopting anything (wrong phrase, `exists:false`) held `needsDeviceLink` forever: mistype the phrase, link by QR, the post-rebind init clears `identityIncomplete` — and the gate stays, with nothing left to retry. **The 0.2.25 "kept test" PINNED it**: it failed the restore at `fetching` and asserted the gate held.
- **The fix, 32+/6-:** `restoreUnfinished => _restoreAdopted` (set when `adoptRestoredIdentity` returns, cleared at `done`) — before adopt nothing is half-done and the gate is held by whatever brought the user to it; after adopt the idempotent retry is the way out. `clearAll()` and the `!isReconnect` branch of `onConnect` reset the machine; the §6.2 rebind is `connect()` with the SAME user id, so it is a reconnect and touches neither.
- **Proof:** the two new tests were RED on `4fae484` (`+8 -2`); F23 (hold on `failed` regardless) and F24 (drop the teardown) each 1 substitution, printed, killed, restored. Frontend **2058/14**, analyzer clean, CI 5/5. **Live on a rebuilt bundle:** fresh browser → gate → valid-checksum WRONG phrase ("abandon ×11 about"; a bad-checksum phrase never reaches the machine) → `Fraza nie pasuje do kopii kluczy tego konta` → QR link pasted on the primary, SAS **467 444** both sides → **the shell**, roster `web · #1 · główne` + `web · #2`; logout → second account → shell.
- **Drive technique that worked:** Flutter's `flt-semantics-placeholder` never populated the tree from a synthetic click, so `observe()` was useless; two isolated Chromes on `--remote-debugging-port`, pixel-coordinate clicks from screenshots, CDP `Input.insertText` for typing, and the link code read from the focused field's mirrored DOM `<input>`. **Lesson:** a term added to a gate predicate needs an owner that clears it — read `clearAll()` when adding state to a process singleton; and ask what the user can DO from the state a test asserts. Also: `verify-claude-frontend-test-counts.mjs` now accepts the with-skips TTY line `All other tests passed!`. Noticed, untouched: the gate's reset copy still says "(1 godzinie z kluczem odzyskiwania)" — not false, but noise for a backed-up account; owner's call.
- **0.2.27 (same evening, `8da367c`):** `settingsKeyChangeWarningsSubtitle` PL/EN shortened to the one sentence the owner could not read (the row is two lines + ellipsis; verified in place); the **rename dialog was shoved off the top under the iOS keyboard** (owner's iPhone screenshot) — `_RenameDialog` now holds the composer's scoped viewport pin (`setIOSComposerViewportPin`) while the field has focus, reverted on blur/dispose. **NOT iOS-verified — no device on this box; owner to confirm.** Web-only deploy, smoke 5/5.

**Date:** 2026-09-08 (part 2) — **D27 (0.2.24): THE OWNER CAN NOW RENAME LINKED DEVICES — AND FOUR DEFECTS D26 SHIPPED THAT DAY ARE FIXED, PLUS A FIFTH THE END-OF-SESSION REVIEW CAUGHT: A RESTORE THAT FAILED AFTER ITS ADOPT STEP WAS INVISIBLE **AND PERMANENTLY NON-RETRYABLE**. Spec (lxxx) clauses 1–6, deployed both tiers, smoke 5/5.** ➡ **`2026-09-08-session-d27-rename-and-review.md`**.
- **Rename needed NO wire and NO server change** — that is why it was cheap. `DeviceListEntry.name` has been in the canonical encoding since §3 and `updateDeviceList` already accepts any DAK-signed list at a higher version, so a rename is exactly the `revokeDevice` shape with `name` set instead of `revokedAt`. Non-obvious rules, all test-pinned: arm the DAK from the Keystore FIRST (the lesson `revokeDevice` learned from a live app-proof); a REVOKED row may not be renamed (a tombstone must not spend a list version); an empty field CLEARS the name and must reach **byte-identical canonical output to a list that was never named**, not `""`.
- **NFC was the one design call.** The storage gate refuses a non-NFC name and the encoder has promised since §3 that the client-side normalization lands with the rename UI. It landed as a **REFUSAL, not a normalization**: Dart core ships no Unicode normalizer and the alternative was hundreds of KB of Unicode tables in a PWA bundle to tidy a device label. `isSignableDeviceName` rejects the combining-diacritic blocks that decompose Latin/Greek/Cyrillic (a macOS/iOS paste — the only way these users make non-NFC text) and deliberately leaves alone marks that are NORMAL in NFC for other scripts. Refused BEFORE signing so no version is spent, with copy that says "type it instead of pasting" because "try again" is a dead end. **A live drive cannot find this — CDP types precomposed.**
- **Clause 2 (badge):** keyed on `deviceId == 1`, so after every reset/restore the live primary lost the "główne" badge and the REVOKED device 1 kept it. I saw it on the 0.2.23 drive and mis-filed it as cosmetic; an advisory pushed back, correctly. Now derived as the **lowest non-revoked deviceId** (the row login resolves); the signed list carries no `isPrimary` and must not grow one. **Clause 3 (platform):** the flipped ceremony recorded every device it linked as `unknown`, because `provisioningHello` carried only the id and the ephemeral — optional `platform` added and relayed. **Clause 4 (copy):** two LIVE strings still promised "72 hours, or 1 hour with a recovery key", one of them in the very dialog about to DEMAND a phrase that restores instantly; three dead (lxxiii)-era keys carrying the same promise were DELETED. `recoveryPhrasePromptBody` KEEPS its "72 → 1" — it belongs to the reset ceremony, where that is still true.
- **Clause 5 (self-alarm):** the restored install raised the RED own-account banner about a change that predated the restore, because the wipe destroyed its dismissal watermark. Fix is **content-based**: `ownKeyBundleStatus` gains `identityReplacedTo` and a row ending at this device's OWN published key is ignored, while one ending anywhere else still alarms. **I first reached for the existing one-shot flag and an advisory caught it** — a restore reports `identityChanged:false`, an account with no audit row sends no instant, so the normalizer bails before the flag is consumed and it would sit armed in prefs eating the first genuine replacement. Reverted before it ran.
- **Clause 6 is the one to remember, and it was LIVE in 0.2.23.** `restoreFromPhrase` cleared `_identityIncomplete` the moment adopt returned — four stages early. That flag mounts the gate **that hosts the restore machine's own progress and errors**, so the shell appeared at adopt and any later failure (20 s nonce wait, 45 s upload ack, roster re-sign) reported to an unmounted widget. Invisible, and PERMANENT: re-entry saw a held identity, and a keyless install has neither `deviceMaterialMismatch` nor `identityUploadLocked`, so it threw `already holds an identity` on every retry — leaving a locally adopted identity whose fresh prekeys were never published while peers fetched the stale bundle. Now cleared only at `done` (recorded as `RESTORE_GATE_RELEASED`), and a re-adopt of the IDENTICAL identity is idempotent while a DIFFERENT one is still refused.
- **The test lesson beats the bug:** my first clause-6 regression asserted on the diag marker and the early-clear mutant **SURVIVED** it — it pinned the marker, not the behaviour. The rewrite starts from the REAL gated state (no local identity + a server saying a bundle exists, driven through `checkOwnKeyBundle`) and asserts `needsDeviceLink`; both mutants then fail. **If a mutant survives, the test is the thing that is wrong.**
- frontend **2055/14 skipped**, backend **1102/62**, analyzer clean, both verifiers re-run. Live-driven: rename stored at list v2 (`"name":"Laptop Zósi"`), cleared at v3 with the `name` key GONE, decomposed paste refused pre-signing while the precomposed spelling went through, and after wipe→restore the roster read `web · #3 · główne` with revoked `web · #1` struck through and unbadged. **Clause 5 has no live proof** (the drive account had zero audit rows — nothing to suppress).
- **Traps:** **`String.replace` treats `$` in the REPLACEMENT as special** — my doc patch contained `` {1,32}$` `` (= "insert everything before the match") and spliced a second copy of `CLAUDE.md` into itself twice while I blamed a phantom watcher; use `() => to`. **`git add -A` staged 51 files from the ignored-by-intent `local/` dir** (no rule existed; my first `check-ignore` read was wrong) — `local/` + `frontend/test-output.txt` are ignored now. **The `edit` tool needs a tag from a `read`, never from `grep`** — two hunks landed in the wrong function and one corrupted `_confirmRevoke`. **CI's lint ratchet is a release gate**: hand-formatted backend edits took prettier deviations 157→171 and failed the run AFTER the tests passed; fixed by prettier-ing only the 15 files the release touched. Dependabot moved master 5 commits mid-session (incl. a firebase major) → rebased twice and re-ran everything. Docker's host mount wedged (`mkdir /run/desktop/mnt/host/c: file exists`) until a full Docker Desktop restart + `wsl --shutdown`.

**Date:** 2026-09-08 (after the D26 deploy) — **DEPENDABOT SWEPT: 7 SECURITY ALERTS CLOSED IN ONE LOCKFILE EDIT, 17 PRs RESOLVED ACROSS THREE WAVES (ONE PARKED FOR THE OWNER), EVERY REFUSED MAJOR CARRIES ITS BLOCKER, AND `dependabot.yml` WAS MANUFACTURING UNMERGEABLE PRs — THAT FIX IS STILL UNPROVEN. PLUS A PC HEALTH PASS. ~~⚠ MASTER NOW CARRIES UNDEPLOYED DEPENDENCY CHANGES ON BOTH TIERS~~ — RESOLVED the same day: the 0.2.24 (D27) deploy shipped them, so the deploy-state block above now describes a master that INCLUDES this sweep.** ➡ **`2026-09-08-session-dependabot-sweep-and-pc-health.md`**.
- **The owner's "7 conflicts" was literal on both counts:** 7 open security alerts (fast-uri ×4 high, qs ×2, browserslist), and the three fixing PRs (#160/#161/#162) all rewrite `backend/package-lock.json`, so merging ANY one conflicts the other two. Fixed as ONE `npm update`-based lockfile commit (`e26c3ac`) that also swallowed dependabot's whole backend-minor-patch group (#154): multer 2.3.0 (4 CVEs), pg 8.23.0, typeorm 1.1.1, undici 7.29.1, `@nestjs/*` 11.2.3, jest 30.5.1, eslint 10.10.0, typescript-eslint 8.70.0. Note the three targets were all TRANSITIVE (none is in `backend/package.json`), so a plain `npm install` moves nothing — it took `npm update fast-uri qs browserslist`. **17 PRs closed across three dependabot waves; only #174 left open, on purpose (see below).**
- **Applied majors, each proven rather than hoped:** firebase_core 4 + firebase_messaging 16 — **the gradle read is NOT the proof and an earlier draft of this entry got it wrong**: both plugins read `project.ext.compileSdk/minSdk/targetSdk` (`build.gradle:43-47` / `:42-45`), i.e. they INHERIT our 36/24/36; the 34/23/Java-17 numbers live in their standalone `local-config.gradle` fallback. Unlike `permission_handler_android` 14.1.0, which HARDCODES `compileSdk = 37` + AGP 9.0.1 + Kotlin 2.3.20. The actual proof is a green **`flutter build apk --debug`**, because `push_service.dart` returns early on `kIsWeb` and the host suite therefore proves NOTHING about this surface. (Also: only removed Dart API is `sendMessage()`, unused; no Firebase JS pinned in `web/`.) google_fonts 6→8 (both majors only DELETE font families; Inter/Archivo/PressStart2P are on neither list) — and because **a green suite cannot see a silent font fallback**, rendering was proven twice: real per-weight families from the API, and the served release bundle fetching five `fonts.gstatic.com` TTFs in a headless browser. Also flutter_local_notifications 22.3.0, emoji_picker_flutter 4.5.4, globals 17.
- **Refused with hard evidence, all written into the PR threads:** `@types/node` 26 (runtime is `node:22-alpine` in BOTH Dockerfile stages — types must not describe APIs the runtime lacks); Nest 12 (`@nestjs/throttler@6.5.0` still peers `^11` only → it would break the WsThrottlerGuard contract; dependabot's partial bump failed its own CI); **firebase-admin 14 — probed 14.3.0 and `admin.messaging`/`admin.credential` are `undefined`**, the legacy namespace is gone, so `push-notifications.service.ts` needs a modular rewrite whose unit tests (module-level mocks) would go green even if it were wrong; undici 8 (`feat!: enable h2 by default` on the SSRF-pinned link-preview path, zero advisory benefit); typescript 7 (ts-jest peers `<7`, typescript-eslint `<6.1.0`); permission_handler 13 (`permission_handler_android` 14.1.0 wants compileSdk 37 + AGP 9 + Kotlin 2.3); sqlite3 3.x/drift (**sqlite3 3.x DELETED `package:sqlite3/open.dart`**, imported by `content_db.dart` for the SQLCipher open override); clock/build_runner (pinned by the Flutter SDK's `flutter_test`/`meta`).
- **`dependabot.yml` was the real bug (`084774e`) — AND THE FIX IS UNPROVEN, do not inherit it as settled:** the `ignore` rules for drift/drift_dev/sqlite3 have been in the file since 2026-08-03, yet #157, #169, #171 AND #173 each bundled them anyway and every one was **unresolvable** (dependabot-core #7523 / discussion #11962 for a group without explicit `patterns`). `084774e` removes the `frontend-minor-patch` group and adds `clock` + `build_runner` with their Flutter-SDK-pin reasons — **but #173 was opened AFTER that push, with base sha `084774e`, still titled "frontend-minor-patch group" and still carrying the same blocked five.** So the yml may not be the enforcement point at all; `@dependabot ignore this major version` comments (stored server-side) were used on #155/#156 as belt-and-braces, and the shape only proves out on the NEXT dependabot run. Same commit **CORRECTED** the drift/sqlite3 rationale: the recorded "native-assets hook needs CMake + MSVC C++ on every local `flutter test`" does NOT reproduce (the host suite runs fine with sqlite3 3.5.2); the real blocker is the deleted `package:sqlite3/open.dart`.
- Verified: backend `nest build` clean, **jest 1096/62**, `npm audit` **0 vulnerabilities**, lint ratchet PASS (894/157); frontend analyze clean, **2027/14**, `flutter build web --release` OK, `flutter build apk --debug` OK; **CI green on `279df93`** (CI + CodeQL).
- **PC pass (two read-only diagnostic agents, then only zero-regret actions):** killed a runaway `dllhost.exe` that had burned **4,033 s of CPU since 06:09** (~one full core, orphaned AppID surrogate); freed **9.06 GB** of leaked `flutter_tools.*` scratch dirs (`%TEMP%` 12.6 → 2.2 GB); upgraded the safe set (gh 2.100, **.NET 8 → 8.0.30**, CMake, App Installer). **Two things are actually wrong and need the owner:** (1) **NOTHING is protecting this box** — Defender stood down for Kaspersky 21.24 and Kaspersky's service never starts (`AMRunningMode = Not running`, zero `avp*` processes); (2) **5 × Kernel-Power 41 / 6008 pairs since 07-12 with no BugCheck** = power loss or hard reset, not software. Machine is capacity-bound: 16 GB RAM (530 MB free with a browser at 4.8 GB), 4 cores measured 100%/83% with no throttling and High-performance already active, and a 118 GB second SSD sitting **99.9% empty** while every cache lives on C:.
- **One PR is deliberately still OPEN: #174 `file_picker` 11.0.2 → 11.0.3.** That is the attachment picker (`chat_action_tiles.dart`), and `utils/web_file_input.dart` exists precisely BECAUSE of file_picker 11.0.2's web DOM behaviour (it styles the `<input>` `display:none` and REMOVES it from the DOM). The 2026-08-19 composer rule applies — nothing ships there without a green repro and the owner's explicit OK, and no host test can see that path. `dependabot/alerts` open = **0**, `code-scanning/alerts` open = **0**. **Do not trust `gh run list --branch master`** — it returned weeks-old rows twice this session; use `gh api repos/Lentach/Fireplace/commits/master/check-runs`. Green run covering the frontend bumps is `084774e`'s (090b67f's and e26c3ac's were cancelled by the next push).
- **Trap paid for:** the `edit` tool resolves a RELATIVE path against the WORKSPACE ROOT (`Desktop/Fireplace`, on another branch), not the cwd — an edit landed in the wrong worktree. In this multi-worktree checkout, give `edit` an ABSOLUTE path. Also: master moved (D26 merged + deployed) mid-session, so the work was parked, rebased and re-verified from scratch.

- **Still binding, from the rolled-off 2026-08-21 T6 entry** (full text in `2026-08-21-session-t6-revocation.md`, closure in decision record §11, settlement spec §12 **(xxi)–(xxix)**): **a LOCKOUT lives in this area — login used to hardcode `deviceId = 1` (`auth.service.ts:74`), so the moment a reset revoked device 1 the correct password minted a token for a revoked device and both session gates correctly refused it.** Login resolves the LIVE PRIMARY (`DevicesService.resolveLoginDeviceId`); the regression test names the lockout. **The session gates deny only on an EXPLICIT `revokedAt` (xxii)** — a MISSING `devices` row must NEVER deny, or the whole pre-Phase-1 install base loses access; that is deliberately the inverse polarity of `DevicesService.isActive`, which gates key-material uploads and must fail closed on absence. **Both predicates stay; do not unify them.** **I6 silence is a separate rule from rejection (xxiii):** `getServedMessageIds` answers a revoked device with NOTHING — an empty list would mean "destroy all of them". **`account_authorizations` is REPLACED, never dropped (xxix)**, and the replacement is admitted only when the STORED enrollment no longer verifies under the account's CURRENT published identity — self-verifying, so no flag and no nullable state (this is the hook T10's (xlv) finally used). **The accept-side gate ((e)/(xxvii)) withholds on VERIFIED data only and its refusals are never terminal**; when the fetch itself FAILS, withholding applies to `originDeviceId >= 2` only, or one broken `getDeviceList` answer silences every conversation of a single-device account. **⚠️ THE APP-PROOF EARNED ITS KEEP:** the revoke button failed live with NOTHING in the server log — `revokeDevice` never armed its DAK from the Keystore, so `signList` threw before any emit, **and the unit suite was green because it pre-armed the engine — a test that could not fail.** Drive the production path. **From the rolled-off 2026-09-06 passcode entry** (`2026-09-06-session-passcode-wake-curtain-merge.md`): wake delivers `hidden → inactive → resumed` and the `_away` latch stamps a departure ONCE; the privacy curtain is the DOM `#fp-curtain`, not a Flutter widget (the Flutter one failed the burst-screenshot proof); open at the owner's call: wall-clock throttle, un-zeroed rekey KEK, `fpwk1:` AAD, non-extractable CryptoKey, Android padlock-during-fullscreen audio.
