# Latest session summary

Newest first, **max 5 entries, each ≤ 900 chars** (`.githooks/pre-commit` enforces 5 and a hard 1000). Each entry links its dated file, which holds the full account. **Standing traps live in `docs/agents/traps.md`** — one line each, grouped by area; grep it for the area you are about to touch. Rotating an entry out of this file: move its `## Traps` lines to `traps.md` if not already there, then delete the entry. No banner, no history paragraphs here.

**DEPLOY STATE (2026-09-12 late):** frontend `0.2.41 / 9d13d25` (web-only via `deploy-web.ps1`, smoke PASSED incl. the `main.dart.js` contains-9d13d25 stale-build gate; code gate = `a3efb73` CI 34710950813 green 6/6, head `9d13d25` is docs-only so only `Analyze` reports there), backend `0.2.41 / 49c77c10` (deployed 18:17Z via `deploy-backend.sh`, gate `30247d5` 6/6; multer 2.3.0 live; `/health` ok). Both tiers now read 0.2.41 — the earlier label skew is gone. Rollback hazards + tags: `docs/agents/traps.md` § Deploy. Re-verify with `/version.json` + `/version` before trusting this line.

**Date:** 2026-09-14 — **The APK is on the owner's phone as device #11 of `bob208`, and sideload updates are proven data-preserving and forward-only.** Clean install (nothing was there before), §6.1 lock fired on real hardware, link ceremony JOINED the account identity (same `identityPublicKey` as devices 1/5, `identity_change_audit` empty). E2E text+voice both directions; self-sync seen AND heard on the iOS PWA primary. The field test found the list previews printing raw `[encrypted]` on a freshly linked device — 0.2.44 (`c7bcee7e`, CI 6/6, suite 2126/14) adds `sentinelPreviewText`, then `install -r` 20043→20044 on the phone kept session, identity and history (device back online 43 s later, no login). NOT run: push-with-app-killed, image. ➡ `2026-09-14-session-apk-field-test-list-preview.md`.

**Date:** 2026-09-13 (part 4) — **0.2.43: unreadable rows explain themselves, a chat that cannot send says so before you type, and a bounced row says why.** `[encrypted]` / `[Decryption failed]` no longer render — one shared `sentinelDisplayText` maps both (every body surface delegates; deciding separately let the long-press replica contradict the bubble). `PeerIdentityChangedRow` is no longer gated on `keyChangeWarnings` (a stale anchor blocks every send) — re-points falsification F11 (**owner-approved**, (lxxix) narrowed). A two-axis review then caught the bounced-row reason being keyed on the PEER alone, blaming a key change for any failure in that chat; it now needs that ROW's own `AccountIdentityMismatch` verdict. The §9 loop caught error-red unreadable on the blue sent fill. 2120 tests green, lint floor 3174→3172. NOT device-verified. ➡ `2026-09-13-session-unreadable-row-copy.md`.

**Date:** 2026-09-13 (part 3) — **The post-wipe decryption fork is SETTLED; the APK is NOT blocked by it.** Four `pm clear` → re-login cycles on `7818786…` against prod: EVERY failing row carries `notifyPeer: true` (`rule: identityReset` in the minting process — `needsKeyUpload` is NOT cleared by the bundle upload, correcting the handoff — and `badMac` + durable `SESSION_RESET` in a later one), and a socket joined to the peer's own user room received `sessionRebuildNeeded {fromUserId}`, including the server's pending replay. No receiver-side defect exists. What blocks the chat is the PEER's identity anchor: their first send bounces `AccountIdentityMismatch` until they confirm the new safety number — by design (xlvi). Sending first does not help; a recovery phrase only helps an ENROLLED account. ➡ `2026-09-13-session-post-wipe-fork-settled.md`.

**Date:** 2026-09-13 (part 2) — **The owed device drill PASSED on the shippable `7818786…`; distribution is unblocked on that axis.** Confirmed the binary under test by hashing the on-device `base.apk`. Push wakes a KILLED app in **3.16 s / 3.29 s** (notification `when=` minus a device-clock read of the send); the tap opens the right chat; and in the SAME process that consumed that tap, logout → password-only login as an ENROLLED account lands on the link gate with nothing over it, scanner still up after 27 s. Also **reproduced the open post-wipe `[Decryption failed]` bug** and settled its discriminator — `kind: noSession, hadSession: false`, receiver-side, NOT bad-MAC; root-cause candidate: `hadIdentityReset` is an unpersisted in-memory flag. 3 throwaway accounts deleted. ➡ `2026-09-13-session-apk-drill-rerun.md`.

**Date:** 2026-09-13 — **APK smoke-driven on prod; a user-visible defect found AND fixed; 0.2.41 → 0.2.42.** 0.2.41 passed smoke 1/2/3/6: register, E2E both legs, push-with-app-killed (tap opens the RIGHT chat — open since 09-02), LINK ceremony (SAS `334 092`, `android · #2`, self-sync). **#175: the link gate was coverable** — a `Stack` child whose `popUntil` fired once on the gated edge, while sticky cold-start reads replayed one account's tap into the NEXT account. Fixed across `23be77d` (NavigatorObserver sweep + launch latch), `fa97358` (`getInitialMessage()` arm) and `633f4dd` (per-login `onTokenRefresh` cancel + the "once per app run" comment that was the wrong model). 3 tests, suite 2112/14, CI 6/6 on `633f4dd`. **Ship `7818786…` only** (two earlier 0.2.42 builds are not-shippable); device drill still owed on it. ➡ `2026-09-13-session-android-release-research.md`.

