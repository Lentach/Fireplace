# Memory workflow update

**Date:** 2026-06-28

## What was done
- Added an explicit "Thinking with files" rule to root memory so future multi-step/debugging/deploy-sensitive work uses persistent file-backed planning (`task_plan.md`, `findings.md`, `progress.md`, or `.planning/<task>/`) instead of relying on chat memory.
- Added an explicit auto-review model rule: auto reviewers/code-reviewers/review subagents/PR reviewers must use the same model class as the primary agent for the session, not `mini` or another downgraded model unless the user explicitly asks for that downgrade.
- Strengthened the blunt-truth tone rule: no "nice guy" softening, no lazy fact-checking, no corporate mush.
- Kept `AGENTS.md` and `CLAUDE.md` aligned.
- Checked the stale "message editing PR open" note. PR #16 (`feat/message-editing`) is merged into `master` (`bf44c6a`, GitHub merged 2026-06-23). The code and tests are present on current `master`; the old summary line is stale.
- Important finding: production backend `/version` currently returns `{"version":"0.0.2","gitCommit":"dev","buildTime":""}`, which means the VM is likely running the dev compose path again, not the truthful production backend image. Frontend `/version.json` reports `0.0.74` while repo is `0.0.75`.
- Ran a freshness audit. Local `master` and `origin/master` are current at `6847ac6`; local `frontend/build/web/version.json` is `0.0.75`. Production is stale: VM checkout is behind `origin/master` by 4 commits, `frontend-build/version.json` on the VM is `0.0.74`, and backend is running `NODE_ENV=development` with `npm run start:dev` (`/version` = `0.0.2/dev`). DB schema has `messages.editedAt`, so the edit-message column exists.

## Key files
- `AGENTS.md`
- `CLAUDE.md`
- `.cursor/session-summaries/2026-06-28-session.md`
- `.cursor/session-summaries/LATEST.md`
- `backend/src/messages/message.entity.ts`
- `backend/src/chat/services/chat-message.service.ts`
- `frontend/lib/providers/messaging/messaging_provider.actions.dart`
- `.planning/2026-06-28-freshness-audit/`

## Verification
- `git diff -- AGENTS.md CLAUDE.md .cursor/session-summaries/2026-06-28-session.md .cursor/session-summaries/LATEST.md` — reviewed docs-only diff.
- Tests not run — documentation/memory-only change.
- `git merge-base --is-ancestor bf44c6a HEAD` — passed; message editing merge is in current `master`.
- `Invoke-RestMethod https://api.github.com/repos/Lentach/Fireplace/pulls/16` — PR #16 is `closed`, `merged: true`, `merged_at: 2026-06-23T03:23:57Z`.
- `Invoke-RestMethod https://fireplace.ignorelist.com/version` — backend reports `0.0.2/dev`, not a truthful prod deploy.
- `Invoke-RestMethod https://fireplace.ignorelist.com/version.json` — frontend reports `0.0.74`.
- `git fetch origin --prune; git status --short --branch; git rev-parse --short HEAD origin/master` — local and origin are both `6847ac6`; only local `.planning/` scratch files are untracked.
- `gcloud compute ssh olek292@fireplace-server --zone europe-central2-a --command <read-only audit>` — VM checkout `e1cff3f`, `origin/master` after fetch `6847ac6`, behind by 4; backend env `NODE_ENV=development`, `APP_VERSION=0.0.2`, `GIT_COMMIT=dev`; process `npm run start:dev`; VM frontend build `0.0.74`; DB columns include `editedAt`.
- `Invoke-RestMethod https://api.github.com/repos/Lentach/Fireplace/pulls?state=open` — open PRs: 0.

## Notes for next session
- Use file-backed planning for anything with multiple phases, debugging, deployment risk, or enough tool work that context drift can bite.
- Auto reviewers must use the same model class as the primary agent. No `mini` reviewer unless explicitly requested.
- Keep bluntness useful: direct verdicts, real evidence, no lazy guessing.
- The edit-message PR is not the concern anymore. The concern is deployment hygiene: confirm VM compose mode/schema before assuming production matches current `master`.
- To make production newest: backend on VM should use `cd ~/fireplace && ./deploy-backend.sh`; frontend should be deployed from this PC with `.\deploy-web.ps1` (never build Flutter on the VM).
