/// Stub — a native install is by definition "installed": there is no browser
/// tab whose storage a cache sweep could evict (multi-device amendment
/// (lxxiv) clause 1).
bool isInstalledDisplayMode() => true;
