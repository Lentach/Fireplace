# Latest session summary

Entries are newest-first. Each one links its dated file, which holds the full account.

**🗺 START HERE IF YOU ARE LOST: `2026-08-05-HANDOFF-audit-state.md`** — plain-language map of the 2026-08-04/05 full-codebase audit: what was found, what merged, what is deliberately parked, and the three owner-only decisions. The two ordered task queues are `.planning/full-audit/REMAINING-WORK.md` (backend) and `.planning/full-audit/frontend/REMAINING-WORK.md` (frontend), both local-only/gitignored. Older long-form: `2026-08-03-HANDOFF-post-b2b-state.md` (B2b gate detail + traps; **its work queue is DONE**) and `2026-08-03-HANDOFF-signal-grade-queue.md` (dated addenda).

**✅ DEPLOY STATE — FRONTEND `0.1.16 / c7c2d0e` LIVE 2026-08-18, BACKEND `0.1.11 / 91535317` (unchanged since 08-16 14:20Z; every release after it was frontend-only — the lag is CORRECT). Master == prod. Smoke 5/5; live `main.dart.js` md5 `bae5fc6d8a69d665b065d03bb11eb426` == local, Giphy key present in the live bundle.** Release train: 0.1.10 (identity guard + forensics) → 0.1.11 (video batch + migration 0012) → 0.1.12 (media picker; also carries §5.4 PRs #137/#138/#139) → 0.1.13 (§5.4 post-deploy-review followups, PR #141) → 0.1.14 (attachment sheet, PR #142) → 0.1.15 (flattened picker, PR #143) → 0.1.16 (iOS popover anchored to the paperclip, PR #145). **⚠️ Post-deploy follow-up still owed from 0.1.11:** the owner overrode the device-A/B gate on the composer motion — verify on his real iOS/Android PWA and be ready to revert the two motion commits (see the video entry below). **⚠️ Also owed from 0.1.16:** only the owner's iPhone can confirm the attachment popover now opens AT the paperclip — desktop cannot reproduce popover anchoring. **⚠️ `deploy-web.ps1` exit-21 silent publish halt** — build succeeds, log dies at the "Publish via ssh/scp" banner; manual staged publish is the workaround (10 releases running) and worktrees need the gitignored `deploy-web.config.ps1` copied in before building.
- **0.1.10's guard + forensics are live inside 0.1.11:** empty keystore → server check; exists → consented-recovery banner, no silent re-mint; UNKNOWN → defer + retry. `BOOT_MARKERS {ls,idb,cache}` + `storage.estimate()` recorded before keystore creation. **Silent identity churn is dead as a mechanism.** Users must fully close + reopen the PWA (never reinstall); Safari can take ~14 h.
- **⚠️ Three 08-14 traps.** (1) `.\deploy-web.ps1 | tail` **swallows a publish-stage failure** — build ok, nothing published; re-run with output redirected. Staged swap = nothing half-ships. (2) **The footer's VERSION half is a live `version.json` fetch and LIES about the running bundle; only the COMMIT half is compiled in** (`auth_screen.dart:205-207`) — `0.1.9 / c01317c` means old JS. (3) **NO `Cache-Control` on any entry file** (ETag only) ⇒ heuristic freshness, no revalidation; the Safari PWA took ~14 h to flip. Queued fix is `no-cache` on the static root — **Flutter output is NOT content-hashed**, so `immutable` by extension would bake a bundle in forever, helping only the NEXT deploy.
- **⚠️ THE CANARY GATE IS NOT EVIDENCE.** `flutter_secure_storage_web` 1.2.1 is `localStorage`, not IndexedDB+WebCrypto, and keeps its AES master key there raw+extractable (`flutter_secure_storage_web.dart:110-116`), so the canary compares localStorage to a localStorage shadow — `CONTENT_KEY_CANARY_LOST` is ~unreachable and `ageDays` never was clearance (one arm, 07-30).
- **Backend: DEPLOYED 2026-08-16 14:20Z at `91535317` (0.1.11, migration `0012` applied).** Carries `checkOwnKeyBundle` (read-only, caller-only, throttled, consumes no OTP) from 0.1.10.

**🔴 `[Decryption failed]` / IDENTITY LOSS — OPEN, CAUSE STILL UNIDENTIFIED. ➡ READ `2026-08-16-HANDOFF-identity-loss-consolidated.md` FIRST.** It supersedes `2026-08-15-HANDOFF-identity-loss-unproven-cause.md` (whose central hypothesis is now ANSWERED — see below) and `2026-08-14-HANDOFF-post-0.1.9-decryption-failed.md` (premise false). **PROVEN:** empty store → login → `initialize()` `absent` branch → `_generateKeys()` → `[identity-churn]` (+2 s, observed 4×) → all prior sessions dead → peers' older messages permanently `[Decryption failed]`. The logout is client-side (54's refresh row is still in the table, never revoked; **refresh tokens do NOT rotate**, `refresh-tokens.service.ts:57-77`, so the client lost or misread `flutter.refresh_token` itself). **ELIMINATED 2026-08-16:** (1) **quota — dead by measurement**, 54 has **25 messages** against a 2000-record cap and 26 sig rows, tens of KB against ~5 MiB, and a failed `setItem` throws and leaves the existing value intact; (2) **silent misread of the identity — dead**, every value is `json.encode`d so the pinned `shared_preferences_web-2.4.3`'s two silent-null vectors cannot fire, and `loadFromStorage` has no catch, so `absent` requires GENUINE emptiness; (3) **two-container theory — refuted** for the 08-14 churn, the push POST 1 s after that login UPDATED the row created `2026-05-13` (upsert unique on `endpoint`, client only calls `getSubscription()`, and on iOS only a standalone PWA can hold a subscription) so the loss happened INSIDE the long-lived Home-Screen PWA; (4) **eviction — excluded by the engines' own policy**, WebKit `performEviction` skips `isPersisted` origins and ITP exempts Home-Screen apps, and both users report `granted: true`. **THE ONE UNVERIFIED PREMISE: was the bucket actually persistent at the moment of loss?** The app records exactly that into a log that dies with the storage, and `initialize()` runs at `encryption_provider.dart:819` BEFORE the persist probe at `:849`. **Every iOS discriminator is architecturally blind** (push is out-of-bucket in `webpushd`; 167 `/web-push-sw.js` fetches in 14 days, 165 Android, **0 iPhone**; the diag log and HTTP cache prove nothing) — **so work user 90 on Android, where both signals are live.** ⛔ NEW RETRACTIONS, MINE: "the unchanged push endpoint proves the bucket survived" (false on iOS) and "quota explains the logout via a stale refresh token" (false, no rotation). **Fix designed, NOT authorised, NOT written:** tri-state guard on the `absent` branch (exists ⇒ refuse + consent via `regenerateIdentityAfterConfirmedLoss()`; none ⇒ generate; **UNKNOWN ⇒ defer**), via a NEW read-only socket event — **never reuse `fetchPreKeyBundle`, it consumes an OTP**. ⚠️ **Open design problem against that fix: `key_bundles` is unique on `userId` with no `deviceId`, so ANY legitimate second-device login hits the "bundle exists" branch** — today it silently churns, so this app is already single-device at the crypto layer; the prompt must be worded and tested for that case, and **whether some of the eight churns are second-device logins rather than wipes has never been audited.**
**⤷ 08-16 UPDATE: handoff §10.1 EXECUTED — storage loss is confirmed for USER 54 ONLY.** Verdict table in `2026-08-16-session-churn-audit.md`; handoff §4.2 + §10.1 amended in place. Of nine dated churn events: **4 proven non-wipe** (43 by Chrome/150 vs his usual /151; 76/92 by an explicit logout + account switch; 58 owner incognito), **3 wipes — all user 54** (08-03 + 08-14 proven same-container, 08-15 probable and deeper — his push subscription died with it), **2 UNKNOWN — both user 90** (cold cache fits fresh context AND dormant-profile eviction). The "two engines both losing storage" inference is UNPROVEN, not forced; the evidence-backed question is iOS-only.
**⤷ 08-16 UPDATE 2: THE §5 FIX IS AUTHORIZED, BUILT, REVIEWED AND DEPLOYED (0.1.10 → live).** The root-cause question (what empties user 54's container) stays open but is now INSTRUMENTED: his next wipe produces a banner instead of a churn, plus a `BOOT_MARKERS` triple answering §10.3 (whole bucket vs localStorage-only). **§5.4 is closed AND LIVE** (PRs #137/#138 shipped inside 0.1.12; post-deploy-review followups PR #141 shipped as 0.1.13): storage read errors are no longer logouts, locally-derived session ends can no longer delete stored tokens, and both repeat-prone durable records (`AUTH_SESSION_END` locals, `AUTH_TOKENS_UNREADABLE`) are deduped so they cannot evict the wipe forensics. Live-backend fault-injection proof is permanent CI (`test_e2e/auth_token_fault_injection_test.dart`). The 54 password reset is MOOT — he logged in with his old password.

**🔴 FCM IS DISABLED IN PRODUCTION.** `FIREBASE_SERVICE_ACCOUNT` is absent from `~/fireplace/.env`, so every backend boot logs "FCM disabled". `google-services.json` IS tracked, so an APK would register tokens the server can never push to — **Android notifications are dead until that var is set.** Web Push VAPID is present, so the PWA is unaffected, which is why this went unnoticed. Fix it before any APK work.

- **Still binding (applies to every entry below):** never run `dart format lib/` (format only what you edited); **ask before opening the browser tool**; `node scripts/lint-ratchet.mjs` runs ONLY in CI — run it before pushing backend changes; `cmd | tail` hides exit codes; never re-derive test counts by arithmetic across a merge; **never give `flutter test` a file list** (per-argument compile cost: 45 files timed out past 11 min, the whole suite runs in 170–310 s); `flutter run -d web-server` serves a blank scaffold once its hot-restart client is lost — restart the process, do not reuse the tab; the E2E register throttle is 10/hr/IP in memory, so `docker compose restart backend` between full `test_e2e` runs and allow 2–5 min to boot.
- **Still binding, from the rolled-off 2026-08-04 entry** (full text in `2026-08-04-session-frontend-audit-fixes.md`): **`refreshSession` timeout maps to `SessionRefreshTransientException`, NEVER `Invalid`** — transient holds the session, invalid clears local auth, so the wrong mapping manufactures the very logout this repo keeps chasing (see the 08-14 entry). **Message send has no ack timer and must not get one** (only invitations do, `_kInvitationAckTimeout`): failing a send the server received releases the exactly-once latch and duplicates it — why FE-024 was skipped deliberately. **wasm nearly deleted the multi-tab Signal lock** — `session_cross_context_lock.dart` + `storage_persist.dart` key on `dart.library.html`, FALSE under `--wasm` ⇒ stub, no lock, no diagnostics, while the probe stays green.
- **Still binding, from the rolled-off 2026-08-05 entry** (`2026-08-05-session.md`): **⛔ DO NOT room-address `newMessage` / `messageEdited`.** Signal decrypt is NOT idempotent and tabs share one session store, so a second tab decrypting the same ciphertext fails into the session-destroying policy. Both use `emitToNewestTab`; push suppression reads that SAME socket. Lifting it needs decrypt-idempotency proof AND both moved room-wide in ONE change.
- **Still binding, from the rolled-off 2026-08-15 entry** (full text in `2026-08-15-session-decryption-failed-root-cause.md`): **the owner's rule is investigate and PROVE, then ASK before writing code — diagnostics and instrumentation count as code.** `4beb1bd` (terminal `missingPreKey` class) was landed on a symptom, reverted at `409c23a`, and **must NOT be re-landed as a first move** — the mechanism was proven to never be in that code path. **⛔ 0.1.9 sealing is CLEARED — do not re-audit** (drain round-trips + fail-closed opens; the checklist is in the dated file). **Three real defects remain OPEN, found statically, none fixed:** (1) `_decryptedLedger` has no `add` path so the spent-key guard at `decrypt.dart:984` is inert within the first-decrypt session; (2) no re-entrancy guard on `retryDecryptActiveConversation`; (3) ratchet consumption and plaintext commit are not atomic (`_persistDecryptedContent`). Parked: six `EXPIRY_STAMP_MISS` on ruchens69's dump.
- **Still binding, from the rolled-off 2026-08-18 0b entry** (full text in `2026-08-18-session-phase0b-registration-lock.md` + `2026-08-18-session-0b-livefire-and-gate-review.md`): the identity lock's signature verifier is **`curve25519-js` on alpine/musl** with a REAL-Flutter-client vector pinned as a backend test — a dependency swap fails loudly; `synchronize` DROPS any index the entity does not declare (proven live); **⚠️ pre-existing flake NOT from this program:** `chat_input_bar_attachment_test.dart` "video-then-caption ordering" fails ~2 runs in 3 and reddens CI intermittently.
- **Still binding, from the rolled-off 2026-08-18 billing/0.1.16 entry** (full text in `2026-08-18-session-actions-billing-and-0.1.16.md`): the repo is PUBLIC since 2026-08-18 (Actions billing wall; pre-flip secret audit clean); **`/actions/runs/<id>/timing` lies (`total_ms: 0`)** — compute billable minutes from job start/complete; **a 404 is NOT proof of revocation** — probe with a known-good credential on the same URL shape first; CI `paths-ignore` skips prose-only commits but CLAUDE.md stays gated so the count verifiers run; if jobs ever become required status checks, use the dummy-job pattern; **owner iPhone confirmation for 0.1.16/0.1.17 attachment popover still owed.**
- **Still binding, from the rolled-off 2026-08-18 Phase 1 entry** (full text in `2026-08-18-session-phase1-per-device-schema.md`): **migration `0015` is NOT code-reversible** — it drops the account-wide uniques the pre-Phase-1 backend upserts against, so rolling the image back without recreating them makes every key-bundle and OTP upload fail with `42P10`. **`nest --watch` recompiles WITHOUT relaunching** (the container says "Found 0 errors" while nothing listens on :3000 and the wire suite hangs) — `docker compose restart backend` and wait 3–5 min for `/health`. **`flutter run -d web-server` serves exactly ONE debug client** — multi-client UI work needs `flutter build web --release` + a static server per origin. Uploads are SESSION-bound (the JWT names the device; only `fetchPreKeyBundle` names one) and a `sendToken` reused against a DIFFERENT conversation is refused as `duplicate_send_token`. Every index is mirrored on its entity because `synchronize` drops what the entity does not declare.
- **Still binding, from the rolled-off 2026-08-18 local-verification entry** (full text in `2026-08-18-session-phase1-local-verification.md`): the local DB is **`chatdb`** (`psql -d fireplace` fails); **`uploadOneTimePreKeys` was ungated on a PUBLISHED identity** — the refused session still wrote 20 OTPs over device 1's slots, which is the hole the landed OTP identity gate closes, and `fetchPreKeyBundle` serving only published-identity rows is what bounded it; **the reset COMPLETION path has still never run live against the per-device tables**, so "deviceId 1 is reused across a reset" vs §5.3's never-reuse remains reasoned, not observed; **Flutter web release + CanvasKit drops every keystroke after the first** — only CDP `Input.insertText` reaches the framework; a refusal snackbar lives ~0.6–2.5 s, so sample the DOM every ~600 ms before calling any refusal silent.

**Date:** 2026-08-21 (session B) — **T5 (self-sync receive, exactly-once lost-ack, `senderListInfo` — spec §5.4 + §5.2 layer 2) BUILT, REVIEWED (PASS, one P3 folded) AND WIRE-PROVEN. Suites green. NOT merged, NOT deployed. ONE THING OWED: the two-device app-proof, which needs browser authorization.** Session detail: `2026-08-21-session-t5-self-sync-lost-ack.md`; closure: decision record §10; NORMATIVE settlement: spec §12 **2026-08-21 block, items (xi)–(xix)**; research: `docs/plans/2026-08-21-t5-self-sync-lost-ack-research.md`. Spine: `8aa8bd0` research → `64fb6cb` settlement → `2b50e9a` stage 0 → `4fbcda0` stages 1+2 → `8a15b4f` docs → `cb7e1fb` wire → `b0d193e` review fold.
- **Three independent reviewers ran on T1–T4 BEFORE the ticket started** (owner's instruction): no P0, no GATE FAIL. Their actionable findings were folded INTO T5 rather than deferred — the **P1** (`DeviceListCache.adopt()` cached `notEnrolled` on an `authorization:null` answer without consulting the rollback pin, silently narrowing a fan-out to device 1 and, once T5 landed, silently killing self-sync) became stage 0 and amendment (xix); the **P2** (no end-to-end test for the token-keyed lost-ack path) became a test that then caught a bad first cut of the review fold. Open P3s: no `@ArrayMaxSize` on `SendMessageDto.envelopes`; `_trackedEvents` vacuity in the wire harness.
- **⚠️ THE RESEARCH REWROTE THE TICKET. Two corrections every future ticket brief should assume it needs:** (1) **the SEND half of self-sync had already shipped in T4** on both tiers, with a green test asserting it — T5 was a RECEIVE-side ticket, not what its brief said; (2) **the "five own-sender guards" list was incomplete and the missing one was decisive** — all five existed where claimed but sit downstream of `MessageModel.needsDecryption` (`senderId != currentUserId`, ~12 callsites), so flipping the five without it leaves self-sync dead with no visible cause. Two guards were missing in the OTHER direction and MUST NOT be flipped: the receipt emit (`history.dart` — device-scoping it is falsification 19 in red) and the edit-echo reconcile.
- **The law that now governs an own row (spec §12 (xi)/(xii)): deny-decrypt unless foreign origin is PROVEN.** `own_origin` → never decrypt, reconcile by token; `(originDeviceId ?? 1) == ownDeviceId` → the same case in legacy shape; otherwise self-sync → decrypt as ordinary inbound against the ORIGIN device's session and never touch the pending-send record. **The device-scoped branch waits for `socketReady` to CONFIRM `ownDeviceId`** — it defaults to 1, so a real device 2 would otherwise treat its own sends as a sibling's and burn the only plaintext copy on `[Decryption failed]`.
- **(xiv) is a deliberate refusal to "harden":** `UNIQUE (senderId, sendToken)` was NOT widened with `originDeviceId`, because a wider key would PERMIT the same token from two devices. The tuple stays the CLIENT's match law; the server contributes uniqueness plus the rule that the token reaches only the origin device.
- **`senderListInfo` landed with its false-alarm discipline** (owner: on EVERY message, so falsification 16 is deterministic): `{ownVersion, ownListHash, peerVersion, peerListHash}` inside the E2E plaintext, SHA-256 over the byte-exact transported `listCanonical` (never a re-serialization — phantom mismatches), a bare claim NEVER alarms or changes trust (I7), a newer claim buys at most ONE re-fetch (one in flight per account + cooldown, Matrix's rule), and own-device skew shows a calm en+pl "syncing your devices" note that borrows nothing from the identity surface. **Reinstall gap accepted and documented** (matches Signal and Matrix), not repaired.
- **libsignal go/no-go, from source:** two devices sharing ONE identity key CAN run a session — `process_prekey_bundle` has no branch rejecting a bundle whose IK equals the local IK, and sameness becomes the `self_session` flag; the IK is per-ACCOUNT by design. Skipped keys are bounded (`MAX_MESSAGE_KEYS = 2000`, `MAX_RECEIVER_CHAINS = 5`), so a very stale self-copy can be unrecoverable — render a benign state, never an alarm. Per-`(peerId, deviceId)` serialization is exactly the right unit: one `ProtocolAddress` = one session record, and `message_encrypt` holds no internal lock.
- **Verified: backend 920/57 · ratchet PASS 903 (floor still 906) · analyze clean · flutter 1479/10sk · wire 41/2sk** (+ falsifications 6 and 14, ZERO new registrations; 16 and 22 as units), both verifiers OK. Root `CLAUDE.md` §7 gained `senderListInfo` **and** the `socketReady` `deviceId` field, which had shipped in T4 undocumented.
- **⚠️ Subagent writers were unavailable this session** (account-level 429, multi-day retry-after). The ticket was implemented by the orchestrator stage by stage with a full suite run before every commit — slower, but every claim is first-hand.

**Date:** 2026-08-21 — **T4 (send fan-out, per-device envelopes, device rooms, per-device history reads — spec §5.2/§5.3) BUILT, GATE-REVIEWED (FAIL → folded) AND APP-PROVEN on two real devices. NOT merged, NOT deployed.** ➡ **PICKING THE PROGRAM UP FRESH? READ `2026-08-21-HANDOFF-phase2-T5-start-here.md` FIRST** — the authoritative self-contained entry point (supersedes the T4 handoff, which now carries a SUPERSEDED banner; it restates every owner rule and trap and carries the T5 brief with verified line numbers). Session detail: `2026-08-21-session-t4-envelope-fanout.md`; closure + deviations: decision record §9; NORMATIVE settlement: spec §12 2026-08-20 block items **(v)–(x)**; research: `docs/plans/2026-08-20-t4-envelope-fanout-research.md`. Spine: `31ce335` research+settlement → `98ad178`/`bbcfe8b`/`80b6035` backend → `c5ddeed`/`1461e9d`/`abae48c`/`1885038`/`b28d268` client → `8c8b12c` wire → `9c42859` review fold.
- **Settled BEFORE code** (research first, per the owner's instruction: 3 codebase scouts + 2 primary-source librarians): DTO growth + legacy→device-1 normalization at ingest; `deviceListStale` carrying `lists[]` + `tempId` so ONE round trip repairs both views (Signal-Server returns ids only and forces a second `/keys` trip — Sesame §3.3 permits the richer answer, so ours is the Sesame shape); **`senderListInfo` DEFERRED to T5** (server is blind to it; falsifications 16/22 are recipient-side); per-device history join + additive `envelopeStatus`; **(ix)** the research itself forced — a new-model row has no ciphertext for its origin device, so the lost-ack reconcile needed the `sendToken` key or the ONLY plaintext copy would strand.
- **⚠️ AMENDMENT (x), forced mid-build — read it before touching the send path.** Requiring a device-list round trip on EVERY send (to prove a single-device account is single-device) taxed the common path and hung 19 suites on an unanswered fetch. So the client fans out ONLY from a list it already holds, and **the SERVER refuses a legacy ciphertext send whenever either party is enrolled**, handing back the signed lists. I5 (never silently drop a device) therefore lands server-side where a client cannot skip it. Corollaries: never fan out without the RECIPIENT's list (own-devices-only envelopes = a row whose recipient reads `none_for_device` forever); the refusal handler MUST resolve parties ABSENT from `lists[]` (enrolled sender + non-enrolled recipient names only the sender, else the resend loops to the cap); **`socketReady` now echoes `deviceId`** because the client cannot derive which device it is.
- **Landed:** ingest refusals before any write (`duplicate_envelope_device`, `unknown_envelope_user`, `self_envelope_for_origin_device`, `unknown_recipient_device`); message row + N envelopes in ONE transaction; `deviceRoom()` + `emitToNewestTab`→`emitToDeviceNewestSocket` (newest socket WITHIN one device) with `newMessage` emitted once PER ENVELOPE; per-device push suppression (skip only if EVERY delivered device is focused); `preKeysLow` → device room; per-device history join with `envelopeStatus` `none_for_device`/`own_origin`; `updateDeliveryStatus` → column-scoped UPDATE (falsification 19); client per-device Signal addressing both directions, verified device-list cache (rollback pin survives invalidation), fan-out send + `sendToken`, stale-list repair capped at 3, honest "sent before this device was linked" placeholder in en+pl.
- **Two bugs the settlement had not anticipated:** `preKeyBundleResponse` echoed no `deviceId` (two in-flight per-device fetches indistinguishable), and the pre-key fetch limiter was keyed `(requester, target user)` with a 750 ms floor — it **REFUSED the second bundle fetch of a two-device peer**, making multi-device session establishment impossible. Both fixed in `bbcfe8b`.
- **⚠️ Review returned GATE FAIL — the BLOCKER is the one to remember.** The lost-ack reconcile keyed `sendToken ?? encryptedContent`, but a legacy send saves its record under the CIPHERTEXT while the token is emitted on every send, persisted, and echoed back to the origin device — so a legacy row carried both, the token won, the lookup missed, and **the only plaintext copy stranded on EVERY lost ack** (every production send is legacy until enrollment ships). Fixed by mirroring the save side (`encryptedContent ?? sendToken`). The suite could not catch it because its own-row helper omitted the echoed token — it now takes it. Two P3s folded with it (the same precedence in reverse leaked records on the success path; `stampEnvelope` was dead code, now wired).
- **Verified: backend 920/57 · ratchet PASS 903 (improved from 906; floor NOT lowered mid-ticket) · analyze clean · flutter 1451/10sk · wire 39/2sk** (+4 falsifications: amendment (x), 5, per-device delivery, 13's marker), both verifiers OK; root `CLAUDE.md` §3 + a full §7 T4 bullet.
- **✅ APP-PROVEN, account 193's two real devices + a fresh peer 297 on a third origin.** One send reproduced the whole path live: `[send] REFUSED legacy send to an enrolled party senderId=297 recipientId=193 stale=193@v2` → client adopted the list and resent as a fan-out → `newMessage emitted to recipient 193` → msg 649 has TWO envelope rows `(193,1)`/`(193,2)` with DIFFERENT ciphertexts and a NULL legacy column → **both devices rendered the same plaintext**, each decrypting its own envelope.
- **⚠️ Process: `hub jobs` status is NOT liveness.** Both dispatched writers died to the same 429, and a third went silent mid-stage while the broker still reported it RUNNING — compare working-file mtimes against the clock (23 min of zero writes = corpse). Recovered work was verified with full suites before being committed, never trusted.
- **⚠️ Docker traps paid:** a foreign `fpcomposer-*` stack held :3000/:5433 (ask before stopping someone else's stack — the owner cleared it); after that the 0a backend booted with `EAI_AGAIN db` and LOST its host port binding — `docker compose down && up` to recreate the network, a bare `restart` does not fix it; the register throttle is in-memory, so a backend restart clears a spent 10/hr/IP budget.

**Date:** 2026-08-20 — **T3 (provisioning ceremony, spec §5.1) BUILT, GATE-REVIEWED AND APP-PROVEN across three web origins. NOT merged, NOT deployed.** ➡ **PICKING THE PROGRAM UP FRESH? READ `2026-08-20-HANDOFF-phase2-T4-start-here.md` FIRST** — the authoritative self-contained entry point (supersedes the T3 handoff; carries everything through T3 plus the T4 brief). Session detail: `2026-08-20-session-t3-provisioning.md`; closure + deviations: decision record §8; NORMATIVE settlement: spec §12 2026-08-20 block (i)–(iv). Commit spine: `69200b2` settlement → `f56347b` backend → `2ccc76e` client crypto/DAK store → `8dc9d20` controller/UI/wire tests → `ca9c6ff` review fold.
- **Settlement pinned BEFORE code:** OOB `fp-link.v1.<id>.<b64url ephPubN>.<platform>` with manual-code equivalence (camera = Phase 3; T3 writes NO names — NFC rider defers to the Phase 3 rename UI); **local RFC-5869 HKDF-SHA256, salt 32 zero bytes** (libsignal 0.8.2 does NOT export HKDFv3 from its barrel); SAS = first 4 bytes BE uint32 mod 10^6 as `XXX XXX`; blob `0x01‖IV16‖CBC‖HMAC32` encrypt-then-MAC, constant-time check BEFORE decrypt; rebind tokens ride the `provisioningCompleted` answer; stage = server MEMORY, socket-bound (restart aborts — no table, no migration).
- **Landed:** stage machine (allocator memoized at open — FIRST `allocateDeviceId` caller; sync CAS one-shot complete, restore-on-stale_version, retire drops the blob), 6 wire events with T2 refusal conventions (`ephPubN` in no payload or log line), ONE commit transaction (devices row + list CAS + `createToken(userId, deviceId)` + login-shape JWT), never-activated-upload rejection at BOTH key-exchange handlers + `touch` inserts only device 1; client link_crypto/dak_store (armed persist gates the enroll emit)/screen-scoped ceremony controller behind ConnectionProvider's single seam; adopt/discardProvisionedIdentity (atomic identity record, enumerated abort discard, service-level invariant locks folded from the review's one P3 NOTE); Devices + both link screens (qr_flutter resolved — QR ships beside the REQUIRED copyable code).
- **Falsifications green:** 8/18/20 + amendment (a) idempotency as wire tests in `full_stack_e2e_test.dart` (ZERO new registrations; T2's `engine` hoisted to main() — the DAK private key exists nowhere else); 15 + blob tamper/wrong-key as link_crypto units.
- **Verified: backend 885/57 · ratchet 906 · analyze clean · flutter 1424/10sk · wire 35/2sk**, both verifiers OK; root CLAUDE.md §3 + §7 provisioning bullet updated. Review: GATE PASS, zero fixes.
- **App-proof (owner granted blanket tool access this session):** primary :8091 enrolled live → keyless :8093 opened → QR + code → manual paste → **both screens SAS `041 588`** → approve → `[provisioning] committed userId=193 deviceId=2 version=2` → rebind: session-bound upload landed bundle + 20 OTPs at `(193,2)`, device 1 byte-untouched (regId 10558, 100 OTPs). **Refusal path** (:8094): SAS `865 298` → CANCEL → opener notified, list stayed v2, no device-3 row, `nextDeviceId`=4 (gap only), N2 storage empty (abort discard). Account 193 now has TWO live devices.
- **⚠️ Rate-limit recovery ran TWICE:** the writer 429-died pre-code (revived, delivered stages 1–2 committed) then stalled 3 h mid-stage-3 (stood down; orchestrator finished from the worktree). Sequential per-stage commits are what made both recoveries cheap — keep that shape.
- **⚠️ Browser traps paid:** never `hub restart` a stale-named static server (retained spec = old cwd = stale bundle); python http.server sends no Cache-Control — CDP `Network.setCacheDisabled` reload or you test week-old JS; Flutter web release a11y tree LAGS navigation for minutes — screenshots are ground truth, snapshot-then-click in ONE cell, focus text fields via a fresh textbox-ref click then CDP `Input.insertText`.

**Date:** 2026-08-19 — **PRIOR-ART RESEARCH DONE (`250c619`) AND BOTH PHASE-2-BLOCKING DECISIONS LOCKED (owner: "do your recommended").** (The fresh-agent entry point is now the 08-20 **T4** handoff — see the entry above.) Session detail: `2026-08-19-session-multidevice-prior-art-and-decisions.md`; full research: `docs/plans/2026-08-19-multi-device-prior-art-research.md` (synthesis keyed to T1–T8 + four fully-cited appendices: Signal source, Matrix spec, WhatsApp/iMessage/MLS/attack papers, our spec map).
- **Decision 1 — deviceIds NEVER reused** (frozen §5.3 L269-272 stands): allocator = **`users.nextDeviceId` counter column in migration `0016`**, atomic `UPDATE … RETURNING`; NOT `MAX+1` over `devices` (row retention must never become a crypto invariant). Research grounds it: Signal reuses ids but only survives via registrationId disambiguation + per-id purge + 410-stale bounce (we have none); Matrix Synapse #17375 shows reuse reattaching stale attestations; WhatsApp/MLS are monotonic.
- **Decision 2 — password change VOIDS a 24 h post-cancel cooldown armed before it — LANDED at `94d030d`, wire-proven.** The predicate (`identity-reset.service.ts:143-155`: innerJoin `users`, `passwordChangedAt IS NULL OR cancelledAt > passwordChangedAt`) + a `warn` on the cooldown branch (it used to log NOTHING; fired 4× in the suite run). Deliberately never cancels pending ceremonies. New wire test in `registration_lock_test.dart` (runs LAST — it retires the harness account's original password): cooldown binds → REST password change → re-login → request = `pending` → cancel → fresh cooldown binds again (so the carve-out is not "disables cooldowns forever"). **Counts moved: backend 774→776/52, wire 25→26/2sk**, flutter 1375/10sk, ratchet 906, analyze clean, both verifiers OK, root `CLAUDE.md` §3 updated. Spec got two dated §12 amendments (decision record + carve-out) — doc stays frozen. Context: NO messenger has a reset cooldown at all; ours is the strict outlier and the carve-out keeps it strict.
- **5 new Stage-0 agenda items** (research doc §5): domain-separation audit of every sig/KDF context (Matrix CVE-2022-39250 class); peers must drop a revoked device's INBOUND sessions (WhatsApp shipped that bug); consider list TTL/ICDC freshness; §5.1 SAS vs ZRTP commitment shape (possible /prototype); provisioning idempotency under duplicate delivery.
- **Planning files live at `.planning/multi-device/` in the MAIN checkout** (gitignored) — upgraded from design-doc scope to program scope this session; maintain THEM, do not mint new ones at the worktree root.
- **Phase 2 Stage 0 is CLOSED — PASS-WITH-AMENDMENTS (three independent reviewers, all grounded at `94d030d`).** Normative output = spec §12 Stage-0 block, amendments (a)–(h); full finding-to-ticket map in `docs/plans/2026-08-19-phase2-stage0-decision-record.md`. Headlines: §5.1 needed an allocator-ordering + session-rebind fix (the primary had no way to learn the id it signs; N's bundle upload would have landed on device 1 and OVERWRITTEN the primary's — found independently twice); signature domain-separation prefixes pinned (`fp-enroll-v1\0`/`fp-list-v1\0`/`fp-dak-rotate-v1\0`, first byte ≠ 0x05, disjoint from the frozen §6.1 layout; NEW falsification 25); §5.5 revocation made bidirectional (receive-time origin-device check, fail closed); §6.2 reset must allocate from `nextDeviceId` (NEVER re-mint device 1) + tear down roster/enrollment with the list version continuing monotonic; 0016 pinned (no backfills, `message_envelopes.messageId` ON DELETE CASCADE is the SOLE §5.6 destruction mechanism, allocator returns PRE-increment). CONFIRMED (do not re-review): the DH-bound SAS subsumes a commitment round GIVEN `ephPubN` never transits the server; the cooldown carve-out grants an attacker nothing; every §7 delta composes with a named landed handler; §8 holds under the OTP gate ordering; all five §5.4 guards located (`messaging_provider.decrypt.dart:962/975/1290/642`, `messaging_provider.history.dart:529`). **Next: T1 (migration 0016) with its §4 decision-record riders — owner wants a fresh agent per ticket.**
- **✅ LIVE-FIRED in the real app (browser + docker, owner-authorized; summary §9):** fresh origin, account 193 — keyless banner (no silent overwrite) → consented re-mint REFUSED with **zero OTP traffic** (option-A stash dropped, pool stayed 20 @ old identity) → 72 h ceremony → cancel → **cooldown refusal logged by the new warn line** → REST password change → re-login → **same request = `pending` (the carve-out, live)** → backdated deadline → cron → `completed ceremony consumed` + `[identity-churn]` to the VERY key the lock had refused + **100 OTPs uploaded 2 s AFTER the publish** (publish-then-keys held). **⚠️ Test account 193's password is now `Fireplace!2620`**, identity `BVVFJ/DuqMwR`, pool 100.
- **T1 (migration 0016) LANDED at `584f2d3` and gate-reviewed — GATE PASS, zero fixes.** `users.nextDeviceId` (default 2) + `DevicesService.allocateDeviceId` (atomic `UPDATE … RETURNING "nextDeviceId"-1` — PRE-increment, never decremented, gaps expected), `account_authorizations` (+`enrollmentCreatedAt`, needed to re-verify enrollment `E`; `fp-enroll-v1\0` prefix quoted in comments), `message_envelopes` (UNIQUE `(messageId,recipientUserId,recipientDeviceId)`, `messageId` FK **ON DELETE CASCADE** = the sole §5.6 destruction mechanism, NO FK on the recipient pair). NO backfills — both tables empty. Proofs: boot path across 3 restarts, indexes survive `synchronize`, cascade live-proven, concurrent allocations distinct. **Counts: backend 776→780/52**, ratchet 906, wire **26/2sk re-run green on the 0016 schema**. Review NOTE folded as a T4 rider (recipientUserId has no users-FK — confirm at the T4 write path). ⚠️ The wire suite fails if launched during backend settle — wait ≥20 s after `/health` flips, and never run it in parallel with anything.
- **T2 (DAK-signed device list) LANDED at `6101774` and gate-reviewed — GATE PASS, zero fixes.** Server: enrollment (first-write-wins via `userId`-PK INSERT, sig_IK over `fp-enroll-v1\0‖userId‖dakPub‖createdAt` vs the PUBLISHED identity), byte-exact `listCanonical` (parse→re-encode→byte-compare; duplicate/ambiguous rejected at parse), sig_DAK `fp-list-v1\0` list with atomic-CAS version law (`stale_version`), wire `enrollDeviceAuthority`/`updateDeviceList`/`getDeviceList` + `deviceListChanged`. **Falsification 25 pinned BOTH directions against the frozen §6.1 layout with real-Dart vectors** (`frontend/tool/device_list_vector_generator.dart`). Client: I7 chain verifier (IK→E→DAK→list, buffer copies — Curve calls mutate) + `DeviceAuthorityEngine` (harness-driven, NO UI; DAK in-memory — T3 persists + wires UI + NFC-normalizes names). Falsifications 2/3/23/25 in; **16/22 correctly deferred to T4/T5** (need `senderListInfo`/envelopes). **Counts: backend 850/55, flutter 1405/10sk, wire 32/2sk**, ratchet 906, analyze clean, both verifiers OK. ⚠️ The writer agent was killed by a rate limit after backend-green — orchestrator finished suites/counts/commit; 2nd flake class sighted once: `unread_badge_sync_test.dart` badging fallback (pre-existing, green on re-runs).
- **⚠️ Environment note:** master moved to `cc8442b` (0.1.17 shipped — PR #148 reverted the #145 popover anchor); a `fireplace-emu` stack from `C:\tmp\fp-emu` squatted ports 3000/5433 mid-session and was later downed externally — check `docker ps` for squatters before assuming the `fireplace-0a` stack owns :3000.


**Date:** 2026-08-19 — **THE OTP IDENTITY GATE IS LANDED AS OPTION A (publish the identity, release the keys on its ack) — server + client, app-proven, all suites green on `feat/takeover-alarm-0a`. NOT merged, NOT deployed.** ➡ **STARTING PHASE 2 (or picking this program up cold)? READ `2026-08-19-HANDOFF-phase2-start-here.md` FIRST** — it carries the owner rules, the full state, every trap, the two open decisions and the Stage 0 → T1..T8 plan; it SUPERSEDES `2026-08-18-HANDOFF-phase2-start-here.md` (which keeps only its §1 preconditions). Build detail: `2026-08-19-session-otp-identity-gate.md`. `wip/otp-identity-gate` (`8d61bde`) is **superseded** — server half only.
- **Server:** `uploadOneTimePreKeys` refuses a tag that is not the account's published identity (`error {message:'identity_locked'}`, logged `warn` — the lock working, not a fault). Carve-outs: nothing published yet (fresh registration) and an unspent **COMPLETED** ceremony (authorized rotation in flight, read without spending); a merely **PENDING** ceremony authorizes nothing.
- **Client (this IS option A):** both upload sites stash the pre-keys and emit only `uploadKeyBundle`; `onKeyBundleUploaded` releases the stash on `success:true` and **DROPS** it on any refusal (`OTP_UPLOAD_DROPPED`). A `success:true` with `identityChanged:true` **mints a fresh pool immediately** — without that the recovered device published an identity with an EMPTY pool, because the epoch purge drops the superseded rows and the ceremony-spending re-upload carries no keys of its own. `onPreKeysLow` routes through the same helper. **Do NOT restore the back-to-back emit.**
- **Counts moved: backend 769→774/52, Flutter 1371→1375/10sk, wire 24→25/2sk**, ratchet still 906, analyze clean, both verifiers OK; root `CLAUDE.md` §3 + §7 updated. `stale_otp_epoch_test` now mirrors the safe order (`reuploadInProductionOrder`); `EventLog.takeError` consumes an EXPECTED refusal so the end-of-run "no unexpected socket errors" assertion keeps its teeth; `encryption_provider_session_rebuild_test`'s fake server had to start ACKing the bundle (it drives the real `initializeE2E`, so it caught the contract change instantly).
- **App proof:** fresh registration now logs the ORDER (`Key bundle upserted` → `Uploaded 20 one-time pre-keys`, user 204); the clobber attempt produces `REFUSED … replacement` and **no OTP traffic at all** — the client dropped them, pool intact at 20 rows (users 193, 205); messaging unaffected. **And the reset CEREMONY was completed end to end for the first time against Phase 1's per-device tables** (backdated deadline + the real per-minute sweep): grant consumed, `identity-churn` old→new, **100 OTPs uploaded under the NEW identity** (user 205) — that closes yesterday's "reset completion never live-fired" gap.
- **⚠️ Three rejected rescues, do not retry them:** awaiting this socket's in-flight bundle upload after one macrotask yield (**proven insufficient** — the trailing frame is dispatched a tick later), a timed ~500 ms poll for that marker (makes a lock's verdict depend on wall-clock latency), and blacklisting identities the lock refused (**fails open** — upload the keys before ever attempting a bundle). Ordering had to be fixed at the source, which is why A is a client change.
- **Trap:** clearing a Flutter text field by zeroing the DOM `input.value` does NOT clear the framework's controller — the next typed username APPENDED to the old one and created the account `pr8963550rc489731`. Focus the field and drive Ctrl+A/Backspace through the framework instead.


