# Latest session summary

Entries are newest-first. Each one links its dated file, which holds the full account.

**🗺 START HERE IF YOU ARE LOST: `2026-08-05-HANDOFF-audit-state.md`** — plain-language map of the 2026-08-04/05 full-codebase audit: what was found, what merged, what is deliberately parked, and the three owner-only decisions. The two ordered task queues are `.planning/full-audit/REMAINING-WORK.md` (backend) and `.planning/full-audit/frontend/REMAINING-WORK.md` (frontend), both local-only/gitignored. Older long-form: `2026-08-03-HANDOFF-post-b2b-state.md` (B2b gate detail + traps; **its work queue is DONE**) and `2026-08-03-HANDOFF-signal-grade-queue.md` (dated addenda).

**✅ DEPLOY STATE — BOTH TIERS CURRENT ENOUGH; NO GATES LEFT.** Backend prod is 1 commit behind (`f6e4aa8`, dev-dep lockfile + a media-cleanup mtime clamp, no migration).
- **Frontend `0.1.9 / 9e27ed4` IS LIVE — 08-14 00:53Z, smoke 5/5.** Ships the server-MITM warning fix (`3d30b88`), the "Verify security keys" door, B2b `sig_*` sealing, the 19 audit fixes. Field first boot: ONE `SIG_SEAL_DRAIN_DONE {sealed: 265}`, `SIG_SEAL_OPEN {sealed: 265, legacy: 0, ms: 60}`, `WEB_SEAL_OPEN {sealed: 241, legacy: 0, unreadable: 0, lostRows: 0}`, **33/33 sessions preserved**, zero `SIG_KEY_UNAVAILABLE`/`SIG_ROWS_UNREADABLE`/`SIG_STORE_FALLBACK`/`CONTENT_KEY_LOST`. **Signal identity keys are now sealed at rest on web.**
- **⚠️ Three 08-14 traps.** (1) `.\deploy-web.ps1 | tail` **swallows a publish-stage failure** — build ok, nothing published; re-run with output redirected. Staged swap = nothing half-ships. (2) **The footer's VERSION half is a live `version.json` fetch and LIES about the running bundle; only the COMMIT half is compiled in** (`auth_screen.dart:205-207`) — `0.1.9 / c01317c` means old JS. (3) **NO `Cache-Control` on any entry file** (ETag only) ⇒ heuristic freshness, no revalidation; the Safari PWA took ~14 h to flip. Queued fix is `no-cache` on the static root — **Flutter output is NOT content-hashed**, so `immutable` by extension would bake a bundle in forever, helping only the NEXT deploy.
- **⚠️ THE CANARY GATE IS NOT EVIDENCE.** `flutter_secure_storage_web` 1.2.1 is `localStorage`, not IndexedDB+WebCrypto, and keeps its AES master key there raw+extractable (`flutter_secure_storage_web.dart:110-116`), so the canary compares localStorage to a localStorage shadow — `CONTENT_KEY_CANARY_LOST` is ~unreachable and `ageDays` never was clearance (one arm, 07-30).
- **Backend:** no gate, **DEPLOYED 2026-08-05 19:26Z at `7a845430`** — carries the unfriend history-wipe fix, so today's 0.1.8 clients are repaired. Smoke 5/5, 0 boot errors, fix confirmed in the container's own `dist/`.

**⚠️ THE OLD `[Decryption failed]` P0 (0.0.139) IS CLOSED, BUT A NEW WAVE IS OPEN — see the 08-14 evening entry + its handoff.** The four hypotheses in `2026-07-29-HANDOFF-decryption-failed-incident.md` are all DEAD; that file is historical, do not work from it. **Never uninstall / clear site data.**

**🔴 FCM IS DISABLED IN PRODUCTION.** `FIREBASE_SERVICE_ACCOUNT` is absent from `~/fireplace/.env`, so every backend boot logs "FCM disabled". `google-services.json` IS tracked, so an APK would register tokens the server can never push to — **Android notifications are dead until that var is set.** Web Push VAPID is present, so the PWA is unaffected, which is why this went unnoticed. Fix it before any APK work.

- **Still binding (applies to every entry below):** never run `dart format lib/` (format only what you edited); **ask before opening the browser tool**; `node scripts/lint-ratchet.mjs` runs ONLY in CI — run it before pushing backend changes; `cmd | tail` hides exit codes; never re-derive test counts by arithmetic across a merge; **never give `flutter test` a file list** (per-argument compile cost: 45 files timed out past 11 min, the whole suite runs in 170–310 s); `flutter run -d web-server` serves a blank scaffold once its hot-restart client is lost — restart the process, do not reuse the tab; the E2E register throttle is 10/hr/IP in memory, so `docker compose restart backend` between full `test_e2e` runs and allow 2–5 min to boot.
- **Still binding, from the rolled-off 2026-08-04 entry** (full text in `2026-08-04-session-frontend-audit-fixes.md`): **`refreshSession` timeout maps to `SessionRefreshTransientException`, NEVER `Invalid`** — transient holds the session, invalid clears local auth, so the wrong mapping manufactures the very logout this repo keeps chasing (see the 08-14 entry). **Message send has no ack timer and must not get one** (only invitations do, `_kInvitationAckTimeout`): failing a send the server received releases the exactly-once latch and duplicates it — why FE-024 was skipped deliberately. **wasm nearly deleted the multi-tab Signal lock** — `session_cross_context_lock.dart` + `storage_persist.dart` key on `dart.library.html`, FALSE under `--wasm` ⇒ stub, no lock, no diagnostics, while the probe stays green.
- **Still binding, from the rolled-off 2026-08-05 entry** (`2026-08-05-session.md`): **⛔ DO NOT room-address `newMessage` / `messageEdited`.** Signal decrypt is NOT idempotent and tabs share one session store, so a second tab decrypting the same ciphertext fails into the session-destroying policy. Both use `emitToNewestTab`; push suppression reads that SAME socket. Lifting it needs decrypt-idempotency proof AND both moved room-wide in ONE change.

**Date:** 2026-08-14 (evening) — **🔴 OPEN INCIDENT, HANDED OFF: `[Decryption failed]` is BACK, and the window is the 0.1.9 deploy. NO CODE ON MASTER — my symptom fix was reverted at `409c23a` on the owner's instruction (`git diff --quiet 7036bd1` clean).**
- **➡ A FRESH AGENT OWNS THIS: read `2026-08-14-HANDOFF-post-0.1.9-decryption-failed.md` FIRST.** It carries the frames, the discriminating evidence list, the proven dead ends and the code map. Do not re-derive from this bullet.
- **Owner's rule, learned the hard way here: investigate and PROVE, then ask before writing code.** I landed `4beb1bd` (terminal `missingPreKey` class) on a symptom before the mechanism was known; it is reverted, recoverable via `git show 4beb1bd`, and must NOT be re-landed as a first move.
- **Window:** frontend-build went live **08-14 00:53:25Z** (VM mtime) with B2b sig sealing as the only key-storage change. **Three `[identity-churn]` lines followed, none before in 72 h:** user 58 (+24 min), **90 (+14.6 h, the `ruchens69` report)**, 54 (+15.8 h — the line the earlier 08-14 entry said to watch for, so his history IS gone). Timing ≈ each device's first 0.1.9 boot given the no-`Cache-Control` flip lag.
- **But the owner's own first 0.1.9 boot was CLEAN** (drain done, 265 sealed, 33/33 sessions, zero SIG_* alarms) — so the killer is conditional. Live frames: (A) the legacy→async `_migrate` copy-then-delete losing rows before `loadFromStorage` reads them ⇒ silent regeneration; (B) sig rows unreadable at runtime (`SigStoreUnreadable` falls into the `unknown` bucket ⇒ `[Decryption failed]` + retry churn) — needs no wipe and would hit a NEW account; (C) pre-deploy debris.
- **Unaudited, first thing to check:** is the B2a content-key rotation strictly scoped to `fp_content_key_`, or can it delete `fp_sig_key_`? Both live in one `flutter_secure_storage`.
- **A second report exists on a different user; its account age is UNKNOWN — ask.** Pre-deploy account ⇒ frame A first, post-deploy ⇒ frame B. Owner will send a today-created-account dump when it recurs.

**Date:** 2026-08-14 — **ADMIN PASSWORD RESET for user 54 + logout-cause chase. NO CODE CHANGED, no deploy.** There is no admin reset endpoint (`POST /users/reset-password` needs the old password), so it was one prod transaction in the `users.service.ts:298-317` order: revoke every refresh token → set the bcrypt hash → stamp `passwordChangedAt`. Login verified 201; my test session was then deleted and the stamp refreshed to kill the access JWT it minted.
- **The server never logs anyone out silently, and did not here.** His refresh row was valid to **2027-08-10** and alive when I deleted it — recovered read-only from `chatdb-20260813T040001Z.dump.gpg` via `pg_restore --data-only -t refresh_tokens` (nothing restored into the live DB). Last slide `08-10 07:11:31`, two seconds before his key-bundle write, both from one fully authenticated cold boot. Zero `[auth-session-end]`, zero `login failed`. **Login screen ⇒ local token loss, always.** He changed his own password 08-03 15:27 (which revokes every session by design) and re-logged in at 15:50 — **that Aug 3 password is the one he forgot.**
- **🔴 OPEN, and key loss is NOT ruled out:** an **offline Logout tap** (both server calls swallowed by `catch (_) {}`, keys intact) and an **origin storage wipe** (keys gone) are byte-identical from the server. Online logout IS excluded — it would have deleted `web_push_subscription` id 370, still present. **Watch for `[identity-churn] userId=54 oldIdentityPrefix=BcNWM+S196eX` on his next login** — that line means his history is gone, its absence means it survived.
- **⚠️ THREE HYPOTHESES KILLED — do not resurrect.** (1) "History is intact" — churn is only observable when the client CONNECTS, and he has not since 08-10, so the clean record covers only up to then. (2) Quota/partial-write loss of the refresh token is impossible: `/auth/refresh` echoes the SAME opaque string (`auth.service.ts:95`, no-rotation by design at `refresh-tokens.service.ts:51-55`), so a swallowed `setString` rewrites a byte-identical value; only a `remove` or an origin wipe can lose it. (3) **The auth screen's reason line is NOT a wipe test** — `_lastSessionEndReason` is in-memory only (`auth_provider.dart:35`, set `:169`, never persisted, never restored), so it reads bare after any cold boot under both branches; I cited it three times before catching that. Use the `E2ePersistentDiag` panel instead: records present ⇒ localStorage survived ⇒ keys intact, and no `AUTH_SESSION_END` around 08-10 is the offline-logout signature (`:181` skips the diag for `explicit_logout`).
- **The web session/identity share ONE evictable localStorage** — tokens (`auth_token_store.dart:31`, secure storage is Android-only) and the whole Signal identity (`signal_stores.dart:26-31`). Session loss and permanent history loss are one event; no key backup by design; no iOS setting changes it. The 7-day WebKit cap does not fit either: storage held the refresh token across 08-03 15:50 → 08-10 07:11, and `web.push.apple.com` proves only "an Apple browser" (macOS Safari 16.1+ pushes to non-installed sites), not a home-screen install. **Unwatched signal:** `refresh_tokens.expires_at` is a per-device daily heartbeat (`auth_provider.dart:297-306`) — 23 of 48 accounts have not slid in >14 days and nothing alerts.
- ➡ Detail: **`2026-08-14-session.md`**.

**Date:** 2026-08-13 — **FIELD DIAGNOSTICS READ + STALENESS SWEEP. No production deploy THAT DAY — `0.1.9` shipped the next day, 08-14; see the banner. Two commits (`f6e4aa8`, `7663047`), CI 4/4, backend 671/671 / 49, ratchet PASS 912→907. Tracker emptied: 0 issues, 0 PRs, 0 alerts.**
- **Everything from 08-05 checks out in the field.** `WEB_SEAL_OPEN {sealed: 258, legacy: 0, unreadable: 0, lostRows: 0}` — B2a up 184→190→258 over ~11 unattended days with **zero loss**; that, not the canary, is the real evidence. `CANARY_OK {ageDays: 15}` reconciles (see banner). `STORAGE_PERSIST` granted, `SESSION_INVENTORY` 33 stable across four reconnects.
- **✅ The recurring duplicate-decrypt storm STOPPED on its own.** Last durable `DECRYPT_DECISION` is `08-03 17:44:13`, nothing for 08-04→08-14, and the newest durable entry is 08-11 so this is not ring eviction. Peers 60/52/83/49's fixed id sets retired — terminal-duplicate retirement worked, it just needed more distinct process lifetimes than expected. **Closed by observation, no fix.**
- **🔴 NEW `PEER_IDENTITY_CHANGED {peerId: 92}` at 08-11 15:24, on a live session, on the vulnerable build.** Third one (90, 54, 92). This does NOT contradict `3d30b88`: detection lives in `isTrustedIdentity` (`signal_stores.dart:511-519`) and the deleted pre-save only poisoned the path where WE fetch a bundle from the server, so inbound `PreKeySignalMessage` still fires. **Detection works for keys arriving in messages and is blind in exactly the direction an active server MITM attacks.** All three peers remain unverifiable on `c01317c` (`getPeerIdentityFingerprint` has one UI caller there — the banner). Verify them through 0.1.9's Safety-section door once live.
- **🔴 A high-severity advisory Dependabot silently failed to deliver, TWICE.** js-yaml `GHSA-5p4m-2wfm-xmqj`; runs `31519706831` (08-11) + `31573038380` (08-12) both errored in `bin/run update_files`, so **no PR ever appeared** and it sat unpatched for 3 days. It cannot bump the requirement (both copies transitive dev-only: `cosmiconfig`←`@nestjs/cli`, `@istanbuljs/load-nyc-config`←`ts-jest`); lockfile `npm audit fix` resolves it. 0 vulns after. **⚠️ NEW TRAP: Dependabot runs under a DIFFERENT workflow name on its own schedule, so `gh run list --branch master --limit 1` after your push never shows it.** Use `gh run list --limit 10` unfiltered, or the Security tab.
- **A latent Windows-only flaky test, found by accident** (669/670 then 670/670 then green in isolation). `media-cleanup.service.ts:143` subtracted mtime from a `nowMs` captured before the loop; **NTFS rounds a just-written mtime UP**, so the difference goes NEGATIVE and an orphan reads "fresh" even at `graceMs=0` — flaking the spec AND **leaking any future-stamped orphan forever**. Age clamped at 0; new spec proven RED against the unclamped code (1 failed/14 passed).
- **Doc gate caught me:** first push went red on `verify-claude-backend-test-counts.mjs` (documented 670, Jest 671). A new backend test REQUIRES the `CLAUDE.md` §3 count bump **in the same push**. Prettier failures in `media/` are pre-existing (verified by stashing) — left alone, inside the ratchet's ±5.
- **Both open issues were stale, closed with evidence.** **#102** brace-expansion: only 1.1.18/2.1.4/5.0.9 remain, all above the advisory floors, 0 vulns in `backend/` and `scripts/smoke/`. **#105** deleted-message plaintext: fixed by `42603d2` on 07-28 **~2.5h after it was filed**, an ancestor of master AND the served `c01317c` (`messaging_provider.events.dart:321-352` — ciphertext captured before `removeWhere`, purge unconditional so delete-for-me is covered, durable backlog first). **Fixed in prod for two weeks while the issue sat open.**
- **Deliberately NOT touched: Flutter deps.** 32 locked older / 31 constrained below resolvable, `flutter_secure_storage_macos` + `js` discontinued, **no advisories** — all patch/minor drift. `pub upgrade` hours before shipping a release that changes at-rest key handling perturbs the crypto-adjacent graph for zero benefit; do it after 0.1.9 is field-verified. `flutter_secure_storage_web` 1.2.1's localStorage key custody is **not fixable by upgrade** — needs the non-extractable IndexedDB `CryptoKey`.
- **Cleanup pending owner:** KEEP `feat/invitations-hex-ui` @ `c01317c` until 0.1.9 is verified (it is the rollback tree), delete after. `origin/feat/cosmic-theme` has 1 orphan commit from 07-20 (starfield `?density=` dev tool) — cherry-pick or delete. **5 stashes on branches that no longer exist**, offered twice, still unanswered.
- ➡ Detail: **`2026-08-13-session-staleness-sweep.md`**.

**Date:** 2026-08-05 (night) — **ADVERSARIAL SECURITY REVIEW (6 specialists + live prod probes). Four commits; owner picked "fix it properly" + "backend now, hold frontend", and the BACKEND IS DEPLOYED at `7a845430`.** Backend **670/49**, Flutter **1256 + 10 sk**, analyze clean, ratchet **912→907**, CI 4/4, smoke 5/5.
- **🔴 FIXED, WAS LIVE: unfriending ONE contact irreversibly purged the decrypted history of EVERY chat on the initiator's device.** The server sent the initiator its OWN id, and the client feeds that into `removeConversationsForUser` (`userOne.id == id || userTwo.id == id`) — you are a participant in all your conversations, so all matched and all were purged, permanently (ratchet consumed the keys). Each side now gets the OTHER party's id; the deploy above repairs 0.1.8 clients with no frontend release. Client also refuses a self-addressed id. Test proven RED first.
- **🔴 THE BIG ONE — web at-rest sealing keeps the lock and the key in the same drawer.** `flutter_secure_storage_web` 1.2.1 writes its raw AES master key into `localStorage` (see banner). B2a (LIVE) rows are unwrappable by ~15 lines of console JS; `fps1:` framing exposes `kid`+`cid`; **web never rotates or destroys content keys**, so deleted-row residue stays decryptable. Real fix = non-extractable `CryptoKey` in IndexedDB, which 1.2.1 cannot do.
- **Also live, none started:** no app lock/PIN/biometric anywhere (HIGH, web AND Android — bypasses all at-rest crypto); **no CSP / `X-Frame-Options` / HSTS on the app document** (only the VM's untracked nginx can fix it); `deleteConversationOnly` never tells the peer to purge; a hostile server can use `getServedMessageIds` as a **remote wipe**; FILE filenames reach the server cleartext; password change does not drop live sockets; signed prekey never rotated.
- **🔴 FIXED (`3d30b88`), WAS LIVE — the identity-change banner could not fire on the server-MITM path.** `encryption_service.dart` pre-saved the SERVER-SUPPLIED identity key before `processPreKeyBundle`, so `isTrustedIdentity` (where the compare + `onIdentityChanged` live) read back the key it had just written and returned early: a compromised server could serve its own self-signed bundle and read everything after, **no banner, no diag**. The pre-save was redundant twice over (our `isTrustedIdentity` is TOFU and never throws; `processPreKeyBundle` persists the identity itself) — deleted. The warning is now **persisted**, cleared only by the new `acknowledgePeerIdentity()`, and verification gained a **proactive door** ("Verify security keys" in the peer's Safety section) since a FIRST-CONTACT substitution raises no change to warn about. 4 detection tests proven RED against the restored pre-save.
- **Confirmed strong, do not re-litigate:** server never sees plaintext (no snippet/search column); media AES-GCM'd pre-upload, media keys never a DB column; every delete path is a hard `DELETE`, media first; push carries no message text; every `logger.*` audited clean; **no drafts ever persisted**; no decrypted media on web disk; no cookies/analytics/CDN tags; TLS 1.3 only; hostile socket `Origin` → 400; backups verified encrypted + offsite.
- **⏰ Trap paid:** a CI red mid-session was NOT the crypto change — `messaging_provider_ledger_gate_test` hardcoded `expiresAt = DateTime.utc(2026, 8, 5, 19)`, which **became a past deadline at 19:00Z today** and then failed every run (an expired served row is skipped before the self-heal). Proven pre-existing at `edaca33`; fixture is now relative to `DateTime.now()`. Scan the suite for other fixed future instants before trusting a green.
- ➡ Detail: **`2026-08-05-session-security-review.md`**.

**Date:** 2026-08-05 (evening) — **INDEPENDENT VERIFICATION OF THE WHOLE 08-03→08-05 WAVE: SAFE. Nothing reverted, nothing half-applied, no rollback needed. NO CODE CHANGED.** Owner was AFK for the wave and asked for a from-source re-check; six parallel agents (2 reviewers, 4 scouts) plus direct commands. Backend **670/49**, Flutter **1247 + 10 skipped**, analyze clean, both verifiers OK, ratchet PASS at 912, last 6 CI runs green, 0 open PRs, **0 dependabot alerts**.
- **Merge integrity MEASURED, which is what the fear actually was:** for `41e2f0b`, `195d894`, `2db65d9`, `dc5bfb5` the merge result is **byte-identical to the branch tip (0 files differ)** — no hunk dropped. The only two conflict resolutions in `eb84451..565d1d9` are **docs-only**; zero deleted files, zero reverts in range. Nine previously-shipped fixes re-confirmed at path:line (listed in the dated file).
- **BE-007 carve-out HOLDS** (`chat-message.service.ts:131,:601` still `emitToNewestTab`; push suppression reads the SAME socket at `:621`; no ciphertext emit went room-wide). Live backend is wire-compatible with the 0.1.8 field client — no event renamed, and the tightened DTOs reject only never-valid values.
- **Prod re-measured live:** `0.1.8/884f6d0c`, `/health` ok, backend `Up 6h (healthy)`, **0 errors in 2000 log lines**, `0011` applied + index present, 97 users / 55 conv / **231 messages (207 at deploy — real post-deploy traffic, clean)**. nginx re-grepped independently: **11 `proxy_pass` / 11 `X-Real-IP`**, no proxying location missing it. `FIREBASE_SERVICE_ACCOUNT` confirmed absent (boot `WARN`).
- **Rollback claim verified against `c01317c`'s own source, not the summary:** the old bundle hits an unguarded `jsonDecode` on an `fpsig1:` value and **throws before** the `absent → _generateKeys()` branch — a frontend rollback breaks web decryption until roll-forward but **destroys nothing**. Also confirmed by ancestry (not dates): B2a IS in `c01317c`, **B2b is NOT** — message text + media keys are sealed for real users today, **Signal identity keys stay cleartext until 0.1.9**.
- **Doc fix, source won:** the "20 s ack bound on accept/decline/send" line below was invitation-only (`_kInvitationAckTimeout`); message send has no ack timer **by design** — corrected in place.
- ➡ Detail: **`2026-08-05-session-post-wave-verification.md`** — full evidence table, ordered Android owner tasks. (Its "canary gate tips 08-06" note is superseded: the gate is not evidence — see the banner.)

