# Chat wallpaper glyph set v7 — reconcile fork + deploy 0.0.110

**Date:** 2026-07-12

## What was done

- **Glyph rework (owner marking loop).** Reworked the chat "temple columns" wallpaper glyph registry `frontend/lib/widgets/hieroglyph_glyphs.dart` from the committed v4 set (28) to **v7 = 36 glyphs**:
  - **Deleted 9 weak signs** originally: cobra, djed, bee, dju, pet, seba2/star, per, door-bolt, foot; later also deleted **was, reed, pool** and runes **gul, ohm, ber, sur** per owner marks.
  - **Reworked the scarab** (Gardiner L1) into a clearer beetle.
  - **Added authentic Gardiner signs:** tyet (V39), shen (V9), heart/ib (F34), crook (S38), flail (S45), hedjet white crown (S1), deshret red crown (S3), nub broad collar (S12), sail (P5), hoe (U6), scribe palette (Y3), recumbent Anubis jackal (E16).
  - **Added 5 Diablo II high runes** as mono-line accents traced from the D2 sprites: Vex, Lo, Jah, Cham, Zod.
  - Final registry = 30 hieroglyph signs + 5 runes + the CC0 leaf hero.
- **Source-of-truth fix.** The file carried a bogus `// GENERATED … regenerate from SVG masters` header, but no generator/masters were ever committed. Renamed `hieroglyph_glyphs.g.dart` → **`hieroglyph_glyphs.dart`** (LSP updated the import in `chat_background_pattern.dart`), rewrote the header to state it is hand-maintained and IS the source of truth. `docs/design/liquid-glass/wallpapers/hieroglyphs.html` is its hand-kept visual preview (identical geometry, generated from one Python source during the session).
- **PR #66** (`feat/wallpaper-glyphs-v7`) merged to master.
- **Fork reconcile + deploy.** Discovered at deploy time that the glyph master (`1f2055f`, 0.0.108) had forked from a live hotfix `fix/stale-otp-epoch` @ `caa322b` (0.0.109 "E2E recovery hardening") already deployed to prod WITHOUT glyphs. Merged the hotfix branch into master (clean, no conflicts, glyphs intact), bumped to **0.0.110** (`9159dba`), deployed **frontend only** via `deploy-web.ps1` from the `fireplace-ping-deploy` worktree.
- Deleted the merged remote branch `fix/stale-otp-epoch`.

## Key files

- `frontend/lib/widgets/hieroglyph_glyphs.dart` — renamed from `.g.dart`; 36-glyph registry + paint fns; header corrected.
- `frontend/lib/widgets/chat_background_pattern.dart` — import updated to the renamed file.
- `docs/design/liquid-glass/wallpapers/hieroglyphs.html` — preview mirror (36 cells), source-of-truth note added.
- `docs/design/liquid-glass/SPEC.md` §10 — glyph note updated to v7 / 36.
- `frontend/test/widgets/hieroglyph_glyphs_test.dart` — new smoke tests.
- `frontend/pubspec.yaml` — 0.0.110.

## Verification

- `flutter analyze --no-fatal-infos` — clean (2 pre-existing infos: `privacy_safety_screen.dart` use_build_context_synchronously, `jumbo_emoji.dart` valid_regexps).
- `flutter test` on the new glyph test + merged encryption/privacy tests — **13 passed**. Glyph test covers: registry = 36 unique / exactly one leaf; deleted names absent + 5 runes present; every `paintFn` executes on a `Canvas` without throwing; real `ChatBackgroundPattern` render under `GlassTheme.dark` on a 400×2400 surface drives the actual `_TempleColumnsPainter` with no exceptions.
- Production LIVE: `/health` `{status:ok,db:ok}`; frontend `/version.json` **0.0.110**; served `main.dart.js` contains commit **`9159dba`** (definitive stale-build detector); backend `/version` 0.0.109/`caa322b` (frontend-only deploy; harmless version split).

## Notes for next session

- **Backend stays 0.0.109/`caa322b`** — E2E hardening already live; only redeploy backend (`deploy-backend.sh` on the VM) for a genuine backend release (it re-runs migrations at boot).
- Local `fix/stale-otp-epoch` branch + `fireplace-e2e-fix` worktree still present and **dirty** (uncommitted l10n files) — `git worktree remove fireplace-e2e-fix && git branch -d fix/stale-otp-epoch` once clean.
- `feat/wallpaper-glyphs-v7` merged (PR #66); remote deletable, local is the main-repo working branch (owner WIP present).
- The phone *mockup* at the bottom of `hieroglyphs.html` still bakes the old v4 background image — illustrative only; the app renders from `hieroglyph_glyphs.dart`. Regenerate if a faithful mockup is wanted.
- On device: fully close + reopen the PWA (never uninstall — wipes E2E keys); Settings footer should read `0.0.110 · 9159dba`.
