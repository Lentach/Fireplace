<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Accent used to tint the monochrome notification small icon (FCM
         default_notification_color). Fireplace ember orange. -->
    <color name="fireplace_notification_accent">#FF6A2C</color>
	<color name="ic_launcher_background">#000000</color>
    <!-- Hot Stone paper — the default theme scaffold (#F7F4F0). Pins the
         launch splash so it cannot follow OS dark mode (?android:colorBackground
         flashed dark before the light front door). Keep in sync with
         RpgTheme.backgroundLight; guarded by test/utils/native_launch_background_test.dart. -->
    <color name="hot_stone_paper">#F7F4F0</color>
</resources>