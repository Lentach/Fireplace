# Latest session summary

**➡ FRESH AGENT: your work queue is `2026-08-03-HANDOFF-signal-grade-queue.md`** — B2 web sealing (with settled design constraints), delete-for-me hard-delete, sweep success diags, Android track. Pair with `2026-08-02-HANDOFF-post-incident-state.md` for the standing invariants.

**✅ P0 CLOSED AND CONFIRMED: `[Decryption failed]` fixed in 0.0.139, several days with zero new reports.** The four hypotheses in `2026-07-29-HANDOFF-decryption-failed-incident.md` are all DEAD (killed by measurement) — that file is historical, do not work from it. Root cause: `2026-07-29-session-decryption-failed-root-cause.md`. **Never uninstall / clear site data.**

**🔴 FCM IS DISABLED IN PRODUCTION.** `FIREBASE_SERVICE_ACCOUNT` is absent from `~/fireplace/.env`, so every backend boot logs "FCM disabled". `google-services.json` IS tracked, so an APK would register tokens the server can never push to — **Android notifications are dead until that var is set.** Web Push VAPID is present, so the PWA is unaffected, which is why this went unnoticed. Fix it before any APK work.

- **Still binding (applies to every entry below):** never run `dart format lib/` (format only what you edited); **ask before opening the browser tool**; `node scripts/lint-ratchet.mjs` runs ONLY in CI — run it before pushing backend changes; `cmd | tail` hides exit codes; never re-derive test counts by arithmetic across a merge; `flutter run -d web-server` serves a blank scaffold once its hot-restart client is lost — restart the process, do not reuse the tab; the E2E register throttle is 10/hr/IP in memory, so `docker compose restart backend` between full `test_e2e` runs and allow 2–5 min to boot.

**Date:** 2026-08-03 (late night) — **RELEASED `0.1.6 / 0014684`, FRONTEND ONLY, smoke 5/5, CI 4/4 — QUEUE ITEM 4 LIVE: terminal-duplicate retirement + `DECRYPT_DECISION` durable dedupe.** A HISTORY row failing `duplicate` with no ledger entry, `recordExists == false` AND `rawReplayExists == false` (both DEFINITE, `null` strictly inert), observed in **3 distinct boot-nonce-gated process lifetimes**, renders retired instead of retrying the consumed ratchet key forever — nothing deleted, resend recovers, any DEFINITE readable source resets the counter, retire drops it. Repeat `(msgId, kind)` `DECRYPT_DECISION` durables now route to the ring only (the cap-80 log was ~90% this noise); dedupe state IS the cache, so eviction/clear re-arms — never permanent. Flutter **1196 + 10 skipped** (+22), analyze clean, verifier OK. Backend stays `0.1.2 / ded8e1a2`.
- **Full gauntlet:** design doc → independent design review (REVISE: R1 boot-nonce not RAM-set, R2 guard-(a) partition is jurisdictional — guard (b) carries the safety through the edit-stale fall-through, R3/R4 test gaps, R5 accepted narrowing — all folded in) → **11 falsification mutations each run RED** → data-loss review **SHIP** (no loss vector) + spec review **SHIP-WITH-FIXES** (drop-counter-on-retire was missing; without it a retired-set-load-failure boot re-retires and burns a SECOND durable — fixed same session).
- **Counter:** `e2e_<uid>_dupterm_v1`, cleartext control record, cap 64 highest-kept, lockless (cross-engine last-write-wins clobbers increments — under-count, safe direction).
- **Post-deploy expectations:** first 3 owner boots emit `DUP_TERMINAL_SEEN {msgId, n}` (ring); then ≤14 one-time `DUP_TERMINAL_RETIRED {msgId, senderId, sessions}` durables for the known rows (peers 49/52/60/83); the rows flip to the retired label; the durable noise stops immediately. **Watch:** a `DUP_TERMINAL_RETIRED` OUTSIDE the known ~14 set = the class is still growing, investigate the producer.
- Trap: an `edit`-tool auto-repair silently swallowed a test fake's override — an "impossible" test failure was a missing override, not async timing. Grep for the override first.
- ➡ Detail: **`2026-08-03-session-terminal-dup-retirement.md`**; spec: **`docs/design/terminal-duplicate-retirement.md`**.

**Date:** 2026-08-03 (afternoon) — **KETOKECZUP iOS LOGOUT: investigated + account recovered, NO code changes, ALL FOLLOW-UP HARDENING DROPPED BY OWNER.** Device-side storage loss (server exonerated: zero refresh 4xx, zero logout POSTs, his row survived and slid daily to 08-02 08:00 UTC; two fully-cold boots with no refresh attempt; no deploy wave to explain them). He was locked out by his OWN wrong password (8× bcrypt failures; lookup IS case-insensitive). **Manual reset done on prod 15:27 UTC** (bcrypt-10 in-container, `passwordChangedAt=now()`, refresh rows purged, login 201 verified then test session revoked) — temp password handed to owner. **Expect a one-time identity reset when he logs in** (his Signal keys almost certainly died in the wipe; OTP xmin still 802 = not yet regenerated). ⚠ **Retracted:** the "standalone iOS omits `Safari/604.1` from UA" heuristic is UNSUBSTANTIATED on modern iOS — never infer PWA-vs-tab from UA; only `navigator.standalone`/`display-mode` on the client can tell. **Owner ruled the wipe unpreventable and dropped beacon/banner/cookie/password-recovery — do NOT build these unprompted**; the decision tree is preserved in the detail file so nobody re-derives it.
- ➡ Detail: **`2026-08-03-session-ios-logout-investigation.md`**.

**Date:** 2026-08-03 (night) — **RELEASED `0.1.5 / bf886a6`, FRONTEND ONLY, smoke 5/5, CI 4/4 — B2a WEB AT-REST SEALING IS LIVE AND FIELD-VERIFIED** (owner dump: `WEB_SEAL_OPEN {sealed: 179, legacy: 0, unreadable: 0, lostRows: 0, ms: 246}`, `WEB_SEAL_DRAIN_DONE {sealed: 190}`, zero fallback/key-loss events, retired set unchanged through deploy + migration + the owner's manual all-conversation deletion — 56 records purged, exactly 56 ledger entries dropped). All web decrypted history + raw-replay + pendsend seal as AES-256-GCM envelopes (`fps1:<kid>:<cid|->:<b64>`) in localStorage, keys in the canary-proven IndexedDB+WebCrypto store; new `SealedWebContentKv` behind the untouched `ContentKv` seam. Backend stays `0.1.2 / ded8e1a2`. Flutter **1174 + 10 skipped**. **NEXT-UP (promoted): retire terminal-duplicate rows + DECRYPT_DECISION durable dedupe — ~14 rows (peers 49/52/60/83) now flood the cap-80 durable log, noise evicts evidence. Owner: fingerprint-verify peers 54 AND 90.**

- **Process ran the full gauntlet:** design doc first → independent design review (REVISE: C1 mint-race CRITICAL, H1 drain-verify order, M1 snapshot totality + 2) → all folded in → implementation → spec review SHIP + data-loss review SHIP-WITH-FIXES (D1 drain CAS, D2 fold sibling check — both fixed same session). **Seven falsification passes, each proven red** (F1 omission→recordExists false, F2 verify-after-write destroyed the only copy, F3 erasure under-select, F4 CAS removed→clobber+resurrect, F5 unfiltered fold, plus the committed C1 hazard test).
- **The five load-bearing rules** (do not weaken; every one has a red-proven test): unseal failure = UNDETERMINED never absent (raw envelope served, snapshot TOTAL, enumeration failure throws → `recordExists` null); proven key loss only inside the `fireplace-e2e-content-keys` Web Lock, retiring only ids with NO readable sibling source, rows never deleted; drain = RAM round-trip verify + compare-and-set BEFORE the in-place overwrite; erasure completeness via deliberate cleartext `cid` (user deletion selects sealed rows in ANY session state, authoritative scans only); control records stay cleartext, read-both forever.
- Post-deploy dump expectations: `WEB_SEAL_OPEN{...,ms}` ring per boot, one `WEB_SEAL_DRAIN_DONE{sealed}` durable when the one-time drain over ~138 records finishes; `CONTENT_STORE_FALLBACK{web-*}` / `CONTENT_KEY_LOST` / `CONTENT_RECORDS_UNREADABLE` are NOT expected — escalate on sight. Unexpected "no longer stored" → dump FIRST, unchanged.
- Traps: SharedPreferences updates its RAM cache even on a REFUSED commit — assert against the platform store; set failure flags BEFORE `openStore()` in tests or the auto-drain races them.
- ➡ Detail: **`2026-08-03-session-web-b2a-sealing.md`**; spec: **`docs/design/web-content-sealing.md`**.

---
### Prior latest ↓

**Date:** 2026-08-03 (later) — **RELEASED `0.1.4`, FRONTEND ONLY, the expiry-stamp destruction fix.** The Storage-sets panel PROVED the LEDGER_RECORD_LOST alarms were a REAL bug destroying live data, not the wipe: retired set held ONLY the five gate entries. Flutter **1142 + 10 skipped**, analyze clean, verifier OK, two independent reviewers (data-loss, spec) both SHIP with no CRITICAL/HIGH.

- **ROOT CAUSE (cause proven, timing to the minute):** a live-received read-mode disappearing message persists with `expiresAt: null`; the ONLY stamp upgrade was the unacked `messageDelivered` event (`events.dart:188-190`) which is silently lossy (race with persist → traceless no-op; missed socket window → never redelivered); the never-read fallback (`createdAt + 1d`) then let the per-minute sweep DESTROY the only plaintext copy of messages the server serves 4 more days — no retire, no ledger cleanup. The five maoi (peer 60, conv 75) messages died this way; server rows confirmed intact. **The ledger's first real catch. Recovery = resend only.**
- **THE FIX RULE (do not weaken): a fallback-derived deadline NEVER authorizes destruction.** `_recordExpiryDeadlineMs` honors only a REAL server stamp; unstamped records do not expire locally — residue is reconcile's job (≤6h past server delete) + the 30-day retention bound. Ordering constraint that forced this shape: the sweep can run BEFORE any history pass (it did, same second, 23:55:12), so the history-pass self-heal in `_restoreFromPersistedPayload` (single point every served row meets its record; payload-gated — an unconditional stamp would be one getAll PER ROW, the 65-77ms/page trap) is recovery, not the guarantee. `EXPIRY_STAMP_MISS` ring diag on a lost stamp.
- **Hygiene shipped with it:** deliberate purges drop ledger entries (`forgetDecryptedMany`, confirmed disk removals only); the wipe returns `wipedIds` and mirrors them into the RAM retired/ledger sets (a same-session wipe can no longer fake LEDGER_RECORD_LOST); **one-time purge-backlog amnesty** (`e2e_<uid>_purge_amnesty_v1`) drops pre-0.1.4 obligations for live UNSTAMPED records so the drain cannot replay the old rule's destruction — reviewer-found, `PURGE_AMNESTY{dropped}` durable diag. `mayDestroyExpiredPlaintext` DELETED (dead, carried the fallback into a destruction decision — tombstone in `message_expiry.dart`; `messageExpiryDeadline`'s fallback is DISPLAY-only).
- **Falsification:** the old sweep test pinned the destructive behavior and went RED against the fix before being rewritten. Net +8 tests; VM purge tests cannot assert `isComplete` (no path_provider → AudioCacheStore reports every id by design).
- Accepted residual (P3): wipe `wipedIds` is a cross-prefix union — a raw-key removal failure still retires the id while raw bytes linger; matches wipe intent, surfaced by `isComplete=false`.
- ➡ Detail: **`2026-08-03-session-expiry-stamp-destruction-fix.md`**.

---
### Prior latest ↓

**Date:** 2026-08-03 — **RELEASED `0.1.3 / 7dc0a9e`, FRONTEND ONLY, smoke 5/5, CI green 4/4 pre-deploy.** Backend stays `0.1.2 / ded8e1a2` by design. Audit session: recent changes verified SAFE for the PWA; FLAG_SECURE + incognito keyboard + hacker-mode Storage-sets inspector shipped; first field diag dump analyzed. Flutter **1134 + 10 skipped**, analyze clean.

- **Owner directive: subagents in this repo use Anthropic models ONLY** — two Codex-backed agents died `usage_limit_reached` mid-session.
- **The decrypt ledger + seam changes (`ac880f6..ded8e1a`) hold under independent re-review: no regressions, confidence 0.9.** All three fail-open rules verified at file:line, all four earlier defects confirmed fixed with falsified tests. Retirement requires `exists==false && replayable==false`, both tri-state.
- **🔴 `LEDGER_RECORD_LOST` × 5 FIRED ON DAY ONE** (owner's dump, 08-02 23:55:12, msgIds 19139/19186/19187/19189/19190, ALL senderId 60 = `maoi`). **TTL race RULED OUT by the server DB:** all five sent 08-01, expire 08-05 (4-day read-based timer), rows intact and not hidden. Retention/LRU RULED OUT (retired check at `decrypt.dart:950` precedes the gate; `stored: 138` ≪ 2000 cap). Two candidates stand: (A) owner pressed "Delete all local history" on 08-02 in the same long-lived tab — benign, because the wipe persists `markRetired` but NEVER syncs the in-memory `_retiredIds`/`_decryptedLedger` (`encryption_provider.dart:551-563`, a real defect worth fixing); (B) genuine record loss, the class the ledger exists to catch. **Discriminator: the retired set's id range — now readable in-app.** Owner is on an iOS Safari PWA (iPhone 14, no Mac, NO devtools, and iOS PARTITIONS home-screen-PWA storage from Safari tabs, so bookmarklets inspect the wrong store) — hence the new hacker-mode **Storage sets** panel (Load → retired/ledger/stored count/min/max/last20, Copy full). Retired set reaching deep into 18-19k with thousands of ids ⇒ wipe (benign); small/old ⇒ real loss, investigate BEFORE B2. Also: the expiry sweep purges its `expired` class without `markRetired`/ledger removal (`:362-366`) — separate latent noise source.
- **`CANARY_OK {ageDays: 5}`, zero canary-lost durables → web B2 sealing is UNBLOCKED** — positive 5-day survival evidence, not absence. Also in the dump: 19102/19105/19106 (peer 60) fail `duplicate` every boot — pre-ledger damage; `PEER_IDENTITY_CHANGED` peer 90 on 07-31 — owner should fingerprint-verify.
- **Shipped: `MainActivity.kt` FLAG_SECURE** gated on `ApplicationInfo.FLAG_DEBUGGABLE` (NOT BuildConfig — generation off under AGP 8; debug keeps emulator screenshots, release+profile protected; `flutter build apk --debug` green). **Add to device smoke: screenshot attempt in the release APK must be refused.** **Shipped: `enableIMEPersonalizedLearning: false`** on the composer + secret-note fields (Android-IME hint, no-op web/iOS, PWA unaffected). Input suite 90/90.
- **Signal-grade scope settled with owner: practical tier** (at-rest sealing + client hardening); sealed sender and encrypted backups explicitly out. Order: owner does keystore backup + `FIREBASE_SERVICE_ACCOUNT`; resolve the LEDGER_RECORD_LOST discriminator; B2; delete-for-me hard-delete; sweep success-diag; later app lock/cert pinning. Android release audit found the config strong (backup lockdown, netsec, signing gates, data-only push payload) — the blockers are unchanged and owner-side.
- ➡ Detail: **`2026-08-03-session-audit-and-screen-security.md`**.

