# Latest session summary

Entries are newest-first. Each one links its dated file, which holds the full account.

**🗺 START HERE IF YOU ARE LOST: `2026-08-05-HANDOFF-audit-state.md`** — plain-language map of the 2026-08-04/05 full-codebase audit: what was found, what merged, what is deliberately parked, and the three owner-only decisions. The two ordered task queues are `.planning/full-audit/REMAINING-WORK.md` (backend) and `.planning/full-audit/frontend/REMAINING-WORK.md` (frontend), both local-only/gitignored. Older long-form: `2026-08-03-HANDOFF-post-b2b-state.md` (B2b gate detail + traps; **its work queue is DONE**) and `2026-08-03-HANDOFF-signal-grade-queue.md` (dated addenda).

**⛔ TWO DEPLOY GATES, BOTH STANDING. MASTER IS AHEAD OF PROD ON BOTH TIERS.**
- **Frontend:** any deploy from master ships the merged-but-undeployed B2b `sig_*` at-rest sealing. Needs a dump proving `CANARY_OK {ageDays > 7}` **AND** a fresh owner OK; any `CONTENT_KEY_LOST`/canary-loss before then is a STOP signal. Prod runs the BRANCH build `c01317c` (0.1.8) and is **deliberately ≠ master** — do not "fix" that. Next master frontend deploy will be 0.1.9.
- **Backend:** master carries an entity change + migration `0011`, so a **staging dress rehearsal is REQUIRED first** (root `CLAUDE.md` §6).

**✅ P0 CLOSED AND CONFIRMED: `[Decryption failed]` fixed in 0.0.139, several days with zero new reports.** The four hypotheses in `2026-07-29-HANDOFF-decryption-failed-incident.md` are all DEAD (killed by measurement) — that file is historical, do not work from it. Root cause: `2026-07-29-session-decryption-failed-root-cause.md`. **Never uninstall / clear site data.**

**🔴 FCM IS DISABLED IN PRODUCTION.** `FIREBASE_SERVICE_ACCOUNT` is absent from `~/fireplace/.env`, so every backend boot logs "FCM disabled". `google-services.json` IS tracked, so an APK would register tokens the server can never push to — **Android notifications are dead until that var is set.** Web Push VAPID is present, so the PWA is unaffected, which is why this went unnoticed. Fix it before any APK work.

- **Still binding (applies to every entry below):** never run `dart format lib/` (format only what you edited); **ask before opening the browser tool**; `node scripts/lint-ratchet.mjs` runs ONLY in CI — run it before pushing backend changes; `cmd | tail` hides exit codes; never re-derive test counts by arithmetic across a merge; **never give `flutter test` a file list** (per-argument compile cost: 45 files timed out past 11 min, the whole suite runs in 170–310 s); `flutter run -d web-server` serves a blank scaffold once its hot-restart client is lost — restart the process, do not reuse the tab; the E2E register throttle is 10/hr/IP in memory, so `docker compose restart backend` between full `test_e2e` runs and allow 2–5 min to boot.

**Date:** 2026-08-05 — **AUDIT REMEDIATION MERGED: #133 (backend, 16 findings), #134 (frontend, 19 — own entry below), #135 (BE-007). Then the stale dependency PRs #130/#131/#132 merged, taking GitHub alerts 7→0 (3 high). CI 4/4 green on every merge incl. `e2e-wire`; zero open PRs. NOTHING DEPLOYED.** Backend **589→670 / 49 suites**, Flutter **1236→1247 + 10 skipped**, lint-ratchet 816→912 (both bumps were new spec-mock code; **no new `any` in source**, verified by diffing added lines). The dep bumps (undici, socket.io-parser, fast-uri) are backend-only and ride the next backend deploy alongside migration `0011`.
- **The backend audit itself: 52 findings, 0 CRITICAL, 109/109 non-spec files.** Verdict: the tier is well built. The unrecoverable class is **confirmed absent** — nothing advances ratchet/session/prekey state on a failed op. OTP claim atomically single-serve; `[rows,rowCount]` correct everywhere; secret-note reveal quotes `"expiresAt"` and only `POST /reveal` burns (no bot/GET path); SSRF holds both layers; all 13 entities match migrations. **Findings stay LOCAL-ONLY** in `.planning/full-audit/` (gitignored) — a concentrated inventory of unfixed vulns in a live E2E messenger.
- **Fixed + merged:** block correctness (BE-006/101/102/103 — `block()` self-heals on retry, requests removed both directions, accept gains an `isBlockedByEither` guard, accept/reject conditional on `status='pending'`); block bypass on reactions/typing/voice (BE-202, 5 s per-pair memo so typing costs ≤1 query/pair/5 s); empty `MEDIA_CLEANUP_GRACE_MS` no longer means ZERO grace (BE-300); expiry-cron re-entrancy guard + batching (BE-150); conversation-pair unique index (BE-250, migration `0011`); `resetPassword` revokes before stamping and `deleteAccount` defers irreversible unlinks past commit (BE-350/351); plus BE-005/152/153/354/356/500/501/502/553.
- **⚠️ BE-007 (NEW, HIGH, found by the cross-tier exchange, not in the original 52).** `onlineUsers` was last-write-wins, so **opening a second tab silently deafened the first** — connected, authenticated, unaddressable — and push suppression read the same map, so the user on the older tab got neither message nor notification. Every emit is now room-addressed via new seam `chat/utils/user-room.ts`; the map and the guarded-disconnect dance are deleted (Socket.IO owns membership).
- **⛔ DO NOT room-address `newMessage` / `messageEdited`.** Signal decrypt is NOT idempotent — it consumes the message key; tabs share one session store, so a second tab's decrypt of the same ciphertext FAILS into the client's session-destroying failure policy. Both use `emitToNewestTab` (reproduces the old target exactly). **Push suppression evaluates that SAME socket** — `.some()` across tabs was tried and backed out: a focused tab A would suppress the push while ciphertext landed on background tab B. Lifting the carve-out needs frontend decrypt-idempotency proof AND moves both to room-wide in ONE change.
- **➡ Queue for later:** `.planning/full-audit/REMAINING-WORK.md`. Next: **BE-301** (last data-loss path, not currently exposed — prod `MEDIA_CLEANUP_GRACE_MS` verified absent), **BE-008** (throttle rejections are invisible: no WS exception filter exists, client listens on `error` only — this BLOCKS BE-004), then **BE-004**. Crypto PR (BE-400/551/402) unblocked with exact client key sizes; **BE-402's prekey-fetch gate was WITHDRAWN** — bound the OTP burn instead, gating bakes product policy into the crypto layer.
- Traps: CI runs on **nothing** for a feature-branch push, and a `CONFLICTING` PR silently runs **no checks** (looks like slow CI — check `gh pr view N --json mergeable`); `git push` once reported `[new branch]` without pushing the commit (verify with `git ls-remote`); backend+frontend test counts share one `CLAUDE.md` line and collide between agents.
- ➡ Detail: **`2026-08-05-session.md`** (remediation) and **`2026-08-04-session.md`** (the audit).
- **✅ BACKEND DEPLOYED 2026-08-05 10:44Z — prod is now `884f6d0c` (was `ded8e1a2`/0.1.2).** Preflight all green: zero duplicate conversation pairs, `X-Real-IP` on 11/11 `proxy_pass` locations, fresh encrypted backup, and a **staging rehearsal against restored production data** where the runner applied `0011` at boot by itself and the wire harness went 13/2-skipped. Post-deploy: `/health` ok, `applied 0011`, index present, **zero** errors in the boot log. Rehearsal trap re-paid: `pg_restore --clean` cannot drop a PK other FKs depend on → `DROP SCHEMA public CASCADE; CREATE SCHEMA public;` first (already documented, still bites). **`APP_VERSION` deliberately still 0.1.8 — `pubspec.yaml` is NOT bumped until the frontend deploy, because `deploy-backend.sh` reads the same line.** ⛔ Frontend still HELD: `CANARY_OK {ageDays: 7}` on 2026-08-05 and the gate is `> 7`, so re-dump 08-06; no `CONTENT_KEY_*_LOST` anywhere, and `WEB_SEAL_OPEN {sealed: 202, legacy: 0, unreadable: 0, lostRows: 0}` says B2a is healthy in the field. Detail: **`2026-08-05-HANDOFF-audit-state.md`**.

**Date:** 2026-08-04 — **FRONTEND AUDIT REMEDIATION: 19 of 31 findings closed, merged as #134 (`195d894`).** Flutter **1236→1247 + 10 skipped**, verifier synced, analyze clean, a real `flutter build web --release` run as a gate. Detail: **`2026-08-04-session-frontend-audit-fixes.md`**; findings + queue (local-only): `.planning/full-audit/frontend/findings.md`, `REMAINING-WORK.md`.
- **Every HTTP call was unbounded.** `api_service.dart` 21/21 now `.timeout()` by class (auth 15 s / upload 60 s / rest 20 s; multipart under ONE deadline). **`refreshSession` timeout maps to `SessionRefreshTransientException`, NEVER `Invalid`** — transient holds the session, invalid clears local auth, so the wrong mapping manufactures the very logout this repo chased. Closed the dead-app case: a stalled refresh left `_isRestoringSession` true forever and AuthGate spinning until force-quit. Same class fixed in `sendGif`, `gif_service`, both `download_utils_*`. **The 60 s upload budget came from audit guidance, not a measured p99** — revisit it if large uploads fail on slow links.
- **A voice note could be destroyed silently.** Teardown during `recorder.stop()` ran `dispose()` → file deleted → unguarded `setState` threw BEFORE the `try` calling `onVoiceSent`. Now `dispose()` defers while a stop is in flight, setStates degrade to field writes once unmounted, and the conversation id is captured at send time (`ChatDetailScreen.dispose` clears `activeConversationId`).
- **⚠️ wasm would have deleted the multi-tab Signal lock.** `session_cross_context_lock.dart` + `storage_persist.dart` keyed on `dart.library.html`, FALSE under `--wasm` ⇒ stub, no lock, no persist request, zero diagnostics — and `verify-session-lock-probe.mjs` stays GREEN because it tests the browser API, not which Dart file compiled. Both now key `dart.library.js_interop`; verified in the built bundle.
- Also: `socketReady` watchdog (a connect that never reaches ready scheduled no reconnect and showed an authenticated empty shell forever); 20 s ack bound on accept/decline/send; mute no longer dropped by the pin handlers (`copyWith` + new clear flags); 1 Hz metadata-row rebuild gated on ephemeral; FCM `onTokenRefresh` no longer holds the boot JWT; denied `storage.persist()` now durable in `E2ePersistentDiag`; timer/blob-URL leaks; `DateTime.tryParse` on socket timestamps.
- **HELD for separate owner-approved PRs (decrypt path, per the audit's rule):** FE-013, FE-020 (instrument first — the audit's fix is disproven), FE-006, FE-001/2/3/5. Still queued small: prune friend requests on `blockUser`, and move **only** the two key-upload emits behind `socketReady` (today they fire pre-auth and the server drops them, so new peers cannot open a session until a reconnect). **FE-019 was already fixed** by `991a6b2` — the audit read a stale shape. **FE-024 skipped deliberately**: failing a send the server received releases the exactly-once latch and invites a duplicate.
- Traps paid: Codex-backed `task` subagents still die `usage_limit_reached` instantly (9/9) — Anthropic-only. Two tests failed *because* the fixes bounded previously-unbounded work (a held refresh / a held accept now park a real timer past teardown); both drain the in-flight action after their assertions, neither assertion weakened.

**Date:** 2026-08-04 — **RELEASED `0.1.8 / c01317c` (0.1.7/faa9dcd superseded same night: add socket now LEADS the comb — trailing socket vanished below the fold with many friends, position test-pinned), FRONTEND ONLY, BRANCH BUILD — smoke 5/5, merge CI 4/4 green. INVITATION UI HEX PASS (two rounds), `feat/invitations-hex-ui` merged to master (`84737bc`) but prod serves the BRANCH commit: owner chose branch-only deploy so the ⛔ B2b gate stays intact.** Round 1: invite-a-friend door in the Chats `+` picker, hex count badges, de-genericized Invitations tab, hex chat-header avatars. Round 2 (owner picked ideas 2/3/4/5): forge animation on accept, hex `+` badge, honeycomb waiting-inbox, hex ping lattice. Branch Flutter 1203 + 10 green; MERGED tree **1236 + 10 skipped** green, analyze clean, count verifier synced (CLAUDE.md §3 1229→1236). Owner approved in the preview harness (`test/preview/invitations_preview.dart`, `?view=inbox|picker|picker-empty|header|ping`). Reviews: designer SHIP-WITH-FIXES (blue-ring HIGH fixed), spec 0 findings, standards 3 P3 left as-is.
- **Shapes:** `HexCountBadge` (`hex_avatar.dart`) = the EXACT pointy-top `hexPath` hexagon for numeric badges (owner rejected a flat-top elongated first cut mid-session: "must be exact hex as rest of them"); `HexPill` (`widgets/hex_pill.dart`) = elongated 120°-cap hexagon, ONLY for word pills (`InvitationStatusPill`). `DashedHexPainter` (public, `hex_avatar.dart`) replaced both private dashed painters in `contact_network_view.dart`, visuals identical.
- **Picker:** comb ends in a dashed "+" socket → new `ChatPickerInviteNew` → `InvitationsScreen` (same door as the Contacts add hex; accept still NEVER inline per §6). Empty picker gets an `Invite someone` button instead of a lone socket (new ARB `chatPickerInviteButton` en+pl). +2 picker tests.
- **Chat header:** both `AvatarCircle`s in `chat_detail_screen.dart` → `HexAvatar` (36 embedded / 52 top-bar) — supersedes the round-4 bare-circle ruling by owner ask.
- **Design review SHIP-WITH-FIXES; HIGH fixed same session:** `HexAvatar` ring = `convItemBorder` at the painter's 0.6 alpha ≈ 1.3:1 on blue — invisible. Touched callsites now ring with `mutedText`. **`conversation_tile` still passes `convItemBorder` — check it on blue.** LOWs left for owner: accent-on-glass in picker hints, "Pending" used both directions, triple-accent accepted card, SPEC §10 title-pill doc drift.
- **Round 2 specifics:** `_ForgeHexAvatar` (invitation_row.dart) plays a 280 ms dash-solder overlay on pending→accepted — depends on the expanded card being a DIRECT sibling of outcome rows (shared peer-id key ⇒ element reuse); `_WaitingComb` (invitations_screen.dart) renders inbound requests as a lattice, tap = expand card, **a lone request auto-expands** (that rule is what kept every single-request test green; `-1` sentinel = user-collapsed); ping rings are const `_PingHexPainter` hexes preserving the file's build-once contract.
- ➡ Detail: **`2026-08-03-session-invitations-hex-ui.md`**.

**Date:** 2026-08-03 — **QUEUE ITEMS §2 + §3 CLOSED, ON MASTER, NOTHING DEPLOYED.** Backend **578→589**, Flutter **1224→1229 + 10 skipped**, both count verifiers synced, lint-ratchet held at 816, CI green on `2d3fb81` (backend) and `b1893c6` (frontend).
- **§2 (`2d3fb81`)**: `hideMessageForUser` hard-deletes once EVERY participant hid the row (set from the conversation relation, never hardcoded 2; empty/missing set = never delete; media before row). Append is ONE atomic `UPDATE…RETURNING`, killing the read-modify-save clobber race. Three guard falsifications run RED first (every→some; empty-set guard; detach removed). **Discovery: `messages.reply_to_message_id` self-FK has NO ON DELETE (`0001_baseline.sql:757`)** — any hard delete of a replied-to row threw 23503. `detachReplies()` now runs in the hide path, `deleteById` (delete-for-everyone was LIVE-BROKEN in prod for replied-to messages), and the per-minute expiry cron (which would have wedged, media already unlinked).
- **§3 (`b1893c6`)**: `sweepDestroyablePlaintext` logs `PLAINTEXT_SWEEP` to the RING on every acting pass ({expired, retired, removed, ids capped 30}); zero passes log once per transition into nothing-due (per-tick zeros would churn the 200-ring — the 0.1.6 noise class). Unconfirmable-clock silence is deliberate (root §7) and now test-pinned. One zero entry per idle stretch in a dump is CORRECT, not a stuck sweep.
- ➡ Detail: **`2026-08-03-session-queue-items-2-3.md`**. Remaining owner blockers: keystore backup, `FIREBASE_SERVICE_ACCOUNT` + real push, fingerprint-verify 54 + 90 — then the Android track.

**Date:** 2026-08-03 — **B2b `sig_*` AT-REST SEALING IMPLEMENTED + DUAL-REVIEWED, ON MASTER (`13a9fd1`), CI 4/4 — ⛔ DEPLOY HELD** (gate detail in the banner above). All web Signal key material (identity, sessions, prekeys, trusted pins) seals as `fpsig1:<kid>:<b64(IV||GCM)>`; keys `fp_sig_key_<kid>` in the canary-measured IndexedDB store, a SEPARATE namespace from content keys (cross-family test). Async seam → no RAM view (`SealedWebSignalKv` behind new `SigWebKv`, DualStorage memoized opener). Control records cleartext. Analyze clean, verifier OK.
- **Load-bearing rules (each red-proven, 15 mutations total):** `read()` on an unsealable value THROWS, never null (absence = identity regen / ratchet reset / prekey reuse); `readAll()` presence-preserving, never value-throwing — two real callers (`_hasPriorInstallResidue`, `_highestStoredPreKeyId`) swallow enumeration throws into absent-equivalents, both hardened fail-closed (residue inconclusive→present; prekey mint aborted, `PREKEY_MINT_SKIPPED`); fallback legal ONLY on a successful zero-envelope probe, and a fallback session re-probes before EVERY write; drain = ONE lock per row NEVER nested — the reviewed design's batch→per-peer nesting was an ABBA deadlock against the ratchet path's per-peer→lazy-open→sig-keys order (advisory catch); session + trusted rows drain under the per-peer lock; nothing destroyed at open, no retirement for key material, rollback = E2E down but intact.
- **Gauntlet:** design review REVISE (2 CRITICAL: swallowed-throw regeneration, drain-lock ratchet rollback — plus 6 more, all folded); data-loss review **SHIP** (no destruction/deadlock path; 2 LOW self-healing residuals, one fixed at source); spec review **SHIP-WITH-FIXES** (fixed same session, incl. dedicated §5.2/§5.3/§5.7 tests run red).
- Post-deploy expectations (when it ships): `SIG_SEAL_OPEN {sealed, legacy, ms}` ring per boot, `legacy` ~126→0, one `SIG_SEAL_DRAIN_DONE` durable; escalate on `SIG_KEY_UNAVAILABLE` / `SIG_ROWS_UNREADABLE` / post-first-boot `SIG_STORE_FALLBACK`.
- ➡ Detail: **`2026-08-03-session-b2b-sig-sealing.md`**; spec: **`docs/design/web-sig-sealing.md`**.
