# Latest session summary

**Date:** 2026-06-10

**Topic:** **Context-menu Copy action shipped (Clipboard Phase 1)** — long-press → Copy on TEXT bubbles writes decrypted plaintext to the system clipboard + `snackbarMessageCopied` confirm. Gate: new `MessageModel.hasCopyablePlaintext` (TEXT, non-empty, not `[encrypted]`/`[Decryption failed]`/`[Encryption not initialized]`); hidden for media/ping; ALLOWED on disappearing messages (explicit decision). Plumbing: nullable `onCopy` through `openMessageContextMenu` → `MessageActionPanel` (row below Reply); `computeMessageContextMenuLayout` gained `panelHeight` param + `kMessageActionPanelRowHeightEstimate=46` (5-row panel clears composer). ARB en/pl. **335 tests green** (was 325), analyze clean. Commits `fdd3aa2`..`8bfa66a`. Spec review also resolved Phase-2 gaps: image-then-caption **ordering contract** (`sendImageMessage` fire-and-forgets `_encryptAndSend` at `send.dart:162` — must await before caption emit), Android URI-only commitContent guard, cursor-aware mixed paste. **Next: Phase 2 plan (staged paste chip); deploy pending (rides with decrypt-cascade fix).** Spec: [docs/superpowers/specs/2026-06-10-clipboard-copy-paste-design.md](../../docs/superpowers/specs/2026-06-10-clipboard-copy-paste-design.md) → [2026-06-10-session-copy-message-text.md](./2026-06-10-session-copy-message-text.md)

**Previous:** 2026-06-10 — **Clipboard copy/paste design (research + spec)** — competitor survey (all 7 apps stage pasted images; long-press→Copy universal) + verified audit; chose context-menu Copy + staged composer chip; web = `paste` DOM event stub/web pair, Android = `contentInsertionConfiguration`, native iOS deferred. → [2026-06-10-session-clipboard-design.md](./2026-06-10-session-clipboard-design.md)

**Earlier:** 2026-06-10 — **E2E decrypt regression: Bad-MAC → session-reset cascade root-caused and fixed** — inbound decrypt-failure path deleted Signal sessions (permanent Bad-MAC loss) + sticky `_liveDecryptFailedPeers` + `_mergeHistorySnapshot` exclusion resurrected terminal rows, re-arming resets every reconnect. Fix: never delete sessions inbound (rebuild *request* only), terminal-row skip, prune, merge pre-seeds all local rows; `notifyPeerRebuild` on identityReset shrinks cache-wipe loss to ~1 msg. New privacy-safe diag events. Suite 325 green. **Next: deploy + capture E2eDiagLog from an affected device.** → [2026-06-10-session-decrypt-cascade-fix.md](./2026-06-10-session-decrypt-cascade-fix.md)
