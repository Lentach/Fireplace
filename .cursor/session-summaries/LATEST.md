# Latest session summary

**Date:** 2026-07-25 — `feat/contact-network` carries the whole visual pass: Contacts **Honeycomb Core** (header search, long-press → chat, **People board**: inbound request port + add cell), the **Chats list redesign** (hex avatars, unread as a lit row edge, live/normal/cold row weights), and the **Settings "local node console"** (owner: *"settings tab looks like a diffrent app"*). Owner drove every step from renders — and from his phone, which is what killed the spine. **Live ephemeral branch deploy is `8029424`** — still **0.0.128**, because branch test deploys never bump semver — smoke 5/5. A "local node bus" (rail down the gutter, item 16) was built, deployed, and **REVERTED on device**. NOT merged, no version bump. **PR #97 is open and ready: https://github.com/Lentach/Fireplace/pull/97.** ➡ **FRESH AGENT: read `2026-07-25-handoff-branch-ready-to-merge.md` — that is the only current pickup brief. `2026-07-24-handoff-contact-network-merge.md` is SUPERSEDED, ignore it.**

## What was done
1. Honeycomb Core (2026-07-23, owner verdict "all is perfect"): `ContactNetworkView` rewritten around the pure `ContactHexLayout` — reticle core top-center with the user's avatar, fixed-width staggered hex field below (4-3 rows on phones, up to 8-7 on desktop, partial last row self-centers), alphabetical order = spatial order, vertical scroll only. Contact avatar fills each hex. Socket stub per hex: **one pin = no conversation, two pins = active chat — owner explicitly KEPT this, do not unify.** Tap fills the route from the hex up to the core (480ms easeInOut + head dot) and opens the card; the strip NEVER appears on bare keyboard focus (focus = halo, Enter activates). Deleted: elliptical-ring engine, drag-to-pin store, Reset button, InteractiveViewer, dogleg traces, identicons.
2. Search moved into the header (2026-07-24): the 54px band under the header is GONE. Closed state = bare magnifier LEFT, title, list/map toggle RIGHT. Open state = full-width `GlassPill` input (inner magnifier, autofocus, ×) with the toggle keeping its slot, so one query still drives BOTH presentations across a view switch; Escape closes.
3. `MainTabScreenHeader` gained `.custom(child:)` (row-replacing, header still owns the geometry) and `leadingGlass` (default `true`) — Chats/Settings call sites untouched by design.
4. Trap paid: `InputDecoration.collapsed` only nulls `border`, so `RpgTheme.inputDecorationTheme.focusedBorder` painted a 2px box inside the glass capsule on focus; the field now sets `focusedBorder: InputBorder.none` explicitly.
5. Chats list redesign: shared `widgets/hex_avatar.dart` (`hexPath`/`HexClipper`/`HexAvatarSurface`/`HexAvatar`) extracted from the honeycomb so both tabs use ONE shape language. `ConversationTile` derives a row weight from data it already had — live (unread/typing) grows to a 50px hex with a second preview line + 3px accent edge + 5% wash, normal keeps the legacy 64px row, cold (>6 days) shrinks to 36px and dims; the stock unread pill is gone (count rides the time in accent), and the hex ring carries a recency ember. Dividers dropped in both lists; the Contacts classic list uses the same hex at the same left offset (owner's row-parity rule). Swipe-delete, active bg, muted icon, typing line, emoji spans and the ephemeral hearth arc all preserved verbatim.
6. Long press (or Shift+Enter) on a hex whose socket is doubled jumps STRAIGHT into the chat — no route fill in front of the zero-duration chat route; tap still fills the route and opens the card. Unwired contacts stay tap-only, so a stray press can never create a conversation. Screen readers get the action with an "Open chat" hint (`Semantics.fromProperties` — this Flutter's `Semantics` has no `hintOverrides`).
7. People board: the core grows an inbound port (`↓ N`, real `pendingRequestsCount`, docking stub, tap → add/invitations) and the field reserves ONE dashed "+" cell with an empty socket. The add cell is `ContactHexLayout.resolve(leadingSlots: 1)` geometry, never a synthetic contact — `NODES`, semantics count, traversal and search stay honest, and a filtered board grows no add cell. Empty accounts keep the board with the copy moved below it.
8. Reviewed by two independent `reviewer` subagents (Standards + Spec, parallel, no cross-talk): **Spec 0 findings**; Standards 4, no P0/P1 — the 480ms route fill vs the playbook's 400ms cap (owner RATIFIED it on device: "works better now, links is slower and visible to user"; now a documented exception in `docs/design/flutter-ui-playbook.md`), plus three open judgement calls: duplicated natural-sort comparator + redundant double sort, repeated `switch (weight)` in `ConversationTile`, and ~20-param `ContactNetworkView`.
9. Owner nit round on `38c12e8`: (a) the core's `Positioned` shrink-wrapped to the `LOCAL NODE` caption, pushing the drawn reticle ~17px right of `layout.coreCenter` — every wire is aimed at that point, so the LEFT first-row feeds visibly stopped short of the rim; the core now sits in a full-width band and a test pins its centre to the field axis. (b) Node chrome moved off `convItemBorder` (a `#E8E3DC`-on-`#FAF8F5` ghost in light themes) onto `colorScheme.onSurface`, and the inner hairline inside each hex + the inner ring inside the reticle are gone — one border per node, none inside it. (c) The add cell moved from the TAIL to the HEAD of the field (`extraSlots` → `leadingSlots`); at 27 contacts it needed a scroll to reach. A core-docked `+` port was built and reverted — no room for its caption beside the centred core caption, and touch has no tooltips. (d) Contacts view mode (map vs list) persists via `SettingsProvider.contactsListView`.
10. **A "power-on scan" entrance effect was built, shown to the owner, and DELETED at his request** ("its not a wow effect its just a scan, get rid of it"). Everything it needed went with it: the scan controller/painter/overlay, `MainShell`'s `TickerMode` wrap (back to byte-identical with master), and the entrance-defer plumbing. Do NOT rebuild it. Worth keeping from the episode: `MainShell`'s `IndexedStack` wraps children in `Visibility(maintainAnimation: true)`, so **an offstage tab's animations run at app boot** — the honeycomb's row-stagger entrance has always been spent behind the Chats tab and has never been seen on a device. That is the status quo and it is FINE (content is simply already drawn on tab entry). Two traps if anyone ever revisits: `TickerMode` mutes frames but NOT the clock (`ticker_provider.dart:44` — "Time still elapses"), so a muted controller jumps straight to completed on unmute; and deferring an entrance until the tab is visible makes the board paint blank on the first frame, which reads as a flash.
11. **Every contact is WIRED** (`cb10c36`): each hex owns a dormant trace along the exact `routePath` the accent fill later travels, so the board reads as a network at rest and the lit route is that same line rather than a second one. The painter's old row-0-only feed block is gone (one source of truth); the add cell gets no trace. The core also rose (`coreCenter.dy` 64 → 48), buying ~1 more visible row. **Fanning each trace to its own rim angle was built and REVERTED** — N rays converging on a 34px circle stay sub-pixel apart for ~180px, so at 100 contacts it filled into two solid grey wedges no alpha ramp short enough to spare row 0 could hide. "Fat at the top" was never geometry, it was **alpha compounding**: 13 traces sharing the rim→exit segment drawn as 13 `drawPath` calls composite 13-deep. Fix is ONE cached combined path (`ContactHexLayoutResult.traces`, memoized `Path? _traces` — the class is therefore no longer `const`/`@immutable`) stroked ONCE, so overlaps union and the weight is identical at 3 contacts and at 300.
12. **Route fill repaints, it no longer rebuilds** (`70919c6`): `AnimatedBuilder(_routeController)` wrapped the ENTIRE `Stack`, so all N nodes (Focus + Semantics + GestureDetector + ClipPath + Image each) rebuilt on every frame of the 480ms fill — ≈**6k subtree builds per tap at N=100**. It now wraps only the `Positioned.fill` CustomPaint. Pinned by a test asserting `identical()` node-widget identity across fill frames, **falsified: verified red against the old structure**.
13. **Avatars fetch lazily** (`30e6b03`): rows arm as they enter the viewport plus one row of lookahead, with a high-water mark so scrolling back never reverts a loaded face to initials. The gate is `ValueNotifier<int> _armedThroughRow` + `ValueListenableBuilder` around each avatar **leaf** — deliberately NOT `setState` on the field, which would reintroduce the exact cost item 12 just removed. Every node is still BUILT, so keyboard traversal, screen readers and scroll-to-reveal still reach all N. Test with 60 contacts, **falsified: verified red without the gate** (60 of 60 armed). `rowPitch` was hoisted onto `ContactHexLayoutResult` (painter and arming math were re-deriving the same formula). Measured before optimizing (throwaway probe, since deleted): build+layout per rebuild = 22.3ms @20 contacts, 28.1 @50, 30.5 @100, 40.6 @200, **109.0 @400** — linear to ~200, cliff after. That is why lazy avatars shipped and the `ListView` rewrite (costs 22 layout tests + breaks focus traversal to unbuilt nodes) was deliberately **deferred**. Note for honesty: on web the browser caps ~6 concurrent connections per origin, so 100 avatars was always a *queue*, not a request storm — this is bandwidth/decode hygiene, not a rescue.
14. **The lazy-avatar gate had a hole, now closed** (`bccd3d1`): `_armedThroughRow` is a high-water mark that was never reset, so ONE scroll to the bottom pinned it at the last row for the rest of the session — every later rebuild with a different contact list then armed ALL N avatars at once while the user sat back at row 0. The search field triggers it exactly (scroll down, filter, clear → 60 of 60 fetch). Fixed with a `didUpdateWidget` that zeroes the mark when the contact run changes, compared **by `id`** because `UserModel` has no `==` and comparing instances would reset on every provider tick. `_armedFloor` deliberately gets NO reset — the build right behind `didUpdateWidget` recomputes it from the current viewport unconditionally. **Falsified: disabling the reset turns the new filter-cycle test red with `Expected: less than <60>, Actual: <60>`.** A second reviewer finding — that a viewport shrink could un-arm visible faces — was **REFUTED by experiment**: the fix was built, then the code was broken to falsify it and the test stayed GREEN (`tall=28, short=28`), because the scroll controller notifies on mount so the mark is already populated before any user scroll. Fix and test both deleted rather than ship defensive code for an unreproducible defect.
15. **Settings rebuilt as the "local node console"** (`956479e`, owner picked variant A from a rendered A/B prototype). The tab was the one Material hold-out: a tinted `GlassSurface` card per row, stock `Icons.*`, a chevron on all 8 rows, four different corner radii and two hardcoded `Colors.white`. Now: **`LocalNodeCore` extracted** out of `contact_network_view.dart`'s private `_LocalReticlePainter` into `lib/widgets/local_node_core.dart` and consumed by BOTH tabs — the Contacts core and the Settings header are literally the same widget, because they are the same entity (you). It stays a CIRCLE among hexes on purpose (owner call); that shape difference is what marks the local node. Rows moved to `lib/widgets/settings_console.dart`: opaque, no dividers, no chevrons, 44px hex at left offset 12 / 12px gap — the exact geometry Chats and the Contacts classic list already use. Section captions (`PREFERENCES`/`SECURITY`/`SESSION`, new ARB keys) copy the type of the ALREADY-SHIPPED Appearance sub-screen so the root and the screen it opens speak one dialect. **The glyphs are drawn, not Material** (owner: *"dont forget to also change whats inside those hexes"*) — `ConsoleGlyphPainter` draws all nine in one 24-unit space at a single 1.6 stroke with round caps. The Appearance row keeps the real `AppearancePreview`, hex-clipped and deliberately OVERSIZED because the miniature paints its own radius-12 border that otherwise cuts arcs across the hex interior. Language chips became `PL`/`EN` codes with the full name on the semantics node: at 320px × textScale 1.6 the full words were a non-flexible ~210px trailing that overflowed the row by 79px.
16. **The local node BUS: built, deployed, then REVERTED on device** (`2b646c1` + `18c97a9`, reverted by `8029424`). Owner asked for the spine and, asked what the rail *means*, gave a rationale that is still worth reading: the app should read as *the inside of a computer* — hexes are a honeycomb, lines are traces, information flows out of the LOCAL NODE (your avatar), and every settings row is a FACET OF YOUR OWN NODE, so wiring each back to the core is literally true. (That is also why it did NOT contradict the Contacts "no shared-rail wiring" rule: that rule stops the honeycomb implying contact-to-contact relationships that do not exist; Settings has exactly ONE node.) It was rendered in three themes, tested, falsified and deployed — and he then saw it on a **physical phone** and said *"the other option was a better choice please revert it to the previous one"*. **A full-height rail reads far heavier at arm's length on a real phone than in a 390×844 desktop render; no screenshot in `docs/design/settings-console/` reproduces that.** Reverted by restoring the three code files to their exact `a342640` contents — byte-identical to a build he had already approved, which beats hand-unpicking a diff. `test/preview/settings_preview.dart` and every render were deliberately KEPT. **Do not re-apply `2b646c1`; it was judged on a device and rejected.** If the wiring is ever retried, the literal fan (N rays from the core, which his words may actually have meant) was never rendered for him.

## Verification
- `flutter analyze --no-fatal-infos` over `lib/` AND `test/`: 0 issues. Full suite: **826 passed / 4 skips** (14 honeycomb layout contracts, 9 header-search/view-mode, 6 row-weight incl. textScale 1.6 on 320px, 4 settings-console, plus core-centering, route-fill widget identity, lazy-avatar arming + contact-run re-arming, long-press, add-cell and inbound-port contracts). Backend untouched at 536. **Never run `dart format lib/` — it reformatted 70 untouched files including `main_shell.dart`, which must stay byte-identical to master. Format only the files you edited.**
- Visual: real ContactsScreen via `contact_network_preview.dart?screen=1[&pending=N]` (cosmic/light/blue, 390×844 + 320×700, search open/filtered, port docked, empty account) and the real `ConversationTile` via `glass_preview.dart` (dark/light/blue/teal, top + cold rows). The final round was rendered in light + cosmic at 390×844 and confirms: core dead-centre with BOTH first-row feeds meeting the rim, hex outlines legible, no inner ring in the reticle, `+` first cell top-left, `NODES 27` honest. **Ask before opening the browser tool — it is not headless and pops a window in front of the owner.** Every deploy verified by `/version.json` gitCommit AND the served `main.dart.js` sha. `graphify update .` run.

## Notes for next session
- **Live ephemeral deploy is `8029424`** (frontend 0.0.128/`8029424`, backend untouched 0.0.127/`3861166`), smoke **5/5**, and the branch tip equals it as of that deploy. Never infer what is live from `git log`; read `/version.json`. That drift bit this session three times, including a note that asserted "tip IS the deploy" and was falsified by the very commit that recorded it. Master untouched at `5a757d3`. **Three ways to prove a branch bundle really changed when semver does not move — use whichever fits.** (a) Grep the served `main.dart.js` for a string literal that is NEW in this build: the `a342640` deploy was proven that way (`PREFERENCES`, `PREFERENCJE`, `SESJA`, `LOCAL NODE`). A matching `gitCommit` alone is weaker — it is a dart-define and recompiles even when other units are reused. (b) When the change adds no new string (pure painter code), `flutter clean` before `deploy-web.ps1`, so freshness is guaranteed by construction; that is what `18c97a9` and `8029424` did. (c) For a REMOVAL, where neither of the above shows the code is *gone*, compare `curl -s .../main.dart.js | wc -c` against the known-good build: A = **6,867,805** bytes, A+bus = **6,869,132**, and the revert landed exactly on 6,867,805. **Do NOT repeat the retired theory that a hex stuck on initials is `_visibleThroughRow` viewport math — that was REFUTED this session.** The formula provably over-arms (it arms `ceil((offset+viewport+rowPitch-140)/rowPitch)` while the last on-screen row is `floor((offset+viewport-110)/rowPitch)`, so there is always ≥1 row of slack), and the arming viewport and the layout viewport are the SAME `safeRect.height`, so safe-area or keyboard insets shrink both identically and cannot desync them. Release path on his explicit OK: bump 0.0.128 → **0.0.129** (PATCH, never `+N`) as the last commit on the branch, merge PR #97, then `deploy-web.ps1` from master + `scripts/smoke/post-deploy-smoke.mjs`.
- **Settings SUB-screens are still the old app** and are the obvious next round: `privacy_safety_screen.dart` and `blocked_users_screen.dart` already use the project's `GlassTopBar`, but their content is stock `Icons.verified_user/key/devices/laptop/terminal/block` with no hexes, so tapping "Privacy & Safety" from the new console lands somewhere half-converted. Appearance is the third. Verdict, renders and the two render-caught defects are in **`docs/design/settings-console/NOTES.md`** (committed).
- Deferred ON PURPOSE: "sent invites as ghosts" needs a backend change — `friends.service.ts getPendingRequests` filters `receiver: { id: userId }`, so the client only ever sees INBOUND requests. Also not built: Chats `+` as a honeycomb picker, blocked users moving out of Settings. Ember is the easiest piece to drop if it reads as noise (`ConversationTile._ember` + `HexAvatar.ember`).
- Long press in the INSTALLED PWA is CONFIRMED WORKING on the owner's phone (`38c12e8` round) — the iOS "Safari eats long-press over canvas" risk did not materialise; no double-tap fallback needed.
- Full detail: ➡ **`2026-07-25-handoff-branch-ready-to-merge.md`** (THE pickup brief — state, the merge steps, owner working style, hard rules, every trap). Then `2026-07-25-session-settings-console.md` (this round in full — the console, the bus and its on-device reversal, the avatar-gate hole, both refutations, the `dart format lib/` incident). Older rounds: `2026-07-24-session-contacts-wiring-and-scale.md` (wiring, the reverted rim fan, both perf fixes + scale probe numbers), `2026-07-24-session-contacts-core-centering.md`, `2026-07-24-session-people-board.md`, `2026-07-24-session-chats-list-redesign.md`, `2026-07-24-session-honeycomb-header-search.md`, `2026-07-23-handoff-honeycomb-search.md`, `2026-07-23-session-honeycomb-core.md`. **`2026-07-24-handoff-contact-network-merge.md` is SUPERSEDED** and carries a banner saying so. All local-only. Design rationale + renders: **`docs/design/settings-console/NOTES.md`** (committed).

---
### Prior latest ↓

**Date:** 2026-07-24 — Removed the top-of-screen "Update available — fully close and reopen the app." nudge on user request (annoying); shipped straight to `master` as frontend **0.0.128**.

## What was done
1. `frontend/lib/screens/main_shell.dart`: deleted the stale-bundle nudge (`_staleNudgeShown` flag + comment, the `kIsWeb` `initState` post-frame `_nudgeIfBundleStale` hook, the method, and the `services/update_check.dart` import). Kept `showTopSnackBar` (still used by the friend-accepted toast).
2. Deleted the now-orphaned service: `services/update_check.dart` + `_stub` + `_web` (only the nudge called `isServedBundleNewer`).
3. Removed the unused `updateAvailableCloseReopen` key from `app_en.arb`/`app_pl.arb`; `flutter gen-l10n` regenerated the getters out of `app_localizations*.dart`.
4. Removed the resolved banner row from `docs/ISSUE-BOARD.md`. Bumped `pubspec.yaml` 0.0.127 → 0.0.128. `graphify update .`.

## Verification
- `flutter analyze lib/screens/main_shell.dart lib/l10n` → No issues found. `git grep` for the key/service/symbol across `frontend/lib` → no matches. Graph: 9218 nodes. Production post-deploy smoke pending below.

## Notes for next session
- Reverts PR #96's stale-bundle nudge only; the underlying stale-PWA reality is unchanged — users still must fully close + reopen after a deploy to activate a new service worker. Never uninstall / clear site data (wipes E2E keys).
- Full: `2026-07-24-remove-stale-bundle-nudge.md`.

---
### Prior latest ↓

**Date:** 2026-07-24 — PWA logout incident root-caused AND hardening released: PR #96 merged, **0.0.127 / `3861166` deployed to production (frontend + backend), smoke passed**. Server auth was CONFIRMED healthy (112/112 refreshes 201 over 14 days); the owner-verified victim suffered a FULL device-side origin-storage wipe (proven via Postgres xmin — tokens AND Signal identity destroyed and regenerated; permanent history loss on that device).

## What was done
1. Ruled out server causes with live VM evidence: `/auth/refresh` deployed + 100% successful; most users hold healthy 365-day sliding sessions; no `[auth-session-end]` warns; secret rotation/clock/CORS refuted.
2. "Logged out after 1 day+" = 24h access-JWT TTL on devices that never call refresh. DB fingerprint: victims' rows never slide (`expires_at` = `created_at`+365d exactly). Top churner was owner's incognito build checks (noise).
3. Verified logout path is E2E-key-safe at source: only `jwt_token`/`refresh_token` removed; re-login reuses on-device Signal store.

## Verification
- Investigation: read-only file:line reads + nginx/docker/psql queries on the VM. Fix branch: backend 536 passed/47 suites; frontend analyze 0 issues, 783 passed/4 skips; targeted regressions for the boot slide (valid-access boot slides once with `X-App-Commit`; slide 401/500 NEVER logs out).

## Notes for next session
- **PR #96 (`fix/pwa-logout`, 0.0.127) is MERGED and DEPLOYED** (backend `deploy-backend.sh` healthy; `deploy-web.ps1` published; post-deploy smoke 5/5; version.json BOM-free with injected `gitCommit`; CORS preflight allows `x-app-commit`). Contents: `navigator.storage.persist()` at every web boot (+`STORAGE_NOT_PERSISTENT` diag), backend `[identity-churn]` WARN on identityPublicKey change, proactive boot session slide, `X-App-Commit` on auth calls, session-end reason + compiled commit on the auth screen, stale-bundle nudge. Backend test count in root CLAUDE.md §3 is 536.
- Devices still on old bundles benefit only after one full close+reopen (the nudge code isn't in their cached bundle). Remind users; never uninstall/clear site data.
- On the next logout report, BEFORE re-deriving anything: (1) grep backend logs for `[identity-churn]` and `[auth-session-end]`; (2) nginx: did the victim's login carry `X-App-Commit`? absent/old = stale bundle; (3) victim's login-screen footer screenshot now shows commit + reason; (4) `refresh_tokens.expires_at` vs `created_at`+365d = never-slid fingerprint. Forensic trap: OTP upsert preserves `createdAt` and overwrites `identityPublicKey` — use Postgres `xmin` for rewrite dating.
- Platform truth (sources verified): `persist()` is real eviction protection on Android/Chrome installed PWAs; on iOS the home-screen install itself is the main exemption and `persist()` is best-effort. Manual site-data wipes are indefensible on any web platform (= Signal Web) — native builds are the only escape.
- NEVER clear site data / rotate JWT_SECRET as a "fix". Full details: `2026-07-24-session.md` (local-only, gitignored by incident rule).

---
### Prior latest ↓

**Date:** 2026-07-23 — PR #95 E2E cross-context ratchet repair deployed to production as frontend 0.0.126 / bundle `0486cb3`; backend intentionally unchanged.

## What was done
1. Created the branch from clean `origin/master`, restored only the 16 reviewed E2E production/test/runbook paths, and verified the diff contains no Contacts files.
2. Merged the 0.0.126 repair: origin-wide account+peer Web Locks, fail-closed missing-API behavior, exact-ciphertext replay, strict 40-record retention, web-only SharedPreferences coherence, provider message-id binding, and anonymized incident runbook.
3. Standards review found no hard documented-standard violation or correctness/concurrency defect; three non-blocking notes: the Web Locks test is vacuous under default VM tests, formatting churn inflates the diff, and lock-name strings could be centralized.
4. Spec review passed every incident requirement with no missing/partial behavior, scope creep, or unsafe implementation.

## Verification
- Isolated focused suite: **23 passed**; analyze: **0 issues**. Earlier full proof: Flutter **791 passed, 4 existing skips**, browser probe `SESSION_LOCK_PASS`, local full-stack Signal harness **11 passed**. PR CI passed both jobs. Production post-deploy smoke passed `/health`, `/version.json` 0.0.126, `/version`, literal bundle SHA `0486cb3`, and Flutter app boot. Graph: 9208 nodes.

## Notes for next session
- PR #95 is merged and deployed. Users must fully close and reopen the PWA; never clear site data. Live frontend is 0.0.126 / `0486cb3`; backend remains 0.0.123 / `4609af2`.
- Highest-value review follow-up: wire `session_cross_context_lock_web_test.dart` into a real Chrome CI lane when the project Chrome launcher is repaired. Manual source-controlled browser probe currently covers the JS boundary.
- Full: `2026-07-23-session.md`.

---
### Prior latest ↓

**Date:** 2026-07-22 — PR #94 merged into `master`; Appearance redesign and compact Settings website link released in frontend 0.0.125.

## What was done
1. Added a restrained FIREPLACE wordmark + localized “About” / “O projekcie” footer action immediately above the app-version block.
2. Wired `https://fireplace.ignorelist.com/welcome/` through `LaunchMode.externalApplication`, preserving the installed PWA.
3. Bumped frontend 0.0.124 → 0.0.125, merged PR #94 (`eb4ba89`), and deployed the resulting `master` bundle.

## Verification
- Focused Settings tests: 3 passed. Flutter analyze: 0 issues.
- Rendered Cosmic, Blue, Dark, Light, and Teal at 390×844 plus Light at 320×700.
- CI passed backend tests plus Flutter analyze/tests. Production smoke passed after the master deploy: health, frontend 0.0.125, bundle commit, Flutter boot. `graphify update .`: 9089 nodes.

## Notes for next session
- PR #94 is merged and the release is permanent on `master`; fully close/reopen the PWA after deployment, never clear site data.
- Full: `2026-07-22-session-settings-about-link.md`.

---



