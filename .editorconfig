# Fireplace — keep agent docs readable (UTF-8, no mojibake)
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
charset = utf-8

[CLAUDE.md]
charset = utf-8
