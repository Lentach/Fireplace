# Latest session summary

Entries are newest-first. Each one links its dated file, which holds the full account.

**🗺 START HERE IF YOU ARE LOST: `2026-08-05-HANDOFF-audit-state.md`** — plain-language map of the 2026-08-04/05 full-codebase audit: what was found, what merged, what is deliberately parked, and the three owner-only decisions. The two ordered task queues are `.planning/full-audit/REMAINING-WORK.md` (backend) and `.planning/full-audit/frontend/REMAINING-WORK.md` (frontend), both local-only/gitignored. Older long-form: `2026-08-03-HANDOFF-post-b2b-state.md` (B2b gate detail + traps; **its work queue is DONE**) and `2026-08-03-HANDOFF-signal-grade-queue.md` (dated addenda).

**⛔ DEPLOY GATES. MASTER IS AHEAD OF PROD ON THE FRONTEND ONLY** (backend prod ≡ master in code; the 08-05 backend deploy closed that gate).
- **Frontend (0.1.9):** ships the held B2b `sig_*` sealing. **⚠️ THE CANARY GATE IS NOT EVIDENCE — verified 2026-08-05.** `flutter_secure_storage_web` 1.2.1 is `localStorage`, not IndexedDB+WebCrypto, and it stores its AES master key there raw+extractable (`flutter_secure_storage_web.dart:110-116`). So `ContentKeyCanary` compares localStorage against a localStorage shadow — they die together, `CONTENT_KEY_CANARY_LOST` is ~unreachable, and `CANARY_OK {ageDays: N}` proves nothing about a durable key store. **B2b would put the unwrapping key in the same store as the sealed rows** (same `ContentKeyManager` as live B2a). Decide on the merits, not on `ageDays > 7`. Prod runs BRANCH build `c01317c` — deliberately ≠ master.
- **Backend:** no gate, and **DEPLOYED 2026-08-05 19:26Z at `7a845430`** — carries the unfriend history-wipe fix, so today's 0.1.8 clients are repaired. Smoke 5/5, 0 boot errors, fix confirmed in the container's own `dist/`.

**✅ P0 CLOSED AND CONFIRMED: `[Decryption failed]` fixed in 0.0.139, several days with zero new reports.** The four hypotheses in `2026-07-29-HANDOFF-decryption-failed-incident.md` are all DEAD (killed by measurement) — that file is historical, do not work from it. Root cause: `2026-07-29-session-decryption-failed-root-cause.md`. **Never uninstall / clear site data.**

**🔴 FCM IS DISABLED IN PRODUCTION.** `FIREBASE_SERVICE_ACCOUNT` is absent from `~/fireplace/.env`, so every backend boot logs "FCM disabled". `google-services.json` IS tracked, so an APK would register tokens the server can never push to — **Android notifications are dead until that var is set.** Web Push VAPID is present, so the PWA is unaffected, which is why this went unnoticed. Fix it before any APK work.

- **Still binding (applies to every entry below):** never run `dart format lib/` (format only what you edited); **ask before opening the browser tool**; `node scripts/lint-ratchet.mjs` runs ONLY in CI — run it before pushing backend changes; `cmd | tail` hides exit codes; never re-derive test counts by arithmetic across a merge; **never give `flutter test` a file list** (per-argument compile cost: 45 files timed out past 11 min, the whole suite runs in 170–310 s); `flutter run -d web-server` serves a blank scaffold once its hot-restart client is lost — restart the process, do not reuse the tab; the E2E register throttle is 10/hr/IP in memory, so `docker compose restart backend` between full `test_e2e` runs and allow 2–5 min to boot.

**Date:** 2026-08-05 (night) — **ADVERSARIAL SECURITY REVIEW (6 specialists + live prod probes). Four commits; owner picked "fix it properly" + "backend now, hold frontend", and the BACKEND IS DEPLOYED at `7a845430`.** Backend **670/49**, Flutter **1256 + 10 sk**, analyze clean, ratchet **912→907**, CI 4/4, smoke 5/5.
- **🔴 FIXED, WAS LIVE: unfriending ONE contact irreversibly purged the decrypted history of EVERY chat on the initiator's device.** The server sent the initiator its OWN id, and the client feeds that into `removeConversationsForUser` (`userOne.id == id || userTwo.id == id`) — you are a participant in all your conversations, so all matched and all were purged, permanently (ratchet consumed the keys). Each side now gets the OTHER party's id; the deploy above repairs 0.1.8 clients with no frontend release. Client also refuses a self-addressed id. Test proven RED first.
- **🔴 THE BIG ONE — web at-rest sealing keeps the lock and the key in the same drawer.** `flutter_secure_storage_web` 1.2.1 writes its raw AES master key into `localStorage` (see banner). B2a (LIVE) rows are unwrappable by ~15 lines of console JS; `fps1:` framing exposes `kid`+`cid`; **web never rotates or destroys content keys**, so deleted-row residue stays decryptable. Real fix = non-extractable `CryptoKey` in IndexedDB, which 1.2.1 cannot do.
- **Also live, none started:** no app lock/PIN/biometric anywhere (HIGH, web AND Android — bypasses all at-rest crypto); **no CSP / `X-Frame-Options` / HSTS on the app document** (only the VM's untracked nginx can fix it); `deleteConversationOnly` never tells the peer to purge; a hostile server can use `getServedMessageIds` as a **remote wipe**; FILE filenames reach the server cleartext; password change does not drop live sockets; signed prekey never rotated.
- **🔴 FIXED (`3d30b88`), WAS LIVE — the identity-change banner could not fire on the server-MITM path.** `encryption_service.dart` pre-saved the SERVER-SUPPLIED identity key before `processPreKeyBundle`, so `isTrustedIdentity` (where the compare + `onIdentityChanged` live) read back the key it had just written and returned early: a compromised server could serve its own self-signed bundle and read everything after, **no banner, no diag**. The pre-save was redundant twice over (our `isTrustedIdentity` is TOFU and never throws; `processPreKeyBundle` persists the identity itself) — deleted. The warning is now **persisted**, cleared only by the new `acknowledgePeerIdentity()`, and verification gained a **proactive door** ("Verify security keys" in the peer's Safety section) since a FIRST-CONTACT substitution raises no change to warn about. 4 detection tests proven RED against the restored pre-save.
- **Confirmed strong, do not re-litigate:** server never sees plaintext (no snippet/search column); media AES-GCM'd pre-upload, media keys never a DB column; every delete path is a hard `DELETE`, media first; push carries no message text; every `logger.*` audited clean; **no drafts ever persisted**; no decrypted media on web disk; no cookies/analytics/CDN tags; TLS 1.3 only; hostile socket `Origin` → 400; backups verified encrypted + offsite.
- **⏰ Trap paid:** a CI red mid-session was NOT the crypto change — `messaging_provider_ledger_gate_test` hardcoded `expiresAt = DateTime.utc(2026, 8, 5, 19)`, which **became a past deadline at 19:00Z today** and then failed every run (an expired served row is skipped before the self-heal). Proven pre-existing at `edaca33`; fixture is now relative to `DateTime.now()`. Scan the suite for other fixed future instants before trusting a green.
- ➡ Detail: **`2026-08-05-session-security-review.md`**.

**Date:** 2026-08-05 (evening) — **INDEPENDENT VERIFICATION OF THE WHOLE 08-03→08-05 WAVE: SAFE. Nothing reverted, nothing half-applied, no rollback needed. NO CODE CHANGED.** Owner was AFK for the wave and asked for a from-source re-check; six parallel agents (2 reviewers, 4 scouts) plus direct commands. Backend **670/49**, Flutter **1247 + 10 skipped**, analyze clean, both verifiers OK, ratchet PASS at 912, last 6 CI runs green, 0 open PRs, **0 dependabot alerts**.
- **Merge integrity MEASURED, which is what the fear actually was:** for `41e2f0b`, `195d894`, `2db65d9`, `dc5bfb5` the merge result is **byte-identical to the branch tip (0 files differ)** — no hunk dropped. The only two conflict resolutions in `eb84451..565d1d9` are **docs-only**; zero deleted files, zero reverts in range. Nine previously-shipped fixes re-confirmed at path:line (expiry-stamp rule, `detachReplies` ×3 paths, terminal-duplicate retirement, FLAG_SECURE, storage-sets panel, B2a seam, `storage.persist()`, `991a6b2` photos guard, media→detach→row).
- **BE-007 carve-out HOLDS** (`chat-message.service.ts:131,:601` still `emitToNewestTab`; push suppression reads the SAME socket at `:621`; no ciphertext emit went room-wide). Live backend is wire-compatible with the 0.1.8 field client — no event renamed, and the tightened DTOs reject only never-valid values.
- **Prod re-measured live:** `0.1.8/884f6d0c`, `/health` ok, backend `Up 6h (healthy)`, **0 errors in 2000 log lines**, `0011` applied + index present, 97 users / 55 conv / **231 messages (207 at deploy — real post-deploy traffic, clean)**. nginx re-grepped independently: **11 `proxy_pass` / 11 `X-Real-IP`**, no proxying location missing it. `FIREBASE_SERVICE_ACCOUNT` confirmed absent (boot `WARN`).
- **Rollback claim verified against `c01317c`'s own source, not the summary:** the old bundle hits an unguarded `jsonDecode` on an `fpsig1:` value and **throws before** the `absent → _generateKeys()` branch — a frontend rollback breaks web decryption until roll-forward but **destroys nothing**. Also confirmed by ancestry (not dates): B2a IS in `c01317c`, **B2b is NOT** — message text + media keys are sealed for real users today, **Signal identity keys stay cleartext until 0.1.9**.
- **Doc fix, source won:** the "20 s ack bound on accept/decline/send" line below was invitation-only (`_kInvitationAckTimeout`); message send has no ack timer **by design** — corrected in place.
- ➡ Detail: **`2026-08-05-session-post-wave-verification.md`** — full evidence table, ordered Android owner tasks. (Its "canary gate tips 08-06" note is superseded: the gate is not evidence — see the banner.)

**Date:** 2026-08-05 — **AUDIT REMEDIATION MERGED + BACKEND DEPLOYED. #133 (backend, 16 findings), #134 (frontend, 19 — own entry below), #135 (BE-007), then dep PRs #130/#131/#132 took GitHub alerts 7→0. CI 4/4 green on every merge incl. `e2e-wire`.** Backend **589→670 / 49 suites**, Flutter **1236→1247 + 10 sk**, ratchet 816→912 (spec-mock code only; no new `any` in source).
- **The backend audit: 52 findings, 0 CRITICAL, 109/109 non-spec files** — the tier is well built, and the unrecoverable class is **confirmed absent** (nothing advances ratchet/session/prekey state on a failed op). **Findings stay LOCAL-ONLY** in `.planning/full-audit/` (gitignored): a concentrated inventory of unfixed vulns in a live E2E messenger.
- **Fixed + merged:** block correctness (BE-006/101/102/103 — self-healing `block()`, requests removed both directions, `isBlockedByEither` on accept, accept/reject conditional on `status='pending'`); block bypass on reactions/typing/voice (BE-202); empty `MEDIA_CLEANUP_GRACE_MS` no longer means ZERO grace (BE-300); expiry-cron re-entrancy + batching (BE-150); conversation-pair unique index (BE-250, migration `0011`); `resetPassword` revokes before stamping, `deleteAccount` defers irreversible unlinks past commit (BE-350/351); plus BE-005/152/153/354/356/500/501/502/553.
- **BE-007:** `onlineUsers` was last-write-wins, so a second tab silently deafened the first (and suppressed its push). All emits are now room-addressed via `chat/utils/user-room.ts`.
- **⛔ DO NOT room-address `newMessage` / `messageEdited`.** Signal decrypt is NOT idempotent — it consumes the message key, and tabs share one session store, so a second tab's decrypt of the same ciphertext FAILS into the session-destroying failure policy. Both use `emitToNewestTab`. **Push suppression evaluates that SAME socket** — `.some()` across tabs was tried and backed out. Lifting this needs decrypt-idempotency proof AND both moved room-wide in ONE change.
- **✅ Deployed 10:44Z, prod `884f6d0c`** (was `ded8e1a2`/0.1.2). Preflight: zero duplicate conversation pairs, `X-Real-IP` 11/11, fresh encrypted backup, staging rehearsal against restored prod data (runner applied `0011` itself; harness 13/2-sk). Post-deploy `/health` ok, `applied 0011`, zero boot errors. Rehearsal trap: `pg_restore --clean` cannot drop a PK other FKs depend on → `DROP SCHEMA public CASCADE; CREATE SCHEMA public;` first. `APP_VERSION` stays 0.1.8 until the frontend deploy (`deploy-backend.sh` reads `pubspec.yaml`).
- **➡ Queue:** `.planning/full-audit/REMAINING-WORK.md`. Next **BE-301** (not currently exposed), **BE-008** (throttle rejections invisible — BLOCKS BE-004), then **BE-004**. **BE-402's prekey-fetch gate was WITHDRAWN** — bound the OTP burn instead. Traps: a feature-branch push runs **no** CI, and a `CONFLICTING` PR silently runs **no checks** (`gh pr view N --json mergeable`); `git push` once reported `[new branch]` without pushing (verify `git ls-remote`).
- ➡ Detail: **`2026-08-05-session.md`** (remediation) and **`2026-08-04-session.md`** (the audit).

**Date:** 2026-08-04 — **FRONTEND AUDIT REMEDIATION: 19 of 31 findings closed, merged as #134 (`195d894`).** Flutter **1236→1247 + 10 skipped**, verifier synced, analyze clean, a real `flutter build web --release` run as a gate. Detail: **`2026-08-04-session-frontend-audit-fixes.md`**; findings + queue (local-only): `.planning/full-audit/frontend/findings.md`, `REMAINING-WORK.md`.
- **Every HTTP call was unbounded.** `api_service.dart` 21/21 now `.timeout()` by class (auth 15 s / upload 60 s / rest 20 s; multipart under ONE deadline). **`refreshSession` timeout maps to `SessionRefreshTransientException`, NEVER `Invalid`** — transient holds the session, invalid clears local auth, so the wrong mapping manufactures the very logout this repo chased. Closed the dead-app case: a stalled refresh left `_isRestoringSession` true forever and AuthGate spinning until force-quit. Same class fixed in `sendGif`, `gif_service`, both `download_utils_*`. **The 60 s upload budget came from audit guidance, not a measured p99** — revisit it if large uploads fail on slow links.
- **A voice note could be destroyed silently.** Teardown during `recorder.stop()` ran `dispose()` → file deleted → unguarded `setState` threw BEFORE the `try` calling `onVoiceSent`. Now `dispose()` defers while a stop is in flight, setStates degrade to field writes once unmounted, and the conversation id is captured at send time (`ChatDetailScreen.dispose` clears `activeConversationId`).
- **⚠️ wasm would have deleted the multi-tab Signal lock.** `session_cross_context_lock.dart` + `storage_persist.dart` keyed on `dart.library.html`, FALSE under `--wasm` ⇒ stub, no lock, no persist request, zero diagnostics — and `verify-session-lock-probe.mjs` stays GREEN because it tests the browser API, not which Dart file compiled. Both now key `dart.library.js_interop`; verified in the built bundle.
- Also: `socketReady` watchdog (a connect that never reaches ready scheduled no reconnect and showed an authenticated empty shell forever); 20 s ack bound on **invitation** accept/decline/send (`_kInvitationAckTimeout`) — **message send has no ack timer and must not get one**, failing a send the server received releases the exactly-once latch and duplicates it; mute no longer dropped by the pin handlers (`copyWith` + new clear flags); 1 Hz metadata-row rebuild gated on ephemeral; FCM `onTokenRefresh` no longer holds the boot JWT; denied `storage.persist()` now durable in `E2ePersistentDiag`; timer/blob-URL leaks; `DateTime.tryParse` on socket timestamps.
- **HELD for separate owner-approved PRs (decrypt path, per the audit's rule):** FE-013, FE-020 (instrument first — the audit's fix is disproven), FE-006, FE-001/2/3/5. Still queued small: prune friend requests on `blockUser`, and move **only** the two key-upload emits behind `socketReady` (today they fire pre-auth and the server drops them, so new peers cannot open a session until a reconnect). **FE-019 was already fixed** by `991a6b2` — the audit read a stale shape. **FE-024 skipped deliberately**: failing a send the server received releases the exactly-once latch and invites a duplicate.
- Traps paid: Codex-backed `task` subagents still die `usage_limit_reached` instantly (9/9) — Anthropic-only. Two tests failed *because* the fixes bounded previously-unbounded work (a held refresh / a held accept now park a real timer past teardown); both drain the in-flight action after their assertions, neither assertion weakened.

**Date:** 2026-08-04 — **RELEASED `0.1.8 / c01317c` (0.1.7/faa9dcd superseded same night: add socket now LEADS the comb — trailing socket vanished below the fold with many friends, position test-pinned), FRONTEND ONLY, BRANCH BUILD — smoke 5/5, merge CI 4/4 green. INVITATION UI HEX PASS (two rounds), `feat/invitations-hex-ui` merged to master (`84737bc`) but prod serves the BRANCH commit: owner chose branch-only deploy so the ⛔ B2b gate stays intact.** Round 1: invite-a-friend door in the Chats `+` picker, hex count badges, de-genericized Invitations tab, hex chat-header avatars. Round 2 (owner picked ideas 2/3/4/5): forge animation on accept, hex `+` badge, honeycomb waiting-inbox, hex ping lattice. Branch Flutter 1203 + 10 green; MERGED tree **1236 + 10 skipped** green, analyze clean, count verifier synced (CLAUDE.md §3 1229→1236). Owner approved in the preview harness (`test/preview/invitations_preview.dart`, `?view=inbox|picker|picker-empty|header|ping`). Reviews: designer SHIP-WITH-FIXES (blue-ring HIGH fixed), spec 0 findings, standards 3 P3 left as-is.
- **Picker:** comb ends in a dashed "+" socket → new `ChatPickerInviteNew` → `InvitationsScreen` (same door as the Contacts add hex; accept still NEVER inline per §6). Empty picker gets an `Invite someone` button instead of a lone socket (new ARB `chatPickerInviteButton` en+pl). +2 picker tests.
- **Chat header:** both `AvatarCircle`s in `chat_detail_screen.dart` → `HexAvatar` (36 embedded / 52 top-bar) — supersedes the round-4 bare-circle ruling by owner ask.
- **Design review SHIP-WITH-FIXES; HIGH fixed same session:** `HexAvatar` ring = `convItemBorder` at the painter's 0.6 alpha ≈ 1.3:1 on blue — invisible. Touched callsites now ring with `mutedText`. **`conversation_tile` still passes `convItemBorder` — check it on blue.** LOWs left for owner: accent-on-glass in picker hints, "Pending" used both directions, triple-accent accepted card, SPEC §10 title-pill doc drift.
- ➡ Detail: **`2026-08-03-session-invitations-hex-ui.md`**.

