---
description:
alwaysApply: true
---

# CLAUDE.md — Fireplace

**Source of truth = this root file + the two tier files.** Root is cross-cutting and stays in context — read it before any Fireplace work. The tier files hold the detailed traps and are **not** auto-injected. Read the matching one **once, before your first change in that tier**, then keep it in context — do not re-read it on every edit:

- Changing anything under `frontend/` (Flutter/PWA/client) → read `frontend/CLAUDE.md` first.
- Changing anything under `backend/` (NestJS/Postgres/server) → read `backend/CLAUDE.md` first.
- Cross-tier or infra/deploy/docs → this root file is enough.

Keep root cross-cutting: if a fact only matters while editing Flutter or NestJS code, it belongs in the tier file, not here. Do not turn root back into a junk drawer.

## 1. Non-negotiable workflow

- Read this root file before any Fireplace app work, and the matching tier file before your first change in that tier (see the header rule). Main agents and spawned subagents both treat these three files as the project source of truth. When delegating, explicitly tell the subagent to read them — subagents do not inherit your loaded context.
- **`C:/Users/Lentach/Desktop/Fireplace` is THE working copy, on `master`** (worktree zoo consolidated 2026-07-22: five stale worktrees incl. `fireplace-ping-deploy` removed; all their branches were fully pushed/merged first). Still check `git status -sb` before assuming a clean master. The landing page lives in its own repo: `Desktop/fireplace-landing` (`Lentach/fireplaceWebsite`).
- **PRIVATE repo** — `Lentach/Fireplace` is private (`gh repo view --json isPrivate` → `true`, verified 2026-07-27; docs long claimed "public", which was wrong). The landing repo `Lentach/fireplaceWebsite` IS public — do not confuse them. A pre-commit hook scans staged changes for secrets and enforces the LATEST budget; activate once per clone with `git config core.hooksPath .githooks`. Bypass only in emergencies (`--no-verify`).
- At session start: read `.cursor/session-summaries/LATEST.md`.
- At task end: write/update `.cursor/session-summaries/YYYY-MM-DD-session.md` and `.cursor/session-summaries/LATEST.md`. Required summary sections: `# title`, `**Date:**`, `## What was done`, `## Key files`, `## Verification`, `## Notes for next session`. New LATEST entry goes on top; older entries shift to `Previous`/`Earlier`.
- **Dated summaries ARE committed** (since 2026-07-27; all 226). A fresh clone sees the full history. **`gitleaks` IS installed on the dev PC** (`gitleaks version` → 8.30.1, verified 2026-07-29), so `.githooks/pre-commit` takes its entropy+rule branch (`gitleaks protect --staged --redact`) and the prefix-only regex is only the fallback for a machine without it — an older note here claimed the opposite. It is still not a substitute for care: a `?key=<64 hex>` bearer token sat in a tracked summary for days under the old regex-only hook. Keep credentials and third-party PII out by hand; write `<REDACTED>` when pasting a URL or curl. A key-NAME constant that trips a rule (e.g. a storage key literal) gets `// gitleaks:allow` on the line, never `--no-verify` on the commit.
- **LATEST.md rotates: ≤5 dated entries, newest on top, and the agent adding one DELETES THE OLDEST** — the single rule `.githooks/pre-commit` enforces. Nothing is lost: each entry links its dated per-session file, which holds the full account. **The word budgets are gone** (owner decision 2026-08-14, ≤3900 total / ≤700 per entry): the shared banner counted as an "entry", so adding a genuinely new binding fact to it got you BLOCKED, and the cheapest escape was always deleting evidence from a summary written minutes earlier — three sessions in a row burned time on the arithmetic instead of the handoff. **NEVER trim a fresh summary to fit a budget; rotate instead.** Keep entries skimmable because every agent and subagent reads them at session start, not because a counter says so.
- For multi-step/debug/deploy-sensitive work, use persistent planning files in `.planning/<task>/` — `task_plan.md`, `findings.md`, `progress.md` live INSIDE that directory, never at the repo root. Re-read before decisions; log failed attempts. Delete `.planning/.active_plan` when the plan it names is finished, or it silently points the next session at completed work.
- Before any change: read the files you touch and trace the code paths. Code/source beats docs and old summaries.
- **Re-verify VOLATILE claims; never inherit them.** Git/branch state, what is live, versions and commits, CI or Dependabot status, test counts, generated-artifact freshness — all must come from a command you ran THIS session. This repo has been confidently wrong on exactly these (a graph reported fresh but built from a stale commit; an alert recorded as fixed when the lockfile was half-upgraded; a handoff pointing at a file whose first line reads SUPERSEDED). Stable facts — architecture, wire contracts, documented traps — can be trusted as written; when source and doc conflict, source wins and you fix the doc in the same commit.
- Scope: change only what was asked. Fix obvious bugs in edited paths; do not add unasked features, abstractions, or cleanup crusades.
- Code, comments, commit messages, logs: English. UI strings may stay localized.
- Tone: brutally blunt — lead with the verdict, no hedging, no flattery ("great question" / "you're absolutely right" are banned). Roast bad code and time-wasting rabbit holes; speak the truth even when inconvenient.
- Auto-review/code-review subagents must use the same model class as the primary session unless the user explicitly asks for a cheaper model.
- Commits: commit at natural checkpoints and `git push` in the same checkpoint (the VM deploys via `git pull`; local-only commits block it). Small/trivial fixes can go straight to `master`; bigger/riskier work uses a feature branch + PR. Feature branches do NOT auto-deploy — the VM pulls `master`, so work goes live only after PR merge. Never merge to `master` without explicit user OK.
- **`node scripts/impact.mjs` is the inner-loop impact hint** — who depends on what you changed, plus the tests that import it, in ~0.6s. `--ref <ref>` for a branch, `--json` for machine use. Import resolution is exact (1639 specifiers, 0 unresolved; conditional Dart imports, bare same-dir specifiers, untracked files, deletions). **Reachability is NOT coverage:** 3 hops, static imports only, blind to NestJS DI, the §7 wire contracts and assets. Running the full tier suites before a commit or PR is required by project policy; nothing enforces it mechanically.
- **Never run `dart format lib/`** (or any whole-tree formatter): format only the lines you edited, or the diff drowns the review and the blame. Same reason `node scripts/lint-ratchet.mjs` exists — it runs ONLY in CI, so run it yourself before pushing backend changes; it fails the build when the warning count rises above the recorded floor.
- `graphify update .` is **no longer a manual step** — `.githooks/post-commit` rebuilds the graph in the background after every commit (`graphify hook status` to verify; needs `git config core.hooksPath .githooks`, same one-time setup as the secret-scanning pre-commit hook).

## 2. Architecture map

Umbra (internal codename: Fireplace) is a production E2E encrypted chat app.

**Naming: the user-facing brand is "Umbra" (since 0.1.20, PR #152); "Fireplace" lives on as the internal codename.** All code identifiers (`FireplaceApp`, `FireplaceColors`, pubspec `name: fireplace`, `package:fireplace/` imports), storage/contract names (secure-storage `FireplaceE2E`, IndexedDB `fireplace-push`/`fireplace-boot-marker`, Web Lock `fireplace-e2e-*`, Android channel `fireplace_messages`), repo name, and the domain keep the old name DELIBERATELY. Do NOT "fix" them: storage names are addresses of live user data (renaming `FireplaceE2E` = identity churn for every user), bundle/application ids are installed-app identity, and identifier renames are blame pollution for zero user value. New user-visible strings say Umbra; new internal identifiers may use either, favoring consistency with their surroundings.

```text
Flutter web/mobile client ⇄ NestJS backend (:3000) ⇄ PostgreSQL 16 (:5433 host)
                         REST + Socket.IO         self-hosted media volume
```

- Client: `AuthGate` → `AuthScreen` or `MainShell` → `ChatDetailScreen`; state is provider-driven. See `frontend/CLAUDE.md`.
- Backend: `AppModule` wires ~15 domain modules (auth, users, chat, messages, media, key-bundles, push, …) — authoritative full map in `backend/CLAUDE.md` §2; `ChatGateway` authenticates sockets and delegates to chat services.
- Media: current media storage is self-hosted under `/app/media` (`avatars/`, `msgs/`). Cloudinary URLs remain accepted only as legacy/backward-compatible media URLs.
- Landing page (`https://fireplace.ignorelist.com/welcome/`): **separate PUBLIC repo `Lentach/fireplaceWebsite`** (extracted from `landing/` 2026-07-22 with git history; owner renamed it from `fireplace-landing` the same day; local clone `C:/Users/Lentach/Desktop/fireplace-landing`). Astro static site, own `CLAUDE.md` + `deploy-landing.ps1`, deploys independently to `~/fireplace/landing-build/` on the same VM. Not part of this repo's CI/deploys.
- Graph context: `node scripts/impact.mjs` (§1) is the first stop for "what does this change affect". `graphify-out/GRAPH_REPORT.md` is a secondary whole-repo view — its per-file symbol inventory is sound and its backend TS import edges measure **86.6% precision / 90.7% recall**, but its **Dart import edges are 0.5% precision / 1.5% recall** (measured 2026-07-27: it collapses every relative specifier such as `../../theme/rpg_theme.dart` into one node attributed to an arbitrary file). Never answer a Flutter dependency question from it. No `graphify-out/wiki/index.md` exists; the ~508 `[[_COMMUNITY_*]]` wikilinks in the report are dead.

## 3. Local commands

```bash
# Terminal 1: backend + DB for local dev
docker-compose up

# Terminal 2: Flutter web
cd frontend && flutter run -d chrome
```

- Ports: backend `:3000`, DB host `:5433 -> :5432`, Flutter web random unless specified.
- Before local start on Windows if stale node processes bite: `taskkill //F //IM node.exe`.
- Phone on WiFi: `cd frontend && .\run_web_for_phone.ps1`, or `flutter run -d web-server --web-hostname 0.0.0.0 --web-port 8080 --dart-define=BASE_URL=http://YOUR_PC_IP:3000`.
- Tests: `cd backend && npm test` (1063 unit tests, 62 suites; verified by `node scripts/verify-claude-backend-test-counts.mjs`). Frontend: `cd frontend && flutter analyze --no-fatal-infos && flutter test` (1761 Flutter tests, 10 skipped; verified by `node scripts/verify-claude-frontend-test-counts.mjs`). Full-stack E2E wire harness (needs `docker-compose up` first, NOT in the fast lane): `cd frontend && flutter test test_e2e` — 43 tests / 2 skipped; see `frontend/CLAUDE.md` §1 and the `e2e-wire` CI job below. Origin-wide Web Lock probe: `node scripts/verify-session-lock-probe.mjs` (also a CI job). **The §6.2 reset teardown + falsification 12 probe is OPT-IN** — `flutter test test_e2e/identity_reset_teardown_test.dart --dart-define=RESET_PROBE=true` — because it spends the shared `/auth/register` bucket; it runs in CI as its own `e2e-isolated-probes` job.
- CI: `.github/workflows/ci.yml` — `backend` (impact self-test, jest, backend count verifier), `frontend` (analyze, flutter test, frontend count verifier), `session-lock` (Web Lock probe in headless Chrome), and `e2e-wire` (full-stack harness against a real backend + Postgres). `e2e-wire` is the only automated check on the §7 wire contracts; it caught two disaster-recovery bugs on its first two runs and its `continue-on-error` was removed after 5 consecutive greens, so a wire regression now **fails the CI run**.
- **CI is DETECTION, not prevention — there is no gate.** Branch protection is a paid feature and the API returns 403 on this private free-plan repo, so nothing mechanically blocks a push or a merge, and §1 still permits small fixes straight to `master`. The rule is procedural and on you: **check the run and never merge or deploy on red.** `gh run list --branch master --limit 1` after pushing. If `e2e-wire` turns flaky, restore `continue-on-error: true` deliberately and record it — never silently disable it.

## 4. Production deploy and safety

Production: `https://fireplace.ignorelist.com`, **OVH VPS** `ubuntu@51.68.138.13` (Warszawa, key-only SSH), repo `~/fireplace`. GCP was fully decommissioned 2026-07-14 — treat any `gcloud` instruction in older docs as historical.

**Deploy is SPLIT** because the VM cannot compile Flutter web without OOM:

- Frontend, from the PC: `git pull ; .\deploy-web.ps1`
- Backend, on the VM: `cd ~/fireplace && ./deploy-backend.sh`
- Verify: `cd scripts/smoke && node post-deploy-smoke.mjs`

**➡ Everything else — script internals, staging rehearsal, backups/restore, the env-var table, branch testing, VM logs — is in `.cursor/rules/production-vm-deploy.mdc`. Read it before any non-trivial prod op.**

Non-negotiable, because each one is silent or irreversible:

- **Trust `gitCommit`, never semver alone.** Flutter happily serves cached code under a bumped version. `/version.json` (frontend) and `/version` (backend) are the truth; `git log` is not.
- **After a frontend deploy the user must fully close + reopen the PWA. NEVER uninstall or clear site data** — that destroys local E2E Signal keys with no recovery.
- **Never run `docker compose down -v`, `docker volume rm`, or `prune --volumes` on prod.** `pgdata` and `media_storage` are user data.
- **Never run a bare `docker compose -f docker-compose.prod.yml up -d`** — without the exported `APP_VERSION`/`GIT_COMMIT` it silently recreates the backend as `0.0.1/unknown`.
- **Never build Flutter web on the VM**, and never use the legacy all-in-one `deploy.sh`.

## 5. Version contract

- The user-visible version is semver from `frontend/pubspec.yaml` ONLY: `0.0.x`, **never** a `+build` suffix anywhere. "Bump by +1" means the PATCH segment (`0.0.1` → `0.0.2`), not `+1` appended. Production releases bump PATCH and state the new version in the commit message; docs/session-only edits do not bump. Minor/major only on explicit ask.
- Settings footer shows `version · gitCommit · buildTime`; backend `GET /version` returns the same triple from `APP_VERSION`/`GIT_COMMIT`/`BUILD_TIME` injected by the deploy scripts.
- Android `versionCode` is internal packaging only — it is NOT the user-facing string, and `+N` never belongs in `pubspec.yaml`. `build-android.ps1` derives it as `major*1_000_000 + minor*10_000 + patch` and passes `--build-number`; Android release gates + keystore setup live in `docs/runbooks/android-release.md`.
- Env vars (incl. the `JWT_SECRET` rotation rule and the VAPID key-match footgun): see the runbook's env table.

## 6. Database and E2E safety

- Dev `docker-compose.yml`: bind-mounted backend, `NODE_ENV=development`, relaxed CORS, TypeORM auto-DDL. Prod `docker-compose.prod.yml`: `NODE_ENV=production`, restricted CORS, `synchronize` **OFF**.
- **Prod schema changes are numbered SQL files in `backend/migrations/`**, applied exactly once at boot by the migration runner; a failed migration aborts boot. Entities define the dev shape, the migration files are prod truth — full contract in `backend/CLAUDE.md` §4. Applied files are IMMUTABLE.
- **⛔ Migration `0015` is not code-reversible.** It DROPS the account-wide `UNIQUE("userId")` on `key_bundles` and `UNIQUE("userId","keyId")` on `one_time_pre_keys` in favour of the per-device ones. The pre-Phase-1 backend upserts with `ON CONFLICT ("userId")` / `("userId","keyId")`, and Postgres needs a matching unique index for that — so rolling the backend image back past `0015` without touching the schema makes EVERY key-bundle and OTP upload fail with `42P10`, breaking E2E setup for everyone until you roll forward again. If a rollback is ever unavoidable, recreate the two old constraints first (safe while every row is `deviceId = 1`). Expect the `0015` boot itself to pause while the partial index on `messages` builds under a write-blocking lock; the runner's `lock_timeout` aborts the boot rather than queueing behind a long transaction, so do not deploy while a backup or a manual `psql` session holds locks.
- Risky deploys get a staging dress rehearsal first; the canonical trigger list lives in the runbook ("Staging dress rehearsal") — do not restate it here, or the two copies drift. Skip rehearsal for UI work.
- Raw SQL must quote camelCase columns: `"deliveryStatus"`, `"createdAt"`. Casing is per entity — check, never guess.
- Backups (`./backup-db.sh`, `./setup-backup-cron.sh`, `./restore-db.sh`) are in the runbook. Dumps hold ciphertext, public keys, usernames/contact graph and password hashes — they cannot decrypt messages but are sensitive.
- **E2E invariant: the server stores Signal ciphertext and metadata, NEVER device private keys.** Device keys live in web localStorage / mobile secure storage. What actually destroys them: clearing site data, a browser-profile reset, iOS storage eviction, or uninstalling the **mobile** app. **Uninstalling the PWA generally does NOT** — it removes the shortcut, not the origin's storage, and `navigator.storage.persist()` (`main.dart`) asks the browser to exempt that origin from eviction. Deleting the account does not wipe local keys either; it only makes them useless. **Never tell a user to uninstall or clear site data as a fix** — that is the action that loses their history, and the previous wording here pointed straight at it (#105).

## 7. Shared wire contracts

- E2E envelope: `{ content, messageType?, mediaUrl?, mediaDuration?, mediaKey?, mediaIv?, linkPreview?, senderListInfo? }`. Ciphertext format is `"{type}:{base64}"` (`3` PreKey, `2` Signal/whisper). Server stores `[encrypted]` plus metadata.
  - `senderListInfo` is `{ ownVersion?, ownListHash?, peerVersion?, peerListHash? }` — the sender's view of BOTH parties' device-list versions, hashed as SHA-256 (base64) over the DAK-signed `listCanonical` bytes exactly as transported (multi-device spec §5.2 layer 2, §12 amendments (xv)/(xvi)). It rides inside the plaintext, so the server never sees, stores or validates it, and it is attached to every message. A party the sender holds no verified list for is ABSENT from the object, never reported as version 0. On receive it is untrusted peer data: a bare claim NEVER alarms and never changes trust (I7); a claim newer than the recipient knows buys at most ONE rate-limited device-list re-fetch and is then discarded; a claim older than the recipient's own verified version (or the same version with different canonical bytes) is a split-view signal, recorded durably for the operator; skew about the recipient's OWN devices renders only the calm "syncing devices" note, never the identity-changed surface. Unknown envelope keys are ignored by `E2eEnvelope.parse`, so an older peer omitting the field is normal.
- Socket readiness clock: after socket auth completes, server emits `socketReady` with `{ serverTime: string, deviceId: number }`; `serverTime` is an ISO-8601 UTC instant stamped at that completion. `deviceId` is which device of the account this socket is (multi-device spec §5.3) — the client cannot derive it, and it is load-bearing twice over: a fan-out send must exclude its own origin device (the server refuses `self_envelope_for_origin_device`), and the self-sync receive law only scopes an own row's origin once this value is CONFIRMED rather than the §8 default of 1 (spec §12 amendment (xii)).
  - Why: this is the client's only trustworthy clock reference for gating destruction of expired message plaintext. Destruction requires a fresh observation (never extrapolated past 30 minutes) and `expiresAt` plus a 5-minute grace for the backend's per-minute cleanup cron.
  - Missing, stale, or unparseable `serverTime` fails silently closed: the client holds and never destroys expired plaintext — no error or log, while plaintext accumulates. This deliberate asymmetry accepts over-retention rather than permanent data loss: the server holds only ciphertext, which the client cannot decrypt after the Signal ratchet consumed the key.
  - Compatibility: older clients ignore the extra field; newer clients against older servers receive no usable clock and therefore never destroy plaintext on expiry.
  - In-session refresh: client emits `getServerTime` (no payload); server replies `serverTime` with `{ serverTime: string }`, same ISO-8601 UTC semantics. The client's sweep timer asks only when its last observation aged past the 30-minute extrapolation bound (floor-limited to one unanswered request per 5 minutes), so a connection that outlives the socketReady observation can still destroy expired plaintext instead of holding it until the next reconnect. An older server simply never answers and the client stays fail-closed.
- Local-plaintext reconciliation: client emits `getServedMessageIds` with `{ requestId: string, messageIds: number[] }` (≤500 ids); server replies `servedMessageIds` with `{ requestId, messageIds }` — the subset it would still serve that account through the history read path (row exists, caller is a participant, not hidden by them, not expired).
  - Why: the client DESTROYS the local plaintext of every id it asked about that is absent from the reply. That is the only mechanism that reaches messages deleted or expired before the metadata stamps existed — those records match no local rule and their server row is already gone, so nothing else would ever revisit them.
  - The reply is authoritative, so the server MUST stay silent on failure rather than answer with an empty list: an empty `messageIds` legitimately means "destroy all of them" (a fully cleared history). A missing reply purges nothing. `requestId` is echoed verbatim so a late or foreign answer cannot be applied to the wrong batch.
  - The existence check MUST remain at least as permissive as the history read path. A false "not served" is irreversible loss; a false "served" only defers cleanup. In particular it carries no `sender != me` or `deliveryStatus` predicate — either would report the caller's own outgoing history as gone.
  - Client throttles to one completed pass per 6 hours per account and only stamps completion when every batch was answered.
- Delete semantics:
  - Delete conversation: messages + conversation removed, friendship kept, WS `deleteConversationOnly`.
  - Unfriend: friend request + conversation + messages removed, friendship removed, WS `unfriend`.
  - Clear history: messages removed, friendship kept, WS `clearChatHistory`.
  - Delete for me: hidden for caller only, WS `deleteMessage mode=for_me`.
  - Delete for everyone: sender-only hard delete for both sides, clears pin, WS `deleteMessage mode=for_everyone`.
- Edit message: sender-only text edit within 15 minutes, from ANY of the sender's devices (spec §5.7 + §12 amendments (xxx)–(xxxiv), Phase 2 T7). Inbound `editMessage { messageId, envelopes?: [{userId, deviceId, ciphertext}], encryptedContent?, senderListVersion?, recipientListVersion? }` — a FULL RE-FAN: one edited ciphertext per recipient device AND per sender's other device. A legacy single `encryptedContent` is still accepted and normalized to a device-1 envelope at ingest (§8), and is REFUSED like a legacy send once either party is enrolled. The server verifies sender + window + envelope shape + list freshness BEFORE any write, then in ONE transaction stamps `editedAt`, re-points `messages.originDeviceId` at the **EDITING** device, and **UPSERTs each envelope CONTENT-ONLY so its stamps SURVIVE** (durability F8: an edit is not an un-delivery, and the §4 projection must never regress). Today that means `deliveredAt`, the only envelope stamp any code path writes (`stampEnvelope`'s single call site, from `messageDelivered`); `readAt` is a declared column and a permitted `stampEnvelope` field that **nothing writes yet** — `markConversationRead` drives the ROW to READ and leaves the envelope alone — so do not build on it without writing it first. The conflict clause names `ciphertext` alone, so both are safe by construction. A device with no prior envelope gets one INSERTED — its `none_for_device` placeholder upgrades, never the reverse — and envelopes of devices dropped since the send are LEFT alone (the delete CASCADE is the only destruction path). Each device is then fanned ITS OWN ciphertext via `messageEdited { messageId, conversationId, content, encryptedContent, editedAt, originDeviceId }` at its newest socket; the editing device is the PRODUCER, so its echo carries `encryptedContent: null`. Refusals: `editMessageFailed { messageId, reason }` keeps its four codes (`not_sender`, `window_expired`, `not_text`, `not_found`) and gains the (v) shape codes (`duplicate_envelope_device`, `unknown_envelope_user`, `unknown_recipient_device`, `self_envelope_for_origin_device`); a stale list answers the EXISTING `deviceListStale` event. An edit NEVER mints or consumes a `sendToken`. **Receivers key their Signal session off `originDeviceId`, which is why an edit must re-point it** — the client adopts it before decrypting, and an own row edited by a SIBLING device is decrypted rather than merely reconciled.
- **A throttled WS request ANSWERS; it never goes silent (2026-08-22).** `WsThrottlerGuard` emits a refusal to the caller and THEN refuses the handler, so the limit keeps its teeth while the caller learns why. The answer rides the request's OWN response event with the stable code `rate_limited` plus `retryAfterMs` — `editMessage` → `editMessageFailed { messageId, reason:'rate_limited', retryAfterMs }`, `provisioningComplete` → `provisioningCompleted { success:false, error:'rate_limited', retryAfterMs }`, and likewise for `openProvisioning`/`provisioningHello`/`provisionDevice`/`cancelProvisioning`/`revokeDevice`/`updateDeviceList`. `pinMessage` → **`messagePinFailed` `{ conversationId, reason:'rate_limited', retryAfterMs }`** (Phase 2 T8, spec §12 (xxxvii)) — a DEDICATED event, not `messagePinned`: the guard refuses before the handler runs, so it knows only what was asked for, and answering `messagePinned` would either unpin a conversation that had a DIFFERENT message pinned or confirm a pin that never happened. The pinning device restores what its optimistic pin overwrote from a pre-pin snapshot; `unpinMessage` stakes no optimistic state and so keeps the `error` fallback. Only handlers that ACTUALLY carry the guard belong in that table — an entry for an unthrottled handler is unreachable code that misrepresents this contract, which is why `uploadKeyBundle` was removed from it. An UNMAPPED handler falls back to `error { message:'rate_limited', event, retryAfterMs }`, which the client already listens to and which already marks in-flight sends failed. **Why in-contract rather than one new `rateLimited` event:** a second refusal convention would be one more thing every future handler must remember, and the client would need a map from that event to whichever optimistic state to unwind — whereas these payloads flow into the settle/revert paths the client ALREADY has, so this needed no client change at all. Before this, `ThrottlerGuard` threw, Nest emitted `exception`, and the client (60+ named listeners plus `error`, never `exception`) heard NOTHING — so a throttled `editMessage` stranded an optimistically applied edit forever, the same divergence the `deviceListStale` edit path was fixed for. Adding a throttled handler that holds client state means adding it to `THROTTLE_ANSWERS`.
- Takeover alarm (Phase 0a, spec `docs/design/multi-device.md` §6.0): `uploadKeyBundle` that REPLACES the stored `identityPublicKey` writes a durable `identity_change_audit` row, emits `ownIdentityReplaced` `{ occurredAt }` to the account's OTHER sockets (uploader excluded), sends a content-free `{ type: 'identity_changed' }` push to every endpoint, and emits `peerIdentityChanged` `{ userId, occurredAt }` to every conversation peer's room. Same-identity re-uploads (the every-connect path) stay silent. The branch fires on legitimate reinstalls too — client copy must say "new device/browser sign-in", never "hacked".
- Registration lock (Phase 0b, spec §6.1): `uploadKeyBundle` that replaces the stored `identityPublicKey` is REFUSED unless it carries `identitySignature` + `nonce` (XEdDSA by the previous identity key over `newIdentityPublicKey ‖ userId ‖ nonce`, verified server-side with `curve25519-js`) or the account has a completed reset ceremony, which the upload SPENDS. Refusal emits `keyBundleUploaded { success:false, error:'identity_locked' }` — nothing written, no audit row, no alarm. Nonces come from `getRegistrationLockNonce` → `registrationLockNonce { nonce }`: 32 CSPRNG bytes, TTL 5 min, bound to the issuing socket, consumed by ONE upload attempt. Same-identity re-uploads and first-ever uploads are unaffected. **The SIGNATURE path is server- and harness-only so far** — no `frontend/lib` code requests a nonce or mints a proof, and a device that lost its keys cannot sign with them anyway, so in the live app the only route through the lock is spending a completed reset ceremony. A signed rotation belongs to Phase 2 / §6.3.
- **§6.2 recovery REBIND (client half, 2026-08-22).** When a reset teardown re-homes an account, `keyBundleUploaded` additionally carries `{ deviceId, access_token, refresh_token }` bound to the NEWLY allocated device. **The client MUST adopt that session and reconnect BEFORE publishing any key material** — the same ack triggers the one-time-pre-key upload, so delivering it first files those keys under the device the teardown just REVOKED, leaving peers served a bundle with no one-time pre-key and the next reconnect refused by the §5.5 gate. `ConnectionProvider` adopts via `onSessionRebound` (wired to `AuthProvider.adoptProvisionedSession`, the same storage path as login and as the §5.1 provisioning rebind), reconnects with `immediate: true` to bypass the connect debounce, and only then delivers the ack. The server half shipped in T6; the client half was missing until T8's probe ran the ceremony end to end.
- Reset ceremony (Phase 0b, spec §6.2/§6.2.1): `resetIdentityRequest { recoveryPhrase? }` → `identityResetStatus { status: pending|existing|cooldown|invalid_phrase|locked, deadlineAt?, shortened? }`; a NEW ceremony also broadcasts `identityResetPending { deadlineAt, shortened, occurredAt }` to the whole user room plus a content-free `{ type: 'identity_reset_pending' }` push. `resetIdentityCancel` → `identityResetCancelResult { cancelled }`, and on a real cancel `identityResetCancelled { occurredAt }` room-wide plus `{ type: 'identity_reset_cancelled' }` push. Delay is 72 h, or 1 h when a valid recovery phrase is presented — the phrase SHORTENS, never silences, and is single-use. `setRecoveryKey { phrase }` → `recoveryKeySet { success }` stores an Argon2id verifier only (19 MiB / t=2 / p=1). Tables `identity_reset_requests` (terminal states; a partial unique index enforces one pending per account) and `recovery_keys`, migration `0014`. A completed ceremony stays spendable for 24 h — an unused grant lapses, because `cancelReset` cannot reach a row that already left `pending`.
- `checkOwnKeyBundle` → `ownKeyBundleStatus` now carries `{ exists, identityReset: {status, deadlineAt, shortened} | null, identityReplacedAt: string | null }`. The extra fields are additive; a missing or malformed payload still means UNKNOWN and MUST NOT authorize key generation. `identityReplacedAt` is what lets a session that was offline during a replacement raise its banner at connect time, and `shortened` is what stops a session reconnecting INTO a 1 h recovery-key ceremony from describing it as the 72 h one.
- Per-device key material (Phase 1, spec §4/§5.1/§8): `fetchPreKeyBundle` takes an OPTIONAL `deviceId` (integer 1..100) naming which device of the target to build a session with; absent means device 1, so a client that predates devices keeps working — the rollout order is server first, clients later. **Uploads do NOT take a device from the payload**: `uploadKeyBundle` and `uploadOneTimePreKeys` write to the device named by the caller's own JWT (`client.data.user.deviceId`, absent claim = 1), because a bundle belongs to the device whose authenticated session uploaded it. Key bundles are unique per `(userId, deviceId)` and one-time pre-keys per `(userId, deviceId, keyId)`, so two devices may both hold keyId 0; the identity-epoch invariant is re-keyed to `(identityPublicKey, deviceId)` at all three sites (purge / claim / count). A device with no bundle of its own is served NOTHING — never a sibling's bundle, which would build a session it cannot decrypt. `keyBundleUploaded` gained `identityChanged: boolean` so the uploading device can tell its own replacement from someone else's. JWTs carry `deviceId`, refresh rows remember it, and `messages` gained `originDeviceId` (server-derived from the JWT, echoed in every message payload) plus an OPTIONAL client-generated `sendToken`, UNIQUE PER SENDER: a retry carrying the same token in the same conversation re-acks the committed row and delivers nothing twice, while the same token aimed at a DIFFERENT conversation is refused as `error { message: 'duplicate_send_token' }` rather than silently re-acking the wrong thread. **`sendToken` is accepted but not yet SENT by the app** — no `frontend/lib` code mints one (only the wire harness does), so a lost ack behaves as it always has until Phase 2 wires §5.4 into the real send path.
- **One-time pre-keys belong to a PUBLISHED identity, and the order is load-bearing (2026-08-19).** `uploadOneTimePreKeys` is refused with `error { message: 'identity_locked' }` when its `identityPublicKey` is not the account's published one — otherwise a session whose bundle the registration lock just refused still upserts over the legitimate device's `keyId` slots, and while those rows are never SERVED (the fetch filter pins the published identity) the victim's pool is emptied until a peer fetch triggers `preKeysLow` (proven live 2026-08-18, user 168). Two carve-outs keep legitimate flows working: nothing published yet (fresh registration), and an unspent COMPLETED ceremony (an authorized rotation in flight); a merely PENDING ceremony authorizes nothing. The client therefore **publishes the identity first and releases the keys on `keyBundleUploaded { success: true }`** (`EncryptionProvider` stashes them; a refusal DROPS them, `OTP_UPLOAD_DROPPED`), and a `success:true` carrying `identityChanged:true` **mints a fresh pool immediately** because the epoch purge just emptied it and the ceremony-spending re-upload carries no keys of its own. Do NOT restore the old back-to-back emit: it raced, the keys usually arrived first, and the server then had to judge key material against an identity the account had not published yet (`stale_otp_epoch_test` mirrors the safe order; the refusal is pinned in `full_stack_e2e_test`).
- **Device material guard (spec §12 amendment (lxiv), 2026-08-30).** A bundle row's `registrationId` may not change while its `identityPublicKey` is unchanged — identity and registrationId are minted together, once, so that shape can only be a DIFFERENT physical install (a revoked linked device whose password login resolved onto the primary's id) clobbering a namespace it does not own; the served bundle would mix two installs' X3DH halves and destroy peers' first messages. `uploadKeyBundle` refuses it with `keyBundleUploaded { success:false, error:'device_material_conflict' }`, nothing written. `uploadOneTimePreKeys` takes an OPTIONAL `registrationId` install proof with the same refusal (absent = pre-(lxiv) client, accepted — such clients predate linking). Client half: a durable material-device stamp (`e2e_<uid>_material_device_v1`), TOFU-written on the first confirmed own-device id, cleared by every authorized re-homing; on contradiction the client refuses E2E duty entirely and routes to re-linking (`DeviceMismatchBanner`).
- Provisioning ceremony (Phase 2 T3, spec §5.1 + §12 amendments (a)-(c)/(i)-(iv)): request-event → response-event, refusals as `success:false` + stable codes, never a bare `error`. `openProvisioning {}` → `provisioningOpened { success, provisioningId, expiresAt }` (enrolled accounts only; the server allocates the deviceId HERE, memoized per stage, and deliberately does NOT answer it — the new device learns its id from the decrypted blob). `provisioningHello { provisioningId, ephPubP }` (any authenticated session of the account; FIRST ephPubP pinned, identical retry idempotent, different one refused) → `provisioningHelloAck { success, deviceId }` to the caller + a `provisioningHello` relay to the OPENER socket. `provisionDevice { provisioningId, blob, listCanonical, listSignature }` verifies sig/version/diff-adds-exactly-the-memoized-id BEFORE staging (retry overwrites the stage) → `provisionDeviceAck` + `provisioningBlob { provisioningId, blob }` pushed to the opener; the blob is opaque client-encrypted bytes (AES-256-CBC + HMAC under HKDF of the SAS-verified DH secret) and `ephPubN` NEVER transits the server (QR/manual code only). `fetchProvisioningBlob` re-serves it, opener-only, until TTL (10 min) or commit. `provisioningComplete { provisioningId }` — opener socket ONLY, one-shot CAS — runs ONE transaction (devices row + list mutation + refresh session) and answers `provisioningCompleted { success, deviceId, access_token, refresh_token }`; the new device reconnects under that token and only THEN uploads keys (`uploadKeyBundle`/`uploadOneTimePreKeys` for a deviceId ≥ 2 without a live devices row are refused: `device_not_active`). Duplicate complete → `already_completed`; a losing concurrent ceremony gets `stale_version` with its stage RESTORED for a v+2 re-sign. `cancelProvisioning` (any session of the account) discards the stage and notifies the opener; `deviceListChanged` broadcasts every commit. Stages live in server MEMORY — a restart aborts pending ceremonies by design.
- Envelope send fan-out + per-device delivery (Phase 2 T4, spec §5.2/§5.3 + §12 amendments (v)-(ix)): `sendMessage` additionally accepts `envelopes: [{userId, deviceId, ciphertext}]` (one per recipient device AND per the sender's OTHER devices), `senderListVersion`, `recipientListVersion`. Presence of `envelopes` makes it a NEW-MODEL send: the row's `encryptedContent` stays NULL and every ciphertext lives in `message_envelopes`, written in the SAME transaction as the message row. A legacy single-`encryptedContent` send is normalized AT INGEST into a one-element device-1 envelope (§8) and additionally keeps its column, so legacy clients read their rows unchanged for the whole rollout window. Exactly ONE envelope per `(userId, deviceId)`: duplicates, third-party recipients, never-activated devices and a self-envelope addressed to the origin device are refused on the send path's existing bare `error` channel with `duplicate_envelope_device` / `unknown_envelope_user` / `unknown_recipient_device` / `self_envelope_for_origin_device`. A stale device-list stamp is refused ATOMICALLY (zero message rows, zero envelope rows) with the response event `deviceListStale { success:false, error:'device_list_stale', tempId?, lists:[{userId, version, listCanonical, listSignature, enrollment:{dakPub, enrollmentSig, enrollmentCreatedAt}}] }` — one entry per stale party, recipient first, so ONE round trip repairs both views; only ENROLLED parties are checked and an absent stamp counts as stale. Ciphertext is addressed PER DEVICE: every socket joins `device:<uid>:<did>` as well as `user:<uid>`, `newMessage`/`messageEdited` go to the device room via `emitToDeviceNewestSocket` (newest socket WITHIN one device — tabs of one device share a session store), and a send with no ciphertext at all keeps its historical single-target delivery to the recipient's device 1. NEVER broadcast one ciphertext to an account: Signal decrypt consumes the message key. Push suppression is per device and skips only when EVERY delivered recipient device has the conversation focused. `preKeysLow` is routed to `device:<uid>:<did>`, `preKeyBundleResponse` echoes `deviceId`, and the pre-key fetch limiter is keyed `(requester, target user, target device)` — a per-user key made multi-device session establishment impossible. History reads serve the REQUESTING device its own envelope, else the legacy column gated to the row's session owner (`deviceId == (originDeviceId ?? 1)` for own rows, `deviceId == 1` for received rows), else the additive marker `envelopeStatus: 'none_for_device' | 'own_origin'` with a null ciphertext; `own_origin` exists so the origin device does not render "sent before this device was linked" over its own sends. `sendToken` is echoed ONLY to a row's origin device (its lost-ack reconcile key, amendment (ix)) and never to a recipient. `deliveryStatus` stays a projection over RECIPIENT envelopes only, written as a column-scoped UPDATE; envelope `deliveredAt`/`readAt` never feed expiry or the read TTL (I9).
- Device revocation (Phase 2 T6, spec §5.5 + §12 amendments (xxi)-(xxvi)): `revokeDevice { deviceId, listCanonical, listSignature }` → `deviceRevocationCompleted { success, deviceId?, listVersion?, error? }` to the CALLER, plus `deviceListChanged { userId, listVersion }` to the account room. The request carries the DAK-signed list that ALREADY shows that device revoked — the server never mints a list version — and the request's `deviceId` must appear revoked in those canonical bytes (`list_device_mismatch`). Pre-write refusals, nothing written: `cannot_revoke_self`, `cannot_revoke_primary` (the primary is the only DAK holder, so a self-revoke would leave the account unable to sign any future version — recovery for a lost primary is the §6.2 reset), `not_primary`, `unknown_device`, `already_revoked`, `invalid_canonical`, plus any `DeviceListRejectedError` code from the shared mutation gate. ONE transaction does the durable half: `revokedAt` stamp (its `IS NULL` predicate is the concurrency arbiter, so a racing duplicate answers `already_revoked`), the signed list mutation, that device's refresh sessions, and that device's key bundle + OTPs. Post-commit only, because each is safe only once the decision is final: the device's push rows AND the account's NULL-`deviceId` push rows (amendment (xxiv) — an unattributable row may be the revoked device's; a survivor re-registers on next start), every pending provisioning stage of the account (amendment (xxv) — all are signed against the pre-revocation list, so all are stale), then `deviceRevoked { userId, deviceId }` to the revoked device's room followed by disconnecting its sockets.
  - The revoked device is TOLD, then dropped (amendment (xxvi)), and the same `deviceRevoked` is emitted on any later connect attempt before the socket is closed — a kicked device shows a stated reason instead of the generic connection-lost banner. Its local plaintext and Signal keys are NEVER touched: revocation is logout semantics, and remote wipe is an explicit §1 non-goal.
  - Two session gates enforce it against a still-valid access JWT (up to 24 h): `handleConnection` refuses the socket, and `JwtStrategy` refuses every guarded REST route (media upload above all) while also exposing `deviceId` on the request principal — which is what lets push registration persist a device id at last. BOTH deny ONLY on an EXPLICIT `revokedAt` (amendment (xxii)): a MISSING `devices` row must never deny, because every pre-Phase-1 account has none until its first connect writes one (§8). This is the inverse polarity of `DevicesService.isActive`, which gates key-material uploads and must fail closed on absence; both predicates stay, each at its own call sites.
  - `getServedMessageIds` answers a revoked device with SILENCE, never an error and never an empty list (I6 + amendment (xxiii)): an empty `messageIds` legitimately means "destroy all of them", so an answer-shaped refusal would remotely wipe the history revocation promises the user keeps.
- §6.2 reset roster teardown (Phase 2 T6, spec §12 amendments (f)/(xxviii)/(xxix)): a reset COMPLETES at the identity change it authorizes — `upsertKeyBundle` now reports `authorizedBy: 'signature' | 'reset' | null`, and only `'reset'` tears the roster down (a signed rotation must not: that account still holds its other devices). The teardown allocates a FRESH `deviceId` from `users.nextDeviceId` and never re-mints device 1 (a fresh identity under id 1 would be positively served the old device 1's legacy ciphertext by the §5.3 fallback), moves the just-uploaded bundle + OTPs onto it, inserts its `devices` row as `isPrimary`, stamps `revokedAt` on every other device, drops every pre-reset refresh session, and issues one for the new id. `keyBundleUploaded` then additionally carries `{ deviceId, access_token, refresh_token }` — the client MUST adopt them BEFORE uploading one-time pre-keys, or those keys land in the namespace the teardown abandoned. Push rows of the whole account are dropped afterwards (they belong to devices just revoked). `users.nextDeviceId` is untouched.
  - `account_authorizations` is REPLACED, never dropped (amendment (xxix)): its `listVersion` must stay monotonic across the reset, and a dropped row would read as not-enrolled, which the (xix) rollback pin refuses. The reset leaves the row in place with an ORPHANED DAK — its `enrollmentSig` was made by the identity just replaced, so every peer fails the I7 chain closed, which is the honest state for an account whose identity changed. Recovery is a replacement enrollment, admitted by exactly one new rule: `enroll` accepts a fresh IK-signed enrollment when the STORED enrollment no longer verifies under the account's CURRENT published identity, at a list version strictly GREATER than the stored one (CAS on the retired version). `enrollment_version_must_be_1` still governs a genuine first enrollment; first-write-wins is otherwise untouched.
  - **A password login resolves the account's LIVE PRIMARY device, never a hardcoded 1** (`DevicesService.resolveLoginDeviceId`). This is load-bearing, not cosmetic: after a reset revokes device 1, a login still claiming device 1 would mint a token for a revoked device, which both §5.5 session gates then correctly refuse — locking the owner out with the right password. Order: live primary, else lowest live id, else device 1 (every pre-Phase-1 account, whose row appears only on first connect).
  - Client side, spec §12 amendments (e)/(xxvii): at decrypt time an inbound row's `(senderId, originDeviceId ?? 1)` must be present-and-not-revoked in the receiver's VERIFIED list for that sender, checked after the (xi) own-row branches so it only gates genuine inbound envelopes. A refusal is silent (a decrypt refusal is not an alarm surface, I7) and NEVER terminal: withheld rows are tracked so the post-retry sweep does not stamp `[Decryption failed]` over them and the recovery pass does not ask the peer to re-key a healthy session. A cache miss (the normal state after any reload — the cache is memory-only) awaits one verified fetch; if that fetch FAILS, withholding applies to `originDeviceId >= 2` only, because refusing device-1 rows would let one broken `getDeviceList` answer silence every conversation of a single-device account (see the (xxvii) rider for why that costs no security). The revoked device itself logs out with a stated reason via `deviceRevoked` and keeps its history.

## 8. Adding cross-tier features

- New WS event: backend DTO + service handler + `@SubscribeMessage` in `chat.gateway.ts`; frontend `SocketService` emit/listen + `ConnectionProvider` routing + provider/model updates.
- New REST endpoint: backend controller/service with `JwtAuthGuard` where needed; frontend `ApiService` call + provider/screen wiring.
- New DB column: backend entity + numbered migration in `backend/migrations/` + mapper payload + frontend model `fromJson`/`copyWith` + tests. Dev auto-DDL does not mean prod is done. Rehearse the migration on the staging stack first (§6).

## 9. Agent skills

### Issue tracker

Issues live in GitHub Issues (`Lentach/Fireplace`) via the `gh` CLI; external PRs are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context: `CONTEXT-MAP.md` at the root points to `backend/CONTEXT.md` and `frontend/CONTEXT.md`; system-wide ADRs go in `docs/adr/`, context-scoped ones in `backend/docs/adr/` and `frontend/docs/adr/`. **All of these are created LAZILY by `/domain-modeling` — their absence is normal, not a gap to fix or flag.** See `docs/agents/domain.md`.

Maintain this file by pruning. If a fact only matters while editing Flutter or NestJS code, put it in the tier file. After adding/removing backend tests, update the count in §3 so `node scripts/verify-claude-backend-test-counts.mjs` stays green.