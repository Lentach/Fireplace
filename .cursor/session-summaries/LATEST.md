# Latest session summary

Entries are newest-first, capped at 5 by `.githooks/pre-commit`. Each one links its dated file,
which holds the full account — **rotating an entry out of this file loses nothing.**

> **The "this file is two tickets behind" banner is gone on purpose.** It was `master`'s copy warning
> readers that the multi-device programme lived on a branch. That branch is now merged into this
> file's history, so the banner had become self-referential and its figures were stale ("(a)–(xxxiv)",
> "T1–T8"). **Standing warnings and history from entries this cap has rotated out** — read the dated
> files before touching those areas: the composer/attachment picker
> (`2026-08-19-session-composer-regression.md` — owner: *"we made huge regress on composer and now
> all old bugs are back"*, ship nothing there without a green repro AND his explicit OK, and
> **never `git revert 0cbf17b`**), PWA notifications (`2026-08-20-session-notif-regression.md`),
> the Actions billing/CI economics history (`2026-08-18-session-actions-billing-and-0.1.16.md`),
> and this programme's own T7/T8 tickets (`2026-08-22-session-t7-edit-refan.md`,
> `2026-08-22-session-t8-harness-sweep.md`), the T10 reset-addressability entry rotated out on
> 2026-09-02 (`2026-08-26-session-t10-reset-addressability.md`, spec (xlv): a completed §6.2 reset
> left the account unreachable for the whole programme; **a harness can only find bugs in the shapes
> it builds** — the never-enrolled reset had a live population of exactly 0; hostile
> `nextListVersion` inflation is bounded by a plausibility ceiling), and the Umbra rename/app-icon
> entry rotated out the same day (`2026-08-26-session.md`: brand sweeps MUST eyeball the RENDERED
> page — split-span wordmarks defeat grep; re-verify subagent compliance claims, one fabricated its
> version bump; owner still owes README screenshot recapture, GitHub repo renames, the domain
> decision), and the T11 entry rotated out on 2026-09-02 (spec (xlvi) is recorded in full at
> `docs/design/multi-device.md` §12, amendment 2026-08-26), and the D1/D2 recovery entry rotated out
> on 2026-09-03 (`2026-08-29-session-d1-d2-recovery.md`, spec (xlvii)–(xlix): read its THREE
> addenda — a warning the user cannot act on is a dead end; recovery state must survive a restart), and the
> 08-30b bounded-gate entry rotated out on 2026-09-03 (`2026-08-30-session-bounded-gate-lxiv.md`, spec (lxiv):
> the revoked-device relogin CLOBBER — a `registrationId` may not change under an unchanged identity, the OTP
> replenishment path bypasses `upsertKeyBundle` so BOTH uploads carry the guard; the own-device confirmation is
> per-socket; residual 8 `_reenrollAfterReset` has no in-flight latch, recorded not fixed), and the 09-02a
> FCM/APK entry rotated out on 2026-09-03 (`2026-09-02-session-fcm-e2e.md`: prod FCM proven on the Pixel_7
> emulator; **versionCode floor 10024**; `am force-stop` silently drops FCM; `FLAG_SECURE` blocks screencap;
> verify an installed APK's dart-defines by byte-searching `kernel_blob.bin`; the signed 0.1.24 APK release
> waits on the owner's passcode feature), and the 09-03 evening (lxxii) entry rotated out on 2026-09-06
> (`2026-09-03-session-lxxii-reset-door.md`: **`deploy-web.ps1` overwrites `frontend/build/web` with the PROD bundle — rebuild
> locally before any 8093 check; a mutation runner once left the mutant ON DISK — `git diff` after every mutation script**;
> the owner still owes the thief-with-password matrix answers), and the 09-04 `feat/video-messages` merge entry
> rotated out on 2026-09-05 (`2026-09-01-session-video-messages.md`: **merging an older release branch must never
> roll the pubspec back**; prod served that branch as a test until the 09-02 `0.2.0` deploy, then master's 0.2.x). For
> multi-device specifically the permanent record is `.planning/multi-device/` (`FINISH-HERE.md`, `progress.md`,
> `task_plan.md`), not this file. The 09-05 night (lxxiii)/(lxxiv) entry rotated out on 2026-09-06
> (`2026-09-05-session-optin-lock-gate.md`: the registration lock is OPT-IN — it arms only once
> `account_authorizations` has a row; `DeviceLinkGateScreen` replaced three banners; a client that sees no
> `linkingEnabled` field must fail CLOSED to the old gate; deploy order backend FIRST — done 2026-09-05).
> The 09-05 (late) video-nits entry rotated out on 2026-09-08 (`2026-09-05-session-video-nits.md`: the
> RETRY branch rebuilt a video payload from url/key/iv/duration only, so a once-failed clip reached the
> peer as a 220 px square with a blank poster — the image branch never had that hole; the WebKit
> `videoWidth == 0` probe fix is still **NOT iOS-verified**, the owner owes a recording from the iPhone
> PWA camera; a route pushed over the chat must RELEASE the inline video slot, and CI never runs on a
> plain branch push — only `master` pushes and pull requests).

**✅ DEPLOY STATE — MASTER IS LIVE, BOTH TIERS (2026-09-08): FRONTEND `0.2.23 / 65affc8`, BACKEND `0.2.23 / 302006e2` (master = `302006e2`, PR #165 merged; CI run 34189449394 green on all 5 jobs before the merge). Multi-device v2 (D26) is RELEASED. Deploy order was BACKEND FIRST because the release adds wire events and migration `0017_identity_backup.sql` — the VM log shows `[Migrations] applying/applied 0017_identity_backup.sql` and all five `recovery_keys.backup*` columns present and NULLABLE; smoke 5/5 (`--commit 65affc8`); the served `main.dart.js` really contains `fp-link.v2`, `restoreSignature`, `getIdentityBackup`, `link-gate-restore`, `account_enrolled_hint` and `BarcodeDetector`, and `/jsqr.js` answers 200. **A CLIENT OLDER THAN 0.2.23 IS SAFE** — every new field is additive and `hasIdentityBackup` absent ⇒ false. Standing rules: PATCH always above what `/version.json` serves — never reuse a version (the 0.2.17 collision rolled live backwards); a bundle without `fpwk1:` unwrap code must never be served to a browser with the passcode ON (every master build has it since `d446a9d`). **Rollback rule: never serve a web build below the passcode merge (`d446a9d`) to a browser with the passcode ON** — every pre-merge master (0.2.17 `a6a6be9`, 0.2.16 `fda92b3`, `c8f662e`, …) lacks `fpwk1:` unwrap code and reads wrapped keys as garbage. **A rollback below 0.2.23 is additionally a DATA-LOSS door now: an account that enrolled a phrase-sealed backup on 0.2.23 has a blob the older client cannot use, and its restore door does not exist there — the user is back on the 72 h reset.** Migration `0017` is additive and safe to leave in place; `0015` is still not code-reversible. VM checkout is on `master`.**

**Date:** 2026-09-08 (after the D26 deploy) — **DEPENDABOT SWEPT TO ZERO: 7 SECURITY ALERTS CLOSED IN ONE LOCKFILE EDIT, 15 PRs RESOLVED, EVERY REFUSED MAJOR CARRIES ITS BLOCKER, AND `dependabot.yml` WAS MANUFACTURING UNMERGEABLE PRs. PLUS A PC HEALTH PASS. ⚠ MASTER NOW CARRIES UNDEPLOYED DEPENDENCY CHANGES ON BOTH TIERS — THE DEPLOY-STATE BLOCK ABOVE DESCRIBES `65affc8`/`302006e2`, NOT `084774e`.** ➡ **`2026-09-08-session-dependabot-sweep-and-pc-health.md`**.
- **The owner's "7 conflicts" was literal on both counts:** 7 open security alerts (fast-uri ×4 high, qs ×2, browserslist), and the three fixing PRs (#160/#161/#162) all rewrite `backend/package-lock.json`, so merging ANY one conflicts the other two. Fixed as ONE `npm update`-based lockfile commit (`e26c3ac`) that also swallowed dependabot's whole backend-minor-patch group (#154): multer 2.3.0 (4 CVEs), pg 8.23.0, typeorm 1.1.1, undici 7.29.1, `@nestjs/*` 11.2.3, jest 30.5.1, eslint 10.10.0, typescript-eslint 8.70.0. **`dependabot/alerts` open count = 0; `gh pr list --state open` = empty.**
- **Applied majors, each proven rather than hoped:** firebase_core 4 + firebase_messaging 16 (their own gradle declares compileSdk 34 / minSdk 23 / AGP 8.3.0 — all BELOW our 36/24/8.11.1; the only removed Dart API is `sendMessage()`, which we never call; web is `!kIsWeb`-gated with no Firebase JS pinned) — **proven with a real `flutter build apk --debug`**, not just the host suite. google_fonts 6→8 (both majors only DELETE font families; Inter/Archivo/PressStart2P are on neither removal list) — and because **a green suite cannot see a silent font fallback**, rendering was proven twice: the API returns real per-weight families and the served release bundle fetches five `fonts.gstatic.com` TTFs in a headless browser. Also flutter_local_notifications 22.3.0, emoji_picker_flutter 4.5.4, globals 17.
- **Refused with hard evidence, all written into the PR threads:** `@types/node` 26 (runtime is `node:22-alpine` in BOTH Dockerfile stages — types must not describe APIs the runtime lacks); Nest 12 (`@nestjs/throttler@6.5.0` still peers `^11` only → it would break the WsThrottlerGuard contract; dependabot's partial bump failed its own CI); **firebase-admin 14 — probed 14.3.0 and `admin.messaging`/`admin.credential` are `undefined`**, the legacy namespace is gone, so `push-notifications.service.ts` needs a modular rewrite whose unit tests (module-level mocks) would go green even if it were wrong; undici 8 (`feat!: enable h2 by default` on the SSRF-pinned link-preview path, zero advisory benefit); typescript 7 (ts-jest peers `<7`, typescript-eslint `<6.1.0`); permission_handler 13 (`permission_handler_android` 14.1.0 wants compileSdk 37 + AGP 9 + Kotlin 2.3); sqlite3 3.x/drift (**sqlite3 3.x DELETED `package:sqlite3/open.dart`**, imported by `content_db.dart` for the SQLCipher open override); clock/build_runner (pinned by the Flutter SDK's `flutter_test`/`meta`).
- **`dependabot.yml` was the real bug (`084774e`):** the `ignore` rules for drift/drift_dev/sqlite3 have been in the file since 2026-08-03, yet #157, #169 AND #171 each bundled them anyway and every one of those PRs was **unresolvable** — matching dependabot-core #7523 / discussion #11962 for a group without explicit `patterns`. **The `frontend-minor-patch` group is now GONE** (update-level `ignore` does work for standalone PRs), `clock` + `build_runner` joined the list, and the drift/sqlite3 rationale was **CORRECTED**: the recorded "native-assets hook needs CMake + MSVC C++ on every local `flutter test`" does NOT reproduce (the host suite runs fine with sqlite3 3.5.2). Backend also got documented major-ignores for `@types/node` and `@nestjs/*`.
- Verified: backend `nest build` clean, **jest 1096/62**, `npm audit` **0 vulnerabilities**, lint ratchet PASS (894/157); frontend analyze clean, **2027/14**, `flutter build web --release` OK, `flutter build apk --debug` OK; **CI green on `279df93`** (CI + CodeQL).
- **PC pass (two read-only diagnostic agents, then only zero-regret actions):** killed a runaway `dllhost.exe` that had burned **4,033 s of CPU since 06:09** (~one full core, orphaned AppID surrogate); freed **9.06 GB** of leaked `flutter_tools.*` scratch dirs (`%TEMP%` 12.6 → 2.2 GB); upgraded the safe set (gh 2.100, **.NET 8 → 8.0.30**, CMake, App Installer). **Two things are actually wrong and need the owner:** (1) **NOTHING is protecting this box** — Defender stood down for Kaspersky 21.24 and Kaspersky's service never starts (`AMRunningMode = Not running`, zero `avp*` processes); (2) **5 × Kernel-Power 41 / 6008 pairs since 07-12 with no BugCheck** = power loss or hard reset, not software. Machine is capacity-bound: 16 GB RAM (530 MB free with a browser at 4.8 GB), 4 cores measured 100%/83% with no throttling and High-performance already active, and a 118 GB second SSD sitting **99.9% empty** while every cache lives on C:.
- **Trap paid for:** the `edit` tool resolves a RELATIVE path against the WORKSPACE ROOT (`Desktop/Fireplace`, on another branch), not the cwd — an edit landed in the wrong worktree. In this multi-worktree checkout, give `edit` an ABSOLUTE path. Also: master moved (D26 merged + deployed) mid-session, so the work was parked, rebased and re-verified from scratch.

**Date:** 2026-09-08 — **MULTI-DEVICE v2 (D26, spec (lxxvi)–(lxxix)): THE RECOVERY PHRASE IS NOW A REAL KEY BACKUP (WIPE → RESTORE THE SAME IDENTITY IN SECONDS, NOT A 72 h LOCKDOWN), THE QR LINKS IN BOTH DIRECTIONS WITH AN IN-APP SCANNER, "CONTACT'S SECURITY NUMBER CHANGED" IS DEMOTED TO A MUTED LINE BY DEFAULT, AND THE PASSCODE LOCK STOPS SABOTAGING LINKING. 0.2.23, commit `54f68a1`, PR #165.** ➡ **`2026-09-08-session-multidevice-v2-d26.md`**.
- **The prod data drove it:** 114 users, 2 enrolled accounts, 0 linked devices, 0 reset requests EVER — but `identity_change_audit` held 3 rows, all `[identity-churn] … via=unlocked`. Three real people wiped their browser data and silently re-minted an identity; for an ENROLLED account the same accident means 72 h, and the phrase only shortened it to 1 h. **(lxxviii)** now seals IK + `registrationId` + DAK as AES-256-GCM under PBKDF2-HMAC-SHA256(600 k, NFKD phrase, 16-byte salt) — migration `0017`, columns on `recovery_keys`. **Signed/one-time prekeys are deliberately NOT sealed** (they rotate and burn; a stale copy is worse than none).
- **Restore preserves the identity:** `uploadKeyBundle { restoreSignature, nonce }` — XEdDSA by the CURRENT IK over `identityPublicKey ‖ userId ‖ nonce`, accepted ONLY when the uploaded identity EQUALS the stored one, so there is **no audit row and no §6.0 alarm**. Order is load-bearing: fetch → unseal (a wrong phrase stops here and spends nothing server-side) → adopt → nonce → upload → ride the EXISTING §6.2 rebind → **only then** OTPs → re-sign the roster at the server-named `nextListVersion` with the PRESERVED DAK → `requestSessionRebuild` per peer. Accepted: fresh bundle under the same identity, one lost in-flight message per peer, old history stays unreadable, phrase possession = takeover (Signal's PIN/SVR posture). **72 h was NOT shortened; QR-as-login stays out of scope (I3).** The phrase is now MANDATORY when enabling linking (random-word confirm; `enableLinking` split into `mintDak()` + `enableLinking()` because the blob carries the DAK).
- **(lxxvii)** `openProvisioning { role?: 'new' | 'primary' }` (default `'new'`, byte-compatible); with `'primary'` the hello relay carries the assigned `deviceId` and `provisionDevice` from a non-primary is refused `not_primary`. Code v2 `fp-link.v2.<id>.<b64url ephPub>.<platform>.<p|n>`, v1 still parses as `n`. **The wire field stays `ephPubP`** and the SAS transcript order stays fixed N-then-P — the role only names which slot a key fills. Both screens show a QR AND scan one; **web never fetches a decoder from a CDN** (`BarcodeDetector` → vendored `web/jsqr.js`, Apache-2.0 — which is why `mobile_scanner`'s web scanner is unused). Android gains an autoVerify `/link` intent-filter + MethodChannel.
- **(lxxix)** `keyChangeWarnings` defaults **false**: the auto-ackable paths (server event, local TOFU) become a muted persisted line, auto-acked. **Refusals stay fail-closed** — `_recordAccountIdentityRefusal` is NOT auto-acked (auto-adopting a mismatched served key would dissolve the (xxxix) anchor and put a *dismissible* banner over a live MITM); refused peers always get the red pill, the own-account banner is never demoted. **(lxxvi)** `linkCeremonyActive` exempts the ceremony from auto-lock, adopt runs `PasscodeWrapHook.run()`, and the lock-screen erase copy is enrolment-aware via a CLEARTEXT prefs hint (the wrapped store is unreadable while locked).
- **frontend 2027/14 skipped · backend 1096/62 · e2e wire 46/9 skipped**, all green; both count verifiers re-run; lint ratchet 906 → 894 (floor lowered). F1–F12 each proven by a printed mutant or a fails-without-change test. Wire falsification: flipping `key-bundles.service.ts:251` `if (!proofValid)` admitted a STRANGER's proof as a restore (3 of 4 tests failed), restored.
- **Live-driven on a real bundle** (app-mode Chrome, because `display-mode: standalone` CANNOT be emulated via CDP — only `prefers-color-scheme` takes): mandatory phrase + wrong-confirm-word refusal → 496-char sealed blob in Postgres → **browser-data wipe → restore from phrase** → `[identity-restore] proof verified`, 20 OTPs purged, device 1 → 3, identity byte-identical, no new audit row, roster re-signed to v2; **flipped QR ceremony** (primary shows `.p`, new device ingests) SAS **814 098** matching both sides, device #4 linked; enrolled-aware erase copy; demoted key-change row off by default.
- ⚠️ **Found, not assigned:** `test_e2e/full_stack_e2e_test.dart` had 4 unfixed `LinkOobCode.ephPubN` callsites — the default e2e run was RED and CI's `e2e-wire` job would have failed, because the batch agents ran `test/` but never `test_e2e/`. **Analyze `test_e2e` too after any lib rename.** `e2eSql`'s default container was `fireplace-0a-db-1` — the ABANDONED worktree's stack, so every SQL-backed test failed locally on a normal checkout (CI resolves it dynamically); now `fireplace-db-1`. **`git add -A` swept 51 files out of the ignored-by-intent `local/` scratch dir into a staged release** — there was no `local/` rule at all and my first `check-ignore` read was WRONG (an advisory caught it); `local/` + `frontend/test-output.txt` are now ignored. **Read `git status --short` before every commit.**
- **Traps:** agents on `claude-fable-5` hit a 429 with `retry-after` ≈ 4.8 h — owner's call, `modelRoles.task` in `~/.omp/agent/config.yml` is now `anthropic/claude-opus-5:medium` (probe before delegating). **Two subagents died at exit 1 mid-mega-edit with work half-landed and never analyzed — a dead agent's claims are worthless until the analyzer and tests run;** the respawn rule is ≤60-line hunks. `docker compose restart backend` clears the 10/h register bucket but needs **~2 min** to recompile before `/health` answers. `hub restart` reuses the RETAINED spec — it resurrected an old Python server on a stale directory serving `0.2.17`; start a fresh name.

**Date:** 2026-09-06 — **PASSCODE LOCK MERGED AND RELEASED (0.2.22). THE PHONE-TEST ROUND: WAKE RE-STAMP, PRIVACY CURTAIN (DOM — THE FLUTTER ONE FAILED THE PROOF), PICKER EXEMPTIONS, DISABLE → SETTINGS; A MASTER-DEPLOY COLLISION RECOVERED BY REBASE.** ➡ **`2026-09-06-session-passcode-wake-curtain-merge.md`**.
- **Wake came back unlocked:** Flutter web delivers `hidden → inactive → resumed` on wake; `inactive` re-stamped `passcode_last_active_at` at the wake instant (proven by reading the stamp on both edges over CDP). `_away` latch: stamp once per departure, cleared by every verdict and by a lock; guards the STAMP only. My "no event fires on screen-off" was WRONG (probe page proved both edges) and is retracted.
- **"Chat → white → lock" on wake:** the chat is the browser's last painted frame; the white was ours. A Flutter-side curtain passed every unit test and FAILED the burst-screenshot proof — a real screen-off leaves 81–397 ms between `blur` and hidden. Shipped a DOM `#fp-curtain` shown synchronously by the page's blur handler and at boot; Dart lifts it post-frame after the verdict. **Setting state is not painting a frame; classify by pixel AND read visually.**
- Attach picker exempt from lock, verdict and both curtains; disable pops to Settings; curtain accent map pinned to `RpgTheme` (two hand-copied hexes were wrong; regex anchored inside the object because keys collide with the background map).
- **Collision:** another agent deployed master `a6a6be9` as "0.2.17" — a reused version, live rolled backwards, passcode gone. Rebased the branch onto it (auth fixes kept, LATEST entry restored), 0.2.20. Rule recorded above.
- Merge: fast-forward, prod already = master, backend untouched. Suite **1957/14**. Open (owner's call): wall-clock throttle, un-zeroed rekey KEK, `fpwk1:` AAD, non-extractable CryptoKey, Android padlock-during-fullscreen audio.

**Date:** 2026-09-06 — **"FRIEND CANNOT REGISTER" WAS A 409 THE CLIENT COULD NOT SPEAK, ON AN ACCOUNT THAT ALREADY EXISTED. THE FRONT DOOR NOW NAMES EVERY REFUSAL, REGISTRATION SIGNS THE USER IN, AND A TAKEN NAME THAT OPENS WITH THE TYPED PASSWORD JUST SIGNS THEM IN. REVIEWED (both axes), DEPLOYED AS `0.2.17 / a6a6be9`, SMOKE 5/5.** ➡ diagnosis **`2026-09-06-session-register-409-diagnosis.md`**, fix **`2026-09-06-session-auth-clarity.md`**.
- **The field case (prod logs + `chatdb`):** `maoi` (user 60, since May) → `POST /auth/register` **409 ×3**; then a **201** at 22:01:54 UTC created **user 114 `ma0i#5269`** from the same browser on cellular after a WiFi handoff — and that account has **0 logins, 0 key bundles, 0 refresh tokens**. He read "Coś poszło nie tak" for all of it and stopped. Owner told him to log in.
- **Root cause was the classifier, not the server:** `_networkErrorCode` matched five message substrings and sent everything else to `unexpectedError`; `package:http` 1.6.0 `browser_client.dart:148-160` re-throws the ENGINE's `TypeError` text, so `Failed to fetch` only ever matched Chromium and iOS WebKit's transport failures ALSO read as "something went wrong". Now `ApiException` carries the STATUS and `classifyAuthFailure(error, attempt:)` maps 400/401/409/429/5xx per door, with transport classified by TYPE (`TimeoutException`, `ClientException`).
- **Registration now signs the user in** (the credentials are in hand; "account created, now sign in" was the step people got lost on), **and a 409 tries those same credentials BEFORE refusing — the friend's case self-heals: pressing "Utwórz konto" on a name he already owns puts him in the app, no message to read.** Only a 409 whose sign-in is refused (someone else's name) shows "already taken" — with `recoverableUsername` + a "Przejdź do logowania" button that prefills the login tab, because naming the refusal alone still sends the user to invent a NEW name, away from the account they just made. Not a new oracle: one ordinary login attempt on the login throttle, behind the stricter 10/h register throttle. A lost register answer keeps its own wording (`registerOutcomeUnknown`) — the request is not idempotent, so the account may exist.
- Also: the register tab STATES the username/password rules before they are hit and enforces the username rule client-side; the status clears on submit and on the first keystroke (it cleared only on a tab switch, which is how a `maoi` error sat under a field already retyped to `ma0i`); Settings → change password / delete account share the same localized mapping instead of appending `$e`.
- Verified live against a real backend in headless Chromium: fresh register → straight into Chats; **same name + same password → straight into Chats (silent recovery)**; same name + different password → named refusal + jump → prefilled login tab; wrong password → "Nieprawidłowa nazwa użytkownika lub hasło"; right one → shell. Suite **1779 / 10 skipped**, analyze clean, verifier re-run against the captured log. **Then reviewed on both axes and DEPLOYED — see the deploy-state block above.**
- **Review outcome:** Standards found **no hard violations** (3 judgement-call smells recorded, none acted on: the username rule now lives in client regex + ARB wording + server `RegisterDto`; the 400-split still sniffs the English word "password" at `auth_provider.dart:103`; the tab-jump mutation has two owners). Spec found **one real bug, fixed in `a6a6be9`** — Settings' wrong-current-password read "Nieprawidłowa nazwa użytkownika lub hasło" in a dialog with no username field, so `AuthStatusCode.wrongPassword` now serves `AuthAttempt.credentialChange`. Spec's second finding (tab `Semantics`/48 dp + spinner colour as scope creep) was a **FALSE POSITIVE** — both predate `90f7056`, proven with `git diff`/`git show`; nothing reverted. **A scope-creep finding is a claim about the DIFF: check it against the diff before acting.**
- ⚠️ **`Desktop/Fireplace` is checked out on `feat/passcode-lock`, NOT master** (master lives in the `Desktop/fireplace-0a` worktree; this work was written on the branch worktree and transplanted by patch). Root `CLAUDE.md` §1 was CORRECTED this session. Two worktree traps paid for here: both review subagents had to echo `git rev-parse HEAD` because the sibling worktrees share one `.git` and a wrong cwd reviews the wrong branch silently; and `deploy-web.ps1` always exits 1 in a worktree after `PUBLISHED_OK` (no `scripts/smoke` node_modules) — run the gate from the main checkout, never `-SkipVerify`.

**Date:** 2026-09-05 (evening → late) — **VIDEO-NITS MERGED; THE `e2e-wire` RED WAS THREE STALE TESTS (FIXED); THE `e2e-isolated-probes` RED WAS A REAL GAP (lxxiii) OPENED — RULED (lxxv), FIXED, PROBE REWRITTEN; MASTER GREEN 5/5 AND DEPLOYED BOTH TIERS AS 0.2.16; `feat/passcode-lock` REBASED, PICKER-GUARD BUG FIXED, 1932/14, DRAFT PR #164 FOR CI, NOT MERGED (owner: "test passcode, then merge").** ➡ **`2026-09-05-session-video-merge-e2e-fix.md`**.
- **"Merge it and deploy, I've tested it" was two different things:** the tested build was the BRANCH (`test/video-nits-0.2.3`); master additionally carried `a71ed33`, untested and red. Owner: merge now, hold deploy until the red is understood. `feat/video-nits` fast-forwarded (PR #163 auto-closed).
- **`e2e-wire` (3 tests) = stale:** they registered never-enrolled accounts and expected `identity_locked`, which (lxxiii) made unreachable (`key-bundles.service.ts:173-195` refuses only `isEnrolled`). Rewritten to the opt-in contract: un-enrolled replacement ACCEPTED + `ownIdentityReplaced` to the other session; then DAK enrolled through the real wire → unsigned upload `identity_locked`, bundle byte-identical, no alarm; OTP refusal moved after T2 enrolment. §6.1 nonce single-spend is no longer observable on the wire (pinned by `chat-key-exchange.service.spec.ts` only). Harness **44/5**, `CLAUDE.md` §3 updated.
- **`e2e-isolated-probes` = REAL GAP, ruled (lxxv) on the owner's delegation:** never-enrolled is now the DEFAULT population, `requestIdentityReset` had no enrolment check, the gate shows the reset door in `checkingOnly` → an un-enrolled owner could run the 72 h ceremony, have device 1 revoked and be stranded un-addressable ((xlv) silence). Now `not_enrolled`, refused BEFORE the phrase (nothing written, phrase intact), after the pending check (old rows stay cancellable). EN+PL copy points at the recovery that works (sign in on the new device). Probe rewritten to prove the unlocked remint under device 1 + `liveDeviceIds == [1]` — the assertions that never ran while it died on its premise. Backend **1066/62**; RESET_PROBE 2/2, ENROLLED_LOCK_PROBE 2/2 locally. Spec §12 D25 (lxxv); root §7 reset line updated.
- **Passcode × video: no code conflict** (`settings_screen.dart` additive, l10n unioned, gen-l10n zero drift), but the advisor caught a REAL runtime bug: `MainShell` fires `noteBackgrounded()` on every visibility loss, so at auto-lock 0 s the attach camera/file sheet → lock → key revocation → web relaunch → pick lost. Fixed on the branch: `PasscodeProvider` reads `composerNativePickerActive` (the same signal the freeze-reload guard uses), holds the immediate lock and skips the foreground verdict while the span is up, still stamps the clock; 3 tests proven by mutation. Branch = `83afc18`, 15 commits on `9a1c439`, byte-identical to a merge, **1932/14**. Android nit still open: a manual padlock during a fullscreen clip with sound leaves audio playing behind the `Offstage` lock.
- Owner's UX verdicts (reload-on-lock, 0 s desktop auto-lock, 6-digit vs alphanumeric) still outstanding; he will test the branch and then decide the merge.
- Traps: local `test_e2e` needs `E2E_DB_CONTAINER=fireplace-db-1` via `cmd /c "set …&& flutter.bat test …"`; the in-memory `/auth/register` 10/h bucket bites on a second run — `docker compose restart backend` clears it; Docker Desktop had QUIT between runs (pipe gone) — `Start-Process 'Docker Desktop.exe'`, daemon up in ~10 s; a fresh backend worktree needs `npm ci` before jest; dart2js escapes non-ASCII, so grep the served bundle for an ASCII substring of a Polish string. `gh repo view --json isPrivate` → **false** (root `CLAUDE.md:20` fixed).


- **Still binding, from the rolled-off 2026-08-21 T6 entry** (full text in `2026-08-21-session-t6-revocation.md`, closure in decision record §11, settlement spec §12 **(xxi)–(xxix)**): **a LOCKOUT lives in this area — login used to hardcode `deviceId = 1` (`auth.service.ts:74`), so the moment a reset revoked device 1 the correct password minted a token for a revoked device and both session gates correctly refused it.** Login resolves the LIVE PRIMARY (`DevicesService.resolveLoginDeviceId`); the regression test names the lockout. **The session gates deny only on an EXPLICIT `revokedAt` (xxii)** — a MISSING `devices` row must NEVER deny, or the whole pre-Phase-1 install base loses access; that is deliberately the inverse polarity of `DevicesService.isActive`, which gates key-material uploads and must fail closed on absence. **Both predicates stay; do not unify them.** **I6 silence is a separate rule from rejection (xxiii):** `getServedMessageIds` answers a revoked device with NOTHING — an empty list would mean "destroy all of them". **`account_authorizations` is REPLACED, never dropped (xxix)**, and the replacement is admitted only when the STORED enrollment no longer verifies under the account's CURRENT published identity — self-verifying, so no flag and no nullable state (this is the hook T10's (xlv) finally used). **The accept-side gate ((e)/(xxvii)) withholds on VERIFIED data only and its refusals are never terminal**; when the fetch itself FAILS, withholding applies to `originDeviceId >= 2` only, or one broken `getDeviceList` answer silences every conversation of a single-device account. **⚠️ THE APP-PROOF EARNED ITS KEEP:** the revoke button failed live with NOTHING in the server log — `revokeDevice` never armed its DAK from the Keystore, so `signList` threw before any emit, **and the unit suite was green because it pre-armed the engine — a test that could not fail.** Drive the production path.
