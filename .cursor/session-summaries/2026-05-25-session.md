# Session 2026-05-25

## Accomplished

- **Release 0.0.14** (earlier): trailing send + voice lock-up on `master`.
- **iOS PWA composer keyboard — Phase 1** (plan `docs/superpowers/specs/2026-05-25-ios-pwa-composer-keyboard-send-plan.md`):
  - **P1.1:** `_ComposerTapSendOverlay` — `onPointerDownRetainFocus` + sync `requestFocus()` on trailing send `pointerDown`.
  - **P1.2:** `_send()` — `hadFocusBeforeSend` gates post-frame `showSoftKeyboardIfHidden`.
  - **P1.3–P1.4:** `force` param on `showSoftKeyboardIfHidden`; reply / action-panel / `_requestComposerFocus` use `force: true`.
  - **P1.5:** `TapRegion` with stable `_composerTapRegionGroup` on `ChatInputBarState` (composer row only).
  - **P1.6:** Extended `chat_input_bar_trailing_send_test.dart` (TapRegion, pointerDown focus, `TextInput.show` mock).
  - **P1.8:** `CLAUDE.md` updated (§1, §7, §9).
- **Version:** still **0.0.14** (no bump until manual M1–M3 + M9 pass).
- **graphify** updated after lib changes.

## Key files

| File | Change |
|------|--------|
| `frontend/lib/widgets/input/chat_input_bar.dart` | pointerDown focus, `_send` gating, TapRegion, `sendMessageForTest` |
| `frontend/lib/utils/soft_keyboard*.dart` | `force` param |
| `frontend/test/widgets/input/chat_input_bar_trailing_send_test.dart` | Phase 1 regression tests |
| `CLAUDE.md` | iOS PWA Phase 1 notes + manual QA gate |

## Tests (CI-local)

```text
flutter test test/widgets/input/chat_input_bar_trailing_send_test.dart  → 22 passed
flutter test test/providers/messaging_provider_composer_focus_test.dart → 1 passed
flutter test test/utils/web_viewport_scroll_test.dart → 2 passed
```

## Rollback

If iPhone PWA QA fails: revert to **`087fdb5f664ae85c2760e428c52af380936f44f7`**.

## Manual QA gate (user — iPhone)

**Merge/deploy blocker:** M1–M3 on **Home Screen PWA**; **M9** on Safari tab. Full matrix: plan §6.2.

| # | Check |
|---|--------|
| **M1** | Trailing Send ×5 — keyboard stays, no jump |
| **M2** | IME Send ×5 — same |
| **M3** | After trailing send, type next message without re-tapping field |
| M4 | Tap message list — keyboard dismisses |
| M5–M8 | Reply, action panel, voice lock, rotation |
| **M9** | Safari tab trailing Send ×3 |

On **M1–M3 pass:** bump `pubspec.yaml` to **0.0.15** and deploy. If M1–M3 still fail → Phase 2 (do not bump).

## Notes

- Phase 2 (`visualViewport` inset) **not** started.
- Widget tests do not run WebKit; manual iPhone validation required.

---

## Update — Composer dual-button layout branch

### Accomplished

- Created and switched to branch **`feat/composer-dual-button-layout`**.
- Implemented approved composer trailing-action redesign:
  - two independent trailing slots (`mic` left, `send` right),
  - send icon centered in right 48x48 slot,
  - send always visible (empty tap stays no-op),
  - mic visible but disabled when draft text is non-empty.
- Enforced right-slot priority:
  1. `isSendingVoice` -> spinner + disabled actions,
  2. `isRecording && isRecordingLocked` -> voice-send affordance,
  3. default text send affordance.
- Preserved focus/keyboard protections (`pointerDown` focus retention + `TextInput.show` gating).
- Updated `CLAUDE.md` composer behavior notes to match the new implementation.
- Ran `graphify update .` after code edits.

### Key files

| File | Change |
|------|--------|
| `frontend/lib/widgets/input/chat_input_bar.dart` | Dual-slot trailing actions, explicit right-slot priority, centered send |
| `frontend/test/widgets/input/chat_input_bar_trailing_send_test.dart` | Reworked tests for dual-slot UX, mic-disable rules, priority behavior |
| `CLAUDE.md` | Updated chat composer behavior documentation |

### Verification

```text
flutter test test/widgets/input/chat_input_bar_trailing_send_test.dart -> 26 passed
flutter analyze -> fails on 6 pre-existing issues outside changed files
```

### Status / notes

- Changes are local on `feat/composer-dual-button-layout`.
- No commit was created.

---

## Update — Review findings applied on `feat/composer-dual-button-layout`

### Accomplished

- Applied accessibility fixes in trailing send controls:
  - text send overlay now exposes semantic tap action,
  - locked voice-send semantics now expose semantic tap action.
- Removed hard 300ms max-tap gating in `_ComposerTapSendOverlay`; tap acceptance now keeps movement guard while allowing slower taps.
- Extended trailing-send tests with semantic action coverage for both text send and locked voice send paths.
- Updated stale `CLAUDE.md` note to match current always-visible send behavior (right slot stays visible; empty tap is no-op).
- Ran `graphify update .` after code edits.

### Key files

| File | Change |
|------|--------|
| `frontend/lib/widgets/input/chat_input_bar.dart` | Added semantic `onTap` paths; removed hard tap-duration cutoff while preserving movement guard |
| `frontend/test/widgets/input/chat_input_bar_trailing_send_test.dart` | Added semantic-action tests and semantic tap execution coverage |
| `CLAUDE.md` | Corrected stale send-visibility note |

### Verification

```text
flutter test test/widgets/input/chat_input_bar_trailing_send_test.dart -> 28 passed
flutter analyze -> 6 pre-existing issues outside edited files
```

### Status / notes

- Branch remains `feat/composer-dual-button-layout`.
- Ready for commit/push from this working tree.

---

## Update — Confirmed in-field mic layout

### Accomplished

- Implemented the confirmed composer UX on `feat/composer-dual-button-layout`:
  - mic moved **inside** the text field as right-aligned suffix affordance,
  - external right side now has only the send slot,
  - fixed gap between field and send slot to **8dp**,
  - mic remains visible but disabled for non-empty drafts,
  - send remains visible/tappable and still no-ops on empty draft.
- Preserved existing recording flow (recording bar replacement), right-slot priority (`spinner` > `locked voice send` > `text send`), and iOS pointerDown/focus/keyboard protections.
- Updated composer docs in `CLAUDE.md` to reflect the new in-field mic behavior.
- Ran `graphify update .` after edits.

### Key files

| File | Change |
|------|--------|
| `frontend/lib/widgets/input/chat_input_bar.dart` | Moved mic into `TextField` suffix, kept external send-only slot with 8dp gap |
| `frontend/test/widgets/input/chat_input_bar_trailing_send_test.dart` | Updated/added assertions for in-field mic placement and 8dp send gap |
| `CLAUDE.md` | Updated composer behavior notes (§1 + §7) |

### Verification

```text
graphify update . -> success (graph rebuilt)
flutter test test/widgets/input/chat_input_bar_trailing_send_test.dart -> 29 passed
flutter analyze -> 6 issues (pre-existing, outside edited files)
```

### Status / notes

- No commit was created in this update.

---

## Update — Semantic send focus parity follow-up

### Accomplished

- Applied the review follow-up for accessibility-path parity in composer send:
  - semantic activation (`Semantics.onTap`) now uses the same focus-retention callback path as pointer activation before `_send()`.
- Extended composer regression test to assert semantic send keeps composer focus after send.
- Kept the fix minimal (no layout/state-priority changes) and aligned with the existing in-field mic + external send model.
- Ran `graphify update .` after code edits.

### Key files

| File | Change |
|------|--------|
| `frontend/lib/widgets/input/chat_input_bar.dart` | `_ComposerTapSendOverlay` semantic tap now retains focus before send |
| `frontend/test/widgets/input/chat_input_bar_trailing_send_test.dart` | Semantic-send test now verifies focus is preserved after send |
| `CLAUDE.md` | Composer note updated to mention semantic-path focus-retention parity |

### Verification

```text
flutter test test/widgets/input/chat_input_bar_trailing_send_test.dart -> 29 passed
flutter analyze -> 6 issues (pre-existing, outside edited files)
graphify update . -> success
```

### Status / notes

- No commit was created in this follow-up.
