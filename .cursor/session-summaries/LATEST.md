# Latest session summary

Entries are newest-first. Each one links its dated file, which holds the full account.

**🗺 START HERE IF YOU ARE LOST: `2026-08-05-HANDOFF-audit-state.md`** — plain-language map of the 2026-08-04/05 full-codebase audit: what was found, what merged, what is deliberately parked, and the three owner-only decisions. The two ordered task queues are `.planning/full-audit/REMAINING-WORK.md` (backend) and `.planning/full-audit/frontend/REMAINING-WORK.md` (frontend), both local-only/gitignored. Older long-form: `2026-08-03-HANDOFF-post-b2b-state.md` (B2b gate detail + traps; **its work queue is DONE**) and `2026-08-03-HANDOFF-signal-grade-queue.md` (dated addenda).

**✅ DEPLOY STATE — FRONTEND `0.1.16 / c7c2d0e` LIVE 2026-08-18, BACKEND `0.1.11 / 91535317` (unchanged since 08-16 14:20Z; every release after it was frontend-only — the lag is CORRECT). Master == prod. Smoke 5/5; live `main.dart.js` md5 `bae5fc6d8a69d665b065d03bb11eb426` == local, Giphy key present in the live bundle.** Release train: 0.1.10 (identity guard + forensics) → 0.1.11 (video batch + migration 0012) → 0.1.12 (media picker; also carries §5.4 PRs #137/#138/#139) → 0.1.13 (§5.4 post-deploy-review followups, PR #141) → 0.1.14 (attachment sheet, PR #142) → 0.1.15 (flattened picker, PR #143) → 0.1.16 (iOS popover anchored to the paperclip, PR #145). **⚠️ Post-deploy follow-up still owed from 0.1.11:** the owner overrode the device-A/B gate on the composer motion — verify on his real iOS/Android PWA and be ready to revert the two motion commits (see the video entry below). **⚠️ Also owed from 0.1.16:** only the owner's iPhone can confirm the attachment popover now opens AT the paperclip — desktop cannot reproduce popover anchoring. **⚠️ `deploy-web.ps1` exit-21 silent publish halt** — build succeeds, log dies at the "Publish via ssh/scp" banner; manual staged publish is the workaround (10 releases running) and worktrees need the gitignored `deploy-web.config.ps1` copied in before building.
- **0.1.10's guard + forensics are live inside 0.1.11:** empty keystore → server check; exists → consented-recovery banner, no silent re-mint; UNKNOWN → defer + retry. `BOOT_MARKERS {ls,idb,cache}` + `storage.estimate()` recorded before keystore creation. **Silent identity churn is dead as a mechanism.** Users must fully close + reopen the PWA (never reinstall); Safari can take ~14 h.
- **⚠️ Three 08-14 traps.** (1) `.\deploy-web.ps1 | tail` **swallows a publish-stage failure** — build ok, nothing published; re-run with output redirected. Staged swap = nothing half-ships. (2) **The footer's VERSION half is a live `version.json` fetch and LIES about the running bundle; only the COMMIT half is compiled in** (`auth_screen.dart:205-207`) — `0.1.9 / c01317c` means old JS. (3) **NO `Cache-Control` on any entry file** (ETag only) ⇒ heuristic freshness, no revalidation; the Safari PWA took ~14 h to flip. Queued fix is `no-cache` on the static root — **Flutter output is NOT content-hashed**, so `immutable` by extension would bake a bundle in forever, helping only the NEXT deploy.
- **⚠️ THE CANARY GATE IS NOT EVIDENCE.** `flutter_secure_storage_web` 1.2.1 is `localStorage`, not IndexedDB+WebCrypto, and keeps its AES master key there raw+extractable (`flutter_secure_storage_web.dart:110-116`), so the canary compares localStorage to a localStorage shadow — `CONTENT_KEY_CANARY_LOST` is ~unreachable and `ageDays` never was clearance (one arm, 07-30).
- **Backend: DEPLOYED 2026-08-16 14:20Z at `91535317` (0.1.11, migration `0012` applied).** Carries `checkOwnKeyBundle` (read-only, caller-only, throttled, consumes no OTP) from 0.1.10.

**🔴 `[Decryption failed]` / IDENTITY LOSS — OPEN, CAUSE STILL UNIDENTIFIED. ➡ READ `2026-08-16-HANDOFF-identity-loss-consolidated.md` FIRST.** It supersedes `2026-08-15-HANDOFF-identity-loss-unproven-cause.md` (whose central hypothesis is now ANSWERED — see below) and `2026-08-14-HANDOFF-post-0.1.9-decryption-failed.md` (premise false). **PROVEN:** empty store → login → `initialize()` `absent` branch → `_generateKeys()` → `[identity-churn]` (+2 s, observed 4×) → all prior sessions dead → peers' older messages permanently `[Decryption failed]`. The logout is client-side (54's refresh row is still in the table, never revoked; **refresh tokens do NOT rotate**, `refresh-tokens.service.ts:57-77`, so the client lost or misread `flutter.refresh_token` itself). **ELIMINATED 2026-08-16:** (1) **quota — dead by measurement**, 54 has **25 messages** against a 2000-record cap and 26 sig rows, tens of KB against ~5 MiB, and a failed `setItem` throws and leaves the existing value intact; (2) **silent misread of the identity — dead**, every value is `json.encode`d so the pinned `shared_preferences_web-2.4.3`'s two silent-null vectors cannot fire, and `loadFromStorage` has no catch, so `absent` requires GENUINE emptiness; (3) **two-container theory — refuted** for the 08-14 churn, the push POST 1 s after that login UPDATED the row created `2026-05-13` (upsert unique on `endpoint`, client only calls `getSubscription()`, and on iOS only a standalone PWA can hold a subscription) so the loss happened INSIDE the long-lived Home-Screen PWA; (4) **eviction — excluded by the engines' own policy**, WebKit `performEviction` skips `isPersisted` origins and ITP exempts Home-Screen apps, and both users report `granted: true`. **THE ONE UNVERIFIED PREMISE: was the bucket actually persistent at the moment of loss?** The app records exactly that into a log that dies with the storage, and `initialize()` runs at `encryption_provider.dart:819` BEFORE the persist probe at `:849`. **Every iOS discriminator is architecturally blind** (push is out-of-bucket in `webpushd`; 167 `/web-push-sw.js` fetches in 14 days, 165 Android, **0 iPhone**; the diag log and HTTP cache prove nothing) — **so work user 90 on Android, where both signals are live.** ⛔ NEW RETRACTIONS, MINE: "the unchanged push endpoint proves the bucket survived" (false on iOS) and "quota explains the logout via a stale refresh token" (false, no rotation). **Fix designed, NOT authorised, NOT written:** tri-state guard on the `absent` branch (exists ⇒ refuse + consent via `regenerateIdentityAfterConfirmedLoss()`; none ⇒ generate; **UNKNOWN ⇒ defer**), via a NEW read-only socket event — **never reuse `fetchPreKeyBundle`, it consumes an OTP**. ⚠️ **Open design problem against that fix: `key_bundles` is unique on `userId` with no `deviceId`, so ANY legitimate second-device login hits the "bundle exists" branch** — today it silently churns, so this app is already single-device at the crypto layer; the prompt must be worded and tested for that case, and **whether some of the eight churns are second-device logins rather than wipes has never been audited.**
**⤷ 08-16 UPDATE: handoff §10.1 EXECUTED — storage loss is confirmed for USER 54 ONLY.** Verdict table in `2026-08-16-session-churn-audit.md`; handoff §4.2 + §10.1 amended in place. Of nine dated churn events: **4 proven non-wipe** (43 by Chrome/150 vs his usual /151; 76/92 by an explicit logout + account switch; 58 owner incognito), **3 wipes — all user 54** (08-03 + 08-14 proven same-container, 08-15 probable and deeper — his push subscription died with it), **2 UNKNOWN — both user 90** (cold cache fits fresh context AND dormant-profile eviction). The "two engines both losing storage" inference is UNPROVEN, not forced; the evidence-backed question is iOS-only.
**⤷ 08-16 UPDATE 2: THE §5 FIX IS AUTHORIZED, BUILT, REVIEWED AND DEPLOYED (0.1.10 → live).** The root-cause question (what empties user 54's container) stays open but is now INSTRUMENTED: his next wipe produces a banner instead of a churn, plus a `BOOT_MARKERS` triple answering §10.3 (whole bucket vs localStorage-only). **§5.4 is closed AND LIVE** (PRs #137/#138 shipped inside 0.1.12; post-deploy-review followups PR #141 shipped as 0.1.13): storage read errors are no longer logouts, locally-derived session ends can no longer delete stored tokens, and both repeat-prone durable records (`AUTH_SESSION_END` locals, `AUTH_TOKENS_UNREADABLE`) are deduped so they cannot evict the wipe forensics. Live-backend fault-injection proof is permanent CI (`test_e2e/auth_token_fault_injection_test.dart`). The 54 password reset is MOOT — he logged in with his old password.

**🔴 FCM IS DISABLED IN PRODUCTION.** `FIREBASE_SERVICE_ACCOUNT` is absent from `~/fireplace/.env`, so every backend boot logs "FCM disabled". `google-services.json` IS tracked, so an APK would register tokens the server can never push to — **Android notifications are dead until that var is set.** Web Push VAPID is present, so the PWA is unaffected, which is why this went unnoticed. Fix it before any APK work.

- **Still binding (applies to every entry below):** never run `dart format lib/` (format only what you edited); **ask before opening the browser tool**; `node scripts/lint-ratchet.mjs` runs ONLY in CI — run it before pushing backend changes; `cmd | tail` hides exit codes; never re-derive test counts by arithmetic across a merge; **never give `flutter test` a file list** (per-argument compile cost: 45 files timed out past 11 min, the whole suite runs in 170–310 s); `flutter run -d web-server` serves a blank scaffold once its hot-restart client is lost — restart the process, do not reuse the tab; the E2E register throttle is 10/hr/IP in memory, so `docker compose restart backend` between full `test_e2e` runs and allow 2–5 min to boot.
- **Still binding, from the rolled-off 2026-08-04 entry** (full text in `2026-08-04-session-frontend-audit-fixes.md`): **`refreshSession` timeout maps to `SessionRefreshTransientException`, NEVER `Invalid`** — transient holds the session, invalid clears local auth, so the wrong mapping manufactures the very logout this repo keeps chasing (see the 08-14 entry). **Message send has no ack timer and must not get one** (only invitations do, `_kInvitationAckTimeout`): failing a send the server received releases the exactly-once latch and duplicates it — why FE-024 was skipped deliberately. **wasm nearly deleted the multi-tab Signal lock** — `session_cross_context_lock.dart` + `storage_persist.dart` key on `dart.library.html`, FALSE under `--wasm` ⇒ stub, no lock, no diagnostics, while the probe stays green.
- **Still binding, from the rolled-off 2026-08-05 entry** (`2026-08-05-session.md`): **⛔ DO NOT room-address `newMessage` / `messageEdited`.** Signal decrypt is NOT idempotent and tabs share one session store, so a second tab decrypting the same ciphertext fails into the session-destroying policy. Both use `emitToNewestTab`; push suppression reads that SAME socket. Lifting it needs decrypt-idempotency proof AND both moved room-wide in ONE change.
- **Still binding, from the rolled-off 2026-08-15 entry** (full text in `2026-08-15-session-decryption-failed-root-cause.md`): **the owner's rule is investigate and PROVE, then ASK before writing code — diagnostics and instrumentation count as code.** `4beb1bd` (terminal `missingPreKey` class) was landed on a symptom, reverted at `409c23a`, and **must NOT be re-landed as a first move** — the mechanism was proven to never be in that code path. **⛔ 0.1.9 sealing is CLEARED — do not re-audit** (drain round-trips + fail-closed opens; the checklist is in the dated file). **Three real defects remain OPEN, found statically, none fixed:** (1) `_decryptedLedger` has no `add` path so the spent-key guard at `decrypt.dart:984` is inert within the first-decrypt session; (2) no re-entrancy guard on `retryDecryptActiveConversation`; (3) ratchet consumption and plaintext commit are not atomic (`_persistDecryptedContent`). Parked: six `EXPIRY_STAMP_MISS` on ruchens69's dump.
- **Still binding, from the rolled-off 2026-08-18 0b entry** (full text in `2026-08-18-session-phase0b-registration-lock.md` + `2026-08-18-session-0b-livefire-and-gate-review.md`): the identity lock's signature verifier is **`curve25519-js` on alpine/musl** with a REAL-Flutter-client vector pinned as a backend test — a dependency swap fails loudly; `synchronize` DROPS any index the entity does not declare (proven live); **⚠️ pre-existing flake NOT from this program:** `chat_input_bar_attachment_test.dart` "video-then-caption ordering" fails ~2 runs in 3 and reddens CI intermittently.

**Date:** 2026-08-19 — **PRIOR-ART RESEARCH DONE (`250c619`) AND BOTH PHASE-2-BLOCKING DECISIONS LOCKED (owner: "do your recommended").** ➡ `2026-08-19-session-multidevice-prior-art-and-decisions.md`; full research: `docs/plans/2026-08-19-multi-device-prior-art-research.md` (synthesis keyed to T1–T8 + four fully-cited appendices: Signal source, Matrix spec, WhatsApp/iMessage/MLS/attack papers, our spec map).
- **Decision 1 — deviceIds NEVER reused** (frozen §5.3 L269-272 stands): allocator = **`users.nextDeviceId` counter column in migration `0016`**, atomic `UPDATE … RETURNING`; NOT `MAX+1` over `devices` (row retention must never become a crypto invariant). Research grounds it: Signal reuses ids but only survives via registrationId disambiguation + per-id purge + 410-stale bounce (we have none); Matrix Synapse #17375 shows reuse reattaching stale attestations; WhatsApp/MLS are monotonic.
- **Decision 2 — password change VOIDS a 24 h post-cancel cooldown armed before it — LANDED at `94d030d`, wire-proven.** The predicate (`identity-reset.service.ts:143-155`: innerJoin `users`, `passwordChangedAt IS NULL OR cancelledAt > passwordChangedAt`) + a `warn` on the cooldown branch (it used to log NOTHING; fired 4× in the suite run). Deliberately never cancels pending ceremonies. New wire test in `registration_lock_test.dart` (runs LAST — it retires the harness account's original password): cooldown binds → REST password change → re-login → request = `pending` → cancel → fresh cooldown binds again (so the carve-out is not "disables cooldowns forever"). **Counts moved: backend 774→776/52, wire 25→26/2sk**, flutter 1375/10sk, ratchet 906, analyze clean, both verifiers OK, root `CLAUDE.md` §3 updated. Spec got two dated §12 amendments (decision record + carve-out) — doc stays frozen. Context: NO messenger has a reset cooldown at all; ours is the strict outlier and the carve-out keeps it strict.
- **5 new Stage-0 agenda items** (research doc §5): domain-separation audit of every sig/KDF context (Matrix CVE-2022-39250 class); peers must drop a revoked device's INBOUND sessions (WhatsApp shipped that bug); consider list TTL/ICDC freshness; §5.1 SAS vs ZRTP commitment shape (possible /prototype); provisioning idempotency under duplicate delivery.
- **Planning files live at `.planning/multi-device/` in the MAIN checkout** (gitignored) — upgraded from design-doc scope to program scope this session; maintain THEM, do not mint new ones at the worktree root.
- **Phase 2 Stage 0 is CLOSED — PASS-WITH-AMENDMENTS (three independent reviewers, all grounded at `94d030d`).** Normative output = spec §12 Stage-0 block, amendments (a)–(h); full finding-to-ticket map in `docs/plans/2026-08-19-phase2-stage0-decision-record.md`. Headlines: §5.1 needed an allocator-ordering + session-rebind fix (the primary had no way to learn the id it signs; N's bundle upload would have landed on device 1 and OVERWRITTEN the primary's — found independently twice); signature domain-separation prefixes pinned (`fp-enroll-v1\0`/`fp-list-v1\0`/`fp-dak-rotate-v1\0`, first byte ≠ 0x05, disjoint from the frozen §6.1 layout; NEW falsification 25); §5.5 revocation made bidirectional (receive-time origin-device check, fail closed); §6.2 reset must allocate from `nextDeviceId` (NEVER re-mint device 1) + tear down roster/enrollment with the list version continuing monotonic; 0016 pinned (no backfills, `message_envelopes.messageId` ON DELETE CASCADE is the SOLE §5.6 destruction mechanism, allocator returns PRE-increment). CONFIRMED (do not re-review): the DH-bound SAS subsumes a commitment round GIVEN `ephPubN` never transits the server; the cooldown carve-out grants an attacker nothing; every §7 delta composes with a named landed handler; §8 holds under the OTP gate ordering; all five §5.4 guards located (`messaging_provider.decrypt.dart:962/975/1290/642`, `messaging_provider.history.dart:529`). **Next: T1 (migration 0016) with its §4 decision-record riders — owner wants a fresh agent per ticket.**
- **⚠️ Environment note:** master moved to `cc8442b` (0.1.17 shipped — PR #148 reverted the #145 popover anchor); a `fireplace-emu` stack from `C:\tmp\fp-emu` squatted ports 3000/5433 mid-session and was later downed externally — check `docker ps` for squatters before assuming the `fireplace-0a` stack owns :3000.


**Date:** 2026-08-19 — **THE OTP IDENTITY GATE IS LANDED AS OPTION A (publish the identity, release the keys on its ack) — server + client, app-proven, all suites green on `feat/takeover-alarm-0a`. NOT merged, NOT deployed.** ➡ **STARTING PHASE 2 (or picking this program up cold)? READ `2026-08-19-HANDOFF-phase2-start-here.md` FIRST** — it carries the owner rules, the full state, every trap, the two open decisions and the Stage 0 → T1..T8 plan; it SUPERSEDES `2026-08-18-HANDOFF-phase2-start-here.md` (which keeps only its §1 preconditions). Build detail: `2026-08-19-session-otp-identity-gate.md`. `wip/otp-identity-gate` (`8d61bde`) is **superseded** — server half only.
- **Server:** `uploadOneTimePreKeys` refuses a tag that is not the account's published identity (`error {message:'identity_locked'}`, logged `warn` — the lock working, not a fault). Carve-outs: nothing published yet (fresh registration) and an unspent **COMPLETED** ceremony (authorized rotation in flight, read without spending); a merely **PENDING** ceremony authorizes nothing.
- **Client (this IS option A):** both upload sites stash the pre-keys and emit only `uploadKeyBundle`; `onKeyBundleUploaded` releases the stash on `success:true` and **DROPS** it on any refusal (`OTP_UPLOAD_DROPPED`). A `success:true` with `identityChanged:true` **mints a fresh pool immediately** — without that the recovered device published an identity with an EMPTY pool, because the epoch purge drops the superseded rows and the ceremony-spending re-upload carries no keys of its own. `onPreKeysLow` routes through the same helper. **Do NOT restore the back-to-back emit.**
- **Counts moved: backend 769→774/52, Flutter 1371→1375/10sk, wire 24→25/2sk**, ratchet still 906, analyze clean, both verifiers OK; root `CLAUDE.md` §3 + §7 updated. `stale_otp_epoch_test` now mirrors the safe order (`reuploadInProductionOrder`); `EventLog.takeError` consumes an EXPECTED refusal so the end-of-run "no unexpected socket errors" assertion keeps its teeth; `encryption_provider_session_rebuild_test`'s fake server had to start ACKing the bundle (it drives the real `initializeE2E`, so it caught the contract change instantly).
- **App proof:** fresh registration now logs the ORDER (`Key bundle upserted` → `Uploaded 20 one-time pre-keys`, user 204); the clobber attempt produces `REFUSED … replacement` and **no OTP traffic at all** — the client dropped them, pool intact at 20 rows (users 193, 205); messaging unaffected. **And the reset CEREMONY was completed end to end for the first time against Phase 1's per-device tables** (backdated deadline + the real per-minute sweep): grant consumed, `identity-churn` old→new, **100 OTPs uploaded under the NEW identity** (user 205) — that closes yesterday's "reset completion never live-fired" gap.
- **⚠️ Three rejected rescues, do not retry them:** awaiting this socket's in-flight bundle upload after one macrotask yield (**proven insufficient** — the trailing frame is dispatched a tick later), a timed ~500 ms poll for that marker (makes a lock's verdict depend on wall-clock latency), and blacklisting identities the lock refused (**fails open** — upload the keys before ever attempting a bundle). Ordering had to be fixed at the source, which is why A is a client change.
- **Trap:** clearing a Flutter text field by zeroing the DOM `input.value` does NOT clear the framework's controller — the next typed username APPENDED to the old one and created the account `pr8963550rc489731`. Focus the field and drive Ctrl+A/Backspace through the framework instead.

**Date:** 2026-08-18 — **FULL LOCAL VERIFICATION OF THE WHOLE PROGRAM AT `49bd92c`: every suite re-run locally on the tree that is actually on the branch, plus a live-fire of the real app. ONE NEW FINDING, UNFIXED, AWAITING OWNER.** ➡ `2026-08-18-session-phase1-local-verification.md`.
- **Numbers, all local at `49bd92c` (not cited from earlier runs):** backend **769/52**, ratchet **PASS at 906**, `flutter analyze` clean, `flutter test` **1371/10sk**, wire harness **24/2sk**, both count verifiers OK. **The gap this closed: the wire harness had last run locally at `8a86169`** — on the two merge commits it was CI-only. Live schema check: all eight per-device columns and every `0015` index present; the local DB is **`chatdb`**, `psql -d fireplace` fails.
- **🟠 NEW FINDING — `uploadOneTimePreKeys` was not gated on the identity being PUBLISHED. FIX BUILT, APP-PROVEN, PARKED on `wip/otp-identity-gate` (`8d61bde`, pushed) — NOT on this branch, and one owner decision away from landing. READ `BRANCH-NOTE-otp-identity-gate.md` on that branch first.** The session whose bundle the lock had just refused still wrote 20 OTPs into `(user, device 1)`, overwriting the legitimate device's slots 0–19 and stamping them with the REFUSED identity (three consecutive log lines at 18:30:00). Bounded by two defences, both observed: `fetchPreKeyBundle` claims only rows tagged with the published identity, so foreign rows are **never served**, and the legitimate device's next connect-time re-upsert purged all 20 — leaving the pool at ZERO until a peer fetch triggers `preKeysLow` (that session gets no one-time pre-key: safe signed-pre-key X3DH, degraded, self-healing). Availability, not confidentiality; the actor already had the password.
- **The parked gate, proven in the REAL APP (2026-08-19, gate verified running inside the container):** the same UI actions that clobbered user 168's pool now leave user 193's intact — `REFUSED unauthorized identity replacement` **and** `REFUSED one-time pre-keys under an unpublished identity`, pool still 20 rows tagged `BTKvz9Dx56yY` (before: flipped to `BdfLnk…`, then purged to 0). Fresh registration still provisions 20 OTPs (the registration-race carve-out), and a live message still decrypted peer-side under the gate. Backend **774/52**, ratchet 906, analyze clean. **Why parked:** `stale_otp_epoch_test.dart:72,76` pins the real client emitting OTPs BEFORE the bundle, so a signature-authorized rotation is refused too and starts with an empty pool. **Three rescues tried and rejected:** awaiting the socket's in-flight bundle upload after one macrotask (proven insufficient — the trailing frame lands a tick later), a timed ~500 ms poll (makes a lock's verdict depend on wall-clock latency), and blacklisting identities the lock refused (**fails open** — upload OTPs before ever attempting a bundle). Real fix = client-side ordering (publish the identity, THEN the keys), best done inside Phase 2 where that emit path is rewritten anyway.
- **✅ The cooldown refusal IS spoken — an earlier "silent refusal" report was an OBSERVATION ARTIFACT.** Refusals are a transient top snackbar (`IdentityResetPendingBanner._reportAnswer` → `showTopSnackBar`, `colorScheme.error`), visible ~0.6–2.5 s and gone by 3 s; sample the DOM every ~600 ms before ever calling a refusal silent. Server matched (`CANCEL_COOLDOWN_MS`, no second row, no second `ceremony started`). Its copy already prescribes *change your password to log them out* — the same lever as the open §6.2 cooldown question.
- **Live-fire (release build, FOUR storages = two origins × two ports; the debug server still serves exactly one client):** B→A and A→B both decrypted live, cold reload across two backend restarts still decrypts, `messages.originDeviceId=1` with `sendToken` **NULL** (the "no production client emits it" caveat, observed not asserted); fresh account → `devices` row + bundle + 20 OTPs at `deviceId=1` + `refresh_tokens.device_id=1`; second storage of the same account → keyless banner, **no silent overwrite**, lock refusal surfaced, 72 h ceremony started, **the other session alarmed**, cancel from the legitimate session cleared it with the identity untouched and **no false takeover alarm** (the `2bf60ea` fix, live).
- **⚠️ NOT exercised live, inherited by Phase 2:** the reset **COMPLETION** path against Phase 1's per-device tables (every ceremony here ended in a cancel), so the top precondition — **`deviceId` 1 is reused across a reset** vs §5.3's never-reuse — is still only reasoned about. Also still true: every session gets `deviceId=1`; user 168 ended with **two `refresh_tokens` rows, both `device_id=1`**.
- **⚠️ Trap that will bite every browser-driven test: Flutter web release + CanvasKit drops every keystroke after the first.** `page.keyboard.type` left `A` in the field, and setting `input.value` + dispatching `input` never reached the framework — only CDP `Input.insertText` (`page.keyboard.sendCharacter` per char) works.

**Date:** 2026-08-18 — **PHASE 1 BUILT AND GATE-REVIEWED on the same branch: per-device key material, sessions and messages (spec §4/§5.1/§8). Invisible for a single-device account; unblocks everything after it. NOT merged, NOT deployed.** ➡ **STARTING PHASE 2? READ `2026-08-18-HANDOFF-phase2-start-here.md` FIRST** — it consolidates the whole program's state, the five preconditions Phase 2 builds on, and every trap paid so far. Build detail: `2026-08-18-session-phase1-per-device-schema.md`.
- **Red first, then green.** Falsification 1 was written and run BEFORE the schema change and FAILED three ways against the single-device server: device 2's bundle overwrote device 1's (`4274` where `4273` was stored), an unknown device was served another device's bundle, and a default-device fetch returned `dev2-otp-2`. After migration `0015` all three pass. The tests live in `full_stack_e2e_test.dart` (not a new file) because `/auth/register` is 10/hr per IP and the suite already spends nine.
- **Migration `0015`, ONE transaction, PLAIN indexes:** new `devices` table `(userId, deviceId)` backfilled so every existing account is its own primary device 1; `key_bundles` unique becomes `(userId, deviceId)` (the old account-wide UNIQUE is what made a second device overwrite the first — dropped by lookup, its name was Postgres-generated); OTPs unique `(userId, deviceId, keyId)`; `refresh_tokens` `+device_id/+device_name` (snake_case, that table's own convention); push tables `+deviceId`; `messages` `+originDeviceId/+sendToken` with a PARTIAL unique index on `(sender_id, sendToken)`. **Every index is mirrored on its entity** — the 0b lesson about `synchronize` dropping what the entity does not declare.
- **The identity-epoch invariant is re-keyed to `(identityPublicKey, deviceId)` at all three coupled sites** (purge / claim / count). `fetchPreKeyBundle` does NOT fall back to another device: absent means absent, because a sibling's bundle builds a session the target cannot decrypt. An authorized identity change now drops every OTHER device's key material — those rows were minted under an identity the account no longer has.
- **JWTs carry `deviceId`** (absent claim = device 1, §8), the socket puts it on `client.data.user`, `consumeAndSlide` returns the session's device so a refresh keeps naming it, and `DevicesService.touch` keeps the row alive on every connect (fire-and-forget: a failure costs a `lastSeenAt`, never the session). `keyBundleUploaded` gained `identityChanged` — see the 0b entry for why. **`sendToken` is SERVER-SIDE ONLY so far — no production client emits it** (grep `frontend/lib`: zero hits; the only emitter is the wire harness). The server accepts it, stores it, and makes a same-conversation retry re-ack the committed row while fanning out NOTHING (Signal decryption is not idempotent, so a second delivery would fail into the session-destroying policy); wiring the real send path to mint tokens is Phase 2 work (§5.4), and until then a lost ack still behaves exactly as it does today.
- **Evidence:** backend 763→**768/52**, ratchet held at **906**, frontend **1370/10sk**, analyze clean, wire harness 19/2sk→**24/2sk** against real backend + Postgres, `0015` applied in the REAL boot path with every index surviving a full boot. **Live-fire in the app, twice:** fresh account → `devices` row (`isPrimary`, `lastSeenAt`) + bundle and 20 OTPs at `deviceId=1`; two accounts invited, accepted, **B→A and A→B messages decrypted on both sides**; cold reload → history still decrypts, no banners; `messages.originDeviceId=1`.
- **Gate review: three independent reviewers, all GATE PASS**, four findings fixed. (1) **Uploads trusted a client-supplied `deviceId`** — an authenticated client could scatter its OWN account's key material across 100 namespaces (no cross-account reach, no lock bypass, but §5.1 says a bundle belongs to the session that uploaded it); `uploadKeyBundle`/`uploadOneTimePreKeys` now take the device from the JWT and only `fetchPreKeyBundle` names one. (2) **A `sendToken` reused against a DIFFERENT conversation** would have re-acked the older row and silently dropped the new message — now refused as `error { message: 'duplicate_send_token' }`. (3) The read-then-create idempotency check could race two retries into an unhandled unique violation — the insert is wrapped and the loser re-acks the winner. (4) `DevicesService.touch` rewrote `isPrimary`/`platform` on every connect, which would have undone a Phase-2 primary handover — it now refreshes `lastSeenAt` only, setting the rest on insert. Also `@IsInt` instead of `@IsNumber` on the device DTOs.
- **The collision proof moved to live Postgres** once uploads became session-bound (the wire suite can no longer fake a second device): inserting a second `(user, device 2)` bundle succeeds, a duplicate is refused by `UQ_key_bundles_user_device`, keyId 0 coexists for devices 1 and 2, and a duplicate `(user, device, keyId)` is refused by `UQ_one_time_pre_keys_user_device_key`. Post-fix: backend **769/52**, ratchet **906**, wire harness **24/2sk**.
- **Final whole-program review before handoff (three more reviewers, all GATE PASS).** One fix: `ownKeyBundleStatus` now carries `shortened`, so a session reconnecting INTO a 1 h recovery-key ceremony stops describing it as the 72 h one. The rest are Phase-2 preconditions, written up in the handoff: **deviceId 1 is reused across a reset** and §5.3 says ids are never reused (wire the device-gated legacy fallback on top of that and a post-reset device gets served the pre-reset history); the identity lock's "lowest deviceId == the account identity" shortcut only holds while all devices share one IK; `purgeSupersededDevices` drops key material but not sessions, push rows or device rows; **the registration-lock SIGNATURE path is harness-only too** (a device that lost its keys cannot sign with them — the live app's only route through the lock is spending a completed ceremony); and `devices`/`refresh_tokens.device_id`/push `deviceId` are written but read by nothing yet.
- **⛔ Migration `0015` is NOT code-reversible** (root `CLAUDE.md` §6): it drops the account-wide uniques the pre-Phase-1 backend upserts against, so rolling the image back without recreating them makes every key-bundle and OTP upload fail with `42P10`. Expect the `0015` boot to pause while the partial index on `messages` builds under a write-blocking lock.
- **⚠️ Two traps that cost hours:** `nest --watch` recompiles WITHOUT relaunching (container says "Found 0 errors" while nothing listens on :3000 — the wire suite then hangs; `docker compose restart backend` and wait 3–5 min for `/health`), and **`flutter run -d web-server` serves exactly ONE debug client** — a second tab gets a blank scaffold and `Failed to create WebSocket debug connection`, so multi-client UI work needs `flutter build web --release` + a static server on two origins. Also: `npx prettier --write src/auth/*.ts` reformatted three untouched files — format the exact files you edited.

**Date:** 2026-08-18 — **ACTIONS BILLING WALL DIAGNOSED → REPO MADE PUBLIC → 0.1.16 SHIPPED (iOS popover anchored to the paperclip, PR #145 `6a35042`; CI PR #147 `4b7c807`).** ➡ **`2026-08-18-session-actions-billing-and-0.1.16.md`**.
- **The repo-wide instant-fail was Actions billing, proven not guessed:** check-run annotation says GitHub would not start the job, and the run history measures **2084 billable min since Aug 1 vs the 2000-min free private-repo allowance**. **`/actions/runs/<id>/timing` lies (`total_ms: 0`) — compute from each job's `started_at`→`completed_at` rounded up per minute.** Flipping the repo PUBLIC restored CI instantly (both open PRs 4/4 with zero code change).
- **Pre-flip full-history secret audit: CLEAN enough to publish.** No private keys, no `AKIA`/`ghp_`/`xox`/`sk-`, no JWT/VAPID/Postgres secrets; `.env`/`*.pem`/`*.jks`/`deploy-web.config.ps1` were NEVER tracked. Two real leaked credentials existed in history (Context7 key, contact-inbox bearer) — **both probed live and both dead/rotated**; the 07-27 note claiming the Context7 key was still live is STALE. Now permanently public and accepted: the Firebase Android API key (public-by-design), `51.68.138.13`/`fireplace.ignorelist.com` topology, owner's email on 865 commits.
- **⚠️ A 404 is NOT proof of revocation** — every `/contact` path 404s publicly, so the inbox-key probe was initially misread as "dead". Settled on the VM: current key → 200, leaked key → 404 on the SAME URL shape, service healthy. Distinguish rejection from absent-route with a known-good credential before ever calling a secret retired.
- **PR #147 CI economics:** 609 of the 2084 min were prose-only commits, 583 of them touching no CLAUDE.md. `paths-ignore` (`.cursor/**`, `docs/**`, `README.md`, `AGENTS.md`, `CONTEXT-MAP.md`) + `concurrency: cancel-in-progress`. **CLAUDE.md stays gated on purpose** so `verify-claude-*-test-counts.mjs` keeps running. Replayed against the real 188-run history: 48 runs skipped, 583 min saved, **0** code commits wrongly skipped. **⚠️ If you ever mark these jobs as required status checks (possible now that protection is unlocked), switch to the dummy-job pattern — a skipped workflow never reports and docs-only PRs would hang on "Expected — waiting for status".**
- **0.1.16:** web-only positioned `input[type=file]` at the paperclip's measured rect (`attachment_picker_web.dart` + a `Builder` in `chat_action_tiles.dart`); `file_picker`'s input has no layout rect, which is why Safari centred the popover. 1315/10sk, CI 4/4, in-app rect proof `left:216 top:788 40×40`, smoke 5/5. **Owner iPhone confirmation still owed — the only true verification.**

