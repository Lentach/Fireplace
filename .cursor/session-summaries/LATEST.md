# Latest session summary

Newest first, **max 5 entries, each ≤ 900 chars** (`.githooks/pre-commit` enforces 5 and a hard 1000). Each entry links its dated file, which holds the full account. **Standing traps live in `docs/agents/traps.md`** — one line each, grouped by area; grep it for the area you are about to touch. Rotating an entry out of this file: move its `## Traps` lines to `traps.md` if not already there, then delete the entry. No banner, no history paragraphs here.

**DEPLOY STATE (2026-09-12 early):** frontend `0.2.40 / d9a7c21` (web-only, CI 34699101487 green 5/5, smoke PASSED), backend `0.2.35 / e6ccae0a` (CI 34377956943 green 5/5, deployed FIRST). Rollback hazards + tags: `docs/agents/traps.md` § Deploy. Re-verify with `/version.json` + `/version` before trusting this line.

**Date:** 2026-09-12 — **The muted "nowe urządzenie — klucze zaktualizowane" line now lives until the next message** ((lxxxiv), 0.2.37→0.2.40, web-only; owner asked how long it stays — source said forever — and picked "a"). `ChatDetailScreen` renders it only while nothing newer exists (timeline `messages.last` AND the list's `lastMessages[convId]`, which covers the pre-history window), and the first superseded build forgets it durably (`dismissPeerKeyChangeNote`). Two defects fixed on the way: (1) `EncryptionService` singleton leaked A's notes/pills/rollback PINS into B's session after logout→login — `initialize` clears five per-account sets when the user id changes; (2) the line fired ONCE PER PEER EVER (`recordPeerIdentityChangedFromServer` early return + muted ack never advancing). Mutants F46–F48 killed; live-driven with two remints. ➡ `2026-09-12-session-c10-key-change-line-lifetime.md`

**Date:** 2026-09-10 — **Workflow 2.0: the wall of text is gone.** Root `CLAUDE.md` 13k → 5.2k tokens (§7 → `docs/contracts/wire.md`), `frontend/CLAUDE.md` 21k → 5.9k (§5/§7/§10 → `frontend/docs/*.md`), moves md5-verified; four `.cursor/rules/*.mdc` route to them; banner → `docs/agents/traps.md`. Accuracy audit: ~574 claims / 42 fixed. Owner: "yes on everything" → cull, model roles, Dart MCP routing (+30-session probation), `umbra-session-end` skill all applied. **⚠ A CONCURRENT SESSION IS LIVE IN THIS WORKTREE (six frontend/spec files incl. `pubspec.yaml` dirty and growing, not mine) — stage by explicit path only.** **Next session first:** `read rule://wire-contracts`; then **4 NEW Dependabot alerts on master (3 high, 1 low — push output 2026-09-10 night; 09-08 sweep had 0)** — triage with `gh api repos/Lentach/Fireplace/dependabot/alerts?state=open`. ➡ `2026-09-10-session-workflow-2.0.md`, `docs/agents/workflow-2.0.md`.

**Date:** 2026-09-09 — **Multi-device's last open ask shipped: the 12 words are offered right after registration (with "Później") and every account without a phrase gets one muted line atop Czaty** ((lxxxiii), 0.2.34→0.2.36). Owner's framing in the spec: PWA users never need to link; the phrase does NOT lock the account; for a browser user it means only forgot-password → still get in. 0.2.35 (both tiers, backend FIRST): the line keyed off `hasIdentityBackup` and nagged the two people who already had paper — now `hasRecoveryPhrase` (any `recovery_keys` row; absent = UNKNOWN), with a red "new words replace the old" warning. 0.2.36: `LinkScanScreen` is full-screen (camera fills viewport, scrim + brackets), iPhone-verified by the owner on prod. Mutants F39–F45b killed; Flutter 2098/14, backend 1119/62. **Not iOS-verified:** the nudge/offer flow. ➡ `2026-09-09-session-c9-phrase-at-the-door.md`.

**Date:** 2026-09-08 (part 5) — **The friend's "server error" was an iOS Safari socket stall** (nginx + audit logs: 4 min silence, then login×3 + register in one second). 0.2.28: `register` settles a lost answer itself — probe, retry once, only a second loss is reported; `registerOutcomeUnknown` deleted. 0.2.29: text pass, 68 strings to one PL line ≤70 chars each. 0.2.30 (lxxxi): pre-link rows hidden behind one pill; clause-5 self-alarm retracted on first hydration. 0.2.31 (lxxxii, both tiers): reset delay 72 h → **6 h**; **`POST /auth/recover` — the 12 words alone set a new password** and return tokens after waiting out the JWT second. 0.2.32/0.2.33: both timing oracles closed with dummy Argon2 verifies (~110 ms either way on prod). Mutants F25–F38 killed. **Owner owes** phone eyeballing of reset statuses, revoke notices, fingerprint sheet, phrase reveal. ➡ `2026-09-08-session-c8-register-ceremony.md`.

**Date:** 2026-09-08 (part 4) — **Final review of 0.2.25 found clause 6 over-held the gate: one mistyped phrase gated the install for the life of the process** — the 0.2.25 "kept test" had PINNED it. Fix (0.2.26, web-only): `restoreUnfinished => _restoreAdopted`, reset by `clearAll()` and the non-reconnect `onConnect`; the §6.2 rebind is a reconnect and touches neither. Two new tests RED on the old commit; F23/F24 killed; live: wrong phrase → refusal → QR link → shell, SAS both sides. 0.2.27: rename dialog holds the iOS viewport pin — **NOT iOS-verified**. Lesson: a term added to a gate predicate needs an owner that clears it; ask what the user can DO from the state a test asserts. Drive technique (two Chromes on CDP, pixel clicks) is in `traps.md`. ➡ `2026-09-08-session-c7-final-review.md`.
