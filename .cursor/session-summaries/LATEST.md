> ## ⚠️ ON MASTER THIS FILE IS TWO TICKETS BEHIND — the multi-device program is on a BRANCH
>
> If you are here to work on **multi-device / Phase 2 (T1–T8)**, stop and switch directories. That work
> lives on `feat/takeover-alarm-0a` in a **separate worktree**, and this file — `master`'s copy — knows
> nothing about T6, T7 or T7.5. Nothing below this banner mentions them, which is the point.
>
> - **Books (current):** `C:/Users/Lentach/Desktop/fireplace-0a/.cursor/session-summaries/LATEST.md`, then
>   the dated summary its top entry names, then that worktree's `CLAUDE.md` §3/§7, the frozen spec
>   `docs/design/multi-device.md` (§12 amendments run (a)–(xxxiv)) and
>   `docs/plans/2026-08-19-phase2-stage0-decision-record.md`.
> - **What is OWED:** `C:/Users/Lentach/Desktop/Fireplace/.planning/multi-device/task_plan.md` — gitignored
>   and main-checkout-only by owner instruction, so it can never be read from the worktree or pushed. T8's
>   scope is rows 14a–14g. `.planning/START-HERE.md` says the same thing at more length.
>
> Everything below this banner is the master/production line (deploys, versions, the 0.1.19 notification
> work) and remains accurate FOR THAT. Nothing is merged or deployed from the multi-device branch.

# Latest session summary

Entries are newest-first. Each one links its dated file, which holds the full account.

**🗺 START HERE IF YOU ARE LOST: `2026-08-05-HANDOFF-audit-state.md`** — plain-language map of the 2026-08-04/05 full-codebase audit: what was found, what merged, what is deliberately parked, and the three owner-only decisions. The two ordered task queues are `.planning/full-audit/REMAINING-WORK.md` (backend) and `.planning/full-audit/frontend/REMAINING-WORK.md` (frontend), both local-only/gitignored. Older long-form: `2026-08-03-HANDOFF-post-b2b-state.md` (B2b gate detail + traps; **its work queue is DONE**) and `2026-08-03-HANDOFF-signal-grade-queue.md` (dated addenda).

**✅ DEPLOY STATE — FRONTEND `0.1.19 / 6699476` LIVE 2026-08-20, BACKEND `0.1.11 / 91535317` (unchanged since 08-16 14:20Z; every release after it was frontend-only — the lag is CORRECT). Master == prod. Smoke 5/5 on both 08-20 deploys, Giphy key verified in each live bundle.** Release train: 0.1.10 (identity guard + forensics) → 0.1.11 (video batch + migration 0012) → 0.1.12 (media picker; also carries §5.4 PRs #137/#138/#139) → 0.1.13 (§5.4 post-deploy-review followups, PR #141) → 0.1.14 (attachment sheet, PR #142) → 0.1.15 (flattened picker, PR #143) → 0.1.16 (popover anchor, PR #145) → 0.1.17 (REVERT of that anchor, PR #148) → **0.1.18 (notification fixes: read-receipt gate + tap fallback + resume recovery, PR #149) → 0.1.19 (frozen pages REPLACED not repaired, PR #150 — see the 08-20 entry; owner field-confirmed "noti works like a charm now")**. **⚠️ The attachment picker is on the 0.1.15 `FilePicker` path; the iOS popover still opens mid-screen. That defect is OPEN and unsolved — see the 08-19 entry before attempting it again.** **⚠️ Post-deploy follow-up still owed from 0.1.11:** the owner overrode the device-A/B gate on the composer motion — verify on his real iOS/Android PWA and be ready to revert the two motion commits. **⚠️ `deploy-web.ps1` exit-21 silent publish halt — NOT reproduced on 0.1.18/0.1.19:** both ran the script end-to-end (exit 0, `PUBLISHED_OK`, in-script verify + smoke) with output redirected to a file (`.planning/notif-regression/deploy-011{8,9}.log`); worktrees still need the gitignored `deploy-web.config.ps1` copied in before building.
- **0.1.10's guard + forensics are live inside 0.1.11:** empty keystore → server check; exists → consented-recovery banner, no silent re-mint; UNKNOWN → defer + retry. `BOOT_MARKERS {ls,idb,cache}` + `storage.estimate()` recorded before keystore creation. **Silent identity churn is dead as a mechanism.** Users must fully close + reopen the PWA (never reinstall); Safari can take ~14 h.
- **⚠️ Three 08-14 traps.** (1) `.\deploy-web.ps1 | tail` **swallows a publish-stage failure** — build ok, nothing published; re-run with output redirected. Staged swap = nothing half-ships. (2) **The footer's VERSION half is a live `version.json` fetch and LIES about the running bundle; only the COMMIT half is compiled in** (`auth_screen.dart:205-207`) — `0.1.9 / c01317c` means old JS. (3) **NO `Cache-Control` on any entry file** (ETag only) ⇒ heuristic freshness, no revalidation; the Safari PWA took ~14 h to flip. Queued fix is `no-cache` on the static root — **Flutter output is NOT content-hashed**, so `immutable` by extension would bake a bundle in forever, helping only the NEXT deploy.
- **⚠️ THE CANARY GATE IS NOT EVIDENCE.** `flutter_secure_storage_web` 1.2.1 is `localStorage`, not IndexedDB+WebCrypto, and keeps its AES master key there raw+extractable (`flutter_secure_storage_web.dart:110-116`), so the canary compares localStorage to a localStorage shadow — `CONTENT_KEY_CANARY_LOST` is ~unreachable and `ageDays` never was clearance (one arm, 07-30).
- **Backend: DEPLOYED 2026-08-16 14:20Z at `91535317` (0.1.11, migration `0012` applied).** Carries `checkOwnKeyBundle` (read-only, caller-only, throttled, consumes no OTP) from 0.1.10.

**🔴 `[Decryption failed]` / IDENTITY LOSS — OPEN, CAUSE STILL UNIDENTIFIED. ➡ READ `2026-08-16-HANDOFF-identity-loss-consolidated.md` FIRST.** It supersedes `2026-08-15-HANDOFF-identity-loss-unproven-cause.md` (whose central hypothesis is now ANSWERED — see below) and `2026-08-14-HANDOFF-post-0.1.9-decryption-failed.md` (premise false). **PROVEN:** empty store → login → `initialize()` `absent` branch → `_generateKeys()` → `[identity-churn]` (+2 s, observed 4×) → all prior sessions dead → peers' older messages permanently `[Decryption failed]`. The logout is client-side (54's refresh row is still in the table, never revoked; **refresh tokens do NOT rotate**, `refresh-tokens.service.ts:57-77`, so the client lost or misread `flutter.refresh_token` itself). **ELIMINATED 2026-08-16:** (1) **quota — dead by measurement**, 54 has **25 messages** against a 2000-record cap and 26 sig rows, tens of KB against ~5 MiB, and a failed `setItem` throws and leaves the existing value intact; (2) **silent misread of the identity — dead**, every value is `json.encode`d so the pinned `shared_preferences_web-2.4.3`'s two silent-null vectors cannot fire, and `loadFromStorage` has no catch, so `absent` requires GENUINE emptiness; (3) **two-container theory — refuted** for the 08-14 churn, the push POST 1 s after that login UPDATED the row created `2026-05-13` (upsert unique on `endpoint`, client only calls `getSubscription()`, and on iOS only a standalone PWA can hold a subscription) so the loss happened INSIDE the long-lived Home-Screen PWA; (4) **eviction — excluded by the engines' own policy**, WebKit `performEviction` skips `isPersisted` origins and ITP exempts Home-Screen apps, and both users report `granted: true`. **THE ONE UNVERIFIED PREMISE: was the bucket actually persistent at the moment of loss?** The app records exactly that into a log that dies with the storage, and `initialize()` runs at `encryption_provider.dart:819` BEFORE the persist probe at `:849`. **Every iOS discriminator is architecturally blind** (push is out-of-bucket in `webpushd`; 167 `/web-push-sw.js` fetches in 14 days, 165 Android, **0 iPhone**; the diag log and HTTP cache prove nothing) — **so work user 90 on Android, where both signals are live.** ⛔ NEW RETRACTIONS, MINE: "the unchanged push endpoint proves the bucket survived" (false on iOS) and "quota explains the logout via a stale refresh token" (false, no rotation). **Fix designed, NOT authorised, NOT written:** tri-state guard on the `absent` branch (exists ⇒ refuse + consent via `regenerateIdentityAfterConfirmedLoss()`; none ⇒ generate; **UNKNOWN ⇒ defer**), via a NEW read-only socket event — **never reuse `fetchPreKeyBundle`, it consumes an OTP**. ⚠️ **Open design problem against that fix: `key_bundles` is unique on `userId` with no `deviceId`, so ANY legitimate second-device login hits the "bundle exists" branch** — today it silently churns, so this app is already single-device at the crypto layer; the prompt must be worded and tested for that case, and **whether some of the eight churns are second-device logins rather than wipes has never been audited.**
**⤷ 08-16 UPDATE: handoff §10.1 EXECUTED — storage loss is confirmed for USER 54 ONLY.** Verdict table in `2026-08-16-session-churn-audit.md`; handoff §4.2 + §10.1 amended in place. Of nine dated churn events: **4 proven non-wipe** (43 by Chrome/150 vs his usual /151; 76/92 by an explicit logout + account switch; 58 owner incognito), **3 wipes — all user 54** (08-03 + 08-14 proven same-container, 08-15 probable and deeper — his push subscription died with it), **2 UNKNOWN — both user 90** (cold cache fits fresh context AND dormant-profile eviction). The "two engines both losing storage" inference is UNPROVEN, not forced; the evidence-backed question is iOS-only.
**⤷ 08-16 UPDATE 2: THE §5 FIX IS AUTHORIZED, BUILT, REVIEWED AND DEPLOYED (0.1.10 → live).** The root-cause question (what empties user 54's container) stays open but is now INSTRUMENTED: his next wipe produces a banner instead of a churn, plus a `BOOT_MARKERS` triple answering §10.3 (whole bucket vs localStorage-only). **§5.4 is closed AND LIVE** (PRs #137/#138 shipped inside 0.1.12; post-deploy-review followups PR #141 shipped as 0.1.13): storage read errors are no longer logouts, locally-derived session ends can no longer delete stored tokens, and both repeat-prone durable records (`AUTH_SESSION_END` locals, `AUTH_TOKENS_UNREADABLE`) are deduped so they cannot evict the wipe forensics. Live-backend fault-injection proof is permanent CI (`test_e2e/auth_token_fault_injection_test.dart`). The 54 password reset is MOOT — he logged in with his old password.

**🔴 FCM IS DISABLED IN PRODUCTION.** `FIREBASE_SERVICE_ACCOUNT` is absent from `~/fireplace/.env`, so every backend boot logs "FCM disabled". `google-services.json` IS tracked, so an APK would register tokens the server can never push to — **Android notifications are dead until that var is set.** Web Push VAPID is present, so the PWA is unaffected, which is why this went unnoticed. Fix it before any APK work.

- **Still binding (applies to every entry below):** **🌍 THE REPO IS PUBLIC (since 08-18) — committed docs are world-readable. NEVER write user PII into anything tracked: no IP addresses, no username↔behavior forensics, no credentials/candidate passwords. Refer to users by numeric id; PII-heavy investigation material goes into the gitignored `.planning/` and gets referenced by path. Existing PII in pre-08-18 history is exposed and stays exposed unless the owner orders a filter-repo scrub (destructive: rewrites SHAs, breaks in-flight branches — coordinate first).** Never run `dart format lib/` (format only what you edited); **ask before opening the browser tool**; `node scripts/lint-ratchet.mjs` runs ONLY in CI — run it before pushing backend changes; `cmd | tail` hides exit codes; never re-derive test counts by arithmetic across a merge; **never give `flutter test` a file list** (per-argument compile cost: 45 files timed out past 11 min, the whole suite runs in 170–310 s); `flutter run -d web-server` serves a blank scaffold once its hot-restart client is lost — restart the process, do not reuse the tab; the E2E register throttle is 10/hr/IP in memory, so `docker compose restart backend` between full `test_e2e` runs and allow 2–5 min to boot.
- **Still binding, from the rolled-off 2026-08-04 entry** (full text in `2026-08-04-session-frontend-audit-fixes.md`): **`refreshSession` timeout maps to `SessionRefreshTransientException`, NEVER `Invalid`** — transient holds the session, invalid clears local auth, so the wrong mapping manufactures the very logout this repo keeps chasing (see the 08-14 entry). **Message send has no ack timer and must not get one** (only invitations do, `_kInvitationAckTimeout`): failing a send the server received releases the exactly-once latch and duplicates it — why FE-024 was skipped deliberately. **wasm nearly deleted the multi-tab Signal lock** — `session_cross_context_lock.dart` + `storage_persist.dart` key on `dart.library.html`, FALSE under `--wasm` ⇒ stub, no lock, no diagnostics, while the probe stays green.
- **Still binding, from the rolled-off 2026-08-05 entry** (`2026-08-05-session.md`): **⛔ DO NOT room-address `newMessage` / `messageEdited`.** Signal decrypt is NOT idempotent and tabs share one session store, so a second tab decrypting the same ciphertext fails into the session-destroying policy. Both use `emitToNewestTab`; push suppression reads that SAME socket. Lifting it needs decrypt-idempotency proof AND both moved room-wide in ONE change.
- **Still binding, from the rolled-off 2026-08-15 entry** (full text in `2026-08-15-session-decryption-failed-root-cause.md`): **the owner's rule is investigate and PROVE, then ASK before writing code — diagnostics and instrumentation count as code.** `4beb1bd` (terminal `missingPreKey` class) was landed on a symptom, reverted at `409c23a`, and **must NOT be re-landed as a first move** — the mechanism was proven to never be in that code path. **⛔ 0.1.9 sealing is CLEARED — do not re-audit** (drain round-trips + fail-closed opens; the checklist is in the dated file). **Three real defects remain OPEN, found statically, none fixed:** (1) `_decryptedLedger` has no `add` path so the spent-key guard at `decrypt.dart:984` is inert within the first-decrypt session; (2) no re-entrancy guard on `retryDecryptActiveConversation`; (3) ratchet consumption and plaintext commit are not atomic (`_persistDecryptedContent`). Parked: six `EXPIRY_STAMP_MISS` on ruchens69's dump.

**Date:** 2026-08-26 — **CAMERA/FILE RETURN LEGS VERIFIED GREEN (retry session); merge gate on PR #151 reduced to the iPhone test alone.** ➡ **`2026-08-26-session-composer-verify-retry.md`** — contains the 4-day-standby recovery playbook (backend needs ~5 min + `/health` polling; NEVER kill a silent `dist/main` — docker logs lag and nest-watch does not respawn a killed child; refresh tokens survive the gap) and the host-lag root cause (a runaway thumbnail-cache dllhost at 47% kernel time, NOT qemu — WHPX idles at 6%; kill dllhost before restarting anything).

**Date:** 2026-08-21 — **COMPOSER/ATTACHMENT REGRESSION: all four symptoms root-caused with proof, fix built + emulator-verified on branch `fix/composer-regression` (PR #151, CI 6/6, pushed, NOT merged, no bump). ⛔ MERGE BLOCKED on the owner's iPhone device-test (orb + placement, keyboard down).** ➡ **`2026-08-21-session-composer-fix.md`**; investigation dossier in worktree `fireplace-composer/.planning/composer-regression/findings.md`.
- **Causes, each proven:** (1) iOS orb/black-flash = file_picker's `<input type=file>` is `display:none` AND detached in the same call stack as `.click()` (source-verified 11.0.2 ≡ 8.1.6) — Safari's popover morphs from a degenerate anchor; (2) iOS mid-screen menu + keyboard dismissal with keyboard up stays SAFARI-owned (08-19 bare-page proof; not claimed by the fix); (3) Android camera-twice = mixed image+video dot-extension accept (emulator s27: Camera/Camcorder/Media, no gallery); (4) "input drops a little" = the 0cbf17b dismiss slide firing on the native surface's keyboard drop (`[kbprobe] SLIDE-START` exactly at the paperclip tap, guard=false at +1.6 s — a timed guard cannot cover it). **Symptom K (dead keyboard after picker) does NOT reproduce at HEAD on Android** — two clean IME-oracle cycles; treat the 08-19 K-repro as a probable mis-tap false positive.
- **Fix shape:** new `pickFileViaAnchoredInput` (RENDERED input at the paperclip rect, in-gesture click, ATTACHED until change/cancel; `cancel` event is the only cancel signal — a window-focus timer provably races a thawed `change`); Android paperclip → glass sheet Galeria/Aparat/Plik with door-specific accepts; iOS keeps the bare Safari menu; desktop/native keep file_picker. `composerNativePickerActive` span (3-min cap) suppresses BOTH the dismiss slide and the 0.1.19 freeze-reload (which was destroying the pending input on camera/file trips — suppression is unit-tested).
- **Verified on emulator:** gallery door END-TO-END (photo picker → staged chip → encrypted send delivered), camera/file doors open the right OS surfaces, no SLIDE-START in the whole flow, keyboard returns. **2026-08-26 retry session: camera AND file RETURN legs now GREEN end-to-end** on a fresh default-GPU boot with an idle host — camera shot → staged chip → IMAGE delivered (DB row 2), Files → `fp-probe.txt` → FILE delivered (DB row 3); the 08-21 failures were environmental (starved emulator; bare `<input capture>` discriminator failed identically). **Still needs an iPhone pass for Safari's rendering with the new anchor; a real-Android pass is a bonus, not a gate.**
- Suite **1330/10sk** (+2, root §3 bumped, verifier green), analyze clean, **CI 6/6 green on PR #151**.

**Date:** 2026-08-20 — **PWA NOTIFICATION REGRESSION (users 48/90) ROOT-CAUSED, PROVEN, SHIPPED TWICE THE SAME DAY, AND FIELD-CONFIRMED FIXED (owner, post-0.1.19: "noti works like a charm now"): 0.1.18/`36a2899` (read-receipt gate + tap fallback + resume recovery) then 0.1.19/`6699476` (frozen pages are REPLACED, not repaired). Both deployed, smoke PASSED; backend unchanged 0.1.11 — correct.** ➡ **`2026-08-20-session-notif-regression.md`**; evidence + questionnaire in gitignored `.planning/notif-regression/`.
- **Two defects, both OLD, neither introduced by 0.1.14–0.1.17:** (1) `markConversationRead` emitted ungated from hidden clients (`messaging_provider.history.dart:625-632` + `:461` — any resync of a mounted chat marks everything READ while the user is away; since ≥06-10); (2) `web-push-sw.js notificationclick` swallowed a rejected `focus()` on a frozen/discarded WebAPK client with NO `openWindow` fallback — tap does visibly nothing; swipe-close+icon-relaunch "fixes" it because a cold boot builds a fresh client+socket. Eliminated with evidence: takeover-alarm on master, backend read-stamping, stale `pushClientState` (suppression-only), SW-update starvation.
- **Fix (frontend-only, wire contracts untouched):** visibility gate in `MessagingActions.markConversationRead` (consults new `ConversationsProvider.isClientVisible`, fail-open), SW `focus()`→`openWindow('/?notify_conv=<id>')` fallback, new `page_lifecycle_web.dart` (`pageshow(persisted)`/`resume` → MainShell `_recoverForeground(markVisible:false)`; `resume` fires BEFORE `visibilitychange`, so recovery must not gate on visibility). Suite **1318/10sk** (count bumped in root §3), SW handler proven in a node VM harness (focus-reject → fallback URL).
- **Expected behavior change: a hidden client now accumulates unread honestly** — "badge stays until I actually open the chat" is the fix working, not a bug.
- **Proof before merge (owner demanded, delivered):** the new regression test FAILS 2/3 against master's providers and passes on the fix; the prod SW in a node VM harness opens NOTHING on a focus-rejected tap vs `/?notify_conv=42` on the fix; backend spec 36/36 re-pins `newMessage` → single newest socket. **S5 answered by owner:** he backgrounds briefly from the chat LIST → never forms the zombie; usage-pattern immunity.
- **Safe user guidance stays Rule-Zero:** after a deploy users must fully close + reopen the PWA once (footer must show the CURRENT release — now `0.1.19 · 6699476`); close stray same-origin browser tabs (`emitToNewestTab` delivers live messages to the newest socket only — deliberate, NOT changed; persistent "no live messages" with a second tab open is THAT, cure = close the tab, real fix = multi-device epic). **NEVER uninstall / clear site data.** Reporter questionnaire still worth sending (Q4 = second-tab discriminator).
- **0.1.19 (PR #150) — the frozen-window regression both reporters confirmed on 0.1.18:** notification-tap revives a Chrome-FROZEN window and the thawed engine is untrustworthy (composer mid-screen at a stale inset — `chat_composer_viewport.dart` shrinks `_keyboardInset` only inside build(); lag; no live messages), Android Chrome PWA only, regressed in the 08-16 mega-deploy (video batch `0cbf17b`). Fix: `freeze` flags the page, `resume` → `location.reload()` once visible (hidden unfreeze ARMS instead — resume precedes visibilitychange), 30 s sessionStorage loop guard → soft recovery, `BOOT_AFTER_FROZEN` boot diag, and the live click handler consults `frozenPageReloadImminent()` before deleting the IndexedDB deep-link (queued click flushes on the same thaw). **Proven live via CDP on the real app:** freeze/thaw ⇒ page replaced itself + healthy MainShell; immediate second freeze/thaw ⇒ NO reload. Suite **1328/10sk**. Field loop closes when reporters retest on footer `0.1.19 · 6699476`.
- **Kaspersky deleted `deploy-web.ps1` from the working tree** (owner confirmed + restored). If a deploy script vanishes again, check the AV quarantine before suspecting git; consider excluding the repo.

**Date:** 2026-08-19 — **0.1.17 reverted the popover anchor (PR #148, `356f3fa`) — AND THE COMPOSER BUG SURVIVED IT. Second half of the day produced NO shipped code: everything built was deleted at the owner's instruction.** ➡ **READ `2026-08-19-session-composer-regression.md` FIRST** (proven facts, the phone-free repro, suspects, owner state); `2026-08-19-session-anchor-revert.md` covers the earlier revert.
- **⛔ OWNER STATE — READ BEFORE TOUCHING THE COMPOSER OR PICKER.** Three releases in a row touched this area and the bug is still there. Owner: *"we made huge regress on composer and now all old bugs are back"*. **Ship NOTHING here without a green repro run AND his explicit OK, and do NOT use him as the test loop** — the repro below needs no phone. He ordered this session's work deleted; do not resurrect it from history.
- **PROVEN, do not re-derive:** (1) the keyboard bug **reproduces on clean 0.1.17** (`fresh: 148ms:down→750ms:UP` vs `after paperclip: 98ms:down`, never rises) so it **predates 08-18/08-19**; (2) it is **NOT the accept list** — the owner's iPhone misplaced the menu identically with the *pre-video* list, so the "video extensions did it" theory in the sibling summary is **DEAD**; (3) it is **NOT `file_picker`** — a one-`TextField` throwaway app does not reproduce it ⇒ the fault is **our composer**; (4) menu PLACEMENT is **Safari's**, proven on a bare static page: keyboard **up ⇒ menu mid-screen**, keyboard **down ⇒ menu at the control**, and the collapse-then-return on cancel is the "flash" half.
- **A `blur()` → `setTimeout` → `.click()` fix CANNOT work on iOS** — Safari silently blocks the deferred open (11 taps, zero menus, zero events; it also wedged the owner's keyboard on a static page). Only same-gesture blur+open remains untested.
- **⚠️ NEVER `git revert 0cbf17b`.** That "video batch" is 60 files and also carries the **0.1.10 identity guard + boot markers** and the **`PeerIdentityChangedBanner` removal**; reverting it resurrects the banner and undoes the churn fix. Any rollback there must be surgical.
- **Top suspect (untested):** the keyboard-dismiss `Transform.translate` in `chat_composer_viewport.dart` from `0cbf17b` — paint-only motion in the documented **banned zone**, whose device A/B has been owed since 0.1.11, and "composer painted where it should not be" matches the owner's top-of-screen screenshot. Second suspect: `_requestComposerFocus` guarded by `if (!_focusNode.hasFocus)` (`chat_input_bar.dart:240`) going stale after the file dialog — that single mechanism predicts BOTH surfaces, since `showSoftKeyboardIfHidden` is iOS-only (`soft_keyboard_web.dart:10`) ⇒ iOS flashes, Android gets nothing.
- **The phone-free repro (the session's most valuable artifact):** Android emulator + Chrome + `adb reverse` so `localhost` is a secure context, then poll Android's own IME oracle — `adb shell dumpsys window InputMethod | grep isVisible` (4 KB; the 580 KB `dumpsys input_method` is too slow). Open chat → tap composer (UP) → paperclip → dismiss chooser → tap composer (**bug: stays down**). Full recipe incl. account seeding SQL in the dated file.
- **Android picker ruling:** its native chooser lists the **camera twice** (`aparat/aparat/plik`) because the accept list mixes `image/*`+`video/*`. Owner: Android **need not** look like iOS, but must offer **image / camera / file**. A sheet doing that was built, emulator-verified, and deleted with everything else. Rejected shapes: `CupertinoActionSheet`, and a dark iOS-UIMenu clone.
- **Owner-observed on 0.1.16:** popover still mid-screen, keyboard still dismissed, PLUS a new regression — after tapping the paperclip, tapping the composer raised the keyboard and instantly dropped it again ("it jumps"); a normal composer tap was fine.
- **⛔ THE PROCESS LESSON, worth more than the code:** 0.1.16 shipped on evidence that was structurally incapable of catching the bug — a desktop DOM-rect assertion, for a symptom that is "where Safari draws its native popover" and "does the virtual keyboard survive". Desktop Chrome has neither. The rect proof was also **inherited from the prior session and re-quoted as freshly run**. The LAN device-probe technique that CAN answer it already existed and was not used before merging. **Platform-specific UI fixes are unverifiable on desktop: probe the real device before merge, or say plainly it is unverified and let the owner decide.**
- **Device data now exists** (5 variants, owner's iPhone, `visualViewport` height beaconed so keyboard collapse is measured not eyeballed): the shipped variant **loses focus BEFORE the tap** (`focus=body`, `composer BLUR` precedes TAP). `preventDefault` on `pointerdown` DOES hold `focus=msg` + `KEYBOARD_UP` at 400 ms. **But nothing survived ~1200 ms — once the native sheet opens, iOS collapses the keyboard in every variant.** ⚠️ **PARTLY SUPERSEDED the same day:** that focus loss explained 0.1.16's *added* breakage only — the underlying keyboard bug reproduces on clean 0.1.17 with the anchor reverted, and the menu-placement half was later proven to be Safari's own (bare static page, no app code). See the composer-regression file.
- **Probe-serving trick (Windows Firewall blocks the LAN route without elevation):** drop the page at `~/fireplace/frontend-build/probe/index.html` — inside the existing static root, **no nginx change** — and beacon to `/log?...`, which 404s but is logged verbatim; read back with `sudo grep -ho 'GET /log?[^ ]*' /var/log/nginx/access.log`. The next deploy wipes it automatically.
- **0.1.17 = `git revert 6a35042` + bump.** Picker sources **byte-identical to 0.1.15** (empty diff vs `318bdf3`). Analyze clean, **1315/10sk** (first run hit the documented media-ordering flake; whole-suite rerun green), **CI 6/6** (CodeQL now included), Giphy key verified pre-publish, `pickAttachmentFileAt` absent from the built JS, smoke 5/5. **OPEN: the iOS popover still opens mid-screen — start any retry from the focus-preserving variant, and get a screen recording first.**

**Date:** 2026-08-18 — **ACTIONS BILLING WALL DIAGNOSED → REPO MADE PUBLIC → 0.1.16 SHIPPED (iOS popover anchored to the paperclip, PR #145 `6a35042`; CI PR #147 `4b7c807`).** ➡ **`2026-08-18-session-actions-billing-and-0.1.16.md`**.
- **The repo-wide instant-fail was Actions billing, proven not guessed:** check-run annotation says GitHub would not start the job, and the run history measures **2084 billable min since Aug 1 vs the 2000-min free private-repo allowance**. **`/actions/runs/<id>/timing` lies (`total_ms: 0`) — compute from each job's `started_at`→`completed_at` rounded up per minute.** Flipping the repo PUBLIC restored CI instantly (both open PRs 4/4 with zero code change).
- **Pre-flip full-history secret audit: CLEAN enough to publish.** No private keys, no `AKIA`/`ghp_`/`xox`/`sk-`, no JWT/VAPID/Postgres secrets; `.env`/`*.pem`/`*.jks`/`deploy-web.config.ps1` were NEVER tracked. Two real leaked credentials existed in history (Context7 key, contact-inbox bearer) — **both probed live and both dead/rotated**; the 07-27 note claiming the Context7 key was still live is STALE. Now permanently public and accepted: the Firebase Android API key (public-by-design), `51.68.138.13`/`fireplace.ignorelist.com` topology, owner's email on 865 commits.
- **⚠️ A 404 is NOT proof of revocation** — every `/contact` path 404s publicly, so the inbox-key probe was initially misread as "dead". Settled on the VM: current key → 200, leaked key → 404 on the SAME URL shape, service healthy. Distinguish rejection from absent-route with a known-good credential before ever calling a secret retired.
- **PR #147 CI economics:** 609 of the 2084 min were prose-only commits, 583 of them touching no CLAUDE.md. `paths-ignore` (`.cursor/**`, `docs/**`, `README.md`, `AGENTS.md`, `CONTEXT-MAP.md`) + `concurrency: cancel-in-progress`. **CLAUDE.md stays gated on purpose** so `verify-claude-*-test-counts.mjs` keeps running. Replayed against the real 188-run history: 48 runs skipped, 583 min saved, **0** code commits wrongly skipped. **⚠️ If you ever mark these jobs as required status checks (possible now that protection is unlocked), switch to the dummy-job pattern — a skipped workflow never reports and docs-only PRs would hang on "Expected — waiting for status".**
- **0.1.16 — SUPERSEDED, REVERTED IN 0.1.17 (see the 08-19 entry above).** Its "in-app rect proof" was desktop-only and could never have caught the real bug; it also shipped a composer-focus regression.

