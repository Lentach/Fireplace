# Skills become repo material: `.claude/skills/` is the set, the home tree is gated off, and rules route to it

**Date:** 2026-09-14 · **Version:** unchanged · **Tiers deployed:** none

## What was done
- **Diagnosed why no skill ever fired.** 51 dirs across three trees, 23 reached the model: `enabledProviders` defaults to `[]` so `~/.claude/skills` (foreign USER root) contributes zero to OMP while project roots load, and `disable-model-invocation: true` on 21 of 39 `~/.agents` skills hides them from the list (`skill://` still works).
- **Made the repo own its skill set.** Vendored 22 skills into `.claude/skills/`: 6 Dart from `kevmoo/dash_skills` @ `38dce749` (4 existing re-pulled at that SHA, + `dart-modern-features`, `dart-seal-type-hierarchies`), 14 from `mattpocock/skills` (MIT), + repo-authored `umbra-session-end` and `flutter-frontend-design`. License texts committed.
- **Gated the machine-wide tree per repo:** new `.omp/config.yml` (`skills.enableAgentsUser: false`) + `!.omp/config.yml` in `.gitignore` (which ignores `.omp/*`) so the gate is tracked and every worktree inherits it. Nothing an agent needs lives in a home dir now.
- **Routing so agents actually use them:** new rule pairs `tests-and-coverage` and `frontend-dart-idioms`, each mirrored into the two surviving trees (`.omp/rules`, `.claude/rules`); `frontend-flutter-design` covers visuals. `AGENTS.md` item 5 carries the conversational trigger list.
- **MCP:** `dart` stays on (two no-op `--disable` flags dropped); added `chrome-devtools` (pinned `@1.9.0`, `--isolated`, telemetry off) and read-only `github` (`actions,pull_requests,issues`, PAT via env indirection, never committed) — both `enabled: false`, flipped with `/mcp enable <name>` so their schemas don't tax every turn.
- **Corrected two research claims:** all five triage labels DO exist and `docs/agents/{issue-tracker,triage-labels,domain}.md` are present (`setup-matt-pocock-skills` ran here ~2026-07), so `code-review`/`triage`/`to-spec`/`to-tickets` are usable. Never re-run that setup skill — it rewrites `CLAUDE.md`.
- **Cleaned the home tree of anything that could shadow the repo.** Deleted the `.Codex`-mangled `planning-with-files`, the stale `~/.claude/skills/flutter-frontend-design`, and the 8 `~/.claude/skills` junctions whose names are now vendored here (Claude Code gives personal precedence over project, so they would have shadowed the committed copies). `domain-modeling` + `git-guardrails-claude-code` junctions stay — not vendored, useful elsewhere. All 38 surviving `~/.agents` targets intact. Snapshot `~/.agents/skills-snapshot-2026-09-14.tar.gz` (38 dirs + `.skill-lock.json`, 96 665 B) was taken AFTER those deletions, so it lacks `planning-with-files`; it exists to make a future purge reversible (7 of those skills are 404 upstream).
- **Dropped Cursor (owner: "we don't use cursor anymore").** `.cursor/rules/` deleted — of the 5 files git tracked there, 4 were name-duplicates OMP already discarded (3 further `.mdc` mirrors written earlier this session were never committed), and the unique 20 KB `production-vm-deploy` was `git mv`'d to `.omp/rules/production-vm-deploy.md` with frontmatter untouched. `.cursor/session-summaries/` stays — not Cursor's, but the handoff store the gate, `umbra-session-end`, `AGENTS.md`, `CLAUDE.md` and CI reference. Mechanism: `docs/agents/skills-and-mcp.md` §1. Pointers repointed in `AGENTS.md` item 4, `CLAUDE.md` §0/§1/§4/§7/§9, `README.md:205`, `deploy.sh:14`, `traps.md`.

## Key files
- New: `.omp/config.yml`, `.claude/skills/` (22 skills + `LICENSE.dash_skills.txt`, `LICENSE.mattpocock-skills.txt`), `{.omp,.claude}/rules/{tests-and-coverage,frontend-dart-idioms,frontend-flutter-design}.md`, `.omp/rules/production-vm-deploy.md` (git-mv'd from `.cursor/rules/`), `docs/agents/skills-and-mcp.md`. Deleted: `.cursor/rules/` (5 tracked files + 3 uncommitted mirrors).
- Edited: `AGENTS.md` (item 5 + the Flutter skill in the area list), `.gitignore` (`!.omp/config.yml`), `.omp/mcp.json`, `.claude/skills/VENDORED.md`, `docs/agents/traps.md` (4 lines), `LATEST.md`.
- Read only: `omp://{skills,settings,mcp-config,cli-reference,rulebook-matching-pipeline}.md`, `~/.agents/.skill-lock.json`, `docs/agents/issue-tracker.md`.

## Verification
- **Fresh headless session** (`omp -p --no-session --tools read`) lists exactly the 19 in-repo listed skills — `obsidian-vault`, `scaffold-exercises`, `migrate-to-shoehorn`, `setup-pre-commit`, `git-guardrails-claude-code`, `planning-with-files` all gone, the 3 hidden ones correctly absent. Before: 23, incl. that noise and no `flutter-frontend-design`.
- **Dart MCP still mounts** after the flag edit: same probe answers YES for a route containing `mcp__dart_analyze_files`, and a direct stdio `initialize` against `dart_mcp_server.bat --disable pub_dev_search` returns `serverInfo.version 1.1.1`.
- **A/B'd the project config** (moved out/back, plus unrelated-key and `.omp/settings.json` variants): MCP mounts every time. The "dart MCP is gone" scare was the smol model answering about a tool named `analyze_files`.
- `git check-ignore -v .omp/config.yml` → `.gitignore:23:!.omp/config.yml` (tracked, so worktree-safe). `chrome-devtools-mcp@latest --version` → `1.9.0` (pinned to it).
- **Junctions removed with `[System.IO.Directory]::Delete(p, $false)`** (link only): `~/.agents/skills` = 38 dirs before and after, targets and repo copies intact. Snapshot listing: 38 dirs, `.skill-lock.json`, all 7 upstream-404 skills.
- **Ported rule verified fresh:** `production-vm-deploy` is the one name in `<domain-rules>`; `rule://production-vm-deploy` returns `# Production server deploy (OVH VPS)`. The file-triggered rules correctly stay out of that listing — they are TTSR/edit-time.
- `node scripts/verify-context-budget.mjs --worktree` → `context budget OK`; `AGENTS.md` 3812 B / 29 lines (caps 4000/40 — under 200 B of headroom left); `CLAUDE.md` 21 937 B (cap 24 000).
- **Reviewed before staging by two `reviewer` subagents** (docs-accuracy + wiring-risk); both returned "incorrect". 8 wiring findings + 4 false claims fixed — list and verified-true set in `docs/agents/skills-and-mcp.md` §6.
- **Rules provably FIRE:** probe writes in headless sessions logged `TTSR condition matched` → `rule marked as injected` for all three new rules, each quoting the injected heading back. Probes deleted.
- **Shipped as `36d8c860`** (55 files; gitleaks + budget gate green, and the gate DID inspect this summary, staged as `A`). Pushed to `master` + `feat/passcode-lock`; CI **6/6 success** on master. **No deploy:** zero lines under `frontend/lib` or `backend/src`, so nothing to ship; prod stays `0.2.46 / 43481643` (measured this session).
- NOT verified: no MCP server was enabled or driven (both ship `enabled: false`; no PAT exists yet); Claude Code was not launched, so its `.claude/rules` firing is unobserved.

## Notes for next session
- Owner-owed: (a) create the fine-grained read-only PAT and `/mcp enable github` if you want CI/job-log introspection; (b) decide whether to purge the remaining 38 `~/.agents/skills` dirs — the gate makes it cosmetic, and 7 of them can no longer be re-pulled upstream; (c) `AGENTS.md` has ~200 B of budget left, so the next pointer addition must displace something.
- Still unwritten, and nothing upstream provides them: a `backend/`-scoped NestJS/Prisma/Jest skill, and a low-freedom deploy skill with `disable-model-invocation: true` so no agent can decide to deploy.
- Traps (also in `docs/agents/traps.md`): the skill-set contract + the `!.omp/config.yml` gitignore trap; probe discovery with a fresh headless session and ask for the real `mcp__*` tool name; a third-party skill body can be mangled (`planning-with-files`); never wire an archived MCP server.
