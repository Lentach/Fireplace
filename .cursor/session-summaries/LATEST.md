# Latest session summary

**Date:** 2026-06-10

**Topic:** **Composer attachment staging + ordering contract (Clipboard Phase 2)** — staged-image chip above the input row (`ComposerAttachmentController` max-1-image, stage-time png/jpeg/gif/webp + 20MB validation; `ComposerAttachmentBar` thumbnail/name/size/✕; TextField never unmounts). **Ordering contract enforced:** `_encryptAndSend`/`sendImageMessage`/`AttachmentHandler.sendImage` → `Future<bool>` completing only AFTER the image's `sendMessage` emit; `ChatInputBar._sendStaged()` awaits it then emits caption; on failure caption restored to field (failed bubble owns retry); `_isSendingStagedImage` guard; trailing send shows when text OR staged. ARB `composerAttachmentRemoveTooltip` en/pl. **347 tests green** (was 335; +12), analyze clean. Commits `59ab1d5`..`345adbf`. No paste source yet — tests feed via `attachmentControllerForTest`/`sendForTest`. **Next: Phase 3 web `paste`-event stub/web pair (+ tooLarge/unsupported snackbar ARB keys), then Phase 4 Android `contentInsertionConfiguration`. Deploy pending.** Spec: [docs/superpowers/specs/2026-06-10-clipboard-copy-paste-design.md](../../docs/superpowers/specs/2026-06-10-clipboard-copy-paste-design.md) → [2026-06-10-session-composer-attachment-staging.md](./2026-06-10-session-composer-attachment-staging.md)

**Previous:** 2026-06-10 — **Context-menu Copy action shipped (Clipboard Phase 1)** — long-press → Copy on TEXT bubbles (`MessageModel.hasCopyablePlaintext` gate; hidden for media/ping/placeholders; allowed on disappearing); nullable `onCopy` through overlay/panel; height-aware layout (`kMessageActionPanelRowHeightEstimate`); ARB en/pl; 335 green. Commits `fdd3aa2`..`8bfa66a`. → [2026-06-10-session-copy-message-text.md](./2026-06-10-session-copy-message-text.md)

**Earlier:** 2026-06-10 — **Clipboard copy/paste design (research + spec)** — competitor survey + verified audit; context-menu Copy + staged composer chip; web = `paste` DOM event, Android = `contentInsertionConfiguration`, native iOS deferred. → [2026-06-10-session-clipboard-design.md](./2026-06-10-session-clipboard-design.md)

**Earlier:** 2026-06-10 — **E2E decrypt regression: Bad-MAC → session-reset cascade root-caused and fixed** — inbound decrypt-failure path deleted Signal sessions (permanent Bad-MAC loss) + sticky `_liveDecryptFailedPeers` + `_mergeHistorySnapshot` exclusion resurrected terminal rows, re-arming resets every reconnect. Fix: never delete sessions inbound (rebuild *request* only), terminal-row skip, prune, merge pre-seeds all local rows; `notifyPeerRebuild` on identityReset shrinks cache-wipe loss to ~1 msg. New privacy-safe diag events. Suite 325 green. **Next: deploy + capture E2eDiagLog from an affected device.** → [2026-06-10-session-decrypt-cascade-fix.md](./2026-06-10-session-decrypt-cascade-fix.md)
