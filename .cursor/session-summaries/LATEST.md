# Latest session summary

Entries are newest-first. Each one links its dated file, which holds the full account.

**🗺 START HERE IF YOU ARE LOST: `2026-08-05-HANDOFF-audit-state.md`** — plain-language map of the 2026-08-04/05 full-codebase audit: what was found, what merged, what is deliberately parked, and the three owner-only decisions. The two ordered task queues are `.planning/full-audit/REMAINING-WORK.md` (backend) and `.planning/full-audit/frontend/REMAINING-WORK.md` (frontend), both local-only/gitignored. Older long-form: `2026-08-03-HANDOFF-post-b2b-state.md` (B2b gate detail + traps; **its work queue is DONE**) and `2026-08-03-HANDOFF-signal-grade-queue.md` (dated addenda).

**✅ DEPLOY STATE — BOTH TIERS CURRENT ENOUGH; NO GATES LEFT.** Backend prod is 1 commit behind (`f6e4aa8`, dev-dep lockfile + a media-cleanup mtime clamp, no migration).
- **Frontend `0.1.9 / 9e27ed4` IS LIVE — 08-14 00:53Z, smoke 5/5.** Ships the server-MITM warning fix (`3d30b88`), the "Verify security keys" door, B2b `sig_*` sealing, the 19 audit fixes. Field first boot: ONE `SIG_SEAL_DRAIN_DONE {sealed: 265}`, `SIG_SEAL_OPEN {sealed: 265, legacy: 0, ms: 60}`, `WEB_SEAL_OPEN {sealed: 241, legacy: 0, unreadable: 0, lostRows: 0}`, **33/33 sessions preserved**, zero `SIG_KEY_UNAVAILABLE`/`SIG_ROWS_UNREADABLE`/`SIG_STORE_FALLBACK`/`CONTENT_KEY_LOST`. **Signal identity keys are now sealed at rest on web.**
- **⚠️ Three 08-14 traps.** (1) `.\deploy-web.ps1 | tail` **swallows a publish-stage failure** — build ok, nothing published; re-run with output redirected. Staged swap = nothing half-ships. (2) **The footer's VERSION half is a live `version.json` fetch and LIES about the running bundle; only the COMMIT half is compiled in** (`auth_screen.dart:205-207`) — `0.1.9 / c01317c` means old JS. (3) **NO `Cache-Control` on any entry file** (ETag only) ⇒ heuristic freshness, no revalidation; the Safari PWA took ~14 h to flip. Queued fix is `no-cache` on the static root — **Flutter output is NOT content-hashed**, so `immutable` by extension would bake a bundle in forever, helping only the NEXT deploy.
- **⚠️ THE CANARY GATE IS NOT EVIDENCE.** `flutter_secure_storage_web` 1.2.1 is `localStorage`, not IndexedDB+WebCrypto, and keeps its AES master key there raw+extractable (`flutter_secure_storage_web.dart:110-116`), so the canary compares localStorage to a localStorage shadow — `CONTENT_KEY_CANARY_LOST` is ~unreachable and `ageDays` never was clearance (one arm, 07-30).
- **Backend:** no gate, **DEPLOYED 2026-08-05 19:26Z at `7a845430`** — carries the unfriend history-wipe fix, so today's 0.1.8 clients are repaired. Smoke 5/5, 0 boot errors, fix confirmed in the container's own `dist/`.

**🔴 `[Decryption failed]` / IDENTITY LOSS — OPEN, CAUSE STILL UNIDENTIFIED. ➡ READ `2026-08-16-HANDOFF-identity-loss-consolidated.md` FIRST.** It supersedes `2026-08-15-HANDOFF-identity-loss-unproven-cause.md` (whose central hypothesis is now ANSWERED — see below) and `2026-08-14-HANDOFF-post-0.1.9-decryption-failed.md` (premise false). **PROVEN:** empty store → login → `initialize()` `absent` branch → `_generateKeys()` → `[identity-churn]` (+2 s, observed 4×) → all prior sessions dead → peers' older messages permanently `[Decryption failed]`. The logout is client-side (54's refresh row is still in the table, never revoked; **refresh tokens do NOT rotate**, `refresh-tokens.service.ts:57-77`, so the client lost or misread `flutter.refresh_token` itself). **ELIMINATED 2026-08-16:** (1) **quota — dead by measurement**, 54 has **25 messages** against a 2000-record cap and 26 sig rows, tens of KB against ~5 MiB, and a failed `setItem` throws and leaves the existing value intact; (2) **silent misread of the identity — dead**, every value is `json.encode`d so the pinned `shared_preferences_web-2.4.3`'s two silent-null vectors cannot fire, and `loadFromStorage` has no catch, so `absent` requires GENUINE emptiness; (3) **two-container theory — refuted** for the 08-14 churn, the push POST 1 s after that login UPDATED the row created `2026-05-13` (upsert unique on `endpoint`, client only calls `getSubscription()`, and on iOS only a standalone PWA can hold a subscription) so the loss happened INSIDE the long-lived Home-Screen PWA; (4) **eviction — excluded by the engines' own policy**, WebKit `performEviction` skips `isPersisted` origins and ITP exempts Home-Screen apps, and both users report `granted: true`. **THE ONE UNVERIFIED PREMISE: was the bucket actually persistent at the moment of loss?** The app records exactly that into a log that dies with the storage, and `initialize()` runs at `encryption_provider.dart:819` BEFORE the persist probe at `:849`. **Every iOS discriminator is architecturally blind** (push is out-of-bucket in `webpushd`; 167 `/web-push-sw.js` fetches in 14 days, 165 Android, **0 iPhone**; the diag log and HTTP cache prove nothing) — **so work user 90 on Android, where both signals are live.** ⛔ NEW RETRACTIONS, MINE: "the unchanged push endpoint proves the bucket survived" (false on iOS) and "quota explains the logout via a stale refresh token" (false, no rotation). **Fix designed, NOT authorised, NOT written:** tri-state guard on the `absent` branch (exists ⇒ refuse + consent via `regenerateIdentityAfterConfirmedLoss()`; none ⇒ generate; **UNKNOWN ⇒ defer**), via a NEW read-only socket event — **never reuse `fetchPreKeyBundle`, it consumes an OTP**. ⚠️ **Open design problem against that fix: `key_bundles` is unique on `userId` with no `deviceId`, so ANY legitimate second-device login hits the "bundle exists" branch** — today it silently churns, so this app is already single-device at the crypto layer; the prompt must be worded and tested for that case, and **whether some of the eight churns are second-device logins rather than wipes has never been audited.**
**⤷ 08-16 UPDATE: handoff §10.1 EXECUTED — storage loss is confirmed for USER 54 ONLY.** Verdict table in `2026-08-16-session-churn-audit.md`; handoff §4.2 + §10.1 amended in place. Of nine dated churn events: **4 proven non-wipe** (43 by Chrome/150 vs his usual /151; 76/92 by an explicit logout + account switch; 58 owner incognito), **3 wipes — all user 54** (08-03 + 08-14 proven same-container, 08-15 probable and deeper — his push subscription died with it), **2 UNKNOWN — both user 90** (cold cache fits fresh context AND dormant-profile eviction). The "two engines both losing storage" inference is UNPROVEN, not forced; the evidence-backed question is iOS-only.

**🔴 FCM IS DISABLED IN PRODUCTION.** `FIREBASE_SERVICE_ACCOUNT` is absent from `~/fireplace/.env`, so every backend boot logs "FCM disabled". `google-services.json` IS tracked, so an APK would register tokens the server can never push to — **Android notifications are dead until that var is set.** Web Push VAPID is present, so the PWA is unaffected, which is why this went unnoticed. Fix it before any APK work.

- **Still binding (applies to every entry below):** never run `dart format lib/` (format only what you edited); **ask before opening the browser tool**; `node scripts/lint-ratchet.mjs` runs ONLY in CI — run it before pushing backend changes; `cmd | tail` hides exit codes; never re-derive test counts by arithmetic across a merge; **never give `flutter test` a file list** (per-argument compile cost: 45 files timed out past 11 min, the whole suite runs in 170–310 s); `flutter run -d web-server` serves a blank scaffold once its hot-restart client is lost — restart the process, do not reuse the tab; the E2E register throttle is 10/hr/IP in memory, so `docker compose restart backend` between full `test_e2e` runs and allow 2–5 min to boot.
- **Still binding, from the rolled-off 2026-08-04 entry** (full text in `2026-08-04-session-frontend-audit-fixes.md`): **`refreshSession` timeout maps to `SessionRefreshTransientException`, NEVER `Invalid`** — transient holds the session, invalid clears local auth, so the wrong mapping manufactures the very logout this repo keeps chasing (see the 08-14 entry). **Message send has no ack timer and must not get one** (only invitations do, `_kInvitationAckTimeout`): failing a send the server received releases the exactly-once latch and duplicates it — why FE-024 was skipped deliberately. **wasm nearly deleted the multi-tab Signal lock** — `session_cross_context_lock.dart` + `storage_persist.dart` key on `dart.library.html`, FALSE under `--wasm` ⇒ stub, no lock, no diagnostics, while the probe stays green.
- **Still binding, from the rolled-off 2026-08-05 entry** (`2026-08-05-session.md`): **⛔ DO NOT room-address `newMessage` / `messageEdited`.** Signal decrypt is NOT idempotent and tabs share one session store, so a second tab decrypting the same ciphertext fails into the session-destroying policy. Both use `emitToNewestTab`; push suppression reads that SAME socket. Lifting it needs decrypt-idempotency proof AND both moved room-wide in ONE change.

**Date:** 2026-08-16 — **CHURN POPULATION AUDIT (handoff §10.1): storage loss confirmed for user 54 only. NO CODE, no deploy, prod reads only.** ➡ **`2026-08-16-session-churn-audit.md`** for the per-churn verdict table and evidence.
- **Method that settled it — the proof runs ONE way:** warm HTTP cache (304s) + a push POST updating a months-old endpoint row proves the SAME long-lived container, because the HTTP cache lives outside the origin bucket and a storage wipe cannot forge a 304. **A cold cache decides NOTHING by itself** — fresh context, user-cleared data, and dormant-profile eviction + independent HTTP-cache LRU all produce it — so cold-cache churns were classified by additional evidence: explicit logout + account switch (76/92), Chrome major version differing from the user's established context (43: churn boot Chrome/150, push-row context Chrome/151 — Chrome never downgrades), or left UNKNOWN (90).
- **Result: 54 is the ONLY confirmed storage-loss case** — 08-03 (`GET /` 304, `main.dart.js` 304, push POST updating the 05-13 endpoint, dated by the OTP batch-insert 15:51:01) and 08-14 (already proven) are same-container wipes; 08-15 has the same warm-cache signature **but the boot-time push POST is ABSENT** (present on every prior boot of that container) ⇒ subscription destroyed ⇒ website-data wipe / web-clip reinstall / revocation — a DEEPER event than 08-14, and a new server-side iOS discriminator two sessions thought impossible.
- **76/92 (08-11):** one Android context, explicit `POST /auth/logout` between the two logins — account switching, not loss. **90 (08-14): UNKNOWN** — fully cold + no in-bucket push subscription reads equally as fresh context or dormant-profile eviction; not decidable server-side; his 07-31 churn is beyond log retention. **58:** owner incognito (known).
- **Consequences:** handoff §4.2 (cross-engine eviction mystery) DOWNGRADED — the Blink half is unproven, nothing forces a cross-engine mechanism; the evidence-backed question is iOS-only (low-storage iPhone 18.5, Home-Screen PWA, three wipes in 12 days, owner's healthy 18.7 PWA loses nothing; iOS disk-pressure website-data purges are a documented class — Apple forum threads 48083/125041/125627). **§5.2 quantified: second-device logins are the largest PROVEN churn class (4 of 9 events proven non-wipe; 90's two unknown; 54's three are wipes), so the §5.1 guard's "bundle exists" prompt is the COMMON path** — word it as "new device/browser sign-in", wipe as the variant.
- **54's timeline tightened:** logged out again by 08-14 22:00 (two iPhone 401s), 5.4 h after the 16:38 login. Whether the 18:23 (08-15) login followed a fresh boot or an app left sitting open is UNDETERMINED — an iPhone-18_5 warm-cache page load exists at 17:07:16 from another NAT IP, attribution uncertain.
- **Still owner-blocked:** ~~commit-vs-discard~~ → owner said COMMIT (done). ~~Password reset for 54~~ → **MOOT, 08-16: he logged in with his old password; no reset needed.** **The §5.1 guard + §5.3 markers are AUTHORIZED for 0.1.10** (owner, 08-16, after confirming the guard moves no key material — it is a read-only `{exists: bool}` check against the public bundle the server already holds). Next contact questions for 54 remain in the session file (did he delete/re-add the icon or clear website data 08-14 evening → 08-15 18:23; any iOS storage-full warning).

**Date:** 2026-08-15 (workstation, not product) — **OWNER'S PC LOST ITS WIRED CONNECTION MID-GAME; ROOT-CAUSED AND FIXED. NO REPO CODE, NO DEPLOY — Fireplace untouched.** Filed only because §1 mandates a summary; skip it if you are here for product state. ➡ **`2026-08-15-session-workstation-nic-outage.md`**.
- **Fault A — the Realtek NIC was being killed by the IOMMU.** `HAL` id 15 `Device: 0x200` = **PCI bus 2 dev 0 fn 0**, uniquely the onboard RTL8168H. Chain repeated 3-for-3: IOMMU fault → ~50 s later `NCSI SuspectArpProbeFailed` → `WerKernel` 1001 `Component NDIS` live-dump request (08-12 03:11:34/03:12:16/03:13:13; 08-14 19:08:58/19:09:51/19:10:35). Memory Integrity is on (`VBS status: 2`, HVCI `Enabled=1`) so VT-d polices DMA; the **2017 Microsoft inbox driver `1.0.0.14`** DMA'd outside its mappings, the engine stalled, **the PHY link stayed up at 1 Gbps** so Windows never re-initialised it — which is why only a cable replug recovered it. Fixed by the MSI OEM Realtek package → **`10.79.50.1003` / 2025-10-03** (`pnputil /add-driver /install`, `oem35.inf`, live rebind, no reboot). A full gaming session since: **IOMMU 0 · ARP-probe 0 · NDIS dumps 0**.
- **The game was the trigger, not the fault.** Dies only in *Heroes of Might & Magic: Olden Era*; reinstalling it changed nothing. **The PS5 was never evidence** — it is on Wi-Fi and shares nothing with this NIC but the router.
- **⛔ RETRACTED, MINE: "the ISP blocks port 53".** Wrong, over-extrapolated from one failed sample. Re-tested 40× per resolver with cache-defeating names: `8.8.8.8` 40/40 @31 ms, `1.1.1.1` 40/40 @28 ms, router 40/40 @27 ms. The 05:41–05:55 DNS blackout was a **~20-minute transient**, cause unidentified, self-resolved.
- **Fault B is NOT the new driver — challenged by the owner, then tested.** The only plausible driver mechanism is a broken **UDP TX checksum offload** (would give "ping fine, DNS times out"); with offload still enabled, UDP/123 NTP to three public servers and a **raw UDP/53 socket to 8.8.8.8** all succeed, so it is excluded — a checksum defect breaks *all* outbound UDP, not port 53 selectively. And `DNS-Client` **id 1014 "none of the configured DNS servers responded" fired 10× in a month, every one on the OLD driver** (07-18 ×2 incl. a real `…steamserver.net` lookup, 08-01, 08-04, 08-05, 08-06, 08-10, 08-11, 08-13, 08-14). **Fault A likewise predates the fix — 5 occurrences 08-11→08-15 00:26, before the 00:50 install.** "Years of nothing then two at once" = one new workload, not one new cause.
- **Wi-Fi dongle: phantom device node REMOVED and `ConfigFlags` RESET.** `Disable-NetAdapter` during the Ethernet-only proof persisted as **`ConfigFlags = 0x00000001` (`CONFIGFLAG_DISABLED`)** on `USB\VID_2357&PID_0138\123456`; the owner then unplugged it, so `Enable-PnpDevice` failed (`Generic failure`, not-present). Cleared by `ConfigFlags = 0` **then** `pnputil /remove-device` ("Device removed successfully"). `Enum` key now gone, `oem25.inf` still in the driver store ⇒ **re-enumerates clean and ENABLED on replug, no user action.** ⚠️ That script's own "NOT CLEARED" line is a **false negative** — `Show-Flags` returned `Object[]` so `-band` threw; an independent re-check confirmed cleared.
- **⚠️ HARNESS TRAP, cost real time:** the `bash` tool **expands `$_`, `$e`, `$p` before PowerShell sees them**, silently mangling inline `powershell -Command "…"` into syntax errors. Write non-trivial PowerShell to a `.ps1` and run `-ExecutionPolicy Bypass -File`. Also: `Get-WinEvent` across *all* logs for a 20-min window **timed out at 300 s** — scope the `LogName`, write to a file, filter with `grep`.
- **Signal-reading, for anyone re-opening this:** `NCSI CapabilityReset` is **benign** (every sleep/boot, ~100 of them) — **`SuspectArpProbeFailed` is the hang signal**; `Microsoft-Windows-NDIS/Operational` is disabled on this box so the usable one is `WerKernel` 1001; the NDIS live dumps were **never written** (`ThrottleCheckResult 0x21`), the *event* is the evidence; **HAL throttles repeated identical IOMMU faults**, so a missing id 15 does not clear the IOMMU. Killed on timing: ephemeral-port exhaustion (`Tcpip` 4231/4266 real, but none after the 08-13 16:16 boot). The 03:55→04:15 "disconnect" was **sleep** (`Kernel-Power` id 42).

**Date:** 2026-08-15 — **ROOT CAUSE FOUND FOR THE WHOLE WAVE; 0.1.9 EXONERATED. NO CODE CHANGED, no deploy, tree clean at `43b301e`.** Two unrelated bugs were being reported as one. ➡ Full account, evidence and retractions: **`2026-08-15-session-decryption-failed-root-cause.md`**.
- **Identity churns are chronic and pre-date the deploy.** Full list: 90 (07-31), 54 (08-03), 76+92 (08-11), then 58/90/54 on 08-14. **Both "broken by the deploy" users had already lost their identity once**, on a ~2-week cycle. The backend log reaches back to the container start `08-05T19:27Z`, so the earlier "none before" claim was a `--since 72h` artifact. Dated independently by `one_time_pre_keys.createdAt` new-row insertions (07-31 17:07:51Z / 08-03 15:51:01Z / 08-11 13:20:56.7Z / 13:23:06.6Z) and by the owner's own `PEER_IDENTITY_CHANGED` lines.
- **🔑 NEW TECHNIQUE — nginx access log fingerprints the running bundle by BYTE SIZE.** 0.1.8 = `7,054,561`, 0.1.9 = `7,073,560`, 08-03's build = `7,026,085`; `304` = served from that container's cache. UTC, ~14 days, needs `sudo`, **rotation naming is off by one (`.12.gz` is Aug 3)**. ⚠️ A `200` does NOT prove a cold cache on a deploy day — the file genuinely changed (three bundles shipped on 08-03 alone). This is how causality got settled with no new client instrumentation.
- **🔑 USER 54'S LOSS HAPPENED ON 0.1.8 — DEFINITIVE, NOT INFERENCE.** His last working client session was `2026-08-10 07:11:33` (three surfaces agree: `key_bundles.updatedAt` frozen there, zero `refresh_tokens` rows, no audit logins). His device **first downloaded 0.1.9 at `12:21:32` on 08-14**, arriving already on the login screen. He never executed that bundle before losing his identity — four idle days on 0.1.8 did it. **⛔ RETRACTED: the `08-14 00:50:08Z login success userId=54` was NOT him** — nginx shows `51.68.138.13 … curl/8.5.0`, the VM running the previous session's reset verification; a curl login has no E2E client and could never churn. His ONLY real client login in the whole history is `16:38:42` on 08-14. (This also voids the earlier "he ran 0.1.9 for 4 h 17 min without churning" framing — he was logged out that whole time.)
- **⚠️ "THE APP KEEPS LOGGING HIM OUT" — CAUSE NOT ESTABLISHED. Two candidates, do NOT act on either yet.** Fact: his refresh row was slid at `21:47:29` (valid to 2027) yet two `401` logins came from his IP at `22:00:19`/`22:00:21` with no page load between. **(a) SELF-INFLICTED LOCAL LOGOUT — the parsimonious one.** `_clearLocalAuthState` never calls `_api.logoutRefresh`, so the server row survives while that same container shows a login screen; reachable from `expired_access_without_refresh` (`auth_provider.dart:229`, fires when the refresh token reads back `null`), `refresh_invalid` (`:241`/`:329`/`:368`) and `access_401_without_refresh` (`:384`). It then calls `await _tokens.clear()`, so **a transient storage read failure is converted into a PERMANENT local logout** — the "manufactured logout" class already flagged in the still-binding notes. **(b) TWO iOS STORAGE CONTAINERS** — Safari and a Home Screen web app have separate localStorage/cookies/HTTP cache, so they hold different Signal identities for one account and whichever logs in last overwrites the server bundle. The no-page-load signature is consistent with a service-worker-cached shell, but Safari registers that SW too, so it proves nothing. **⛔ I asserted (b) as solid on the reasoning "a live session and a login screen cannot coexist in one container" — that reasoning is FALSE (see (a)). Do not delete an app icon or reset a password on it.** Discriminator: ask him which surface he opens, and get a dump showing `expired_access_without_refresh` / `refresh_invalid` in the durable log.
- **🔑 THE DISCRIMINATOR ALREADY EXISTS — `AUTH_SESSION_END` IS DURABLE.** `auth_provider.dart:168-186` writes `E2ePersistentDiag.record('AUTH_SESSION_END', {reason, source, hasRefresh, …})` for **every** session end except `explicit_logout`. So a dump states in writing why the user was logged out, and it works from the container he CAN reach — no new instrumentation, no deploy. Read it: `reason: password_changed` ⇒ our reset or his own change (harmless); `reason: expired_access_without_refresh` with `hasRefresh: false` ⇒ **candidate (a) proven** (the stored refresh token read back null and the app cleared itself via `_tokens.clear()`); `reason: refresh_invalid` ⇒ the server rejected the refresh; **no `AUTH_SESSION_END` at all while he sits on a login screen ⇒ that container was never logged in = candidate (b)**. ⚠️ The durable log is per-container and dies with the storage, so its absence proves nothing on a wiped container — users 90/100 and the owner all show zero `AUTH_SESSION_END`, which for 90 only means his log begins after the loss.
- **The eviction fingerprint:** user 54's 08-14 loss left his HTTP cache **alive** (`304` at 16:35:51) while his token and identity died. A site-data clear / reinstall / new device destroys the HTTP cache too; **script-writable-storage eviction does not.** His 08-03 loss is the same sequence on 0.1.8 (8× `401`, re-login, new identity 2 s later). ⛔ **This entry originally called 08-03 "cold cache" — WRONG, corrected by the 08-16 audit:** the access log shows `GET /` 304, `main.dart.js` 304, all assets 304 and push POSTs updating the May-13 endpoint at 15:51 — 08-03 was **warm**, i.e. the same proven same-container wipe as 08-14 (see `2026-08-16-session-churn-audit.md`).
- **User 58 (Marzen) is the owner's own Safari incognito build-check account** — same IP as `deploy-web.ps1`'s `curl`/`node` smoke at 00:53:26. Private mode = empty storage by design, so that churn is self-inflicted. **Every incognito build-check burns an identity and gives its peers `[Decryption failed]`; check `/version.json` instead of logging in.**
- **Morion (100) is a SEPARATE, pre-existing bug — closed.** Two logged-in contexts on one origin (Chrome tab + installed PWA, proven from ONE durable log holding both `STORAGE_PERSIST_DENIED` from the tab and `granted: true` from the PWA). The tab live-decrypted msg 20342, consuming the ratchet key, and was frozen inside the non-atomic window between `decrypt.dart:1070` and `:1125`. `chat.gateway.ts:162-174` sends `newMessage` to every context by design.
- **Three real defects found statically, all pre-0.1.9, none fixed:** (1) `_decryptedLedger` has **no `add` path** (`encryption_provider.dart:43,:242,:301,:312-314,:624,:732,:1051,:1082`), so the spent-key guard at `decrypt.dart:984` is **inert within the session that first decrypts a message**; (2) no re-entrancy guard on `retryDecryptActiveConversation:113-115`; (3) ratchet consumption and plaintext commit are not atomic, and an empty-plaintext decrypt persists nothing with no diagnostic (`_persistDecryptedContent:138-142`).
- **⛔ 0.1.9 sealing is CLEARED — do not re-audit.** `_drainOne` round-trips + compare-and-sets + reads back (`sealed_web_signal_kv.dart:369-390`); seal-open fails closed and rethrows `SIG_KEY_UNAVAILABLE` rather than reporting absence (`:151-171`, `signal_stores.dart:126-134`); `read` throws instead of returning null; `readAll` preserves unreadable rows as raw envelopes; the residue check was **tightened** (`encryption_service.dart:349`); `_migrate` is byte-identical to pre-deploy; `clearAllKeys` is reachable ONLY from account deletion (`settings_screen.dart:157`) — the "on logout" comment at `content_key_manager.dart:33-34` is **stale**; no dependency or `web/` change shipped.
- **Retracted this session, do not resurrect:** the keyId `0..19` "fresh install" signature (upsert is per `(userId, keyId)`, in place); push-endpoint survival as proof of storage survival (iOS holds it at OS level); **the UA cannot distinguish an installed PWA from a Safari tab** (the owner's own PWA carries `Version/… Safari/604.1`) — the only reliable discriminator is the client's own `STORAGE_PERSIST {granted}`; the wasm/cross-context-lock angle (`deploy-web.ps1:93` builds JS, `--no-wasm-dry-run` only skips the check).
- **🔴 OPEN — the obvious next step is BLOCKED.** `E2ePersistentDiag`, `CANARY_OK` and `STORAGE_PERSIST` are **per-container** localStorage, and the diagnostics screen lives behind Settings, so a logged-out container cannot report. Two ways out: (a) no code — he logs in and dumps immediately, accepting another identity mint; (b) **PROPOSED, needs permission** — extend the auth screen's diagnostic footer (`auth_screen.dart` ~`:202`) to dump the durable log while logged out (needs a PATCH bump + deploy; a Safari PWA can take ~14 h to pick it up). Cheapest question that beats both: **ask him whether he taps the Home Screen icon or opens Safari.** **Fixes proposed, NONE written:** await persistence before minting an identity on the fresh-install branch; warn when unpersisted; audit the `_clearLocalAuthState` → `_tokens.clear()` paths for manufactured logouts; then `_decryptedLedger.add` + a write-ahead marker for the Morion class.
- **Still binding, from the rolled-off 2026-08-14 (evening) entry:** **the owner's rule is investigate and PROVE, then ASK before writing code — diagnostics and instrumentation count as code.** That entry existed because `4beb1bd` (terminal `missingPreKey` class) was landed on a symptom before the mechanism was known; it was reverted at `409c23a`, is recoverable via `git show 4beb1bd`, and **must NOT be re-landed as a first move** — this session proved the mechanism was never in that code path at all.
- Parked, uninvestigated: six `EXPIRY_STAMP_MISS` in one history pass on ruchens69's dump (20275, 20266, 20269, 20271, 20272, 20273).

**Date:** 2026-08-14 — **ADMIN PASSWORD RESET for user 54 + logout-cause chase. NO CODE CHANGED, no deploy.** There is no admin reset endpoint (`POST /users/reset-password` needs the old password), so it was one prod transaction in the `users.service.ts:298-317` order: revoke every refresh token → set the bcrypt hash → stamp `passwordChangedAt`. Login verified 201; my test session was then deleted and the stamp refreshed to kill the access JWT it minted.
- **The server never logs anyone out silently, and did not here.** His refresh row was valid to **2027-08-10** and alive when I deleted it — recovered read-only from `chatdb-20260813T040001Z.dump.gpg` via `pg_restore --data-only -t refresh_tokens` (nothing restored into the live DB). Last slide `08-10 07:11:31`, two seconds before his key-bundle write, both from one fully authenticated cold boot. Zero `[auth-session-end]`, zero `login failed`. **Login screen ⇒ local token loss, always.** He changed his own password 08-03 15:27 (which revokes every session by design) and re-logged in at 15:50 — **that Aug 3 password is the one he forgot.**
- **🔴 OPEN, and key loss is NOT ruled out:** an **offline Logout tap** (both server calls swallowed by `catch (_) {}`, keys intact) and an **origin storage wipe** (keys gone) are byte-identical from the server. Online logout IS excluded — it would have deleted `web_push_subscription` id 370, still present. **Watch for `[identity-churn] userId=54 oldIdentityPrefix=BcNWM+S196eX` on his next login** — that line means his history is gone, its absence means it survived.
- **⚠️ THREE HYPOTHESES KILLED — do not resurrect.** (1) "History is intact" — churn is only observable when the client CONNECTS, and he has not since 08-10, so the clean record covers only up to then. (2) Quota/partial-write loss of the refresh token is impossible: `/auth/refresh` echoes the SAME opaque string (`auth.service.ts:95`, no-rotation by design at `refresh-tokens.service.ts:51-55`), so a swallowed `setString` rewrites a byte-identical value; only a `remove` or an origin wipe can lose it. (3) **The auth screen's reason line is NOT a wipe test** — `_lastSessionEndReason` is in-memory only (`auth_provider.dart:35`, set `:169`, never persisted, never restored), so it reads bare after any cold boot under both branches; I cited it three times before catching that. Use the `E2ePersistentDiag` panel instead: records present ⇒ localStorage survived ⇒ keys intact, and no `AUTH_SESSION_END` around 08-10 is the offline-logout signature (`:181` skips the diag for `explicit_logout`).
- **The web session/identity share ONE evictable localStorage** — tokens (`auth_token_store.dart:31`, secure storage is Android-only) and the whole Signal identity (`signal_stores.dart:26-31`). Session loss and permanent history loss are one event; no key backup by design; no iOS setting changes it. The 7-day WebKit cap does not fit either: storage held the refresh token across 08-03 15:50 → 08-10 07:11, and `web.push.apple.com` proves only "an Apple browser" (macOS Safari 16.1+ pushes to non-installed sites), not a home-screen install. **Unwatched signal:** `refresh_tokens.expires_at` is a per-device daily heartbeat (`auth_provider.dart:297-306`) — 23 of 48 accounts have not slid in >14 days and nothing alerts.
- ➡ Detail: **`2026-08-14-session.md`**.

**Date:** 2026-08-13 — **FIELD DIAGNOSTICS READ + STALENESS SWEEP. No production deploy THAT DAY — `0.1.9` shipped the next day, 08-14; see the banner. Two commits (`f6e4aa8`, `7663047`), CI 4/4, backend 671/671 / 49, ratchet PASS 912→907. Tracker emptied: 0 issues, 0 PRs, 0 alerts.**
- **Everything from 08-05 checks out in the field.** `WEB_SEAL_OPEN {sealed: 258, legacy: 0, unreadable: 0, lostRows: 0}` — B2a up 184→190→258 over ~11 unattended days with **zero loss**; that, not the canary, is the real evidence. `CANARY_OK {ageDays: 15}` reconciles (see banner). `STORAGE_PERSIST` granted, `SESSION_INVENTORY` 33 stable across four reconnects.
- **✅ The recurring duplicate-decrypt storm STOPPED on its own.** Last durable `DECRYPT_DECISION` is `08-03 17:44:13`, nothing for 08-04→08-14, and the newest durable entry is 08-11 so this is not ring eviction. Peers 60/52/83/49's fixed id sets retired — terminal-duplicate retirement worked, it just needed more distinct process lifetimes than expected. **Closed by observation, no fix.**
- **🔴 NEW `PEER_IDENTITY_CHANGED {peerId: 92}` at 08-11 15:24, on a live session, on the vulnerable build.** Third one (90, 54, 92). This does NOT contradict `3d30b88`: detection lives in `isTrustedIdentity` (`signal_stores.dart:511-519`) and the deleted pre-save only poisoned the path where WE fetch a bundle from the server, so inbound `PreKeySignalMessage` still fires. **Detection works for keys arriving in messages and is blind in exactly the direction an active server MITM attacks.** All three peers remain unverifiable on `c01317c` (`getPeerIdentityFingerprint` has one UI caller there — the banner). Verify them through 0.1.9's Safety-section door once live.
- **🔴 A high-severity advisory Dependabot silently failed to deliver, TWICE.** js-yaml `GHSA-5p4m-2wfm-xmqj`; runs `31519706831` (08-11) + `31573038380` (08-12) both errored in `bin/run update_files`, so **no PR ever appeared** and it sat unpatched for 3 days. It cannot bump the requirement (both copies transitive dev-only: `cosmiconfig`←`@nestjs/cli`, `@istanbuljs/load-nyc-config`←`ts-jest`); lockfile `npm audit fix` resolves it. 0 vulns after. **⚠️ NEW TRAP: Dependabot runs under a DIFFERENT workflow name on its own schedule, so `gh run list --branch master --limit 1` after your push never shows it.** Use `gh run list --limit 10` unfiltered, or the Security tab.
- **A latent Windows-only flaky test, found by accident** (669/670 then 670/670 then green in isolation). `media-cleanup.service.ts:143` subtracted mtime from a `nowMs` captured before the loop; **NTFS rounds a just-written mtime UP**, so the difference goes NEGATIVE and an orphan reads "fresh" even at `graceMs=0` — flaking the spec AND **leaking any future-stamped orphan forever**. Age clamped at 0; new spec proven RED against the unclamped code (1 failed/14 passed).
- **Doc gate caught me:** first push went red on `verify-claude-backend-test-counts.mjs` (documented 670, Jest 671). A new backend test REQUIRES the `CLAUDE.md` §3 count bump **in the same push**. Prettier failures in `media/` are pre-existing (verified by stashing) — left alone, inside the ratchet's ±5.
- **Both open issues were stale, closed with evidence.** **#102** brace-expansion: only 1.1.18/2.1.4/5.0.9 remain, all above the advisory floors, 0 vulns in `backend/` and `scripts/smoke/`. **#105** deleted-message plaintext: fixed by `42603d2` on 07-28 **~2.5h after it was filed**, an ancestor of master AND the served `c01317c` (`messaging_provider.events.dart:321-352` — ciphertext captured before `removeWhere`, purge unconditional so delete-for-me is covered, durable backlog first). **Fixed in prod for two weeks while the issue sat open.**
- **Deliberately NOT touched: Flutter deps.** 32 locked older / 31 constrained below resolvable, `flutter_secure_storage_macos` + `js` discontinued, **no advisories** — all patch/minor drift. `pub upgrade` hours before shipping a release that changes at-rest key handling perturbs the crypto-adjacent graph for zero benefit; do it after 0.1.9 is field-verified. `flutter_secure_storage_web` 1.2.1's localStorage key custody is **not fixable by upgrade** — needs the non-extractable IndexedDB `CryptoKey`.
- **Cleanup pending owner:** KEEP `feat/invitations-hex-ui` @ `c01317c` until 0.1.9 is verified (it is the rollback tree), delete after. `origin/feat/cosmic-theme` has 1 orphan commit from 07-20 (starfield `?density=` dev tool) — cherry-pick or delete. **5 stashes on branches that no longer exist**, offered twice, still unanswered.
- ➡ Detail: **`2026-08-13-session-staleness-sweep.md`**.



