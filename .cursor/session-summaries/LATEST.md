# Latest session summary

**Date:** 2026-07-12 (release candidate 0.0.109 — stale OTP hardening, cache durability, diagnostics)

**Topic:** Untagged legacy OTP uploads now fail closed with `identity_epoch_required`; frontend replenishment defers when identity lookup is unavailable and later uploads tagged OTPs. The incident harness covers the legacy identity race, the preserved replay boundary, and a two-party audio-cache durability/reload path. The production cache action clears downloaded audio only, preserving decrypted history, media keys, Signal sessions, and pending-send plaintext. Diagnostics provide current-session, current-build, 24-hour, historical, and full-log filters plus repeated-failure grouping and explicit diagnostic-only clearing. Release semver is now `0.0.109` (deployed `0.0.108` remains unchanged).

**Verification:** Backend Jest **462 passed / 43 suites**; root test-count verifier passed; full Flutter suite **667 passed**; local and production-mode staging wire harnesses **11 tests passed each**; diagnostic utility tests **2 passed**; diagnostic widget test **1 passed**; analyze has no errors except the pre-existing regex info in `jumbo_emoji.dart`.

**Safety:** No merge, deployment, rollback, browser-data clearing, identity regeneration, session deletion, or history deletion. Production remains owner-gated.

**Previous:** 2026-07-11 (incident harness extension — legacy/new identity epoch invariant RED; production code frozen)

**Date:** 2026-07-11 (stale-OTP E2E fix + duplicate/durability hardening — **DEPLOYED to prod, 0.0.108 / `59191f2`, both tiers**; branch `fix/stale-otp-epoch`, NOT yet merged to master)

**Topic:** **Every incoming message showed `[Decryption failed]`; peers 37↔63 could not message either direction. Root cause PROVEN (not hypothesised): commit `bfbbe4e` (0.0.96, 07-08) started serving real one-time pre-keys oldest-first, activating a pool of STALE OTPs from identity epochs users regenerated weeks ago — dead keys whose private half is gone on-device → PreKey (ctype 3) X3DH fails → `Bad Mac`.** Durable fix = server-side identity-epoch binding: OTP rows carry the uploading `identityPublicKey`; `fetchPreKeyBundle` only claims rows matching the CURRENT bundle identity (load-bearing guard), so a stale row is never served regardless of upload order / old clients / tabs / reconnect races. Migrations `0003` (purge unused — emergency stop-the-bleed, X3DH works OTP-less), `0004` (dedup + unique `(userId,keyId)` index → upload UPSERTs), `0005` (nullable `identityPublicKey`). `countUnusedPreKeys` current-epoch-only; `upsertKeyBundle` purges superseded-epoch unused rows; client tags OTPs on both upload paths. Prior agent's uncommitted worktree snapshotted to `backup/prekey-epoch-wip-20260711`, branch deleted, rebuilt from clean master per owner. Emergency OTP-less mitigation documented (the fix already achieves it via 0003). SECONDARY (0.0.107, ALSO fixed, distinct bug): the durable `kind:duplicate` `[Decryption failed]` entries (e.g. msg 15284 / peer 49 bob210) are history re-decrypt hitting DuplicateMessage when the plaintext is absent locally — root was a SILENT plaintext-write drop (`saveDecryptedContent` swallowed failures; victim had persistent-storage GRANTED so NOT eviction, and 15284 is a high id so NOT the 2000-cap prune). Fix (B) `duplicate` marks failed in-memory only, no longer persists `[Decryption failed]` → a later launch can recover if the plaintext row survives, and never re-keys so no reset loop; (A) `saveDecryptedContent` checks `setString`'s bool (read-back is vacuous), retries once, records `DECRYPT_PERSIST_FAILED` on throw/false. HONEST LIMIT: prevents/softens FUTURE loss only — does NOT resurrect already-burned messages (bob210's 15284 stays failed) and does NOT cover a crash between ratchet-advance and plaintext persist.

**Verification:** deployed-backend RED/GREEN (real libsignal + Postgres) — the EXACT deployed commit `b7708ed` FAILS `test_e2e/stale_otp_epoch_test.dart` with `Bad Mac`, `fix/stale-otp-epoch` PASSES (proof on production code, not synthetic). Crypto-layer `stale_otp_badmac_test.dart` **3/3**; provider-level `messaging_provider_duplicate_recovery_test.dart` proves `duplicate → persist:false` (no durable poison) with a `badMac` contrast. After merging `origin/master`: backend `npm test` **462/43** + verifier green; **full `flutter test` 669** (liquid-glass widget tests + E2E together); `flutter analyze` clean. **DEPLOYED to prod 0.0.108 / `59191f2`, verified:** public `/version` (backend) + `/version.json` (frontend) both `0.0.108`, `/health` ok, served `main.dart.js` contains `59191f2` (fresh build, not cached), prod Postgres shows migrations `0003–0005` + `UQ_one_time_pre_keys_user_key` + purged pool (0 unused OTPs). **REGRESSION CAUGHT + FIXED mid-session:** the first deploy used `fix/stale-otp-epoch` cut from BEFORE the liquid-glass merge, so the `0.0.107` frontend reverted the liquid-glass UI (and the notes PR #64 backend); recovered by merging `origin/master` into the branch (liquid-glass + notes + E2E fix, `0.0.108`) and redeploying both tiers. Full handoff: [2026-07-11-session-stale-otp-epoch.md](./2026-07-11-session-stale-otp-epoch.md).

**Previous:** 2026-07-10 (code-scanning triage — SSRF hardening + CI permissions + dependency bumps)

**Date:** 2026-07-10 (code-scanning triage — SSRF hardening + CI permissions + dependency bumps)

**Topic:** **All open GitHub security alerts triaged and fixed on branch `fix/code-scanning-alerts` → PR [#59](https://github.com/Lentach/Fireplace/pull/59) (base `master`, NOT merged — awaiting owner OK; CodeQL+Dependabot re-run on the PR is the proof).** Actual scope was 3 CodeQL alerts + 30 Dependabot alerts (all npm/backend). CodeQL 3/3 fixed at root cause, zero suppressions: `js/request-forgery` critical in `link-preview.service.ts` (`2a26bd3`) replaced the regex host blocklist with two enforced layers — byte-parsed `isPrivateIp` for the URL-literal (kills decimal/hex/octal IPv4, mapped/NAT64 IPv6, 0.0.0.0, CGNAT, ULA, link-local) + an undici `Agent.connect.lookup` **resolve-and-pin** that refuses any hostname resolving to a private address, closing the DNS-rebinding residual the code previously documented as open; manual per-hop redirect validation retained. Two `actions/missing-workflow-permissions` fixed by top-level `permissions: contents: read` (`f87cc80`). Dependabot 29/30 cleared by semver-safe `npm audit fix` (`a6ed352`: protobufjs 7.6.5 incl. critical RCE, js-yaml, fast-uri, fast-xml-parser, node-forge, picomatch, socket.io-parser, flatted, @babel/core; post-fix audit high=0 critical=0). **1 deferred, needs owner sign-off:** `uuid` (moderate) is only reachable via firebase-admin's transitive chain and needs a firebase-admin MAJOR bump (13→14). Added `undici` as a direct dep for the pinned agent; its `connect.lookup` wiring is proven by a live-loopback-server E2E test (fail-open would serve a request).

**Verification:** backend `npm test` **452 passed / 43 suites** (was 416; count verifier green), `tsc --noEmit` clean, `graphify update` 8225 nodes/11698 edges. No frontend changes. Full local handoff (gitignored): [2026-07-10-session-code-scanning-alerts.md](./2026-07-10-session-code-scanning-alerts.md); sensitive triage in `docs/audit/2026-07-10-code-scanning-triage.md` (local-only, public repo).

**UPDATE (same day): PR #59 MERGED (`7b1528b`).** Post-merge CodeQL confirmed all 3 code-scanning alerts `state=fixed` (0 open). Follow-up for the last Dependabot alert (#65 `uuid`, moderate) → branch `fix/uuid-dependabot-alert`, PR [#60](https://github.com/Lentach/Fireplace/pull/60), NOT merged. **Finding: the firebase-admin 13→14 bump is NOT the fix** — fb14's google-cloud chain still pulls old uuid, and v14's modular SDK drops the `admin.apps/credential/messaging()` namespace this service uses (tsc/build fails). Real fix = scoped `overrides.firebase-admin.uuid = ^11.1.1` (lockfile delta = uuid 9.0.1→11.1.1; our code uses uuid nowhere; chain only calls `v4()`). firebase-admin stays 13.x. `npm audit` = **0 vulnerabilities**, tsc clean, 452 tests green. **Next: owner OK to merge PR #60; a real fb14 upgrade (modular-SDK migration + live FCM smoke test) is optional future currency work, not required.**

**UPDATE 2 (same day): Node runtime 20 → 22 LTS** → branch `chore/node-22-upgrade`, PR [#61](https://github.com/Lentach/Fireplace/pull/61), NOT merged. Node 20 nearing maintenance EOL; bumped all pins — `backend/Dockerfile` (builder+runtime), `Dockerfile.dev`, `docker-compose.yml`, `ci.yml` `setup-node` — plus new `engines.node >=22`. `@types/node` stays `^22` (tracks runtime). Also an in-range `npm update` dep refresh (@eslint/js, @types/node, prettier patches; no manifest ranges changed). Majors deliberately left for separate decisions (typescript 7, undici 8 [load-bearing for SSRF agent], firebase-admin 14 [Node-unblocked now but still needs modular-SDK migration + FCM smoke test], @nestjs/schedule 6, dotenv 17, globals 17). **Verified: PR #61 CI all green on Node 22** (Backend tests `npm ci`+`npm test`, Flutter, CodeQL); `npm audit` 0, tsc clean, 452 tests. Local Docker build not possible (daemon down) — CI is the Node-22 proof. **Gate before prod: staging rehearsal (CLAUDE.md §6) since the Dockerfile changed.**

**UPDATE 3 (same day): all three PRs MERGED + backend DEPLOYED to prod.** PRs #59/#60/#61 merged to `master` (`b7708ed`). Backend redeployed on the OVH VPS via `deploy-backend.sh` (VM was behind at `356cf4a`, so SSRF hardening + uuid override + Node 22 all went live together). Verified: container `node --version` = **v22.23.1**, health `healthy` at 10s, public `/version` commit `b7708ed`, `/health` `{status:ok,db:ok}`, Socket.IO handshake ok, startup logs clean (only pre-existing benign FCM-disabled warning), `npm audit` 0. Staging rehearsal skipped (local Docker down); substituted a build-on-prod-host rehearsal + health-gated cutover + functional smoke — clean, no rollback. Backend only — frontend not redeployed (no frontend code changed; that's the separate PC `deploy-web.ps1` flow). No version bump (still 0.0.104 + new commit) — `pubspec.yaml` left untouched (owner's uncommitted ping/0.0.105 WIP). Deploy record also in the local dated session file.

**Previous:** 2026-07-10 (ping visual/sonic identity live in production — 0.0.105/2afdd50)

**Date:** 2026-07-10 (ping visual/sonic identity live in production — 0.0.105/2afdd50)

**Topic:** **The approved original Fireplace pulse-burst now replaces every ping sparkles/megaphone mismatch. One reusable centerless 8-ray `PingGlyph` uses rounded strokes with longer cardinal and shorter diagonal rays: 24 px action tile, 18 px beside `PING!`, 50 px in the overlay. The overlay adds two restrained translucent orange pulse rings behind the same white glyph. No Wire vector was copied.**

**Sound, merge, and deploy:** approved **420 ms 660→990 Hz warm double-tap** is integrated as `assets/sounds/ping_alert.wav`; obsolete MP3 removed. Focused isolated-release suite **28 passed**. Release commit `a31fab2` was pushed and owner merged PR #62 as master `2afdd50`; merged master was rebuilt and atomically published to OVH. Production smoke passed: `/health`, frontend `/version.json` **0.0.105**, backend `/version` **0.0.104/b7708ed**, served `main.dart.js` contains exact SHA **2afdd50**, fresh Chromium app boot rendered. Full handoff: [2026-07-10-session-ping-identity-research.md](./2026-07-10-session-ping-identity-research.md).

**Previous:** 2026-07-10 (Telegram-like media sizing follow-up — 0.0.104 deployed for owner test)
**Date:** 2026-07-10 (Telegram-like media sizing follow-up — 0.0.104 deployed for owner test)

**Topic:** **Production screenshot exposed the 0.0.103 sizing defect: `mediaWidth / devicePixelRatio` made low-resolution GIFs collapse to 96px squares. Fixed at `fef8fdf`: ordinary and low-resolution IMAGE/GIF media now scale to bounded chat width, preserve aspect ratio with `BoxFit.contain`, and never crop; tall media is capped at 400px / 52% viewport instead of 480px / 65%; media bubbles gain a 1.25px theme-aware outline.** The encrypted metadata boundary and legacy 220px no-dimension fallback are unchanged. Version bumped to 0.0.104.

**Production and merge:** owner explicitly requested feature-branch production testing before merge. Before merge, CI caught that the outline changed context-menu geometry and that attachment-send widget tests no longer awaited asynchronous media metadata extraction. Fixed at `c08d007` by painting the outline as a non-layout foreground decoration and adding a deterministic awaited test seam. Local full suite **637 passed**, analyze clean, all GitHub checks green. PR [#58](https://github.com/Lentach/Fireplace/pull/58) **merged to `master` as `c429915` on 2026-07-10**. The merged master frontend was then deployed normally: live `/version.json` reports **0.0.104**, served `main.dart.js` contains **`c429915`**, `/health` is OK, and fresh Chromium app boot passed. Full handoff: [2026-07-10-session-media-preview-telegram-followup.md](./2026-07-10-session-media-preview-telegram-followup.md).

**Previous:** 2026-07-09 (encrypted media-preview aspect-ratio fix — PR #58 open)

**Date:** 2026-07-09 (encrypted media-preview aspect-ratio fix — PR #58 open)

**Topic:** **Shipped owner-approved IMAGE/GIF preview correction at `dca8ccb` / PR [#58](https://github.com/Lentach/Fireplace/pull/58): shared `MediaPreviewFrame` preserves normal aspect ratio, caps `>3:1` panoramas and `<1:2` portraits with intentional contain/letterboxing, keeps a 96px tiny-media minimum, and retains the old 220px geometry for legacy messages.** `ImageMessageContent` and `GifMessageContent` now use `BoxFit.contain`; fullscreen remains contain. **VIDEO remains out of scope** because Fireplace still has no video message renderer, upload/poster path, or player.

**Encrypted-only wire contract:** new IMAGE/GIF dimensions and optional `mediaThumbHash` reside solely in the Signal-encrypted `E2eEnvelope`; they flow through optimistic state, pending-send/lost-ack recovery, decrypt cache, history merge, and retry. The outer socket emit, backend DTO/entity/mapper/log, REST payload, and database are unchanged. Old/partial/invalid metadata stays safe and uses the stable legacy fallback.

**Verification:** focused envelope/geometry/containment/privacy suite **31 passed**; `flutter analyze --no-fatal-infos` clean; `flutter build web --release --no-wasm-dry-run` succeeded; `graphify update` succeeded (8,212 nodes / 11,677 edges). Independent GPT-5.5 review passed the contract; its initial concern about `fast_thumbhash` async isolate APIs was adjudicated: this code calls only synchronous APIs and the web release build passes. The code comments preserve that web constraint. Full handoff: [2026-07-09-session-media-preview-implementation.md](./2026-07-09-session-media-preview-implementation.md).

**Previous:** 2026-07-09 (media-preview crop research and owner approval — implementation next)

**Date:** 2026-07-09 (media-preview crop research and owner approval — implementation next)

**Topic:** **Image/GIF chat previews crop because both force an available-width × 220 logical-pixel box and draw with `BoxFit.cover`; full-screen uses `contain`, proving the source is intact.** Source trace: `ImageMessageContent` and `GifMessageContent` load/decrypt then render `cover`; `ChatMessageBubble` caps bubble width at 85% and media surface hard-clips. **There is no VIDEO message type, renderer, uploader, or player in Fireplace** — video poster handling is future scope, not a third existing path. The E2E envelope has type/URL/duration/key/IV but no dimensions or placeholder; a recipient cannot reserve true geometry before decrypt today.

**Owner-approved design:** deliver **Stages A+B+C** for IMAGE/GIF: one shared bounded `MediaPreviewFrame`; `BoxFit.contain`; exact W×H from `mediaWidth`/`mediaHeight` **inside the Signal-encrypted envelope only**; optional encrypted ThumbHash fade-in. Normal ratios retain native geometry; `<1:2` tall images and `>3:1` panoramas cap their frame and **letterbox, never crop**. Legacy messages without dimensions retain the current 220px fallback. **Never put dimensions/hash in outer socket payload, backend DTO/entity/mapper/log, REST payload, or DB.** Recommendation cites Telegram Android/TDLib (W×H + min/max), Signal Android (W×H + BlurHash), WhatsApp Web private-protocol schema (W×H + JPEG thumbnails; clearly unofficial); no public Messenger consumer-source claim. Full report: `docs/review/2026-07-09-media-preview-rendering-proposal.md`. No application code changed; implementation is now authorized. → [2026-07-09-session-media-preview-rendering-proposal.md](./2026-07-09-session-media-preview-rendering-proposal.md)


**Previous:** 2026-07-09 (own-message "[encrypted]" field report — triaged: lost messageSent ack; fix GATED on owner go)

**Topic:** **Owner's own sent message showed `[encrypted]` mid-conversation (sender side only; recipient read it fine) — triaged from diag dump + screenshot; NOT an E2E failure, NOT the race back, NOT peer-version skew.** Smoking gun: `SEND_EMIT` → `SOCKET_DISCONNECT {intentional:false}` 3s later → no `RECV_MSG` echo ever — the `messageSent` ack (tempId→realId) was lost, so the plaintext in `_pendingSendContent` was never persisted under the real id; a Signal sender can't decrypt its own ciphertext → sender's copy stays `[encrypted]` forever (msg ~14667; recipient unaffected). Diagnosis HEAD-validated: messaging/ack layer byte-identical 0.0.95→0.0.101 (socket_io 3.1.6 bump is transport-only). **Fix designed, NOT built — owner said "wait before you implement any changes":** provider-layer reconcile of unacked pending sends on history merge (conversation + tempId-timestamp proximity) + durable pending-send map; zero crypto surface. Also in the dump: 07-08 duplicates from senders 63/62 — dating SQL RUN on the OVH VPS (rows themselves expired/deleted; times BRACKETED via monotonic neighbor ids 14382/14395/14438): 14389-91 minted 06:22-11:09 CEST, 14423 minted 11:09-15:53 CEST — **both POST-fix (deploy 02:00 CEST)**, so "pre-fix damage" is DEAD for these. Receiver (hub, updated) rejected wires poisoned at the SENDER → leading hypothesis: 63/62 still ran cached ≤0.0.93 PWA bundles (fix is client-side; old bundle mints duplicates until fully closed+reopened); the `_sessionTails` tripwire (Step 3A) CANNOT be excluded from DB data alone. DECIDER: ask 63 + 62 to fully close+reopen and confirm footers ≥0.0.94; any duplicate they mint AFTER that = Step 3A, alarm. Peer-58 badMac batch: created 07-08 22:30 UTC (post-fix) but kind=badMac = regenerated-identity session mismatch, NOT the lock race — machinery responded, watch only; badMac ×5 from peer 58 (07-09 00:31) = 07-07 regeneration leftover, machinery responded, self-healing. Runbook gained the lost-ack signature row + the DATING RULE (never classify duplicates by DECRYPT_DECISION time). → [2026-07-09-session-lost-ack-triage.md](./2026-07-09-session-lost-ack-triage.md)

**Independent review (same session, reviewer agent — 0.0.94 lock + runbook + lost-ack design):** **shipped `_sessionTails` lock = SOUND, zero blocking** (all four mutators serialized, leaf-level, error containment + `_encryptInFlight` bookkeeping correct, gated probe pins BOTH race directions). **The lost-ack fix design above is SUPERSEDED — do NOT build the timestamp-proximity matcher** (burst sends put candidates ms apart; a wrong match persists the WRONG plaintext under a real id — permanent, worse than `[encrypted]`; also breaks under same-tempId retries and multi-device). REVISED design (in the runbook row): durable pending-send map keyed by a **hash of the emitted ciphertext** at `SEND_EMIT` (per-user prefix, wiped by `clearAllKeys`, lifecycle decoupled from the onConnect `_pendingSendContent` clears), reconciled on history merge by **exact `encryptedContent` equality** (history rows carry it; ciphertext unique by ratchet construction). Hygiene applied: stale pre-fix comments in the gated probe fixed; runbook gained the volatile-ring caveat on the lost-ack signature + a "Known accepted edges" section (buildSession/deleteSession lock not probe-pinned; `_sessionTails` not cleared across `initialize()` — ~unreachable; `clearAllKeys` deletes sessions outside the queue — privacy hygiene). NOTE for deploy tooling: prod is on the OVH VPS now — the old `gcloud compute ssh` path to GCP is dead (timed out); use the VPS SSH from the migration runbook.

**BUILT (same session, owner go): 0.0.102, branch `fix/lost-ack-pending-send-reconcile` — PR owed.** Durable pending-send records: ONE SharedPreferences key per EXACT emitted ciphertext (`e2e_${uid}_pendsend_v1_<ct>` — deliberately NOT a shared JSON blob; concurrent-save RMW on one blob is the `_sessionTails` lost-update shape, caught in review-advisory), written at `SEND_EMIT` (fire-and-forget, never delays a send), consumed on `messageSent` ack, TTL 72h/cap 40, wiped by `clearAllKeys` + `clearDecryptedContentCache`, NOT cleared on reconnect. History-decrypt own-message branch reconciles: peek → persist under real id → VERIFY by read-back (`saveDecryptedContent` swallows failures; consume-first would destroy the only surviving plaintext copy — second advisory catch) → take; durable `SEND_ACK_RECONCILED{persistVerified}` diag. Tests (Tester agent): 13 green — store contract 9 (peek/take/exact-match/TTL/cap/per-user/wipes/corrupt-json) + provider 4 (lost-ack happy path, mismatch stays `[encrypted]` + record intact, ack self-cleaning, and the persist-fails-keeps-record branch with proven teeth vs an unconditional-take refactor). Full suite 625/625, analyze clean, graphify updated. Deploy: frontend-only (`.\deploy-web.ps1` after merge).

**Previous:** 2026-07-09/10 (production hardening: migrations + FKs + non-root + offsite backups — MERGED PR #55+#56, DEPLOYED 0.0.104, offsite LIVE)

**Topic:** **Prod-readiness items 1–4 + offsite backups shipped and LIVE: SQL migration system (runner at boot, `backend/migrations/`, exactly-once `schema_migrations`, baseline STAMPED on existing DBs / executed on empty), user FKs with CASCADE on `key_bundles`/`one_time_pre_keys`/`fcm_token`/`web_push_subscription`/`secret_notes`, backend container non-root `USER node` (media volume chown in deploy scripts), VPS backups verified end-to-end (cron live, all 3 artifact types decrypt-tested).** Verified pre-merge: backend Jest 416/416 (43 suites, +9 runner spec), staging rehearsal (empty-DB baseline execution + real prod-dump restore + true prod path + harness 7/7), adversarial review MERGE-READY zero P1 (P2 db-connect retry fixed `e1db060`). **Prod deploy 07-10 (`./deploy-backend.sh`, 0.0.104/`356cf4a`): logs `stamped 0001`+`applied 0002`, all 5 `fk_*` CASCADE constraints catalog-asserted on live data (80 users, zero orphans found), runs as `node`, media writable, /health+/version green.** **Offsite backups LIVE (option B→R2 after B2 pricing ambiguity):** Cloudflare R2 bucket `fireplace-backups` (private, lifecycle delete 31d prefix `vps/`, **Bucket Lock 30d = append-only — DeleteObject 4xx-verified even with the R&W token**), rclone upgraded 1.60→v1.74.4 (apt rclone's CreateBucket preflight caused phantom `AccessDenied`; fixed with `no_check_bucket=true` + upgrade), cron line carries `BACKUP_RCLONE_REMOTE=fireplace-r2:fireplace-backups/vps`, first real run `offsite verified: 3/3`, acceptance drill: dump pulled back FROM R2 decrypts to `PGDMP`. One R2 token secret leaked into chat mid-setup (rclone `config update` echoes raw secrets — trap!) → rotated immediately; current token never exposed. **STILL OWED (owner): healthchecks.io check → add `BACKUP_HEALTHCHECK_URL=` to the cron line (dead-man ping only fires on verified offsite success); optional: send one image on live 0.0.104 to field-verify media writes under non-root.** → [2026-07-09-session-prod-hardening.md](./2026-07-09-session-prod-hardening.md)

**CLOSED (same day):** healthchecks.io dead-man wired — owner created the check, cron line now carries `BACKUP_HEALTHCHECK_URL=https://hc-ping.com/f0ec…2709`, live-fire run: `offsite verified: 3/3` → `healthcheck pinged` (dashboard green). Ping fires ONLY on verified offsite success; a silently failing night pages by absence. Offsite backup chain fully closed. **Image send field-verified by owner on live 0.0.104** — first real media write through nginx under the non-root container; the entire hardening batch (migrations, FKs, non-root, offsite + dead-man) is field-complete, NOTHING owed. Footer note: frontend bundle correctly shows `c429915` (newest frontend-touching commit; git-verified zero frontend changes after it — do NOT redeploy web just to sync the footer).

**Previous:** 2026-07-09 (agent skills switch: obra/superpowers → mattpocock/skills — done, docs-only, no bump)

**Topic:** **Agent-skill stack switched from obra/superpowers (plugin v5.0.7, uninstalled) to mattpocock/skills (34 active skills, installed globally via `npx skills add`).** Owner chose full switch after a researched comparison (superpowers = autonomous-run methodology; Pocock = low-ceremony workflow enforcement: `/grill-with-docs`, `/tdd`, `/diagnosing-bugs`, `/triage`, `/to-spec`, `/implement`; never run both routers). Ran `/setup-matt-pocock-skills`: repo config committed — `docs/agents/{issue-tracker,triage-labels,domain}.md` (GitHub Issues via `gh`, PRs NOT a triage surface, default 5 triage labels created lazily), root `CONTEXT-MAP.md` (multi-context: `backend/CONTEXT.md` + `frontend/CONTEXT.md` created lazily by `/domain-modeling`), `CLAUDE.md` §9 pointer section. Old skill names (`brainstorming`, `writing-plans`, `subagent-driven-development`) are GONE — use Pocock equivalents. Machine-level changes (installs) are not in the repo; reinstall superpowers anytime via `/plugin install superpowers@claude-plugins-official`. → [2026-07-09-session-skills-switch.md](./2026-07-09-session-skills-switch.md)

**Previous:** 2026-07-09 (action-panel transitions — ALL bugs DEVICE-CONFIRMED FIXED: iOS on 0.0.99, Android void on 0.0.100; PR #54 awaiting merge OK)

**Topic:** **Lower-action-panel keyboard bugs — all levers shipped in ONE build (branch `fix/action-panel-transitions`, PR #54, `33f060c`, 0.0.99) and DEPLOYED to the VPS, smoke ALL PASS; DEVICE A/B = MERGE GATE.** Executed the handoff §4 plan + two NEW user-reported bugs (BOTH platforms): chat-tap-with-panel keyboard close-then-bounce-back, and ping tile dismissing the keyboard. Shipped: **H1** (`keyboardVisible` ORs composer focus → ergonomic buffer never resizes the open panel mid-keyboard-animation), **H2** (vv listener setStates only on the inset>0 boolean flip), **H3** (action panel instant mount/unmount — SizeTransition/controller deleted, matches the emoji panel), **H5** (pre-arm resurrected from `699b671` behind the diag-overlay FLASH toggle, default OFF — the 0.0.92 probe never covered the panel+focus path), **bounce fix** (`dismissForChatSurfaceTap` unfocuses on iOS too; the keep-focus gate made the tap's DOM blur fight the focused framework node; 07-03 panel-survives contract preserved), **ping keyboard-neutral** (tile in `FocusGuardArea` for iOS + `onPingSent` post-frame refocus off iOS, gated on focus-at-pointer-down). E2E send dispatch and the DOM guard untouched; 0.0.93 Android panel-tap dismissal preserved (test-covered).

**Adversarial review found a REAL P1, fixed pre-deploy:** the H2 gate severed the only mechanism re-measuring the load-bearing focus-guard rects during the iOS pan (viewport repositions per vv event; composer subtree rebuild is skipped — identical widget instance) → first tap after every keyboard rise would have blurred (FG-OFF catastrophe), and FLASH-ON would have masked it, poisoning the A/B. Fix: `FocusGuardArea` listens to the shared inset source and re-measures post-frame per event (registration-only, no rebuilds). **P2 also fixed:** `lastKnownKeyboardInset` persisted mid-hide partials (progressive hide leaves e.g. 120px) → episode-max persistence; plausibly explains the 0.0.92 "FLASH unobservable" verdict. Verification: analyze clean, **full suite 609/609** (+8 new tests: 7 Tester-agent regressions + guard-tracking), post-deploy smoke ALL PASS (bundle sha `33f060c`). **OWED: owner device A/B per the dated summary's protocol (footer gate `0.0.99 · 33f060c` first); never merge PR #54 without explicit OK.** → [2026-07-09-session-action-panel-transitions.md](./2026-07-09-session-action-panel-transitions.md)

**Device A/B round 1 (owner, 0.0.99): iOS flash GONE + lower panel behaves as intended — field-confirmed. Android white void on keyboard hide REMAINED** — owner's screenshot timeline showed the composer pinned to the TOP of the band: Flutter layout settled, only the CANVAS short. Mechanism: installed-PWA OS window grows on keyboard hide, Chrome exposes the strip before the canvas covers it (flutter/flutter#179208); the strip renders the unstyled (white) host document. `interactive-widget` meta irrelevant (OS window resize) and untouched. **Round 2 fix (0.0.100 `79a3cf1`, DEPLOYED + smoke ALL PASS): paint the document** — static `html,body{background-color:#17181A}` in index.html (default dark scaffold; also kills first-paint white flash) + `utils/web_document_background{,_stub,_web}.dart` synced to the ACTIVE theme in `MaterialApp.builder`. Inert CSS only. Test parses live index.html vs `webDocumentBackgroundCss(RpgTheme.backgroundDarkGray)` (drift-proof). **CLOSED (owner, same day): Android void CONFIRMED GONE on 0.0.100 — all three bug classes field-verified fixed.** Remaining: owner merge decision on PR #54; note the H5 pre-arm shipped dormant (FLASH default OFF, never needed — flash died from H1/H2/H3) → candidate for deletion in a follow-up per the 0.0.93 no-dormant-machinery precedent.

**FULL CLOSURE (same day): owner OK'd + PR #54 MERGED to master (`2595085`), branch deleted. Follow-up landed on master (`5ab6eb8`, 0.0.101): the dormant H5 pre-arm DELETED FOR GOOD** (owner-approved; flash died from H1/H2/H3 alone — signals, persistence, pre-arm Listener/state machine, viewport fold, FLASH toggle all removed, −234 net lines; diag overlay back to readout-only; `FocusGuardArea` per-inset-event re-measure KEPT — load-bearing, independent). Verified: analyze clean, 91 tests green across input/inset/portrait/background suites. **Prod intentionally stays on the device-verified 0.0.100/`79a3cf1`** — 0.0.101 is behavior-invariant and rides the next release. Nothing owed.

**Previous:** 2026-07-08 (late wave: typeorm 1.0 + file_picker 11 — RELEASE 0.0.98 DEPLOYED, smoke ALL PASS)

**Topic:** **Second Dependabot wave (both MAJORS, both failed CI) migrated properly and released as 0.0.98 (`d8cf61c`).** **typeorm 0.3.30→1.0.0** (supersedes #52): official codemod fixed the only compile break (string-array `relations` → object form, 4 files/27 errors); audited EVERY high-level where/delete/update criteria for the new null/undefined-THROWS default — all DTO/JWT-validated or guarded (`replyToMessageId`) → kept the strict default (fail-safe: throws contained, never silently matches-all); source-verified semantics from installed package (`OrmUtils.normalizeWhereCriteria`; opt-outs `invalidWhereValuesBehavior.{null,undefined}` = ignore/sql-null). Validated: 407/407, dev backend on 1.0.0 in-container, harness 7/7 (incl. OTP UPDATE..RETURNING tuple path), staging prod-mode boot. **file_picker 8→11** (supersedes #53): `FilePicker.platform.pickFiles` → static `FilePicker.pickFiles` (1:1 params); DELETED the manual web-registration shim triple (init_file_picker_web/stub + main.dart call) — v11 is federated, and registration was PROVEN not assumed: fresh build's generated `web_plugin_registrant.dart:31` contains `FilePickerWeb.registerWith(registrar)` (socket3 lesson applied — mechanical evidence for the web path, since mocked tests/analyze can't catch MissingPluginException). Suite 601/601, web build green, harness 7/7. **Release:** staging rehearsal (0.0.98 prod image healthy + harness) → deploy-backend (0.0.98/d8cf61c) → deploy-web (clean full run — the earlier silent-halt did NOT recur, supporting the transient-lock diagnosis) → smoke ALL PASS. **Owner device check owed: attach + send a FILE in a chat on 0.0.98** (v11 web picker behavior is the one thing only a real device proves) + reopen PWA (footer 0.0.98 · d8cf61c). → [2026-07-08-session-deps-tier2-release.md](./2026-07-08-session-deps-tier2-release.md)

**Post-release review (Opus adversarial): correct, ZERO blocking.** Independently verified: codemod nested-relations correctness, 77 where-sites re-audited clean, LEFT→INNER join change moot (no nullable:false relations), raw query() tuple shape unchanged in 1.0, @nestjs/typeorm peer floor cleared, no dangling shim refs. One P3 (pre-existing, NOT patch-introduced): file_picker `cancelUploadOnWindowBlur` default can spuriously cancel iOS Safari picks when `change` lands >1s after refocus (large iCloud/HEIC) — user sees silent no-op; if field-observed, pass `cancelUploadOnWindowBlur: false` at chat_action_tiles.dart:128. Device check still owed: attach+send a FILE on live 0.0.98.

**CLOSED (owner device check):** file attach + send CONFIRMED WORKING on the live 0.0.98 PWA — file_picker 11 web path field-verified; release fully closed, nothing owed.

**Previous:** 2026-07-08 (dependency tier 2 + RELEASE 0.0.97 DEPLOYED — smoke ALL PASS)

**Topic:** **All 5 remaining Dependabot PRs closed out; 0.0.97 (`09b7098`) released to prod, backend + frontend, smoke-verified.** Merged: #46 typeorm 0.3.30 (wire-verified + prod-mode staging boot), #41 eslint 10 (baseline-compared: identical 966 lint problems on 9 AND 10 → neutral; NB `npm run lint` has been failing on master all along — pre-existing debt). Landed directly on master: **socket_io_client 2→3.1.6** (dependabot branch stale/conflicting) after real validation: frontend suite 601/601 + wire harness 7/7 vs dev AND vs prod-mode staging; #43 closed superseded. Rejected + dependabot-major-ignored: **#44 TS 7** (ERESOLVE: ts-jest@29.4.11 rejects TS7; trap: first "green" build/tests ran on leftover TS5.9 node_modules from the failed `npm ci` — verify what's INSTALLED), **#42 flutter_secure_storage 10** (upstream #1043 field-confirmed key loss on exactly 9→10, `resetOnError=true` default; holds mobile Signal keys; web unaffected → zero benefit, catastrophic tail risk). **Release:** deploy-backend (0.0.97/09b7098, health ok) + deploy-web (version.json 0.0.97) + `scripts/smoke` ALL PASS (bundle contains 09b7098; headless boot). **Deploy trap (root cause unproven, transient):** deploy-web.ps1 twice halted silently at the publish header (exit 21, nothing published) while the FILE intermittently read as missing across tools (git `D`, FileNotFound) then self-healed; not OneDrive/Defender/ssh; likely indexer-AV file lock during build IO. Rule: silent halt at publish header ⇒ publish did NOT run — re-run with `-SkipBuild` (~6 s). Hardened: line-112 ssh now exit-checked. Owner action: fully close+reopen PWA (footer 0.0.97 · 09b7098). → [2026-07-08-session-deps-tier2-release.md](./2026-07-08-session-deps-tier2-release.md)

**Post-release review caveat (Opus adversarial, P2 — monitor, not hotfix):** ALL socket_io_client-3 validation (601 tests + harness ×4) ran on the Dart VM = the **dart:io connector**; socket.io 3.1.5 rewrote the WS transport (`package:web_socket`) with a SEPARATE **browser connector** — the path 100% of prod web/iOS-PWA users actually run — and NO test exercised it (smoke's headless boot never opens a socket). Exposed risk: browser reconnection under iOS suspend/resume (ConnectionProvider resume probe — historically the most incident-prone path, 0.0.54/0.0.58). Owner asked to device-check live messaging + a background/resume cycle on 0.0.97; if anything's off or permanent coverage is wanted, build the browser-connector probe (dart2js page driving real SocketService in Chromium, CDP net-drop → reconnect → assert delivery). Otherwise: reviewer confirmed socket_service.dart is 3.1.6-API-compatible, webcrypto pin+scoped ignore keeps security PRs alive, deploy-web guard correct — zero blocking.

**CLOSED (owner device check, same day):** live messaging both directions + background/resume cycle CONFIRMED WORKING on the real iOS PWA at 0.0.97/09b7098 — the browser connector + resume path is field-verified; socket_io_client 3 migration fully closed. Browser-connector probe stays optional backlog (build only before the NEXT transport-touching bump).

**Previous:** 2026-07-08 (staging dress-rehearsal stack — MERGED as PR #48; then Dependabot safe-tier merges + webcrypto pin)

**Topic:** **Local prod dress rehearsal shipped (wishlist #3, last open item): `staging.ps1` + `docker-compose.staging.yml` + `.env.staging.example`.** Boots the REAL prod compose (built image, `NODE_ENV=production`, `synchronize:OFF`, restricted CORS) isolated as project `fireplace-staging` (backend `:3100`, db `:5533`, own volumes, dummy secrets — NEVER the real `JWT_SECRET`). **A GATE, not a ritual** (per review): rehearse only deploys touching entities / manual SQL / prod compose / Dockerfile / bootstrap code; non-goals nginx/TLS, host perms, devices. Commands: `up`, `seed-schema` (one-shot dev-boot creates schema from entities → flips back to prod), `restore <dump[.gpg]>` (throwaway-container gpg, passphrase prompted never stored), `sql` (`ON_ERROR_STOP` + `lock_timeout=10s`), `harness` (wire harness vs `:3100`), `status/down/destroy`. **Verified: seed-schema 11 tables → prod-mode healthy → wire harness 7/7 against the prod-mode stack — first ever full-E2E run under production config off the VPS**; `sql` smoke green. `restore` gpg path awaits one owner-passphrase run. Traps: PS 5.1 curly-quote parse break on BOM-less UTF-8 em-dashes (script stays ASCII); `docker cp` file→existing-dir nesting (pre-clean `rm -rf`). Docs: CLAUDE.md §6 gate rule + §8 rehearse-first. → [2026-07-08-session-staging-stack.md](./2026-07-08-session-staging-stack.md)

**Closure (same day):** PR #48 reviewed independently (Opus, adversarial): merge-ready, 0 blocking; 5/6 findings applied (`ddf88b0` — headline: `restore` defensively clears `STAGING_NODE_ENV` so a leaked dev flag can never auto-DDL restored prod data; exact `restore-db.sh` mirror; UTF-8 passphrase pipe; own `fireplace-staging-backend:latest` image tag; compose≥2.24 preflight); full lifecycle re-verified; owner merged. **Dependabot wave (from same-day CodeQL/Dependabot enablement): merged safe tier #37/#38 (actions), #39 (22 backend minor/patch), #40 (5 frontend incl. libsignal 0.7.4→0.8.2), #45 (form-data), #49 (ws/engine.io/socket.io-adapter), #50 (grpc-js), #51 (qs). #47 (multer/platform-express security) auto-closed by dependabot: "dependencies are up-to-date now" — absorbed by #39. #49 + libsignal 0.8.2 validated on the REAL WIRE: dev backend recreated on merged master, in-container ws 8.21.0/engine.io 6.6.9 confirmed, harness 7/7.** CodeQL java-kotlin/swift failures self-resolved (config already trimmed to js-ts+actions at 19:37; Dart unsupported by CodeQL). **TRAP + pin (`e2365c9`): #40's webcrypto 0.6.0→0.6.1 native-assets hook hard-requires CMake — absent on the PC → ALL local `flutter test` broke (+ deploy-web.ps1 build risk; CI green only because ubuntu ships cmake); winget install blocked (UAC/no user-scope installer). PINNED webcrypto=0.6.0; unpin after owner installs CMake in an admin terminal.** Remaining PRs (deliberate): #46 typeorm patch (staging-rehearsal candidate), #44 typescript 5.9→7.0 + #41 eslint 10 (dev-dep majors), **#42 flutter_secure_storage 9→10 (DANGER: Signal key storage — migration bug = irrecoverable key loss) and #43 socket_io_client 2→3 (DANGER: the wire) — dedicated sessions with harness + key-persistence checks.** Prod backend NOT redeployed yet — deploy once after the remaining backend tier lands.

**Previous:** 2026-07-08 (agent instruction consolidation — MERGED to master `080d660` + backend DEPLOYED; earlier same day: OMP tooling cleanup + Dart/TS LSP)

**Topic:** **Instruction-docs mess cleaned — `CLAUDE.md` is now the single source of truth (branch `docs/instruction-cleanup`, commit `d461b0e`, PR #36, docs-only, no version bump).** Before: same facts at 3 compression levels (`AGENTS.md` ~183 lines, root `CLAUDE.md`, 6 `.cursor/rules/*.mdc`) drifting independently. After: `AGENTS.md` → 22-line universal bootstrap (Codex/Cursor/OMP all land on CLAUDE.md); CLAUDE.md absorbed the unique nuggets (subagent-context rule, full commit/branch-deploy policy + branch-test-before-merge protocol, VM log grep tip, PWA cache-bust nuance, "+1 = PATCH" version interpretation, Android versionCode note, deploy-runbook pointer, test-count maintenance); deleted 4 alwaysApply rules duplicated verbatim (`version-bump`, `code-in-english`, `session-summaries`, `graphify`); kept `On-every-check.mdc` (Cursor loader) + `production-vm-deploy.mdc` (runbook, now pointed to from §4). `pubspec.yaml` version comment redirected. Verifier green (407/42). Git incident: concurrent `checkout master && pull` mid-session put the commit on local master → repaired (branch -f, master reset to origin, FF push; origin/master never touched). **PR #36 awaits user merge OK.** Same day earlier: OMP harness cleanup (context7 MCP dedup, node_repl disabled, generate_image call-blocked, Dart+TS language servers wired + verified). → [2026-07-08-session-docs-consolidation.md](./2026-07-08-session-docs-consolidation.md) · [2026-07-08-session-agent-tooling.md](./2026-07-08-session-agent-tooling.md)

**Closure (same day):** owner merged PR #36 (`080d660`) and PR #35; both branches deleted (remote + local). **Backend deployed on the VPS via `./deploy-backend.sh` — public `/version` = `0.0.96 / 080d660`, `/health` ok** — the OTP serve fix is LIVE. Frontend intentionally NOT redeployed (zero runtime change; harness isn't compiled into the app), so the Settings footer stays `0.0.95` until the next real frontend release — cosmetic drift vs backend `/version`, self-heals.

**Previous:** 2026-07-08 (E2E wire harness + OTP serve fix, 0.0.96 — branch `feat/e2e-wire-harness`, MERGED as PR #35)

**Topic:** **New full-stack two-account E2E Signal wire harness (`frontend/test_e2e/`, run: `docker-compose up` → `cd frontend && flutter test test_e2e`; NEVER in the default suite/CI) — and it found a REAL production bug on its first run.** Two headless clients run the app's real `ApiService`+`SocketService`+`EncryptionService` (real libsignal) against the local backend: register → WS key upload → friendship → conversation → PreKey(3:)/whisper(2:) round trips → mid-conversation session rebuild → edit accept/reject → reactions, asserting decryption + payload shapes both ends. Fresh accounts every run BY DESIGN (server serves stale unused OTPs oldest-first; reuse ⇒ phantom bad-MAC). **The bug:** `key-bundles.service.ts` `fetchPreKeyBundle` misdestructured Postgres `UPDATE…RETURNING` (`[rows, rowCount]` trap, CLAUDE §4) → **every bundle ever served had `oneTimePreKeyId: null` while burning the OTP `used=true`** — X3DH always ran without OTP forward secrecy; unit specs never caught it because their mocks encoded the wrong rows-only shape (now fixed). 7/7 harness green ×2, backend 407/407, analyze clean. **OWED: PR review/merge OK + `./deploy-backend.sh` on the VPS after merge.** Flagged (NOT fixed): identity-regeneration leaves stale OTPs server-side → purge-on-reupload candidate. → [2026-07-08-session-e2e-wire-harness.md](./2026-07-08-session-e2e-wire-harness.md)

**Previous:** 2026-07-08 (GCP → OVH production migration — EXECUTED, DNS cut over)

**Topic:** **Production moved to OVH VPS-1 `vps-53b896a3` @ 51.68.138.13 (Warszawa, ubuntu@, key-only SSH) with ZERO data loss.** Full runbook: target hardened (ufw/swap/Docker), `.env`+passphrase+`/etc/letsencrypt` moved machine-to-machine (sha256/fingerprint-verified), backend+frontend 0.0.95/283ecb7 staged and owner-smoke-tested via hosts override (live E2E text/emote/image/ping/GIF between fresh accounts), cutover = GCP backend freeze → final delta restore (parity exact; 6-message delta = expired-by-design, SQL-verified) → GCP nginx **relay → VPS** for stale DNS (TTL 3600) → owner flipped FreeDNS. Nightly encrypted backup cron NOW LIVE on the VPS (04:00, live-fire + `PGDMP` probe — never existed on GCP). Traps fixed: fresh pgdata vs `.env` DB_PASS (`ALTE…

**Previous:** 2026-07-07/08 (E2E cross-race fix — MERGED, DEPLOYED, field-confirmed closed)

**Topic:** **E2E "[Decryption failed]" — the post-session fleet reports were a REAL surviving race; root-caused, deterministically reproduced, FIXED (0.0.94, branch `fix/e2e-encrypt-decrypt-cross-race`, PR owed).** Round 7's "not a code regression" was incomplete: 0.0.90 serialized encrypt-vs-encrypt only, while `EncryptionService.decrypt`/`buildSession`/`deleteSession` had NO service-level lock — an encrypt racing a decrypt on the SAME SessionRecord lost one store (decrypt's stale write-back rolled the sender chain back) → next encrypt reused a chain counter → receiver `DuplicateMessageException ('old counter')` → permanent `[Decryption failed]` on brand-new messages. Reproduced with a gated SessionStore (`debugWrapSessionStore` seam): pre-fix fails with the verbatim field error; naive concurrent probes DON'T collide (deterministic microtask phase — why the prior probe found nothing). Fix: ONE per-peer tail queue `_sessionTails` over all four session mutators (leaf-level, non-reentrant — documented). Timeline: race is ancient; PR #30 removed the accidental send stagger (plaintext preview POST) + note bursts + resume churn surfaced it. Verified: gated probe red→green, send-race probe still green, full suite 601/601, analyze clean. HONEST SCOPE: already-branded rows are cryptographically unrecoverable (resend); regeneration guard + persistence test still open; post-deploy, confirm via an affected device's E2ePersistentDiag dump. → [2026-07-07-session-e2e-cross-race-fix.md](./2026-07-07-session-e2e-cross-race-fix.md)

**Closure (07-08):** **Merged as PR #33 (`fac70f4`), deployed; prod `/version.json` = 0.0.94 (tree-verified identical to built commit `585c96d`); INCIDENT CLOSED with field confirmation.** First post-deploy report classified via the new runbook (`docs/runbooks/e2e-decryption-failed.md`, committed same branch): friend's (user 54) durable dump = ONE entry, msg 14149 `kind:duplicate, isHistory:true` — DB `createdAt` 20:46 CEST 07-07, ~5h BEFORE the fix existed (commit 01:45 CEST 07-08); friend confirmed content was NEVER visible → **pre-fix sender-side wire damage (H1)**, NOT the 3C persist-gap (3C stays theoretical, zero field evidence — build the decrypted-once marker only if a duplicate hits a message the receiver had read, or anything sent after 07-08 02:00 CEST). Post-deploy telemetry clean: two overlapping history passes + resume double-reconnect churn, zero new failures; later pre-fix messages (14154/14166/14177) decrypt fine — intermittent-lost-update signature, self-healed. Also verified: `feat/metadata-privacy-hardening` NOT in master / never deployed → provably unrelated to the incident; E2E-adjacent hunks are log-level-only; safe to merge later (bump its stale 0.0.91 pubspec; requires backend deploy).

**Previous:** 2026-07-07 (composer keyboard/viewport)

**Topic:** **Composer keyboard/viewport review → device-verified fixes on `refactor/composer-keyboard-viewport` (0.0.93 DEPLOYED to prod, PR #32 — awaiting merge OK).** Fresh 3-agent audit: trunk coherent, NOT a rewrite; real defect class = no single keyboard-visibility truth (3 consumers trusted `MediaQuery.viewInsets`, which reads 0 on iOS WebKit). Shipped: `sharedKeyboardInsetSource()` singleton + pure `keyboard_inset_math.dart` (first vv-arithmetic tests); **D2/D3/D4** consumer fixes; **Android PWA bug** chat-tap-with-lower-panel now dismisses keyboard (device-CONFIRMED; iOS keeps 07-03 keep-focus gate pending user ruling). **Device-probe verdicts (0.0.92 build):** DOM focus guard **LOAD-BEARING** (FG-OFF → every send tap dismissed keyboard; documented in frontend/CLAUDE.md).

**Previous:** 2026-07-07 (metadata privacy — quick wins; **07-08: PR #34 MERGED (`cf6c51c`) + backend DEPLOYED + frontend 0.0.95/`28cd685` deployed by owner — public `/version` 0.0.95/cf6c51c, `/health` ok, `Web Push VAPID initialized`.** **Push QA PASSED (owner-confirmed):** app-closed cards on iOS + Android PWA still show sender name — BY DESIGN: web-push body is RFC 8291 encrypted to the device; only the cleartext relay-visible `topic` header was dropped. Finding: `FIREBASE_SERVICE_ACCOUNT` unset on VM → FCM push ALWAYS disabled in prod (pre-existing; web push is the only live push path). Possible future feature (owner aware, NOT requested): lock-screen privacy toggle — generic "New message" cards, SW-side setting, no payload change. **STILL OWED: `setup-backup-cron.sh` on the VM with an off-VM passphrase** — the single biggest open risk)

**Topic:** **Metadata-privacy quick wins shipped (branch `feat/metadata-privacy-hardening`, commit `6015b6b`, pushed; PR + backend deploy OWED, NOT merged).** Implemented top-3 from the same-day audit; no content-E2E change; frontend bundle unchanged. **(1) Content-free push:** FCM `data` now only `{type,conversationId}` — dropped `senderName`+unread counts that transited **Google readable** (Android handler never showed senderName → zero UX change); Web Push dropped the `conv-<id>` `topic` header (**cleartext to relay**), body stays encrypted so senderName/counts remain private. **(2) Log min:** per-message/push/key logs `log`→`debug` (prod-silent), stripped raw endpoint from a warn, `docker-compose.prod.yml` json-file caps (10m×3, were unbounded). **(3) Backups:** new `setup-backup-cron.sh` (gpg passphrase 0600 off-argv + daily cron) closes the "never enabled on VM" gap. Docs: push contract §9 + log note + backup helper §6, count 405→407. **Verified:** backend build clean, `npm test` **407/42 green** (+2 push-privacy regressions via Tester agent), verifier OK, graphify updated. Audit report stays local (gitignored). **Owner next (AGENTS.md: test branch BEFORE merge):** on VM `git fetch origin && git checkout feat/metadata-privacy-hardening && ./deploy-backend.sh` → verify `/version` (0.0.91) + `/health` → device-QA push (still wakes, deep-links; generic FCM body / encrypted web-push body) → ONLY THEN PR + merge with your OK; separately run `setup-backup-cron.sh` on the VM w/ your off-box passphrase. → [2026-07-07-session-metadata-privacy-quickwins.md](./2026-07-07-session-metadata-privacy-quickwins.md)

**Previous:** 2026-07-07 (metadata privacy audit)

**Topic:** **Metadata privacy audit + hardening roadmap (research only, NO code).** Content E2E out of scope; audited what the server/breach/seizure/backup/Google-Apple can see about who-talks-to-whom, when, sizes, contact graph, IPs, push tokens. File:line-verified leak inventory + Signal/SimpleX/Threema/Matrix comparison (sourced) + feasibility-ranked roadmap, all in gitignored `docs/audit/2026-07-07-metadata-privacy-audit.md` (**DO NOT COMMIT** — public repo). Threat model: single self-hosted server, operator=owner → real adversaries are **breach/seizure/backup/third-party push**, not "the operator" (sealed sender rejected as theater here). Findings: DB persists full contact graph + per-msg sender/conv/createdAt (indexed) + deliveryStatus/editedAt/expiresAt + messageType/mediaUrl/duration + ciphertext length (no padding) + **reactions=emoji+userIds cleartext**; prod logs leak recipient id + online-roster dump per msg; docker-compose.prod has **no log rotation**; **FCM payload carries senderName+conversationId readable by Google**, web-push `conv-<id>` topic cleartext to relay; pushClientState reports on-screen conv. Good: no lastSeen/presence-fanout, exact-match search (no enum), UUID+encrypted blobs, hashed refresh tokens. Top-5: (1) enable encrypted backups ~1h, (2) log min + retention caps 2-4h, (3) content-free push 3-6h, (4) opt-in receipts/typing + purge rejected requests 1-2d, (5) pad ciphertext buckets 2-4d; big track = ephemeral-by-default (delete-on-delivery). 6 owner questions batched at report end. → [2026-07-07-session-metadata-privacy-audit.md](./2026-07-07-session-metadata-privacy-audit.md)

**Previous:** 2026-07-07 (round 5, same session)

**Topic:** **E2E send-race — "burst of notes → [Decryption failed]" root-caused, reproduced, FIXED (0.0.90, branch `fix/e2e-send-race`, PR owed).** Field: 2 of ~10 rapid anti-quantum-note sends arrived terminally undecryptable. Deterministic repro with real libsignal both sides: `EncryptionService.encrypt` had **no per-recipient serialization** — concurrent encrypts interleave at store awaits, load the same ratchet state, emit **duplicate chain counters** → receiver `DuplicateMessageException` on 9/10 concurrent sends (sequential control 10/10). Not note-specific; notes just send tightest (no preview fetch). Fix: tail-chained per-recipient queue in `encrypt()`; burst now 10/10. New regression `encryption_send_race_probe_test.dart`; roundtrip/send-path suites green; analyze clean; invariant in frontend/CLAUDE.md §5. Notes were never lost — ciphertext stayed server-side; sender can resend links. **Owner: PR + merge `fix/e2e-send-race`, then deploy-web.ps1 (frontend-only fix; backend deploy still owed for quantum-note `c105983`).**

**Previous:** 2026-07-07

**Topic:** **Quantum Note — root cause + full feature pass (0.0.89, PR #30 MERGED to master `c105983`).** Reveal **never worked** (raw SQL `expires_at` vs `"expiresAt"` 42703 + TypeORM DELETE `[rows,rowCount]` shape) — fixed, plus invisible `#error`, `no-store`, exit links, and the web preview-proxy **plaintext+`#key` leak** (now fragment-stripped first URL only). Review: **merge-ready, 0 blocking**; both findings applied (key validated BEFORE destructive POST — corrupt fragment can't burn; `^[0-9a-f]{32}$` token gate). **Banner card** replaces the raw URL in chat. Fragment protocol `#key[&c=conv][&e=expiryMs]` (server-blind) → "← Open Fireplace" deep-links into the source chat via `/?notify_conv=`; banner shows live **self-destruct countdown** + grey **destroyed** flip at exact note death; note TTL **overrides** the chat disappearing timer; TTL set **1h/6h/12h/24h**; PL truncation fixed; Privacy & Safety explainer (en/pl); legacy links compatible. Owner device-tested pre-merge on prod (branch deploy) — all confirmed working. Backend **405/42**, frontend 47 note tests, analyze clean. **Deploy owed: VM `git checkout master && ./deploy-backend.sh` (verify `/version` = c105983-era SHA, not dev) + PC `.\deploy-web.ps1` (0.0.89).** → [2026-07-07-session-quantum-note-root-cause.md](./2026-07-07-session-quantum-note-root-cause.md)

**Previous:** 2026-07-06

**Topic:** **Hetzner migration prep — guided pre-migration checklist.** Verified GCP SSH access, data sizing (pgdata 65 MB + media 17 MB → NO Hetzner Volume needed), CX22→**CX23** pricing (€5.49 + €0.71 IPv4 ≈ €6.20/mo excl. VAT), generated PC ed25519 key (`SHA256:DMwr…XjM`), found backups NEVER configured + **gpg missing on the VM** (installed gnupg 2.4.4, user-approved), full backup dry-run green (gpg AES256 artifacts, `PGDMP` decrypt roundtrip, byte-exact scp to PC, test artifacts deleted). **NO-GO** until user: Hetzner 2FA+project, pubkey upload, FreeDNS edit-access proof, real passphrase → real backup. Resume: `.planning/hetzner-migration-prep/findings.md` → [2026-07-06-session-hetzner-prep.md](./2026-07-06-session-hetzner-prep.md)

**UPDATE (same day):** Hetzner **banned the account** at signup (risk-scoring, no reason, re-registration blocked) — all Hetzner-specific prep obsolete; provider-agnostic prep (ed25519 key, sizing, backup mechanics, FreeDNS gate) survives. Alternatives researched: **OVH (recommended, Warsaw DC, VPS-1 ~8 GB ≈ €6-8.5/mo)**, netcup (€5.91/mo 12-mo), Contabo (€4.99 but mandatory KYC). Awaiting provider choice. Pivot details: `.planning/hetzner-migration-prep/findings.md`.

**FINAL (2026-07-08 01:19 UTC): GO.** User pivoted to **OVH**: account + 2FA + SSH key `lentach-pc`, **VPS-1 2027 (2c/4GB/40GB, Warsaw, month-to-month 23,62 zł brutto) PAID** — delivery pending; first runbook step = panel Reinstall with Ubuntu 24.04 + key (order flow had no key dropdown; caught preselected 26.04 → corrected). FreeDNS edit access screenshot-proven. Backup passphrase set after a paste-mangling saga incl. **one passphrase leaked into chat → rotated** (32-char openssl, in password manager); real `backup-db.sh` → 3 gpg artifacts decrypt-probed (`PGDMP`) + downloaded to PC `%USERPROFILE%\fireplace-backups\`. Exit-1 scare investigated: script `SCRIPT_RC=0`, blame plink transport. **Migration runbook may start** (re-map Hetzner console steps to OVH).

**BOX LIVE (2026-07-08 ~02:05 UTC) — HANDOFF READY.** VPS delivered + reinstalled + verified: `vps-53b896a3` @ **51.68.138.13**, Ubuntu 24.04.4 x86_64, Warsaw, key-only SSH + sudo green (OVH gotcha: image ships `ubuntu` with an expired temp password blocking BatchMode even after key auth — user cleared it via one interactive login with the emailed secret). **Fresh agent: read `.planning/hetzner-migration-prep/findings.md` — HANDOFF block at top — then execute the migration runbook prompt** (GCP → this box; DNS cutover last).

**Previous:** 2026-07-06

**Topic:** **Emote composer: native-emoji verdict + send-scroll + panel-swap animation (0.0.88).** On `fix/emote-composer-scroll-anim`. **A (native per-platform emoji): DROPPED as impossible** — no bundled font exists; Flutter web CanvasKit/Skwasm always rasterizes its own downloaded Noto Color Emoji (HTML renderer removed in 3.29; upstream flutter#166769/#157918 open; only "workarounds" are Apple's proprietary ~180MB font or DOM platform-views). **B:** emote-panel sends didn't scroll — text sends only *appeared* to scroll via the keyboard-open `didChangeMetrics` path; new pure `utils/chat_auto_scroll.dart`: own sends ALWAYS scroll, peer messages keep near-bottom+badge. **C:** keyboard→emote-panel switch dropped the panel from the top — composer block anchored at `bottom: _keyboardInset` mounts the panel while the inset is still ~300px, then rides it down; new `composerBottomPanelPinned` notifier (synced via single mutation point `_setEmojiPickerVisible`, deferred reset in dispose to dodge the locked-tree assert) pins the composer at `bottom: 0` + derives list padding from the same effective inset → panel occupies the keyboard's space from frame one, the dismissing keyboard reveals it. analyze clean; 69 input-suite tests green (8 new). PR open, NOT merged; device QA owed. → [2026-07-06-session.md](./2026-07-06-session.md)

**Previous:** 2026-07-05 — **Emote/emoji rendering — Telegram parity (0.0.87).** Fixed 4 emoji-message bugs on `fix/emote-message-layout`. **P1+P2:** the emoji-only bubble branch laid the emote + `MessageMetadataRow` side-by-side in a `Wrap`, shoving lone emotes toward center; replaced with a side-aligned `Column` so indicators stack UNDER the emote for all counts. **P3:** jumbo tiers retuned `1→82,2→78,3→64,4→52,5+→44`. **P4:** new `buildInlineEmojiSpans`/`isEmojiGrapheme` + `kInlineEmojiFontSize=18` enlarge inline emoji in mixed text (text/URLs stay 14px), mirrored in the context-menu replica. Follow-ups (same session): reply-to-emote bubble stretch (greedy `Align` removed → bubbles hug content) + persisted iOS sent-emote indicators (theme+bubbleless color bug → on-surface metadata color + `MessageMetadataRow.onChatSurface`). Merged to master (`d111f9b` + cherry-pick `9e58fe1`), deployed. → [2026-07-05-session.md](./2026-07-05-session.md)

**Previous:** 2026-07-04 — **Sticky sessions auth hardening (0.0.86).** Root-caused persistent spontaneous logouts to backend refresh-token single-use rotation; switched refresh sessions to stable 365-day sliding opaque tokens (SHA-256 stored), added `[auth-session-end]` logs + AuthGate restore spinner, cleared local auth after password reset, documented persisted `JWT_SECRET`, bumped `0.0.86`. → [2026-07-04-session-sticky-sessions.md](./2026-07-04-session-sticky-sessions.md)

**Previous:** 2026-07-02 — **Frontend audit gap fill.** Closed the 2026-07-01 frontend audit gaps with 32 high-signal tests; analyze clean; frontend suite green in that session.

**Previous:** 2026-07-01 — **Frontend test suite audit (resumed after window crash).** Recovered state from `.planning/frontend-test-audit/`; both session-1 "duplication" suspects cleared (test-runner progress-reporter artifact, not real duplication). 4 parallel agents swept all 82 test files: deleted 6 files that tested nothing real (stub-returnsNormally, test-local reimplementations, tight-constraint tautology, constant-equals-itself), pruned 5 in-file dead tests, fixed a vacuous friends_provider clear-state test + stale BUG comment. Filled the headline gap: `encryption_service_roundtrip_test.dart` — both parties real `EncryptionService` over real libsignal, asserting 3:→2: type transition, ratchet advance, utf8/emoji. Analyze clean; **402 tests green** (422 − 24 pruned + 4 new). → [2026-07-01-session-frontend-suite-audit.md](./2026-07-01-session-frontend-suite-audit.md)

**Previous:** 2026-07-01 — **Backend test suite audit.** Audited every backend unit spec plus stale e2e boilerplate; deleted only the useless `/` Hello World e2e corpse and its dead `test:e2e` config/script; added 18 critical-path unit tests for auth/session, media REST guard/limit/path validation, focused-recipient push skip, unread query filters, and mapper metadata. Backend Jest is now 346 tests / 41 suites; verifier, full suite, graphify update, and code review passed. → [2026-07-01-session-backend-suite-audit.md](./2026-07-01-session-backend-suite-audit.md)

**Previous:** 2026-07-01 — **Production readiness audit — NOT READY.** Audited `docs/claude-rebuild` at `8fb8624`; local verification is green (`flutter analyze --no-fatal-infos`, full `flutter test` = 421 tests, backend build, backend Jest = 328 tests/41 suites, CLAUDE test-count verifier). Created visible report `docs/review/2026-07-01-production-readiness-audit.md`. Public prod checks are the blocker: `/health` is OK, but backend `/version` still reports stale fallback metadata (`0.0.2`, `dev`, empty build time) and frontend `/version.json` reports `0.0.76` while the audited branch is `0.0.75`; no deploy was performed. Verdict: do not claim production-ready until backend/frontend deploy freshness and live E2E/push/media smoke evidence are proven. → [2026-07-01-session-production-readiness.md](./2026-07-01-session-production-readiness.md)

**Previous:** 2026-07-01 — **Production readiness audit — NOT READY.** Audited `docs/claude-rebuild` at `8fb8624`; local verification is green (`flutter analyze --no-fatal-infos`, full `flutter test` = 421 tests, backend build, backend Jest = 328 tests/41 suites, CLAUDE test-count verifier). Created visible report `docs/review/2026-07-01-production-readiness-audit.md`. Public prod checks are the blocker: `/health` is OK, but backend `/version` still reports stale fallback metadata (`0.0.2`, `dev`, empty build time) and frontend `/version.json` reports `0.0.76` while the audited branch is `0.0.75`; no deploy was performed. Verdict: do not claim production-ready until backend/frontend deploy freshness and live E2E/push/media smoke evidence are proven. → [2026-07-01-session-production-readiness.md](./2026-07-01-session-production-readiness.md)

**Earlier:** 2026-07-01 — **Production cleanup review.** Reviewed recent frontend/backend/scripts/docs churn and deleted only confirmed garbage: obsolete frontend root widget re-export shims, unused backend `RefreshTokensService.deleteExpired()`, stale Cloudinary-only media wording, broken root manual JS flow scripts plus their root npm manifest/lockfile, and stale manual E2E docs. Hardened `scripts/verify-claude-backend-test-counts.mjs`; verification passed; no version bump. → [2026-07-01-session-production-cleanup.md](./2026-07-01-session-production-cleanup.md)

**Earlier:** 2026-07-01 — **CLAUDE.md split rebuild.** Rebuilt the repo instruction split from current source; added explicit Codex bootstrap rule in `AGENTS.md`; verifier passed (`328 tests, 41 suites`). → [2026-07-01-session-claude-rebuild.md](./2026-07-01-session-claude-rebuild.md)

**Earlier:** 2026-06-29 — **Chat entry split-screen lag fix (0.0.76).** Investigated the screenshot: old conversations tab + bottom nav were visible under a partially slid-in `ChatDetailScreen`, matching a frozen/default iOS-style `MaterialPageRoute` transition rather than desktop split layout. Added `utils/instant_opaque_route.dart` and switched mobile/chat-entry paths in conversations, contacts, and notification deep-link navigation to opaque zero-duration routes. Added regression `test/utils/instant_opaque_route_test.dart`; route test, notification nav test, and `flutter analyze` passed; `ChatRouteReview` found no blocking issues; ran `graphify update .`. Commit `d6bf1c0` pushed to `origin/fix/chat-entry-instant-route`; PR creation blocked by missing `GH_TOKEN`. → [2026-06-29-session.md](./2026-06-29-session.md)

**Previous:** 2026-06-28

**Topic:** **Auto-review model memory.** Added root memory in `AGENTS.md` and `CLAUDE.md`: auto reviewers, code-reviewers, review subagents, and PR reviewers must use the same model class as the primary agent for the session. No `mini`, cheap, or lower-capability reviewer unless the user explicitly asks for that downgrade. Docs-only change; no tests needed. → [2026-06-28-session.md](./2026-06-28-session.md)

**Previous:** 2026-06-28

**Topic:** **Memory workflow update + message-editing PR triage + freshness audit.** Added explicit root-memory rules in `AGENTS.md` and `CLAUDE.md`: use persistent file-backed planning for real multi-step work. Checked stale message-editing note: PR #16 is merged into `master`, not pending. Freshness audit verdict: local `master`/`origin/master` are current at `6847ac6`, local frontend build is `0.0.75`, open PRs = 0. Production is NOT newest: VM checkout is behind `origin/master` by 4 commits, VM `frontend-build/version.json` is `0.0.74`, and backend is running dev mode (`NODE_ENV=development`, `npm run start:dev`, `/version`=`0.0.2/dev`). DB has `messages.editedAt`. To make prod current: backend on VM via `./deploy-backend.sh`; frontend from PC via `.\deploy-web.ps1`; never build Flutter web on the 2 GB VM. → [2026-06-28-session.md](./2026-06-28-session.md)

**Previous:** 2026-06-25

**Topic:** **iOS composer focus "flash" — post-0.0.68 attempts REVERTED to the jump→flash state (0.0.73).** Per user ("revert all changes from the jump→flash point"), rolled back the 0.0.69 pointer-down pre-arm, the 0.0.70–0.0.71 cosmetic focus mask, and the 0.0.72 font-16/rAF probe to the **0.0.68 viewport-pin** state. They regressed without killing the flash: the pre-arm fired the pin's `position:fixed` on pointer-DOWN → in a **browser tab** that bounced the browser toolbar on every composer tap; the mask + stacked blur-teardown made keyboard-**hide** laggy. **Kept:** the pin (jump→flash), `onTapOutside`, collapse-guard, portrait guard, inset. Deleted `utils/web_ios_composer_focus_mask{,_web,_stub}.dart` + test. Analyze clean; 44 input tests green (E2E send-flow intact). **Still OPEN:** the residual <1s focus flash (iOS compositor pan to Flutter's below-fold `<textarea>`) — solve non-invasively, NOT host `position:fixed`; see `docs/review/ios-composer-keyboard-flash-handoff.md`. `font-16` (iOS auto-zooms inputs <16px on focus) is a safe re-addable 1-liner. **Next:** deploy `0.0.73`, confirm no toolbar-bounce / no laggy hide.

**Previous:** 2026-06-24 — **Composer keyboard/focus/viewport — audit → fixes → DEVICE-CONFIRMED → cleanup (0.0.65).** Audited the most band-aided subsystem (`docs/review/composer-keyboard-audit.md`), shipped targeted fixes on `review/frontend-prod-readiness`, tested live on prod via `deploy-web.ps1`, iterated to the real root causes — **NO E2E send-path change** (SEND_START/ENCRYPT/EMIT tests green throughout). **Device-confirmed:** **B** laggy keyboard-hide gap — `ChatComposerViewport` collapse-debounce now defers ONLY when `composerKeyboardCollapseGuard` is armed (send/edit/staged/action-toggle, 600ms); a genuine dismiss collapses immediately. **C** in-app Send keyboard bounce — REAL cause was Flutter `EditableText`'s default `onTapOutside` unfocusing on any tap outside …

**Previous:** 2026-06-22 — **Message editing — design → plan → implementation → review (0.0.62).** Text-only message editing for the E2E messenger, delivered end-to-end (spec → plan → impl → code-review → fixes). An edit is a **NEW ciphertext over the existing Signal session**; the server (sender-only, 15-min window, text-only, blind) swaps `encryptedContent` + stamps `editedAt` → broadcasts `messageEdited`, rejects via `editMessageFailed`. Recipient re-decrypts (live/deferred; offline reconnect via an `editedAt`-stamped plaintext cache); sender optimistic + full revert on reject. Branch `feat/message-editing` — PR open, NOT merged; device QA + prod SQL (`ALTER TABLE messages ADD COLUMN "editedAt" timestamp NULL;`) owed. → [2026-06-22-session-message-editing.md](./2026-06-22-session-message-editing.md)

**Previous:** 2026-06-22 — **Production readiness — cutover live + media fix + backups + backend release blockers.** Prod was silently running in **dev mode** (`/version`=`0.0.2/dev`, no rate limiting, TypeORM auto-DDL on live data). Took it to a verified, hardened, **live** prod backend at **`0.0.61 / b055724`**. Shipped (all merged, deployed): healthcheck IPv4 fix (`e74be18`); production deploy path (`docker-compose.prod.yml` + `deploy-backend.sh`, truthful `/version`, latent `dist/main.js` entrypoint fix); media serving decoupled from `NODE_ENV` (`7e1c196`); **backups** PR#14 (`backup-db.sh`/`restore-db.sh`/`setup-backup-bucket.sh` — passphrase-file, encrypted `.env`, **atomic restore** w/ `--single-transaction` + guaranteed restart, E2E-safe); **backend release blockers** PR#13 (global `HttpThrottlerGuard` w/ X-Real-IP — HTTP throttling was **inert**; Secret Notes CSP nonce; WS `passwordChangedAt`; `messageType` validation; loopback port binding; 316 tests); uninstall-warning PR#15 (`0.0.61`). **Verified on VM:** schema complete under `synchronize:OFF` (refresh_tokens, secret_notes, pinned*, passwordChangedAt, idx_messages_conv_created); nginx sets `X-Real-IP` on all locations; `/health` ok. **CRITICAL:** backend deploy is ONLY `git pull && ./deploy-backend.sh` — a bare `docker compose up` reverts prod to dev mode. **Open (operational):** in-app smoke test; backups cron + passphrase file (off-VM); `.\deploy-web.ps1` to show the 0.0.61 warning; uptime monitor. → [2026-06-22-session-prod-readiness.md](./2026-06-22-session-prod-readiness.md)

**Previous:** 2026-06-21 — **Split root CLAUDE.md into nested backend/ + frontend/ files + memory/skill housekeeping** — the single 316-line root `CLAUDE.md` (`alwaysApply: true`) loaded its whole bulk every session. Split move-verbatim into three: slim root (cross-cutting Rules, §0 Quick Start, §1 Architecture Topology, §2 How-To, §3 Shared Wire Contracts, §4 Environment & Config + new tier-pointer block) **316 → 114 lines**; new **`backend/CLAUDE.md`** and **`frontend/CLAUDE.md`** — auto-loaded by Claude Code per working dir.

**Previous:** 2026-06-18 — **Media-orphan gap (I1) — cron grace period + orphan observability (backend-only + frontend diag)** — closed both halves of I1 with NO change to upload→send ordering or the E2E envelope. **(1) Cron race:** `cleanupOrphanedFiles()` (daily 03:00) deleted any file not referenced by a non-expired row — incl. a blob uploaded seconds ago whose `sendMessage` emit is still in flight. Fix: a **grace period** — `fs.stat` each unreferenced file, SKIP if `now - mtime < MEDIA_CLEANUP_GRACE_MS` (default 15 min, env-overridable; uses **file mtime**, not message timestamps); swept on a later run once aged past the window. **(2) Observability:** method now returns `MediaCleanupSummary {scanned,deleted,orphan,expired,graceSkipped}` and logs it to stdout (→ docker logs) — two queries (non-expired set + all-referenced set) classify each delete as **ORPHAN** (no row → upload-ok/send-failed gap) vs **EXPIRED** (row exists but expired). **(3) Client signal:** `_encryptAndSend` logs `MEDIA_ORPHAN_LIKELY{tempId}` to `E2eDiagLog` on failure paths when a non-empty `mediaUrl` is present (blob uploaded, send failed) — id-only; text/ping never log. **Retry-reupload: checked, NOT a problem** — `retryFailedMessage` reuses the model's `mediaUrl`+`mediaKey`+`mediaIv` (no re-upload); the re-upload branch fires only when the original upload produced no URL (no orphan to multiply). **TDD RED→GREEN:** backend spec failed first (`cleanupOrphanedFiles` returned `void`) → **10 passed** (was 7); frontend test failed first (not logged) → **11 passed**. Full backend **310/40** (`verify-claude-backend-test-counts.mjs` OK); full frontend **386 passed**; analyze clean. Feature branch `fix/media-orphan-grace-and-metric` + PR. **Backend-only behaviour** → live after merge to `master` via `git pull && docker compose restart backend` (no image build); **not version-bumped**. **VM verification owed (Untested = not done):** tail `docker compose logs … | grep "Cron cleanup done"` for the new counts; confirm a fresh blob isn't swept. → [2026-06-18-session-media-orphan-grace-and-metric.md](./2026-06-18-session-media-orphan-grace-and-metric.md)

**Previous:** 2026-06-17 — **Voice playback via Web Audio — no iOS media-control card (0.0.60)** — follow-up to the ping fix (SAME branch `fix/ios-ping-media-session`), explicitly requested by the user: voice-message **playback** on web also showed an iOS media-control card (same mechanism — `just_audio` → HTML `<audio>` → MediaSession). **Fix:** new `VoicePlayer` abstraction (`widgets/audio/voice_player.dart` facade; conditional import `voice_player_native.dart` else `voice_player_web.dart`); `PlaybackController` rewritten onto it (public API unchanged: builder/`message`/`clearAudioCache`; added `playerFactory` test seam). **Native** = thin 1:1 just_audio wrapper (behaviour identical, keeps Android OS controls). **Web** = Web Audio engine on a **shared** `AudioContext` (`AudioBufferSourceNode` ⇒ no MediaSession ⇒ no card): one-shot source so play/pause = stop+restart-at-offset, seek = new source at offset, speed = `playbackRate` (pitch-shifts, same as today), position = `ctx.currentTime` ticker, completion = identity-guarded `onended`; gesture-unlock `resume()` on every window gesture (first play resumes post-await). **Web load is now bytes** (`fetchMediaBytes`→decrypt→`setAudioBytes`), not a blob URL; legacy unencrypted/Cloudinary also fetches bytes (avoids CORS). `utils/audio_blob_url_*` now unused (left in place). **TDD:** `playback_controller_test.dart` first (RED: no `playerFactory`) — play/pause/seek/speed + two-controller coordinator wiring via a fake player; Web Audio engine is DOM-only → device QA. **`flutter analyze` clean; `flutter build web` compiles; full suite 384 green (was 379). v0.0.60.** One `.\deploy-web.ps1` now tests BOTH ping + voice. Frontend-only; live only after merge to `master`. **UNVERIFIED ON DEVICE = NOT DONE:** iPhone matrix owed (voice plays + no card + pause/seek/speed/one-at-a-time + native unchanged). → [2026-06-17-session-voice-web-audio.md](./2026-06-17-session-voice-web-audio.md)

**Previous:** 2026-06-16 — **iOS ping media-control card — route ping through Web Audio, no MediaSession (0.0.59)** — narrow iOS-Safari-PWA-only bug: receiving a PING left a stale, un-startable media-control card (Control Center / lock screen). Root cause (verified from source): `PingEffectOverlay._playPingSound` played `ping_alert.mp3` via `just_audio`'s `AudioPlayer`, which on web (`just_audio_web`) uses an **HTML `<audio>` element** → registers a **MediaSession** → iOS shows the card; for a one-shot ping it lingers. Confirmed ping is the ONLY web sound on this path (incoming-pop is `if (kIsWeb...) return`-gated; voice left alone, out of scope). **Fix:** new conditional-import facade `utils/ping_sound.dart` (`if (dart.library.html) ping_sound_web.dart` else `ping_sound_stub.dart`). Web decodes the asset once and plays via a transient `AudioBufferSourceNode` (`package:web` `AudioContext`) — **no MediaSession**; `primePingSound()` (called from `ChatDetailScreen.initState`, no-op native) installs a one-shot window-gesture listener to `resume()` the iOS-`suspended` context so a ping outside a gesture still sounds. Native unchanged (just_audio). Overlay now calls `playPingSound().ignore()` fire-and-forget, owns no `AudioPlayer`; visual/timing untouched. **TDD:** `ping_sound_test.dart` (RED first: missing facade). Web path DOM-only → invisible to `flutter test`; constructing a just_audio player in the VM leaks `MissingPluginException`, so the only VM-safe assertion is `primePingSound()`'s no-op (the new prod call site) — `playPingSound()` stays covered by `ping_effect_overlay_test.dart`. **`flutter analyze` clean; `flutter build web` compiles the js_interop path; full suite 379 green (was 377). v0.0.59.** Feature branch `fix/ios-ping-media-session` + PR — does NOT auto-deploy (goes live only after merge to `master`). **UNVERIFIED ON DEVICE = NOT DONE:** real-iPhone matrix owed (ping sounds + visual fires + NO card + no lingering; regression: incoming-pop, voice, Android). → [2026-06-16-session-ios-ping-media-session.md](./2026-06-16-session-ios-ping-media-session.md)

**Previous:** 2026-06-16 — **Live-receive Bug B — server-side stale-socket eviction (backend fix)** — a SECOND, independent cause of "received messages don't appear live; notification arrives instead", confirmed from an on-device diag log AFTER the resume re-assert (Bug A) shipped. Signature: a peer message (msgId 9900) had **no `RECV_MSG` at all** (never reached the client) and only surfaced via a later history refetch, while messages that DID arrive appended fine — i.e. server never emitted `newMessage`. Root cause in `ChatGateway` (`backend/src/chat/chat.gateway.ts`): `onlineUsers` is `Map<userId, socketId>` (one socket/user) and `handleDisconnect` deleted the entry **unconditionally**. On iOS suspend/resume the device reconnects with a NEW socket; ~20s later the abandoned OLD socket times out on the server and its `handleDisconnect` deletes `onlineUsers[userId]` — **evicting the live NEW socket**. From then on `onlineUsers.get(recipientId)` is `undefined` → peers' `newMessage` falls back to push (own sends still echo via `client.emit`, so only peer messages vanish). **Fix:** guarded delete — only `delete` when `onlineUsers.get(userId) === client.id`. TDD: `chat.gateway.spec.ts` new "stale-socket guard" group (RED: live socket evicted → `undefined`; GREEN after guard). Backend suite **307 green** (was 305, 40 suites). **No frontend version bump** (backend-only; deploy via `docker compose build backend`; verify `/version` `gitCommit`). Why missed last time: I let the client-drop explanation fully account for the push symptom and didn't check the server's connection bookkeeping; diagnostic only instrumented arrived-messages, blind to never-delivered. CLAUDE.md: backend gotcha + count 305→307. → [2026-06-16-session-live-receive-server-stale-socket.md](./2026-06-16-session-live-receive-server-stale-socket.md)

**Previous:** 2026-06-16 — **Live-receive drop fixed — iOS PWA resume re-assert (0.0.58)** — confirmed root cause of "received messages don't appear live in the open chat (web push fires; reopen fixes; can still type/send)" via the temp `E2eDiagLog` diagnostic. Captured log showed the smoking gun: after an iOS background→resume + socket reconnect, `RECV_MSG`/`ADD_TO_STATE` fired with `activeId:null, paginationConvId:-1, appendedToOpenChat:false` while the chat for conv 77 was on screen. Mechanism: `ChatDetailScreen.dispose()` (fires on iOS resume churn) → `closeConversation` + `clearMessages` null `activeConversationId`/`_paginationConversationId`; `_addMessageToState`'s gate (`msg.conversationId == (activeId ?? paginationId)`) then **drops** incoming messages (while `updateLastMessage` still reorders the conv list — matches "list updates, chat doesn't"); recovery otherwise races `_onSocketReady` (re-fetches only if `activeConversationId != null`). **Fix (TDD):** new `utils/chat_resume_reassert.dart` `reassertOpenConversationOnResume(convs, messaging, convId)` (= `openConversation` + `loadCachedMessages` + `getMessages`); `ChatDetailScreen.didChangeAppLifecycleState(resumed)` calls it → re-sets active id (+ `pushClientState`) and refetches deterministically. RED→GREEN via `chat_resume_reassert_test.dart` (unit + real-lifecycle host test; the real screen can't full-mount — `build` needs `AuthProvider.currentUser`). `flutter analyze` zero; full suite **377 green**. **Diagnostic kept in place — remove the `RECV_MSG`/`RECV_QUEUED`/`ADD_TO_STATE` entries once the fix is confirmed on-device.** Bumped `0.0.57 → 0.0.58`. → [2026-06-16-session-live-receive-resume-fix.md](./2026-06-16-session-live-receive-resume-fix.md)

**Previous:** 2026-06-14

**Topic:** **Notification icon — white-square small icon fixed on all push surfaces (0.0.56)** — root cause: the status-bar/small icon slot (web `showNotification` **`badge`**, native FCM/local **small icon**) is rendered from **alpha only**, so it must be **white-on-transparent**; both slots pointed at full-colour opaque PNGs (`badge:'/icons/Icon-192.png'`; native `icon:'@mipmap/ic_launcher'`) → solid alpha mask → white square. User provided the real Fireplace brand artwork (campfire+shield tile). Generated assets from it via new `frontend/tool/generate_notification_icons.py` (Pillow): **mono white flame silhouette** (inner negative-space teardrop, reads as fire at 24dp) → `res/drawable-{mdpi..xxxhdpi}/ic_stat_fireplace.png` + `web/icons/notification-badge-96.png` (audited: opaque pixels pure-white, rest transparent); **full-colour campfire** (cropped from the tile, no frame/wordmark) → `web/icons/notification-icon-512.png`+`-192.png`. Wired: SW `icon`/`badge`; native init + `AndroidNotificationDetails` `@drawable/ic_stat_fireplace`; `AndroidManifest` FCM `default_notification_icon`+`default_notification_color`; new `res/values/colors.xml` (`#FF6A2C`). Icon-only — grouping/tap/deep-link/badge/clearing untouched; PWA launcher/manifest icons left as the Flutter logo (out of scope); iOS web push shows the Home-Screen icon (ignores `icon`/`badge`). CLAUDE.md Commits rule split (small→master, substantial→feature branch+PR, no auto-deploy until merge) + Notification-icons gotcha added. **flutter analyze zero, version-footer test green, v0.0.56.** On a feature branch `fix/notification-icon` + PR — **does NOT auto-deploy; goes live only after merge to `master`. Device matrix still owed (Untested = not done).** → [2026-06-14-session-notification-icon.md](./2026-06-14-session-notification-icon.md)

**Previous:** 2026-06-13

**Topic:** **Storage-durability probes — stop fixing, START NAMING THE LAYER (0.0.55)** — after 10 rounds of fix-on-fix, called it: this is an ARCHITECTURE problem, no more behavioural patches until evidence names the failing layer. Established (analysis): 0.0.53 fixes ARE deployed+working (logs show `tempId`, single `SESSION_RESET`/peer, no `SESSION_DELETE`, `needsRebuild:false`); remaining failures are session **ABSENCE** (`ctype:2 hasSession:false`), a different mode than the **destruction** every prior fix targeted; OTP depletion already ruled out (no `InvalidKeyIdException`, only NoSession/BadMac); identity surviving (`needsKeyUpload:false`) argues AGAINST wholesale localStorage eviction. The one blind spot = storage durability. Added **observe-only** instrumentation to close it: `SESSION_INVENTORY{count,peerIds}` at every init (cross-reload drop with no matching delete ⇒ storage), `SESSION_STORE_DELETE{peerId}`, `SESSION_STORE_WRITE_FAIL{peerId,err}` (designed around the SharedPreferences in-memory-cache caveat — read-back can't prove durability, only cross-reload inventory can), `STORAGE_PERSIST{supported,granted}` (new `utils/storage_persist*.dart`; also requests eviction exemption — app never called `navigator.storage.persist()` before). **372 green, analyze zero, v0.0.55.** NEXT: deploy → fresh convo on TWO devices, capture BOTH sides aligned by msgId, **reload once**, compare `SESSION_INVENTORY` → the capture NAMES the layer (code/establishment vs storage), THEN commit direction. Do NOT write another behavioural fix first. → [2026-06-13-session-storage-durability-probes.md](./2026-06-13-session-storage-durability-probes.md)

**Previous:** 2026-06-12 — **Resume liveness probe — "push arrived but message missing until relaunch" root-caused + self-healing fix (0.0.54)** — `ensureReconnectIfNeeded` no-opped when socket.io CLAIMED connected, but iOS suspends the transport in background and the flag lies until ping timeout (zombie socket) → resume fetched nothing; message stayed server-side until full relaunch. Now: claims-connected resume → immediate re-sync (`getConversations`+`getMessages(active)`) + 6s liveness probe (`_serverResponseCounter` bumped by `conversationsList`/`messageHistory` responses); silence → forced lossless `connect()`. Full resume-chain instrumentation: `RESUME_CHECK{socketConnected}`/`RESUME_RESYNC`/`RESUME_PROBE_TIMEOUT`, `SOCKET_CONNECT/DISCONNECT/READY`, `LIFECYCLE`/`TAB_VIS` (MainShell), `CONV_LIST{ignoredEmpty}`, `HISTORY_REQ/RESP` + `HISTORY_DROP{reason}` on every silent `onMessageHistory` exit. 3 probe tests (fakeAsync, fake SocketService via injectable ctor; new seam `setIdentityForTest`). **371 green, analyze zero, v0.0.54 — deploy, repro: background → peer sends (push) → reopen via icon; expect message ≤~6s. If still broken: COPY DIAG BEFORE RELAUNCH (in-memory).** Also today: first device diag capture analyzed = designed Class A one-message loss + clean recovery (cascade fix visibly working). → [2026-06-12-session-resume-liveness-probe.md](./2026-06-12-session-resume-liveness-probe.md)

**Previous:** 2026-06-12 — **E2E regression round 2 — lossful send-side rebuild + keyed-media reset loop + exactly-once send (0.0.53)** — receiver LOG B (conv 87, peer 58, msg 8489) fully explained, three fixes verified against installed `libsignal_protocol_dart 0.7.4` source: (1) **`ensureSession`'s rebuild deleted the SessionRecord before `buildSession`** — wiping current + all 40 archived ratchet states — while `processPreKeyBundle` archives the current state itself (`session_builder.dart:139`) and `decryptFromSignal` iterates `previousSessionStates`; the delete was pure destruction and turned the peer's in-flight whisper (ctype **2** — whisperType=2, docs said 1) into `No valid sessions. [Bad Mac!]`. Now builds OVER the record (`SESSION_ARCHIVED_FOR_REBUILD`, lossless). (2) **Restored keyed-media rows re-armed the reset machinery forever**: received voice/image keeps `content=='[encrypted]'` by design, but four predicates counted placeholder-only as "undecrypted" → every history pass in a media chat fired `SESSION_RESET{historyRetry}` + set `markSessionRebuild` (which the retry loop never cleared, since usable rows are skipped) → the next send rebuilt (pre-fix: deleted) a valid session. New `_isUnresolvedEncryptedInbound` (placeholder AND `!_hasUsableDecryptedContent`) used by all four; `_requestSessionRebuildForPeer` no longer touches our own session and is deduped once-per-peer (`_rebuildRequestedPeers`, cleared on decrypt success, kept across reconnects). (3) **Exactly-once send**: `_emittedSendTempIds` latch before encrypt (duplicate → `SEND_DUPLICATE_BLOCKED`, no ratchet advance; released on failure for retry); `SEND_START`/`SEND_EMIT` log `tempId`. LOG A verdicts: no one-tempId multiplier exists in source (4×114-char bursts ≈ PING spam; fresh capture now decisive); key re-upload harmless (same stored keys, idempotent upsert, OTPs untouched). **Fail-before/pass-after: integration test (real two-party libsignal, NEW `encryption_provider_session_rebuild_test.dart`) fails pre-fix with the exact prod exception; cascade 2→1 emits; dup-send 2→1; media-loop emit→silence. 368 green, analyze zero. Version 0.0.53 — NEXT: deploy + sender↔receiver log capture (no SESSION_DELETED_FOR_REBUILD ever; SESSION_RESET ≤1/peer; media chats silent).** → [2026-06-12-session-rebuild-loss-fix.md](./2026-06-12-session-rebuild-loss-fix.md)

**Earlier:** 2026-06-12 — **Context-menu fix: huge messages made Copy unreachable** — user-reported post-deploy: long-press on a very tall message positioned the action panel OFF-SCREEN (the layout's last-resort branch pins the bubble bottom to the composer clearance and pushes panel+emoji above the viewport — panel top goes negative, only the emoji row was clamped). Fix in the pure function: `computeMessageContextMenuLayout` now returns `previewHeight` — when the bubble exceeds `(available)/1.02` it clamps the preview and centers the emoji→preview→panel stack vertically; overlay crops the replica top-aligned (`ClipRect`+`OverflowBox(minHeight/maxHeight: full)`) with a `ShaderMask` fade at the cut edge (full message remains under the blur). Competitor-standard (iOS context menus / Telegram crop the preview). +4 tests (3 pure: clamp bounds/geometry incl. 5-row panel + off-screen-top anchor; 1 widget: 2000dp bubble → panel+emoji on-screen, Copy tappable, dismisses). **364 green, analyze zero. Version 0.0.52 — redeploy + retest long-press on the huge message.** Clipboard QA so far: copy ✓, photo paste both directions ✓. → [2026-06-12-session-context-menu-huge-bubble.md](./2026-06-12-session-context-menu-huge-bubble.md)

**Earlier:** 2026-06-12 — **Final notification pass — "5 then 5" race, sweep hardening, Android badge verdict** — (1) duplicate-count cards root-caused as a backend race: `flush()` deletes its bucket before awaiting the unread summary, so a message landing in the await window is counted in flush #1 AND opens bucket #2 → identical re-announcement; fixed with a 10s `lastSent` `(unreadCount, unreadTotal)` suppression tracker (server-side skip is safe), +3 tests. (2) iOS app-closed group-clear: code was already correct (closeNotificationsForTag runs first in notificationclick) — the gap is WebKit SW-lifetime visibility; hardened with filtered+unfiltered getNotifications union in the SW and a best-effort PAGE-context close in NotificationCleaner (untried lever; evaluate on device). (3) Android badge "stuck on 1" = PLATFORM: Chromium-Android has no Badging API; the icon badge is the launcher's notification-COUNT (1 card per conv by design); numeric message-count badge impossible for an Android PWA, MIUI dot-only; `badge.support {badgingApi}` diag added to E2E log for on-device triage. **Backend 295 / frontend 360 green, analyze zero, verifier OK. Version 0.0.51 — NEXT: deploy + device matrix (iOS same-user tap-clear best-effort, rising counts, Android regression).** → [2026-06-12-session-final-push-pass.md](./2026-06-12-session-final-push-pass.md)

**Earlier:** 2026-06-11 — **iOS push iteration 3 — deep-link dead-chat fix (Option A) + sender-name cards + iOS tray truth** — v0.0.49 device results: badge WORKS; merge only within bursts; tap clears one card; older notifications opened a dead chat (empty + send eats text). Root causes: (1) notification nav was unvalidated — stale conv id from an old card mounted `ChatDetailScreen` where `sendMessage`'s no-`orElse` `firstWhere` threw `StateError` before the optimistic add (typed text vanished); fixed via Option A gate in `MainShell` (consume only after `hasLoadedConversationsOnce`, navigate only if `getConversationById(id) != null`, stale → conversations tab) + `firstOrNull` hardening in `sendMessage`/`sendVoiceMessage`; (2) iOS tray truth (device-confirmed): WebKit never implements tag replace (258922) AND `getNotifications()` only sees current-SW-instance notifications → replace-in-place ACROSS SW restarts is impossible from a PWA; cards pile per burst with correct cumulative counts; Android/desktop fully correct; future path = Declarative Web Push (iOS 18.4+). Cards now titled `<senderName>` + body `N new messages` (backend `senderName` in payload, schedule-time, latest-wins). **Backend 292 (+2) / frontend 360 (+2) green, analyze zero. Version 0.0.50 — NEXT: deploy + run the multi-sender on-device matrix; swipe pre-deploy zombie cards once.** → [2026-06-11-session-push-deeplink-option-a.md](./2026-06-11-session-push-deeplink-option-a.md)

**Earlier:** 2026-06-11 — **iOS PWA notifications + badge — post-mortem + unified fix (2nd attempt)** — prior fix verified deployed yet broken; root causes: (1) app-side tray/badge ops targeted the WRONG service worker (`serviceWorker.ready`/`.controller` resolve the Flutter SW at `/`, never the push SW at `/web-push-scope/` — every clear/sweep was a silent no-op); (2) `listenForNotificationClicks` missing `navigator.serviceWorker.startMessages()` — WebKit queues SW→page messages forever, so notification taps focused the app but never navigated; (3) iOS killed-PWA click opens `start_url` dropping the `?notify_conv=` URL; (4) iOS never implements notification `tag` replace (WebKit 258922) → stacking; (5) `UnreadBadgeSync` refused to clear badges it didn't write → stale counts, and the SW fell back to per-burst `unreadCount=1` when the unread summary failed → "reset to 1". Fix: SW emulates tag replace (close-then-show), group-clear on tap + tag-derived convId, IndexedDB deep-link handoff (`fireplace-push`/`kv`, 5-min TTL, drained in `main()` + tab-visible + cleared on live-handled click), new `PushSwChannel` (`getRegistration('/web-push-scope/')`) as the ONLY app→push-SW route, single badge writer = push SW (`set-badge`/`clear-badge`/`close-conv`/`sweep`), `UnreadBadgeSync` gated on `hasLoadedConversationsOnce` + always-writes-zero, badge write skipped when `unreadTotal` absent. **358 tests green (+5), analyze clean, Bug 3 nav preserved. Version 0.0.49 — NEXT: deploy (flutter clean + gitCommit check + PWA cache-bust; iOS refetches SW on next launch) then run the on-device verification matrix.** → [2026-06-11-session-ios-push-badge-fix.md](./2026-06-11-session-ios-push-badge-fix.md)

**Earlier:** 2026-06-10 — **Clipboard feature COMPLETE — Phase 4 Android keyboard paste** — composer `TextField` gained `contentInsertionConfiguration` (`allowedMimeTypes: kStageableImageMimeTypes`; only the Android engine emits `commitContent`, no platform gate needed). `_onKeyboardContentInserted`: `_canAcceptPaste` gate → URI-only payload (`hasData==false`, no `content://` resolver) → honest `snackbarPastedImageUnavailable` (en/pl) → else `_onPastedImage(bytes, mime, pastedFilenameForMime(mime))` reusing the full Phase 2/3 staging path. All four phases now shipped: Copy (context menu) · staging + ordering contract · web paste · Android keyboard paste. **353 tests green** (day started at 325; +28 across the feature), analyze clean. Commits `1cc297f` + docs; feature total `fdd3aa2`..`1cc297f`. **NEXT ACTION: deploy** (decrypt-cascade fix + clipboard Phases 1–4 ride one `./deploy.sh`; version bump; stale-build trap: `flutter clean`, verify `gitCommit`, PWA cache-bust) **then manual QA**: web paste matrix (Chrome/FF/Safari image+text+mixed; iPhone callout Paste; recording-ignored), Android Gboard chip, iOS long-press Copy. Spec: [docs/superpowers/specs/2026-06-10-clipboard-copy-paste-design.md](../../docs/superpowers/specs/2026-06-10-clipboard-copy-paste-design.md) → [2026-06-10-session-android-keyboard-paste.md](./2026-06-10-session-android-keyboard-paste.md)

**Earlier:** 2026-06-10 — **Web paste source (Clipboard Phase 3)** — `composer_paste{,_stub,_web}.dart` triple: capture-phase `window` `paste` listener; `preventDefault()` ONLY on image (text-only flows natively; `navigator.clipboard.read()` avoided); mixed paste inserts text at cursor; tooLarge/unsupported snackbars; 351 green. Commits `19ef19e`, `0272672`. → [2026-06-10-session-web-paste-source.md](./2026-06-10-session-web-paste-source.md)

**Earlier:** 2026-06-10 — **Composer staging + ordering contract (Phase 2)** — `ComposerAttachmentController`/`ComposerAttachmentBar`; send chain → `Future<bool>` post-emit; caption after image, restored on failure; 347 green; `59ab1d5`..`345adbf`. → [2026-06-10-session-composer-attachment-staging.md](./2026-06-10-session-composer-attachment-staging.md)

**Earlier:** 2026-06-10 — **Context-menu Copy action (Clipboard Phase 1)** — long-press → Copy on TEXT bubbles (`hasCopyablePlaintext` gate); height-aware panel layout; 335 green; commits `fdd3aa2`..`8bfa66a`. → [2026-06-10-session-copy-message-text.md](./2026-06-10-session-copy-message-text.md) · **Clipboard design spec** → [2026-06-10-session-clipboard-design.md](./2026-06-10-session-clipboard-design.md)

**Earlier:** 2026-06-10 — **E2E decrypt regression: Bad-MAC → session-reset cascade root-caused and fixed** — inbound decrypt-failure path deleted Signal sessions (permanent Bad-MAC loss) + sticky `_liveDecryptFailedPeers` + `_mergeHistorySnapshot` exclusion resurrected terminal rows, re-arming resets every reconnect. Fix: never delete sessions inbound (rebuild *request* only), terminal-row skip, prune, merge pre-seeds all local rows; `notifyPeerRebuild` on identityReset shrinks cache-wipe loss to ~1 msg. New privacy-safe diag events. Suite 325 green. **Next: deploy + capture E2eDiagLog from an affected device.** → [2026-06-10-session-decrypt-cascade-fix.md](./2026-06-10-session-decrypt-cascade-fix.md)
