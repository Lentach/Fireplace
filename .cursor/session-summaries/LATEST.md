# Latest session summary

**Date:** 2026-06-10

**Topic:** **Clipboard copy/paste design (research + spec, no code)** — decision-ready design for copy-message-text + paste text/images into the composer. Scope confirmed: copy included; images-only paste; v1 = 1 staged image + caption; web parity required. Competitor survey: long-press→Copy universal; all 7 apps *stage* pasted images (none send instantly). Audit: copy slots into `MessageActionPanel` (panel-height estimate must become row-count-aware); bubbles not selectable; current image flow has no staging/caption; paste reuses `AttachmentHandler.sendImage` → `sendImageMessage` → `EncryptedMediaUploadService` (no new unencrypted path). Platform: web = `paste` DOM event via new stub/web pair (NOT `navigator.clipboard.read()`); Android = `contentInsertionConfiguration` (no plugin); native iOS deferred (PWA covers iOS). Recommended: context-menu Copy (TEXT only, placeholder/failed guards) + staged chip above input row (TextField never unmounts); send = image then caption text. Spec: [docs/superpowers/specs/2026-06-10-clipboard-copy-paste-design.md](../../docs/superpowers/specs/2026-06-10-clipboard-copy-paste-design.md). Awaiting spec review → writing-plans. → [2026-06-10-session-clipboard-design.md](./2026-06-10-session-clipboard-design.md)

**Previous:** 2026-06-10 — **E2E decrypt regression: Bad-MAC → session-reset cascade root-caused and fixed** — inbound decrypt-failure path deleted Signal sessions (permanent Bad-MAC loss) + sticky `_liveDecryptFailedPeers` + `_mergeHistorySnapshot` exclusion resurrected terminal rows, re-arming resets every reconnect. Fix: never delete sessions inbound (rebuild *request* only), terminal-row skip, prune, merge pre-seeds all local rows; `notifyPeerRebuild` on identityReset shrinks cache-wipe loss to ~1 msg. New privacy-safe diag events. Suite 325 green. **Next: deploy + capture E2eDiagLog from an affected device.** → [2026-06-10-session-decrypt-cascade-fix.md](./2026-06-10-session-decrypt-cascade-fix.md)

**Earlier:** 2026-06-10 — **Task 6 — NotificationCleaner conditional-import triple** — stub/io/web `NotificationCleaner` for the push-notification sweep. Commit `4cf8943`; 4/4 unit tests; analyze clean. Not yet wired (entrypoint + callsites in later tasks).
