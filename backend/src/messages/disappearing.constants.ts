/** Minimum user-configurable disappearing timer (seconds). */
export const DISAPPEARING_MIN_SECONDS = 5;

/** Maximum user-configurable disappearing timer (30 days). */
export const DISAPPEARING_MAX_SECONDS = 2592000;

/** Never-read retention for read-mode messages (1 day from send). */
export const DISAPPEARING_MAX_UNREAD_SECONDS = 86400;
