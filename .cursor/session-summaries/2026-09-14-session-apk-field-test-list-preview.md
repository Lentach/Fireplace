# The APK is on the owner's phone as device #11, four field-found defects are fixed, and prod went 0.2.41 → 0.2.46 on both tiers

**Date:** 2026-09-14 · **Version:** 0.2.43 → **0.2.46** · **Tiers deployed:** **both** (`0.2.46 / 43481643`, smoke 5/5; the phone APK is 0.2.46 too)

Evidence, measurements and method detail: `.planning/apk-field-test-2026-09-13/findings.md`.
Fresh-agent handoff: `.planning/apk-field-test-2026-09-13/handoff-next-session.md`.

## What was done
- Built 0.2.43 → 0.2.46 (versionCodes 20043-20046), each signer cert MEASURED against the record-of-truth `8e9a6bf3…5cdf405d` (`build-android.ps1` only asserts the DN). Installed all four on the owner's phone IN PLACE.
- **Linked the phone to `bob208` as device #11** and drove the smoke checklist on prod: §6.1 lock on real hardware, link ceremony, E2E text/voice/image both directions, self-sync to the iOS primary, push-with-app-killed in **3.54 s** opening the RIGHT chat, disappearing-message hard delete.
- **0.2.44 (`c7bcee7e`): a freshly linked device's ENTIRE chat list printed raw `[encrypted]`.** The list preview was the one surface 0.2.43's sentinel sweep missed — it is not a body surface, and list rows come from `getLastMessagesBatch(convIds, userId)` which resolves no per-device envelope, so `envelopeStatus` is always absent and `displayAsEncryptedPlaceholder` (needs the legacy ciphertext column) misses every new-model row. Added `sentinelPreviewText()`; `[encrypted]` maps to the NEUTRAL label because a waiting row must not be accused.
- **Backend (`741c0420`, `302c5956`): conversation METADATA now reaches every device of every party it concerns.** `disappearingTimerUpdated`, `messagePinned`, `messageUnpinned` → both users' rooms; `conversationMuteUpdated` → the muter's room ONLY (telling the peer would leak being muted). All four had emitted to the ACTING socket, so the actor's own linked devices were the only participants left uninformed — owner-reported on his 3-device account. `wire.md` gained the contracts; it had documented only the throttle refusal, never the success audience.
- **0.2.45 (`eb455f5b`): the shell pays the status-bar inset once.** `main_shell.dart` wrapped the usually-empty banner stack in a `SafeArea` that was a SIBLING of the tabs, so the tabs still saw the full `padding.top` and `MainTabScreenHeader` spent it again. Also extracted `widgets/media/{swipe_down_to_dismiss,fullscreen_image_viewer}.dart` so stills and GIFs dismiss on swipe down like video does; GIF fullscreen is now edge-to-edge (flagged under the composer/media explicit-OK rule).
- **0.2.46 (`43481643`): the key-loss warning moved off the version footer into Privacy & Safety** — owner's call after I pushed back on deleting it (native is the one platform where it is literally true).
- **Deployed both tiers** on his go-ahead. Device cap: he confirmed **3 stays** (`1 primary + 2 linked`, already the law) — SETTLED.

## Key files
- Edited: `frontend/lib/utils/message_display_text.dart`, `widgets/conversation_tile.dart`, `screens/{main_shell,settings_screen,privacy_safety_screen}.dart`, `widgets/main_tab_screen_header.dart`, `widgets/message/{image,gif}_message_content.dart`, `l10n/app_{en,pl}.arb`, `backend/src/chat/services/chat-conversation.service.ts`, `backend/src/chat/chat.gateway.ts`, `docs/contracts/wire.md`, `frontend/docs/composer-media.md`, `docs/runbooks/android-release.md`, `docs/agents/traps.md`, `CLAUDE.md` (counts 2120→2133, 1119→1123), `frontend/pubspec.yaml`
- New: `frontend/lib/widgets/media/{swipe_down_to_dismiss,fullscreen_image_viewer}.dart`; tests `conversation_tile_sentinel_preview_test.dart` (6), `main_tab_header_inset_test.dart` (2), `fullscreen_image_viewer_dismiss_test.dart` (5), +3 in `chat-conversation.service.spec.ts`
- Read only (load-bearing): `providers/messaging/messaging_provider.{decrypt,events,history,send}.dart`, `widgets/audio/playback_controller.dart`, `backend/src/messages/{messages.service,message.mapper,message-expiry.util}.ts`

## Verification
- Frontend **2133 / 14 skipped**, analyze at the **3168**-info floor; backend **1123 / 62 suites**, knip clean, lint-ratchet PASS at the **898** floor; both count verifiers OK.
- **CI 6/6 green on every code commit**: `c7bcee7e`, `741c0420`, `302c5956`, `eb455f5b`, `43481643` (`gh api …/commits/<sha>/check-runs` — never `gh run list`).
- Every new test is red-green proven: the tile's 4 of 6 fail pre-fix; the header's falsification pins `2 × inset`; the fan-out tests fail with `to` called on the peer room only; the viewer's zoom case caught a real dispose-time `createTicker` crash (lazy `late final` controller).
- **Header fix device-measured, same phone before/after:** status-bar inset 104 px, Chats title top **269 px → 165 px**. Emulator cross-check on the fixed build: inset 136 px, title 194 px; the leftover is 22.2 dp vs 22.1 dp across densities 440/420, so the model holds on both machines.
- **Warning move emulator-verified** (release 0.2.46, throwaway deleted after): the row renders in Privacy & Safety's Security section and the Settings screen no longer matches `odinstal`/`czyść`/`historia`.
- **Deploy:** backend `starting → healthy` in 10 s, `/version` `0.2.46/43481643`, `/health` `{"status":"ok","db":"ok"}`, `fireplace-db-1` NOT recreated (up 35 h); web smoke **5/5** including the `main.dart.js` contains-`43481643` stale-build gate.
- **NOT verified:** the un-enrolled flip-flop drill (item 7 — deliberately skipped, it would damage the peer account); VIDEO rendering (`24391` arrived with a `mediaUrl`, nobody confirmed it draws); the swipe-down gesture on a REAL device (needs an image in a chat, hence a peer — 7 widget tests cover it; the owner was asked to swipe one photo).

## Notes for next session
- Owner-owed, the ONLY one left: publish the APK as a GitHub Release or keep hand-delivery. Cert is a ONE-WAY DOOR — nothing published yet, so Play App Signing can still adopt this keystore via PEPK.
- **Docs owed, blocked by a concurrent skills/MCP session holding the files:** `LATEST.md`'s DEPLOY STATE line is FLATLY WRONG (still claims both tiers 0.2.41 — they are `0.2.46 / 43481643`), its new entry is unwritten, and `traps.md` needs the four lines below plus striking the now-answered device-cap question. Full list in the handoff §D. Mind the 5-entry cap: that session is adding an entry too.
- The "0 s voice note" and "messages didn't disappear" reports are both **CLOSED as not-defects** — see findings §5 before re-investigating either.
- Traps to add: sibling `SafeArea`s each pay the FULL inset (nested ones do not — the outer strips the padding, which is why the obvious falsification is wrong); `InteractiveViewer` claims the vertical drag whenever `panEnabled`, so a dismiss gesture must arm only at rest scale; an `AnimationController` as a lazy `late final` is CONSTRUCTED INSIDE `dispose` if nothing ever touched it; a disappearing message does not count down from send (`expiresAt` is stamped on READ, 1-day backstop, `EVERY_MINUTE` cron hard-deletes).
