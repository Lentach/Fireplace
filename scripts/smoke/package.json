{
  "name": "fireplace-smoke",
  "private": true,
  "type": "module",
  "description": "Post-deploy smoke test — run on the PC after deploy-web.ps1 / deploy-backend.sh",
  "scripts": {
    "smoke": "node post-deploy-smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.53.0"
  }
}
