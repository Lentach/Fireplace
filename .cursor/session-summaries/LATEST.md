# Latest session summary

Entries are newest-first, capped at 5 by `.githooks/pre-commit`. Each one links its dated file,
which holds the full account — **rotating an entry out of this file loses nothing.**

> **The "this file is two tickets behind" banner is gone on purpose.** It was `master`'s copy warning
> readers that the multi-device programme lived on a branch. That branch is now merged into this
> file's history, so the banner had become self-referential and its figures were stale ("(a)–(xxxiv)",
> "T1–T8"). **Standing warnings and history from entries this cap has rotated out** — read the dated
> files before touching those areas: the composer/attachment picker
> (`2026-08-19-session-composer-regression.md` — owner: *"we made huge regress on composer and now
> all old bugs are back"*, ship nothing there without a green repro AND his explicit OK, and
> **never `git revert 0cbf17b`**), PWA notifications (`2026-08-20-session-notif-regression.md`),
> the Actions billing/CI economics history (`2026-08-18-session-actions-billing-and-0.1.16.md`),
> and this programme's own T7/T8 tickets (`2026-08-22-session-t7-edit-refan.md`,
> `2026-08-22-session-t8-harness-sweep.md`), the T10 reset-addressability entry rotated out on
> 2026-09-02 (`2026-08-26-session-t10-reset-addressability.md`, spec (xlv): a completed §6.2 reset
> left the account unreachable for the whole programme; **a harness can only find bugs in the shapes
> it builds** — the never-enrolled reset had a live population of exactly 0; hostile
> `nextListVersion` inflation is bounded by a plausibility ceiling), and the Umbra rename/app-icon
> entry rotated out the same day (`2026-08-26-session.md`: brand sweeps MUST eyeball the RENDERED
> page — split-span wordmarks defeat grep; re-verify subagent compliance claims, one fabricated its
> version bump; owner still owes README screenshot recapture, GitHub repo renames, the domain
> decision), and the T11 entry rotated out on 2026-09-02 (spec (xlvi) is recorded in full at
> `docs/design/multi-device.md` §12, amendment 2026-08-26), and the D1/D2 recovery entry rotated out
> on 2026-09-03 (`2026-08-29-session-d1-d2-recovery.md`, spec (xlvii)–(xlix): read its THREE
> addenda — a warning the user cannot act on is a dead end; recovery state must survive a restart), and the
> 08-30b bounded-gate entry rotated out on 2026-09-03 (`2026-08-30-session-bounded-gate-lxiv.md`, spec (lxiv):
> the revoked-device relogin CLOBBER — a `registrationId` may not change under an unchanged identity, the OTP
> replenishment path bypasses `upsertKeyBundle` so BOTH uploads carry the guard; the own-device confirmation is
> per-socket; residual 8 `_reenrollAfterReset` has no in-flight latch, recorded not fixed), and the 09-02a
> FCM/APK entry rotated out on 2026-09-03 (`2026-09-02-session-fcm-e2e.md`: prod FCM proven on the Pixel_7
> emulator; **versionCode floor 10024**; `am force-stop` silently drops FCM; `FLAG_SECURE` blocks screencap;
> verify an installed APK's dart-defines by byte-searching `kernel_blob.bin`; the signed 0.1.24 APK release
> waits on the owner's passcode feature), and the 09-03 evening (lxxii) entry rotated out on 2026-09-06
> (`2026-09-03-session-lxxii-reset-door.md`: **`deploy-web.ps1` overwrites `frontend/build/web` with the PROD bundle — rebuild
> locally before any 8093 check; a mutation runner once left the mutant ON DISK — `git diff` after every mutation script**;
> the owner still owes the thief-with-password matrix answers), and the 09-04 `feat/video-messages` merge entry
> rotated out on 2026-09-05 (`2026-09-01-session-video-messages.md`: **merging an older release branch must never
> roll the pubspec back**; prod served that branch as a test until the 09-02 `0.2.0` deploy, then master's 0.2.x). For
> multi-device specifically the permanent record is `.planning/multi-device/` (`FINISH-HERE.md`, `progress.md`,
> `task_plan.md`), not this file.

**✅ DEPLOY STATE — MASTER IS LIVE, BOTH TIERS, 2026-09-05 late evening: FRONTEND `0.2.16 / fda92b3`, BACKEND `0.2.4 / 9a1c4396` (`APP_VERSION` is read from the pubspec at the VM's checkout, which was `9a1c439` = 0.2.4 when `deploy-backend.sh` ran; the frontend bump to 0.2.16 came one commit later — the backend `version` string is cosmetic, the `gitCommit` is the truth). Smoke 5/5 from the main checkout (`--commit fda92b3`); `deploy-web.ps1` in the worktree exited 1 AFTER `PUBLISHED_OK` as documented. First time in front of users: everything since `5d669ce` — the whole `feat/video-nits` line (already tested live as 0.2.5→0.2.15), `a71ed33` (lxxiii)/(lxxiv) opt-in lock + gate, and (lxxv) the un-enrolled reset refusal. VM is back on `master` (was `test/video-nits-0.2.3`). Served bundle positives: `tylko MP4` 1, `reset nie jest potrzebny` 1, `not_enrolled` 2; `Blokada kodem` 0 (passcode is NOT on master — correct). No migrations/entities/compose/Dockerfile changed since the previous backend `7e957d6`. **Rollback:** frontend redeploy `c8f662e`; backend `git checkout test/video-nits-0.2.3 && ./deploy-backend.sh` (→ `7e957d64`). Master CI GREEN 5/5 on `9a1c439` — first fully green master since `5d669ce`.**

**Date:** 2026-09-05 (evening → late) — **VIDEO-NITS MERGED; THE `e2e-wire` RED WAS THREE STALE TESTS (FIXED); THE `e2e-isolated-probes` RED WAS A REAL GAP (lxxiii) OPENED — RULED (lxxv), FIXED, PROBE REWRITTEN; MASTER GREEN 5/5 AND DEPLOYED BOTH TIERS AS 0.2.16; `feat/passcode-lock` REBASED, PICKER-GUARD BUG FIXED, 1932/14, DRAFT PR #164 FOR CI, NOT MERGED (owner: "test passcode, then merge").** ➡ **`2026-09-05-session-video-merge-e2e-fix.md`**.
- **"Merge it and deploy, I've tested it" was two different things:** the tested build was the BRANCH (`test/video-nits-0.2.3`); master additionally carried `a71ed33`, untested and red. Owner: merge now, hold deploy until the red is understood. `feat/video-nits` fast-forwarded (PR #163 auto-closed).
- **`e2e-wire` (3 tests) = stale:** they registered never-enrolled accounts and expected `identity_locked`, which (lxxiii) made unreachable (`key-bundles.service.ts:173-195` refuses only `isEnrolled`). Rewritten to the opt-in contract: un-enrolled replacement ACCEPTED + `ownIdentityReplaced` to the other session; then DAK enrolled through the real wire → unsigned upload `identity_locked`, bundle byte-identical, no alarm; OTP refusal moved after T2 enrolment. §6.1 nonce single-spend is no longer observable on the wire (pinned by `chat-key-exchange.service.spec.ts` only). Harness **44/5**, `CLAUDE.md` §3 updated.
- **`e2e-isolated-probes` = REAL GAP, ruled (lxxv) on the owner's delegation:** never-enrolled is now the DEFAULT population, `requestIdentityReset` had no enrolment check, the gate shows the reset door in `checkingOnly` → an un-enrolled owner could run the 72 h ceremony, have device 1 revoked and be stranded un-addressable ((xlv) silence). Now `not_enrolled`, refused BEFORE the phrase (nothing written, phrase intact), after the pending check (old rows stay cancellable). EN+PL copy points at the recovery that works (sign in on the new device). Probe rewritten to prove the unlocked remint under device 1 + `liveDeviceIds == [1]` — the assertions that never ran while it died on its premise. Backend **1066/62**; RESET_PROBE 2/2, ENROLLED_LOCK_PROBE 2/2 locally. Spec §12 D25 (lxxv); root §7 reset line updated.
- **Passcode × video: no code conflict** (`settings_screen.dart` additive, l10n unioned, gen-l10n zero drift), but the advisor caught a REAL runtime bug: `MainShell` fires `noteBackgrounded()` on every visibility loss, so at auto-lock 0 s the attach camera/file sheet → lock → key revocation → web relaunch → pick lost. Fixed on the branch: `PasscodeProvider` reads `composerNativePickerActive` (the same signal the freeze-reload guard uses), holds the immediate lock and skips the foreground verdict while the span is up, still stamps the clock; 3 tests proven by mutation. Branch = `83afc18`, 15 commits on `9a1c439`, byte-identical to a merge, **1932/14**. Android nit still open: a manual padlock during a fullscreen clip with sound leaves audio playing behind the `Offstage` lock.
- Owner's UX verdicts (reload-on-lock, 0 s desktop auto-lock, 6-digit vs alphanumeric) still outstanding; he will test the branch and then decide the merge.
- Traps: local `test_e2e` needs `E2E_DB_CONTAINER=fireplace-db-1` via `cmd /c "set …&& flutter.bat test …"`; the in-memory `/auth/register` 10/h bucket bites on a second run — `docker compose restart backend` clears it; Docker Desktop had QUIT between runs (pipe gone) — `Start-Process 'Docker Desktop.exe'`, daemon up in ~10 s; a fresh backend worktree needs `npm ci` before jest; dart2js escapes non-ASCII, so grep the served bundle for an ASCII substring of a Polish string. `gh repo view --json isPrivate` → **false** (root `CLAUDE.md:20` fixed).

**Date:** 2026-09-06 — **VIDEO NITS ROUND 2 (`feat/video-nits` head `383ceb1`, branch test 0.2.13 + backend): THE OWNER'S "PLAYS ONCE, THEN BROKEN" WAS A SERVER 429 — `GET /media/msgs/*` WAS THROTTLED 60/MIN PER IP WITH NO CACHE HEADER; TAP ON A PLAYING CLIP WAS DEAD FOR TWO STACKED PLATFORM-VIEW REASONS; FULLSCREEN NEVER RESUMED AT ITS OWN POSITION; NEWEST-VISIBLE-WINS ARBITER; TELEGRAM CHROME + ANIMATED SWIPE-DOWN + LOOP.** ➡ **`2026-09-06-session-video-nits-2.md`**.
- **The 429 (the real bug):** found only after 0.2.12 printed `stage · bytes · error` on the fullscreen error surface and the owner screenshotted `fetch_decrypt 0B Exception Media fetch failed 429`. Every image and video blob shares one 60/min bucket per client IP (both phones on one WiFi), and inline autoplay multiplied fetches (scroll-in, fullscreen open, fullscreen close, sender post-ack refresh). Fix: 600/min + `Cache-Control: private, max-age=1y, immutable` (blobs are UUID ciphertext, never rewritten; nginx forwards the header through X-Accel — verified live). Desktop/emulator never reproduced it because they never reached 60. **Lesson: put the failing stage on the user-visible surface early — three owner round-trips went by on "blurred / doesn't play" before the log arrived.**
- **Arbiter:** "latest requester wins" was mount order — on chat open the second-to-last clip played, the last sat blurred. Now `request(…, priority: createdAt.ms)` → bool; lower is denied and retries on release; a bubble whose own load failed must NOT self-retry (first cut looped every frame — and would have hammered the throttle). Verified on the sender's chat ×3 re-entries: the bottom clip plays each time.
- **Swipe-down:** release now animates the stage off the bottom (or back), measured y 236→296→…→764→off in ~230 ms with the stage scaling; pop only after it's gone. Reduce-motion snaps.
- **Android gallery "Gotowe" greyed with nothing picked** is Android's own photo picker (the browser's `<input type=file>`), not app UI — back gesture/arrow cancels it. Not actionable in the PWA.
- **Heavy video on the PWA (owner's read, agreed):** whole-file AES-GCM decrypt into RAM, blob URL, no ranged streaming, no hardware-decoder control — the PWA is for ≤20 MB clips; the native app is where long/heavy video belongs. The sender-side WebCodecs transcode plan still helps the PWA if wanted; not started.
- Suite **1768 / 10 skipped** (count verifier OK); backend media spec 14/14. Reviewer subagent run at the end — see the dated file for its findings.
- **Tap on the PLAYING bubble (owner, both phones): cause 1** — on Flutter web the `<video>` is a platform view and the engine drops pointer events whose DOM target sits inside one; the touch landed on `VIDEO[flt-platform-view]` and the bubble's `GestureDetector` never saw it (the paused poster's tap hit `FLUTTER-VIEW` and worked — which is why every earlier "fullscreen works" check passed: it tapped before playback started). **Cause 2**, after adding `pointer_interceptor`: the interceptor's own render box is a platform view whose recognizer wins the arena sweep over any ANCESTOR detector — the touch hit `DIV[flt-platform-view]` and still nothing opened. **The gesture target MUST be the interceptor's CHILD.** Both the bubble and the fullscreen stage are wired that way now.
- **Resume position was always the handoff:** `showDialog`'s future resolves at pop, the view's `dispose` (which reported the position) runs after the exit transition — measured 27.4 s in fullscreen → inline restarted at 21.7 s. Position is now reported from `PopScope.onPopInvokedWithResult`; measured continuous on 0.2.11 (swipe at 10.0 → inline 11.5 after 2.5 s; chevron at 7.0 → 8.2).
- **Telegram chrome (owner screenshots):** inline `0:12 🔇` pill TOP-LEFT is the mute toggle (`video_inline_mute_toggle`), bottom corners left to the bubble's time/ticks; fullscreen: back chevron top-left (`video_fullscreen_close` key kept), centered `Ty`/sender + `Dziś · 04:52` header pill (`MessageDateSeparator.dayLabel` lifted public), seek pill `elapsed — slider — remaining` (`video_remaining_label`), swipe-down dismiss (22 % of height or a 700 px/s fling; backdrop thins, stage scales; two regression tests), fullscreen LOOPS at end-of-clip.
- **Also this round:** diag keyed on `step:reason` (first-branch masking fixed), receiver size guard admits the 16-byte AES-GCM tag (`maxCiphertextBytes`, boundary test). Owner reported inline autoplay WORKING on both phones from 0.2.8 on.
- **Traps:** the emulator's Vulkan backend died mid-session (`VK_ERROR_DEVICE_LOST`) — `hub restart emu-video`, Chrome's session survived; a stray `data:` noise tab hangs the DevTools `/json/list` fetch — close it; `screenrecord` cannot make a dense clip from a static/noise screen (<0.6 MB at 20 Mbps) — dense-clip bisect abandoned in favour of the unit test.

**Date:** 2026-09-05 (late) — **VIDEO NITS ON `feat/video-nits` (worktree `Desktop/fireplace-video`, off `a71ed33`): SQUARE BUBBLES HAD TWO CAUSES (WebKit `videoWidth == 0` at `loadedmetadata` + the video RETRY branch dropping geometry), TELEGRAM-STYLE INLINE MUTED AUTOPLAY WITH A ONE-SESSION CAP, SEEK BAR OUT OF THE ANDROID GESTURE ZONE, "NAGRAJ WIDEO" DOOR ON ANDROID. Not merged (PR #163); LIVE as the 0.2.5 branch test — see the deploy-state block above.** ➡ **`2026-09-05-session-video-nits.md`**.
- **The retry bug is the one that matches the owner's "3 of 4" pattern:** `retryFailedMessage`'s video branch rebuilt the pending payload from url/key/iv/duration only, so a clip that failed once reached the peer as a 220 px legacy square with a BLANK poster and the sender's own persisted copy lost geometry on the ack merge — the image branch never had this hole. Found because my own first smoke send failed (receiver had no bundle yet) and the retry reproduced the white square exactly. Regression red→green.
- **The probe fix is NOT iOS-verified** (no iOS device here): `video_probe_web.dart` now waits ≤3 s for `resize`/`loadeddata` when WebKit reports 0×0 at `loadedmetadata`, and labels `.mov` blobs `video/quicktime`. Owner must record from the iPhone PWA camera and check both ends.
- **Inline model:** `VideoMessageContent` autoplays muted+looping when `SettingsProvider.autoplayVideos` (default ON, Settings → Preferences switch) and ≥60% visible; `InlineVideoArbiter` allows ONE live session app-wide (each is a decrypted ≤20 MB blob); speaker toggle inline; tap → `showVideoFullscreen(startAt:)` → returns position → bubble resumes. **Receiver rows mount with the plaintext `mediaUrl` but no key** — `didUpdateWidget` re-evaluates when the decrypt pass lands them, or the receiver never autoplays without a scroll (found live, regression pinned).
- Live smoke in headless Chromium, two accounts, both ends: portrait bubble + poster, inline loop, unmute, fullscreen handoff both directions, settings OFF ⇒ zero `<video>`, receiver autoplay after cold entry with the cap holding. Suite **1762/10sk**, analyze clean. Post-review: seek-bar clearance is now `max(64, inset+12)` (uniform on iOS), the probe re-reads DURATION after the geometry wait (NaN on the same WebKit path), and a route pushed over the chat (Settings, another chat, the fullscreen dialog itself) RELEASES the inline slot — `localToGlobal` alone reported the bubble on screen under an opaque route; the restart is gated on the dialog future because `isCurrent` flips at pop START while the future settles after the exit transition. Each mechanism falsified in isolation.
- Traps: DDC dev server hot restart with 3 clients attached times out and KILLS the server; it does not boot on `127.0.0.1` (use an incognito `createBrowserContext()` on `localhost` for the second account); Flutter semantics clicks time out in the browser tool — use `page.mouse.click`; `hub start` needs `cmd.exe /c` for `docker`/`flutter` on Windows.
- ⚠️ **CI never ran on the branch push** (`ci.yml` triggers on `master` pushes and `pull_request` only) — PR opened so the frontend job + count verifier actually run; check it before merging. **And the BASE is red:** master `a71ed33`'s CI run failed on `E2E wire harness` and `E2E isolated probes` (`Expected: 'identity_locked'`, `Expected: false`) — the 0.2.4 opt-in-lock change, NOT this branch; `Flutter analyze and tests` and `Backend tests` were green there. "Never merge or deploy on red" applies to the base first.
- Post-review design call kept: the fullscreen dialog RELEASES the inline session (one ≤20 MB blob at a time, not two during fullscreen) and re-loads on return at the returned position — `loadDecryptedMediaBytes` has no cache, so the cost is one re-fetch + decrypt per fullscreen close, paid deliberately for the memory ceiling on phones.

**Date:** 2026-09-05 (night) — **(lxxiii)+(lxxiv) BUILT, FALSIFIED F1–F10, LIVE-PROVEN ON A LOCAL BUNDLE, COMMITTED AS 0.2.4 — THE REGISTRATION LOCK IS OPT-IN, "LOST KEYS" IS A FULL-SCREEN GATE, A WEB PRIMARY MUST BE INSTALLED. Cross-tier: backend deploy REQUIRED before web.** Owner's three yes/no answers: opt-in lock YES, install rule YES, "QR as login" DROPPED (I3). ➡ **`2026-09-05-session-optin-lock-gate.md`**; spec §12 (lxxiii)/(lxxiv) + §8 rewritten.
- **The lock arms when linking is enabled.** `IdentityLockedError` at BOTH refusal sites only if `account_authorizations` has a row (`KeyBundlesService.isEnrolled`); un-enrolled replacement = `via=unlocked`, same audit row + §6.0 alarm (keyed on `identityChanged`, not on `authorizedBy` — verified, nothing widened). `ownKeyBundleStatus` gains `linkingEnabled`; the client treats an ABSENT field as `true` (fail-closed to the old gate). Un-enrolled accounts are password-only takeover-able, LOUDLY — Signal-without-PIN; owner accepted, the cure is one tap.
- **`DeviceLinkGateScreen` replaces three banners** (`IdentityDamagedBanner`, `DeviceMismatchBanner` DELETED; the lock-refused branch of `IdentityResetPendingBanner` removed — it keeps only the countdown for OTHER sessions). `AuthGate` renders the gate above an `Offstage` `MainShell` while `needsDeviceLink || identityCheckUnavailable`; the shell stays mounted (socket + guard + reset hydration live under it). UNKNOWN now has a flag that is deliberately NOT cleared on disconnect — an offline keyless install shows "checking + retry", never a keyless shell. A gate arming while a route is pushed pops to root.
- **Live proof (local bundle, 4 browser contexts):** un-enrolled 699 wiped → shell + `IDENTITY_GUARD_UNLOCKED_REMINT` + server `via=unlocked`; enrolled 697 wiped → gate → reset → 71 h countdown → Anuluj → link state → Wyloguj; NEW 700: plain tab shows install-first, standalone shows the button → dialog → DAK → phrase offer; keyless N → gate QR → jsQR → P deep link → SAS `613 779` both → approve → `LINK_IDENTITY_ADOPTED` → N in the shell; clean P page shows the install nudge.
- ⚠️ **Harness trap that cost 20 min:** `page.evaluate` runs in an ISOLATED world — a `window.matchMedia` override there is invisible to Dart, and a CDP new-document script override (main world) is invisible to `evaluate` probes and PERSISTS across `goto`. Open a NEW page to shed it before claiming a web branch is broken.
- ⚠️ **Copy claim caught before ship:** "linking cancels the reset automatically" was FALSE (`cancelReset` reachable only via `resetIdentityCancel`); the gate now says cancel first. A real auto-cancel in `_onLinked` is a small queued follow-up.
- Suites: backend **1063/62**, Flutter **1748/10sk** (post-rebase over the 0.2.3 video release), analyze clean, both verifiers OK. Deploy order: `deploy-backend.sh` on the VM FIRST (the client's unlocked remint needs the server's `via=unlocked`; against the old server it fails closed to the gate), then `deploy-web.ps1`, then smoke from the main checkout.

**Date:** 2026-09-05 — **0.2.3 IS LIVE (frontend `5d669ce`, smoke 5/5; backend untouched `0.2.0 / 5ffef19b`): THE VIDEO-MESSAGES WORK IS FINALLY IN FRONT OF USERS.** Frontend-only release — bump `0.2.2 → 0.2.3` then `deploy-web.ps1`. The delta over the previous live build is exactly the video branch's 8 frontend commits plus the `95dd243` merge, so this closes the *"video messages are OFF prod until `feat/video-messages` merges to master"* item the 09-02 deploy record left open. Verified: `/version.json` → `0.2.3 / 5d669ce`, `/health` ok, headless boot ok, and the served `main.dart.js` literally contains `5d669ce`. CI green on `5d669ce` (run 33925447274). ➡ **`2026-09-05-session-passcode-relock-and-master-merge.md`**.
- **⚠️ THE SMOKE GATE CANNOT CATCH A STALE-BUT-CONSISTENT BUILD.** It compares the SERVED commit against the commit it just built, so building from a tree at the wrong commit — e.g. **`fireplace-0a`, which still holds local `master` at the pre-merge `90b4273`** — would publish `0.2.3` with NO video work and still pass 5/5. Always `git rev-parse HEAD` in the build tree first, and grep the served bundle for a **localized STRING** (this build has `"tylko MP4"` and `"maks. 3 minuty"`), never a Dart identifier — dart2js renames those, so `videoUnsupportedFormat` returns 0 on a perfectly good build.
- **On the PWA an oversize (>20 MB) video is still REFUSED, not transcoded.** `video_transcode_stub.dart` has `bool get isVideoTranscodeSupported => false;` and `chat_input_bar.dart:476` returns early, so the transcode branch is unreachable on web and `"Kompresowanie wideo…"` is absent from the bundle. Native transcode is Android-only and **no APK was built** — build one when the owner wants that path tested.
- ⚠️ **`frontend/pubspec.yaml` has CRLF endings:** `sed -i 's/^version: 0\.2\.2$/…/'` matches NOTHING and reports success (the 09-02 deploy logged this as a "perl misfire"). Drop the `$`, or bump with an editor, then re-grep. And **`deploy-web.ps1` exits 1 with `PUBLISHED_OK` already in the log** when `scripts/smoke` deps are missing in the build worktree — the publish SUCCEEDED, only the gate refused to run. Do not use `-SkipVerify` and do not rebuild: run `node scripts/smoke/post-deploy-smoke.mjs --commit <sha>` from the main checkout, which has the deps.
- ⚠️ **Also dead: the claim that the backend answers `{"version":"0.0.1","gitCommit":"unknown"}`.** Verified twice: `/version` → **`0.2.0 / 5ffef19b`**, re-stamped by the 09-02 backend deploy. That note had been carried forward unverified since 09-01.
- Unrelated and still true: `feat/passcode-lock` is NOT merged and NOT live (`5d669ce` does not contain it); it is stacked on the old `feat/video-messages` head and needs rebasing onto `95dd243`.

- **Still binding, from the rolled-off 2026-08-21 T6 entry** (full text in `2026-08-21-session-t6-revocation.md`, closure in decision record §11, settlement spec §12 **(xxi)–(xxix)**): **a LOCKOUT lives in this area — login used to hardcode `deviceId = 1` (`auth.service.ts:74`), so the moment a reset revoked device 1 the correct password minted a token for a revoked device and both session gates correctly refused it.** Login resolves the LIVE PRIMARY (`DevicesService.resolveLoginDeviceId`); the regression test names the lockout. **The session gates deny only on an EXPLICIT `revokedAt` (xxii)** — a MISSING `devices` row must NEVER deny, or the whole pre-Phase-1 install base loses access; that is deliberately the inverse polarity of `DevicesService.isActive`, which gates key-material uploads and must fail closed on absence. **Both predicates stay; do not unify them.** **I6 silence is a separate rule from rejection (xxiii):** `getServedMessageIds` answers a revoked device with NOTHING — an empty list would mean "destroy all of them". **`account_authorizations` is REPLACED, never dropped (xxix)**, and the replacement is admitted only when the STORED enrollment no longer verifies under the account's CURRENT published identity — self-verifying, so no flag and no nullable state (this is the hook T10's (xlv) finally used). **The accept-side gate ((e)/(xxvii)) withholds on VERIFIED data only and its refusals are never terminal**; when the fetch itself FAILS, withholding applies to `originDeviceId >= 2` only, or one broken `getDeviceList` answer silences every conversation of a single-device account. **⚠️ THE APP-PROOF EARNED ITS KEEP:** the revoke button failed live with NOTHING in the server log — `revokeDevice` never armed its DAK from the Keystore, so `signList` threw before any emit, **and the unit suite was green because it pre-armed the engine — a test that could not fail.** Drive the production path.
