import 'dart:typed_data';

/// Off-web stub: no DOM capture input. Native mobile takes the
/// image_picker camera path in the caller instead.
Future<({String name, Uint8List bytes})?> captureCameraMedia() async => null;
