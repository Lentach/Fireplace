# Latest session summary

**➡ FRESH AGENT: read `2026-08-02-HANDOFF-post-incident-state.md` — verified state, open work, and the traps.**

**✅ P0 CLOSED AND CONFIRMED: `[Decryption failed]` fixed in 0.0.139, several days with zero new reports.** The four hypotheses in `2026-07-29-HANDOFF-decryption-failed-incident.md` are all DEAD (killed by measurement) — that file is historical, do not work from it. Root cause: `2026-07-29-session-decryption-failed-root-cause.md`. **Never uninstall / clear site data.**

**🔴 FCM IS DISABLED IN PRODUCTION.** `FIREBASE_SERVICE_ACCOUNT` is absent from `~/fireplace/.env`, so every backend boot logs "FCM disabled". `google-services.json` IS tracked, so an APK would register tokens the server can never push to — **Android notifications are dead until that var is set.** Web Push VAPID is present, so the PWA is unaffected, which is why this went unnoticed. Fix it before any APK work.

- **Still binding (applies to every entry below):** never run `dart format lib/` (format only what you edited); **ask before opening the browser tool**; `node scripts/lint-ratchet.mjs` runs ONLY in CI — run it before pushing backend changes; `cmd | tail` hides exit codes; never re-derive test counts by arithmetic across a merge; `flutter run -d web-server` serves a blank scaffold once its hot-restart client is lost — restart the process, do not reuse the tab; the E2E register throttle is 10/hr/IP in memory, so `docker compose restart backend` between full `test_e2e` runs and allow 2–5 min to boot.

**Date:** 2026-08-02 — **RELEASED 0.1.0 / `a60610f`** (first minor bump), **BOTH surfaces deployed, smoke 5/5**; then the **decrypt ledger was built on top and is NOT deployed** (master `4ff1c52`). Backend is live as `0.0.140 / da120460` with the per-minute secret-note sweep. Backend **578/47**, Flutter **1133 + 10 skipped**, analyze clean, lint-ratchet held at its 817 floor.

- **`CONTENT_KEY_CANARY_LOST` does NOT gate the Android APK — the earlier handoff wording was wrong.** `ContentKeyCanary.checkAndArm()` is `if (!_isWeb) return;` (`content_key_canary.dart:96`), a no-op on native: it measures web IndexedDB+WebCrypto, while Android is Keystore-backed. **It still gates web B2 sealing — keep it.** Full reasoning in the handoff.
- **The release keystore is SINGLE-COPY.** `frontend/android/keystore/fireplace-release.jks` lives on the dev PC and nowhere else. Untracked and ignored (verified), but one disk failure ends updates for every install permanently. The runbook now carries fingerprint → copy → **read-back-and-compare**, plus the rule that the `.jks` and plaintext `key.properties` must never share a backup object. **Still outstanding; only the owner can do it.**
- **Secret notes: expired ciphertext now swept every minute, was daily at 03:00.** An UNREAD expired note sat in `secret_notes` for up to ~24h past its TTL. The API refuses to serve it, but the AES key travels in the note URL, stored as ordinary plaintext message content — DB access plus device access read a note the UI already called self-destructed. `secret-notes.service.ts:53` now matches `MessageCleanupService`, and **the cadence is pinned by a fail-before-proven test** because nothing behavioural would catch a revert. **DEPLOYED** 2026-08-02 as backend `0.0.140 / da120460`, `schema up to date`, no migrations.
- **The web read path changed and prod now runs it** — `_authoritativeSnapshot()` moved into the `ContentKv`/`PrefsContentKv` seam. Test-equivalent, not byte-identical source. On any `[Decryption failed]` report, grab the `E2ePersistentDiag` dump FIRST; it caps at 80 and rotates.
- **⚠ MASTER IS AHEAD OF PROD AGAIN, and this time the delta GATES DECRYPTION.** The ledger can call `markRetired`, which is permanent, and it has never run against a populated real store. Prod frontend is `0.1.0 / a60610f`; master is `4ff1c52`. **A future `deploy-web.ps1` is not routine — treat it as a release of this feature** and take a localStorage snapshot first.
- **The decrypt ledger closes the "lost record bricks the row" hole.** `e2e_<uid>_ledger_v1` records ids whose plaintext was persisted at least once, so a missing record is finally distinguishable from one never decrypted; `_decryptMessageAsync` consults it before the ratchet and renders "no longer stored" instead of burning the row. **Three fail-open rules — do not simplify:** recorded only after a CONFIRMED commit of real plaintext; absence decided by the tri-state `recordExists`, never `getDecryptedContent() == null` (also null for unbound-user and any caught exception); an edit drops the entry via `invalidateDecryptionCache`. Gate falsified.
- ➡ Detail: **`2026-08-02-session-canary-gate-and-keystore.md`**; merge evidence in **`2026-08-02-session-android-branch-merge-verify.md`** (branch only).

---
### Prior latest ↓
**Date:** 2026-08-02 — **RELEASED 0.0.140 / `3a33bf9`, FRONTEND ONLY, smoke 5/5**, master CI green pre-deploy. Backend stays `0.0.136 / 6fb36bf` BY DESIGN. Flutter **1085 + 5 skipped**, analyze clean, count verifier OK.

- **The wipe was the most dangerous button in the app.** "Delete all local history" never called `markRetired`, so after a wipe the server kept serving those rows, hydration found no record, the re-decrypt hit a consumed ratchet key, and the user's ENTIRE history rendered a permanent `[Decryption failed]` — the incident's exact symptom, self-inflicted, all at once. Retention and LRU eviction already retired for this reason; the user-initiated wipe was the one destroyer that did not. Fail-before proven (`Actual: Set:[]`).
- **Reconciliation is live again.** `62b7b93` re-enabled it but was never deployed, so the offline-delete catch-up gap stayed open in production through all of 0.0.139. The temporary `kReconcileDefaultEnabled` gate and its pinning test are GONE — do not go looking for them.
- **`feature/android-encrypted-store` is now MERGED** (PR #111 → `ac880f6`; the branch ref is kept only because the owner's working copy sits on it). Master was merged into it repeatedly and every pass auto-merged every code file, which is the signal that the seam refactor worked. The authoritative read lives in `ContentKv.authoritativeSnapshot()` — `PrefsContentKv` answers on web only (it also backs iOS/desktop/Android-fallback, so the `kIsWeb` gate stays inside it), `NativeContentStore` answers null because its view cannot be clobbered. **Android was never affected by the incident.**
- **THREE DELIBERATE ASYMMETRIES — do not "restore consistency", each one re-arms destruction:** `storedMessageIds()` reads the cache (under-enumeration ⇒ reconcile purges LESS); `_messageIdsMatching` is split by `authoritative` (true for user-requested deletes, which must not miss; false for the automatic sweep); the LRU prune counts via the cache (eviction is permanent). Governing rule: **over-retention is recoverable, over-destruction is not.**
- **Falsify the reload-race suite before trusting it** — set `PrefsContentKv.debugForceAuthoritative = false` and confirm three tests go red. Green alone proves nothing; the harness can stop reproducing.
- **The incident itself — fixed in `0.0.139 / 15a368c` (frontend only, smoke 5/5), now closed.** `SharedPreferences.reload()` refills its cache from a snapshot taken across an await, so a concurrent write survives on disk but vanishes from the cache; a missed plaintext record re-decrypts a consumed ratchet key → `DuplicateMessage` → permanent `[Decryption failed]`. Web-only and pre-existing; 0.0.136's 60s timer + launch drain/sweep/reconcile is what made it fire. A second, independent bug shipped in the same release: `saveDecryptedContent` let the `[Decryption failed]` label overwrite real plaintext (explains the 27 `badMac` rows from sender 60, **not** the 42).
- **Killed by measurement**, so nobody re-chases them: reconcile (`findServedMessageIds` SQL → `SERVED 45/45`), expiry (broken ids carried `expiresAt = 2026-08-11`), retention/LRU (both `markRetired`; retired ids are never decrypted, these were), quota (fresh records written with no `DECRYPT_PERSIST_FAILED`), key format/codec, `_userId == null`.
- `E2ePersistentDiag` is capped at 80 and rotates — in an incident, grab the dump FIRST. The expiry sweep still logs nothing on success; that gap made this diagnosis much harder than it needed to be.
- ➡ Detail: **`2026-08-02-HANDOFF-post-incident-state.md`**, **`2026-08-02-session-android-branch-merge-verify.md`** (branch merge + falsification evidence), **`2026-07-29-session-decryption-failed-root-cause.md`**.

---
### Prior latest ↓

**Date:** 2026-07-29 — **Android Phase 2 DONE on `feature/android-encrypted-store`** (pushed, PR open, **NOT merged**, no version bump; master 0.0.138 merged IN). The encrypted native message store — issue #105 M4, the **distribution gate**. Master 0.0.138 merged in and re-measured: Flutter **1102 + 10 skipped**, analyze clean, **on-device acceptance 7/7**, real two-account emulator run clean. ➡ Detail: **`2026-07-29-session-android-phase2.md`**.

- **Decrypted plaintext, media keys, pending-send and the JWT are out of the readable XML on Android.** Content lives in a SQLCipher DB (`files/fp_content.db`, Drift), keys in Keystore-backed secure storage. It all sits behind `ContentKv`, a seam that mirrors `SharedPreferences` EXACTLY — the swap was type-level, so **web/desktop are byte-identical by construction** and every 0.0.135/0.0.136 purge rule above the seam is untouched; non-Android returns `PrefsContentKv`.
- **Load-bearing rules** (full text: `frontend/CLAUDE.md` §5): runtime throw unless `PRAGMA cipher_version` answers (a plain sqlite3 ignores `key` and looks sealed while being plaintext); keys are ARMED (write → fresh read-back) before use; a null key inventory means change NOTHING (a transient Keystore error must never read as key loss); shredding is rotate-and-destroy with durable gen counters, audio resealed BEFORE keys die; any open failure falls back to prefs + `CONTENT_STORE_FALLBACK` — loud status quo, never an unencrypted DB.
- **Blocker caught only on device:** just_audio serves unsealed voice bytes through a `127.0.0.1` proxy and API 28+ blocks cleartext, so sealed voice notes would have failed in RELEASE only. Fixed with a **loopback-only** `network_security_config.xml` (no `base-config`; debug adds `10.0.2.2`). Never widen it.
- **Evidence:** emulator (Pixel_7, android-34) register → befriend → A sends → B live-decrypts → B replies; after `force-stop` the prefs XML holds only the diag list + two wallpaper keys (no JWT, no `e2e_*`, no `sig_*`), both markers absent from db/wal/secure-storage bytes, main-db header is a random salt. Both messages still render after a cold start — the received one is a consumed PreKey message, so it can only come from the persisted local record. **Traps:** the on-device `grep` errors `no REGEX` and prints nothing (pull the files, search host-side); the store is at-rest protection, NOT an offline cache (chat list is socket-gated).
- **Gates before distribution:** owner's real-phone smoke (voice play/seek/cached replay/legacy files, push, upgrade from a pre-Phase-2 install) and his explicit merge OK. The `.jks` off-PC backup is STILL outstanding.
- Pins that must not drift: `drift ^2.31` / `sqlite3 ^2.9` / `sqlcipher_flutter_libs 0.6.8` (sqlite3 3.x needs MSVC on every local `flutter test`) and `webcrypto` 0.6.0. `flutter test integration_test -d <device>` is the only place the real Keystore/SQLCipher/webcrypto run — it cannot run in CI. Correction: **gitleaks IS installed** (8.30.1), so the pre-commit hook runs its entropy branch, not the regex fallback.

---
### Prior latest ↓
**Date:** 2026-07-29 — **RELEASED 0.0.138 / `bf80602`, FRONTEND ONLY, smoke 5/5.** PR **#110** merged (`ce1ab79`) with owner approval, bump in `bf80602`, CI green on both master commits before deploy. Backend untouched — `/version` stays `0.0.136 / 6fb36bf` **BY DESIGN**. Flutter **1075 + 5 skipped**, analyze clean, count verifier OK. Two-axis review (Standards + Spec, parallel `reviewer` agents): both **correct**, three findings actioned.

- **Three owner-reported invitation gaps, all shipped.** (1) A pending outbound invitation on the Contacts board said so only via `Icons.send_outlined` — while `_buildGhostSemantics` already announced "invitation sent" to screen readers. The word `invitationStatusPending` now renders under the name in `colorScheme.primary` (cosmic 12.88:1, blue 6.33, dark 5.88, light 4.72, teal 5.24). (2) The Chats `+` badge counted inbound requests but opened a friends-only sheet — `InvitationsScreen` was reachable from exactly ONE place in the app. (3) Picker hexes were avatar-only, i.e. one initial for anyone without a photo.
- **`showChatHoneycombPicker` now returns `ChatPickerChoice`** (`ChatPickerFriend` | `ChatPickerReviewInvitations`). Inbound senders LEAD the comb as accent terminals (`HexAvatar(ember: 1)` + `Pending`) and route to `InvitationsScreen`. **It never accepts inline** — accept/decline own the #106 failure+retry machine; a second copy would drift. Badge stays on `pendingRequestsCount`: the backend re-emits it after accept AND decline (`chat-friend-request.service.ts:525/:622`), so it is server truth.
- **Owner caught a real wire bug in a screenshot** (pre-existing, reproduced at `invites=0`): feeds sliced the corners off the row-0 hexes. `routePath` did its sideways travel ACROSS the row plane — and a pointy-top lattice has no straight vertical channel AND no clean long diagonal, because a wide row's gaps sit exactly over the narrow row's centres. Now: verticals only inside gap corridors, sideways only in the band between rows, rim fan per CORRIDOR (≤ `columnsWide+1`) not per contact, gap ties break OUTWARD for mirror symmetry. **A horizontal-rail version was built and REJECTED by the owner** — do not reintroduce. Clean at 8/40/**200** nodes.
- **Trap:** measure captions with `DefaultTextStyle.of(context).style.merge(style)`, never the bare style — `Text` merges into the ambient default and its line height is missing from the bare measure (~4px/line). One line hid in the board's 9px row slack; two lines threw `RenderFlex overflowed by 4.0 pixels`.
- **Zero new ARB keys** — every string already existed, so no `flutter gen-l10n`. Drive-by: the badge's hardcoded `Colors.red`/`Colors.white` → `colorScheme.error`/`onError` (playbook §1).
- **Review caught a regression I authored: whole-file `write` over a STRUCTURAL read.** `chat_honeycomb_picker.dart`'s `didChangeDependencies` body had been elided by the summary read and got reconstructed, not ported — the latch moved above the `disableAnimationsOf` check, so reduce-motion switched on mid-entrance was ignored (playbook §9). Restored + fail-before test (0.637 opacity). **Never `write` a whole file whose bodies you only saw elided; `git show <base>:<path>` the original first.** Caption measurement also collapsed into `utils/caption_metrics.dart`, which does the `DefaultTextStyle.merge` internally so the trap is unreachable at a third callsite.
- **`deploy-web.ps1` builds THE CHECKOUT IT LIVES IN** (`$repo = Split-Path -Parent $MyInvocation.MyCommand.Path` + `Set-Location $repo`) — not your cwd, not master. The main copy was on the owner's `feature/android-encrypted-store` with untracked WIP, so running `…/fireplace/deploy-web.ps1` would have shipped that branch. Deployed from the worktree instead (gitignored `deploy-web.config.ps1` already there, same md5). **`fireplace-wt-invitation` is now the ONLY checkout on master** — left in place deliberately.
- **Owner: fully close + reopen the PWA** → Settings `0.0.138 / bf80602`. **Never uninstall or clear site data.**
- ➡ Detail: **`2026-07-29-session-invitation-visibility.md`**.

---
### Prior latest ↓
**Date:** 2026-07-29 — **RELEASED 0.0.137 / `53b2610`, FRONTEND ONLY, smoke 5/5.** PR **#109** merged (`9792293`) with owner approval, bump in `53b2610`, CI green on both. Backend untouched — `/version` stays `0.0.136 / 6fb36bf` **BY DESIGN**. Flutter **1071 + 5 skipped**, analyze clean, count verifier OK. Worktree + branch removed.

- **"Ugly white arrow" was a contrast bug.** The old 42px `primary` disc hardcoded `Colors.white` on the glyph: **1.56:1 on `cosmic`** (#8FD8FF — effectively invisible), 2.57:1 on `blue`, 3.02:1 on `dark`. Three of five themes under the 3:1 non-text gate, while `RpgTheme.readableOn` already computed the right answer.
- **Shipped `HexSendButton`** — the app's own pointy-top hexagon (`hexPath`/`kHexWidthRatio`, shared with the Chats avatars and the Contacts honeycomb) under an accent-derived ember gradient; owner picked it from five rendered candidates. **PAINT ONLY:** `_ComposerTapSendOverlay` still owns the 48×48 hit region, tooltip and semantics, or an `IconButton` there wins the gesture arena and blocks hold-to-record. Voice-send layer untouched. Fail-before proven; contract in `frontend/CLAUDE.md` §7.
- `themeOptionBlue` PL: "Głęboki niebieski komunikatora" → **"Głęboki granat z jasnym błękitem"** (calque, and redundant with its own tile title "Niebieski"). `l10n.yaml` makes **`app_pl.arb` the template** — edit it, then `flutter gen-l10n`.
- **Trap:** hot restart (`R`) into a `flutter run -d web-server` with **no client attached** does not republish the bundle ("Recompile complete. No client connected.") — later page loads serve stale JS and clearing the browser cache does nothing. Stop and restart the process. On Windows a supervised `flutter` needs `cmd.exe /c flutter.bat …`.
- **`pull_request` CI does not dispatch while a PR is `CONFLICTING`** — no run is created at all, so `gh pr checks` says "no checks reported" and it reads like Actions is broken. Master moved twice mid-session and `LATEST.md` conflicted each time, silently killing CI. Merge master in, resolve, push; then the run appears. Corollary: resolve LATEST at merge time, do not chase it from a branch.
- **Owner: fully close + reopen the PWA** → Settings `0.0.137 / 53b2610`. **Never uninstall or clear site data.** Also worth a device check: the hex is smaller than the old disc (40 tall × ~34.6 wide vs a 42 circle), but the **tap target is unchanged at 48×48**.
- ➡ Detail: **`2026-07-29-session-composer-hex-send.md`**.
