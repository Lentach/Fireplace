# Session 2026-05-15 — Chat composer newline vs duplicate send

## Accomplished

- Fixed “double send” / poor newline UX in the chat composer: `TextField` already used `TextInputAction.send` + `onSubmitted` while `RecordingController` showed a second send when `hasText`, so both paths sent the message and multiline felt wrong.
- Replaced the trailing send affordance with a **newline** control (`Icons.keyboard_return`, localized tooltip `chatComposerNewlineTooltip`) wired to `_insertNewline()` in `ChatInputBar`.
- Wrapped the composer `TextField` in `CallbackShortcuts` so **Ctrl+Enter** and **Cmd+Enter** still send on web/desktop where IME “Send” is often absent.
- Regenerated l10n; ran `flutter test` (all passed). Ran `graphify update .`.

## Key files modified

- `frontend/lib/widgets/input/chat_input_bar.dart` — `_insertNewline`, `CallbackShortcuts`, `RecordingController` prop rename.
- `frontend/lib/widgets/input/recording_controller.dart` — `onSend` → `onInsertNewline`, trailing UI.
- `frontend/lib/l10n/app_en.arb`, `app_pl.arb` — `chatComposerNewlineTooltip`.
- `frontend/lib/l10n/app_localizations*.dart` — via `flutter gen-l10n`.
- `CLAUDE.md` — composer send/newline/mic behavior.
- `graphify-out/` — refreshed by graphify.

## Notes / next session

- `flutter analyze` still reports existing **web** `dart:js_util` URI issues in this environment; no new analyzer **errors** from these edits (removed redundant `dart:typed_data` import after adding `services.dart`).
